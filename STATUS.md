---
name: bmad-fork-status
description: Volatile state of the Mason-BMAD fork. Read by mason-bmad-workflow-expert skill on every invocation.
---

# Mason-BMAD Fork — STATUS

> Update whenever you ship a change to the fork, absorb upstream, or change the shipped/designed status of a feature.

**Last updated:** 2026-06-19 by bison-ops session.

---

## Now

The compact, always-current state. The skill reads THIS block + the top of `## Changelog` on every invocation — it does not need the archive.

- **Latest wave:** onboarding redesign — **`onboard-project.sh`: one-command new-project onboarding** (`fc456214`). Removes every hurdle hit during the cash-recovery setup: the v6.8.0 installer ships the incompatible **skills layout** (`.claude/skills/bmad-*`) with none of the fork's custom layer; the sync silently **skips** a project lacking `_bmad/bmm/workflows`; CLAUDE.md must **pre-exist** for section-sync. The script: git init → clone the reference project's `_bmad` base (per `~/.bmad-reference`) → **generate a CLEAN `config.yaml`** (regenerated from a heredoc, NOT sed-patched from the clone — so the reference's deploy contract / phase-rationale comments / skill-pins never leak; the sync then backfills `autonomous_mode` from config-defaults) → CLAUDE.md from template → register in `~/.bmad-targets` → sync. Self-review hardening: **guards against a skills-layout reference** (requires the old-layout marker `_bmad/bmm/workflows/1-analysis`, else halts — defuses the `~/.bmad-reference` "point here after `bmad-cli install`" landmine, whose comment was also rewritten to forbid it), clears the reference's sidecar memory + stale sync-stamp. Tested end-to-end on throwaway dirs: hardened quick-dev, full upstream base, 4 custom skills, hooks, 40 commands, **0 contamination**. Companions: the **`bmad-onboard` skill** + the global `CLAUDE.md` "New project bootstrap" section (both in `~/.claude`, **live immediately, NOT git-tracked**) so "install the BMAD fork" triggers it without re-hitting the wall; `sync-bmad-workflows.sh` SKIP message now self-documents the remedy; `custom/MIGRATION-v6.8-skills-plan.md` parks the eventual skills-layout cutover (decisions resolved). **Fork-local tooling — NO project sync owed** (onboard-project.sh + the SKIP message live in the fork, not distributed to the 14). Pushed `myfork/custom`. **All four follow-ups closed in `02c892e4`** (see next changelog entry): targeted `--only` sync, complete-reference-health guard, global-asset backup (`custom/claude-global/` + `install-global-assets.sh`), and migration Phase 6 (retarget-at-cutover). The one remaining open item is Phase 6 itself — retarget onboarding to skills-native when the migration lands.
- **Prior wave:** quick-dev — **existing-code provenance check before modifying purposeful code** (`6f96a335`). Owner-driven from a live bison-ops session: an implementer was about to remove a scraper gate (`allCardUrls.length <= 1`) as an "obvious simplification"; checking `git log -S` revealed it was a deliberate guard from a prior PR (keep delivered multi-parcel orders on a separate path), which forced a deeper read and proved the removal safe only because a downstream guard still covered the original case. Institutionalizes that "moment of reflection." Three-point wiring (mirrors how Regression Surface is handled): workflow.md CRITICAL RULE ("Don't delete what you don't understand"); step-03-execute new gated PRE-FLIGHT (trace provenance → classify deliberate vs incidental → understand intent → autonomous/non-autonomous branch if the change would undo a guard; skips on pure-add / greenfield-no-consumers); step-04 §6 backstop checkbox + failure mode. Brownfield-safety posture, autonomy-scoped (proceed+log only when the protected case has test coverage, else halt). Mode 1 self-review: 0 blocking. Budget: step-03 289/350, not dense. markdownlint 0; validate:budget OK; full fork suite green. Pushed `myfork/custom` → `6f96a335`. **Sync owed (14/14)** to distribute. Follow-up: same provenance check is a candidate for dev-story + design-implement (both modify existing code) — quick-dev only today.
- **Prior wave:** design-ingest — **cope with a superseded handoff: tolerant supersede-awareness** (`354be3f9`). Owner wanted to hand `design-implement` a handoff that may already be done but has since been superseded, and have it cope gracefully (not a bare "already applied, no deltas" no-op, not a regression-to-stale apply). Put the coping in `design-ingest` (the non-destructive catalog+pause half = the safe checkpoint before any code moves). step-01 derives `{target_slug}`, resolves `{handoff_supersede_status}` (active | superseded | no_brief | ambiguous) against briefs in `implementation_artifacts`, **tolerates every value (never refuses)**, and stamps `ingest.supersede_status`/`superseded_by`/`source_brief` into the manifest; step-03 LEADS the pause with the heads-up. `design-implement` reads the stamp — symmetric: no hard refuse, no silent apply — superseded+no-deltas → self-explaining no-op, superseded+deltas → HALT for explicit confirm (applying is INTENT autonomy). `brief-revision-policy.md` §6 amended + new §8 (ingest-tolerance) so policy ⇄ workflow agree; honest limit documented (no_brief on raw-URL; diff-the-two scoped out as a future workflow). Mode 1: 0 blocking. markdownlint 0; fork test suite green; pushed `myfork/custom`; **synced 14/14**. **Follow-on (`d848e916`):** `design-implement` now also copes on a **direct URL/bundle run** (no ingest in front, so no stamp) — step-01 §SHARED.1a resolves supersede independently (derive slug from the frame inventory, match against briefs, same contract as ingest step-01 §5) and on `superseded` SURFACES + HALTS before the apply pipeline. So all three input paths cope, not just the stamped manifest. Re-sync owed for the follow-on. Open follow-up: the thefbaprep 06-17→06-18 chain that prompted this isn't materialized on disk (no 06-18 brief; 06-17 lacks the Block-A supersede block) — to exercise either path, both briefs need to exist with proper provenance.
- **Prior wave:** design-implement — **drilled-frame COMPOSITION axis (§2d-bis): the drawer analog of page-shell** (`d0c4e368`). Owner showed Claude-Design-vs-built supply-order drawer screenshots — "how does it not notice the visual deltas." Root `contract-dimension-gap`: §2 compares each section's inner pixels and §2f-bis certifies each section *exists*, but nothing compared a drilled frame's **composition** (section order, grouping under named headings, header/footer chrome) — so a drawer ships every component "matching" while wholly recomposed (`Cost & sourcing`→`Economics`, standalone `Lifecycle`→folded, `Related records`→`Routing & source`, black footer→blue). Page-shell blind spot (PR #2017) inside a drawer; footer sharpest (not a cataloged section → no row). Plus reporting flaw: the step-02b capability preflight (never looks at pixels) buried the whole recompose in one "#8 treatment, buildable: yes" line. Fix: step-02b §3 flags `{frame_composition_deltas}` + names it in the preflight; step-03 §2d-bis emits a Frame-composition row per drilled frame (order + grouping + chrome), Tier-1, footer in scope; Critical Rule + state var + step-04 metric/failure + checklist. Add-not-cede. Mode 1: 0 blocking. Pushed `myfork/custom`; linter green. **Synced 14/14.**
- **Prior wave:** design-implement — **resumable + checkpointed apply for large ingest manifests** (`0fefe11c`). The symmetric twin of the design-ingest wave (`293dfcc9` fixed INGEST overflow; this fixes APPLY overflow). On an `ingest_manifest` run, step-04 applies frame-by-frame, persists each frame's dispositions back into the manifest at its boundary (durable before any compaction), auto-resumes by skipping `✓ applied` rows (re-run the SAME command in a fresh session to continue), and self-checkpoints at a frame boundary (soft budget ~one heavy frame / ~10–12 sections) — a clean terminal exit that still delivers the slice + prints the resume command, never a single-window walk over 48 sections that the compaction trap silently corrupts. Optional `{frame_scope}` explicit slice override. New vars `{frame_scope}` / `{resume_prior_dispositions}` / `{run_completion_mode}`; Critical Rule + step-04 §5a + §9 report branch + step-03 §2f-bis budget note + step-01 resume-read + checklist item. Grounded in the owner's live 13-frame/48-section supply-orders blocked-stock manifest. Mode 1: 0 blocking. Pushed `myfork/custom`; budget linter green. **Sync owed (13/13).** Follow-up: design-ingest's review-pause still frames apply as "run it straight off this manifest" one-shot — align it to mention auto-checkpoint/resume.
- **Prior wave:** context-budget durable principle — **make the fork expert context-aware + able to recommend sub-workflow / sub-agent delegation** (`e8a9cf93` + skill v1.6). The owner flagged that long / instruction-dense workflows hit "context fatigue" — the model compresses and drops detail. Named precisely (research-grounded): **context rot** (length — quality declines monotonically well before the window fills), **curse of instructions** (density — the dominant mode for instruction-heavy markdown; reliability slips past ~10 simultaneous must-dos), **lost-in-the-middle** (position), **effective window ≈ 50–65%** of advertised, and the **compaction trap** (auto-summarization drops exact figures / edge-cases / constraint wording first — the dropped exceptions ARE the spec). New skill reference `references/context-budget.md`: the phenomenon + the decomposition decision-rule (read-heavy/parallelizable → sub-agent fan-out returning a distilled artifact · long sequential build → per-phase fresh-context with a durable manifest · write-one-coherent-artifact → single-threaded), reconciling Anthropic's multi-agent research win with Cognition's *Don't Build Multi-Agents*, plus the non-negotiable handoff contract (objective · output schema · tools/sources · boundaries; durable state in a file, not chat). Skill v1.6: principle woven into Mode 1 (Review) + Mode 2 (Author), new Mode-3 root-cause class `context-budget-overflow`, rolled v1.5 to `versions/`. Fork integration (`e8a9cf93`): create-workflow/step-03-build builds within budget (one job/step, ≤~10 must-dos, pointer-over-inline, per-step shape classification + handoff contract; new verify item + failure mode); orchestrate-workflows/step-02-health-check gains a Context Budget audit section (`overdense-step` / `inlined-corpus` / `buried-constraint` / `undelegated-read`). Skills aren't synced (the SKILL.md change is live immediately); the two fork workflow edits are **Sync owed (13/13)**. Coheres with the design-ingest wave (`293dfcc9`), which was itself a context-overflow fix — this is the general principle beneath it. Pushed `myfork/custom` → `e8a9cf93`; pre-commit suite green.
- **Prior wave:** design-ingest — **new workflow + `ingest_manifest` input_kind + §2f-bis section-coverage gate** (`293dfcc9`). Two failures running design-implement on a ~140KB JSX bundle: (1) the whole bundle overflowed one ingest context, so the exhaustive per-component catalog got shortcut; (2) delegating ingest by feature-area with narrow agent prompts let a whole drawer SECTION (Reconciliation block) and a whole section (SellerSmart dispatch) fall in the seam — §2f frame-coverage is frame-granular ("drawer present? yes → green") and is structurally blind to a section dropped *inside* a present frame (its inner primitives match elsewhere). Root class `contract-dimension-gap` (the section axis was missing under a present frame) + the context-budget cause. Fix is a decoupled front-half + a named structural gate: new `implement/design-ingest/` (sibling of design-synthesize) ingests URL/bundle, builds the frame inventory, then FANS OUT one isolated agent **per frame** (the fan-out unit is the frame, never a feature-area) to enumerate that frame's COMPLETE top-level section list + component×property catalog — context bounded. **FRAME-COMPLETENESS GATE:** an empty section list for a drawn frame HALTS. Emits a durable manifest (`manifest-schema.md`) with a pre-seeded grid scaffold (every (frame, section) already a row, UNVERIFIED), then PAUSES for a reviewable section-inventory handoff. design-implement gains `input_kind: ingest_manifest` (MANIFEST PATH in step-01 — skips download/extract/re-catalog; refusal gate on an incomplete manifest) and **§2f-bis Section-coverage** (step-03): every top-level SECTION of every present frame is a mandatory grid row, denominator from the gated scaffold (authoritative) or enumerated in-context on URL/bundle runs; a present frame's missing section is Tier-1, counted in `{delta_count}`. Same named-gate shape as page-shell §2d and frame-coverage §2f after their misses. Grounded in the owner's live supply-order drawer (a present drawer that shipped with NO Reconciliation block). Mode 1 self-review: 0 blocking (no brief consumption → provenance N/A; grounding gate satisfied; autonomy scoped — catalogs, never infers intent, pauses for review; worktree-safe — sync auto-discovers). Pushed `myfork/custom` → `293dfcc9`; pre-commit suite green. **Sync owed (13/13)** to distribute.
- **Prior wave:** design-implement — **authoritative §13-lookup denominator: the rendered "Linked records" list (the "often-missed link-to-record drawers" fix)** (`4910a3e1`). The lookup drawers (Catalog item / Route warehouse / Shipping lane / Supply source / Inbound batch / Import run) often went unchecked against Claude Design because §2f's frame-coverage denominator was harvested ONLY from bundle *self-declarations* — `<script src>` comments, per-frame banners, lookup→target maps. Those are declarations and under-enumerate (or describe a different conditional state), so a lookup they forgot to list never entered `{design_frame_inventory}` → §2f couldn't flag a frame it never knew existed (the live Orders detail drawer renders a `Shipping lane` row that a script-comment harvest drops). Root class `contract-dimension-gap` (the frame axis existed, but its denominator *source* was incomplete). Fix, **add-not-cede** (step-01 already opens the drawer): URL.3a **source 5** captures `{design_linked_record_rows}` — the drawer's rendered "Linked records" rows, the AUTHORITATIVE one-row-per-lookup denominator — and reconciles every row against a `§13-lookup` frame (no match → re-trace or `LOOKUP UNDER-ENUMERATED`, never dropped; harvested §13-lookup count ≥ row count). step-03 §2f reconciles before emitting (denominator-source-agnostic: brief/URL/manifest), routes under-enumerated rows, and sweeps a "present" drawer for **depth** so a thin `code — name` stub vs a rich bundle drawer surfaces as `MISSING in impl` rows (frame-present ≠ frame-matches). step-04 §9 disclosure leads with the linked-records denominator + enumerates under-enumerated/thin; workflow.md state var + Critical Rule; checklist +2. Grounded in the owner's live supply-order detail drawer screenshot (6 linked records incl. Shipping lane). Mode 1: 0 blocking. Pushed `myfork/custom` → `4910a3e1`; pre-commit suite green; synced 13/13.
- **Prior wave:** design-implement — **step-02b capability delta made SYMMETRIC — catch the UPLIFT, not just the regression** (`f2d70fd1`). The "running blind" miss: a redesign that ADDS capability the live page lacks (a new analytics/disposition band, lane-by-handler segmentation, an action column, co-views) DROPS nothing, so step-02b's DROP-only contract passed it clean — and with nothing foregrounding the uplift, the run anchored on token-debt evidence and emitted a backwards SCOPE verdict ("production is a structural superset, just treatment/token alignment, no net-new capability") *before the map had even finished resolving production*. This is the mirror of every prior design-implement patch (page-shell §2d / frame-coverage §2f / content-lane §2c: "an enumerated item is silently ABSENT while looks-right passes") — inverted: a capability silently ADDED-but-unbuilt while "nothing to drop" passes. Root class `contract-dimension-gap`, **add-not-cede** (both capability inventories are already gathered, so the ADD axis is verifiable from evidence in hand). Fix: step-02b §3 computes the delta BOTH ways — ADDED + DEEPENED promoted from "informational"/"flows through the grid" to first-class `{added_capabilities}`/`{deepened_capabilities}` → `{uplift_capabilities}`; §4b names a non-empty uplift and sets strategy ≥ `additive` (no halt — building the handoff is decision autonomy, unlike a drop which is intent); §4's empty-DROP branch no longer auto-concludes restyle. A **running-blind gate** (workflow.md Critical Rule + step-02b MANDATORY) forbids "restyle-only / superset / no-net-new" as a prior or from treatment evidence — the scope verdict is an OUTPUT of §3. step-03 §2h tags uplift sub-structure `capability-build` (not collapsed to "MISSING component → restyle"); step-04 builds it (§1 fix-type + §5 ledger) and discloses each in a new §9 "Capabilities built" section, the mirror of "Capabilities removed (orphaned actions)". Grounded in the owner's live inbound-flow supply-orders run (flat `/orders` table vs a handoff with lanes + analytics/disposition band + action column + co-views). Mode 1 self-review: 0 blocking. Pushed `myfork/custom` → `f2d70fd1`; pre-commit suite green; synced 13/13.
- **Prior wave:** design-implement — **transcription fidelity + render-compare done-gate (copy & chrome)** (`e91bccd3`). The grid is CSS-only, so copy and frame chrome (header/breadcrumb/footer) have no row — a green grid manufactured false confidence while a cost-recon drawer shipped with a generic header, relabeled footer, and paraphrased copy vs the handoff. Fix = name the prohibited move (transcribe, don't design — covers copy & chrome; grid is necessary-NOT-sufficient) in workflow.md Critical Rules + step-04 §5b transcription pass + render-compare done-gate, deviations riding the existing ledger. Add-not-cede (verifiable from source). Synced 13/13. **Prior wave:** regression-surface preflight (step-02b): handoff-vs-production capability delta + strategy gate (`71656fb1`). New entry behavior on a redesign: before building the grid or touching code, inventory what the CURRENT built page does (routing, §13 linked records, economics/cost-recon, composite status/header, activity/audit, wired mutations) vs what the handoff delivers, and surface every capability the handoff would DROP. Non-empty dropped set → **HALT and ask the implementation strategy** (restyle-only keep-all · additive · partial per-capability · replacement) rather than silently reproducing the redesign's omission — whether a drop is intended or accidental is *intent*, which the workflow can't infer. Proactive front end to step-04 §9's post-hoc orphaned-action backstop (the sweep greens out on a dropped capability because its inner primitives exist elsewhere — so the check runs BEFORE the grid). step-01 announces the check up front; step-02 → step-02b → step-03; step-03 §2h flags `capability-protected` rows; step-04 gains `deferred(capability-protected)` + a §9 strategy disclosure; workflow.md +5 state vars + Critical Rule + sequence; checklist Step-2b section. Autonomous mode defaults non-destructive (restyle-only) + discloses, never silently replaces; skips cleanly on a new surface; does NOT mis-score an undrawn-but-promised frame (brief §7 / `{design_frame_inventory}`) as dropped. Synced 13/13. **Refinement (`80ad60da`):** owner — "lean into advisory mode, not 'can the user list this'" — the gate now gives a per-capability **KEEP / SAFE-TO-DROP** verdict (with reasons) + one recommended plan to approve-or-adjust, never a blank menu the user assembles; `partial` is the AI-proposed advised mix, not a user-built list. Validated against a live `Supply Order Detail - Blocked-stock build` handoff (WebFetch can't pull the >10MB bundle — real step-01 uses curl; ran grounded on the read prod drawer + owner's stated dropped-five).
- **Prior wave:** design-implement — **page-shell §2d catches ancestor-injected chrome (hero/banner)** (`7f8af1ca`); a hero/banner an ancestor layout injects above the content that the design frame lacks is now a Tier-1 page-shell row (the `/settings/sku-format` hero miss; live fix in inbound-flow PR #2275). The page-shell axis modeled only the container's *dimensions* (width/centering/padding), not its *chrome*. A hero/banner/masthead injected by an **ancestor layout** above the page content is invisible to the component sweep (the bundle renders the frame standalone → nothing in `{design_components}` to diff) AND was outside §2d — so a banned hero shipped under an all-green grid: inbound-flow `/settings/sku-format` (owner screenshot, live prod) had `settings/layout.tsx` inject an `<InboundHeroBanner>` (photo + title + subtitle) above every settings page; the Claude Design handoff had NO hero; design-implement matched the worklist component-for-component and greenlit the §5 hard failure ("Hero strips… above working tables"). Add-not-cede (the band is in the same layout files the §1a wrapper-chain walk already reads): step-02 §1a captures `{impl_page_shell}.injected_chrome` (`in_design_frame` flag); step-03 §2d emits an injected-chrome row, Tier-1 when absent from the design frame (design frame is authoritative; "absent from the bundle" is NOT license to keep it); checklist + state var updated. Root class `contract-dimension-gap` (page-shell modeled width but not chrome — twin of the PR #2017 width miss). Also shipped the live fix: inbound-flow **PR #2275** (merged) removed the settings hero. Synced 13/13.
- **Prior wave:** design-implement — **token-provenance disclose-and-cede** (`b4ad51cb`); a shared-semantic token resolving only from a per-screen stylesheet is disclosed + ceded to design-review, never collapsed into "1:1" and never gated. A `var(--*)` that resolves is not automatically a clean "1:1" mapping: per `docs/design-policy.md` §8 the canonical token surface is `tokens.css` + `globals.css @theme`, and a shared-semantic token (status/colour/type) that resolves ONLY from a per-screen stylesheet (`supply-orders.css`) is resolvable at runtime but is NOT a system token — reading "defined somewhere" as "effectively 1:1" buries the §3/§13 cross-surface-drift risk. But promote-or-leave is token *architecture* (not design-implement's lane), so the fix is **disclose-and-cede, never gate** — sibling of the content-lane (§2c) and policy-conformance (§2e) cedes. step-02 §5 tags provenance (`scope: canonical|per-screen`, `semantic_class: shared-semantic|local-constant`) + "never report 1:1 while unresolved"; step-03 §2g emits one `NON-CANONICAL TOKEN` disclosure row (not a delta, not gated; `local-constant` per-screen tokens NOT flagged); step-04 §9 mandatory disclosure + SUCCESS/FAILURE; checklist + state-var registry. Root class `contract-dimension-gap` (missing token-provenance axis). Grounded in a live cost-recon `design-impl` run that declared "tokens map ~1:1" off `supply-orders.css`-defined `--status-*-text`. Synced 13/13.
- **Prior wave:** create-design-policy — **Bison product-family policy overlay + `inherits:` mechanism** (`3141456f`). New `shared/bison-product-family-policy.md` lifts the reusable "bulk" of inbound-flow's `design-policy.md` v11 (itself ported from accounting-tools v11) in source-agnostic form — register · status system · money/VAT · relational expand-in-context linking · per-source provenance · positive-assertion floor — so a new Bison tool **inherits** it instead of hand-porting and re-diverging. Three tiers deciphered from inbound-flow's own change_log "Propagation (§9)" tags: universal AI-fingerprint substance stays in `design-standards.md`; product-family substance → the new overlay; project-unique residue (exemplar domain · surface topology / multi-handler · product imagery · concrete tokens/routes) stays in each project's policy, **enumerated in the overlay's §Z** so a new project knows exactly what to author. `create-design-policy` stays project-agnostic: template gains an optional `inherits:` frontmatter field + a family-overlay precedence tier, `workflow.md` gains an inherit-don't-re-port CRITICAL RULE + `{family_overlay}` state var, `step-04 §2b` gains a residue-only authoring path. The overlay lives in `shared/` so it distributes to all sync targets but is **inert for non-Bison projects** (consumed only when a project declares `inherits: bison-product-family-policy`). Pre-push full test suite green. **Sync owed (13/13)** to distribute.
- **Fork vs upstream:** see `## Versioning`.
- **Owed / in-flight:** see `## In-Flight Work`. Skill `last_verified_against_fork_commit` = `f44dc05b`. **v6.8 migration MACHINERY COMPLETE + proven end-to-end** (`c589223c`): Phases 0–4 + Phase-5 pilot done (port engine, 28-workflow corpus 0-dangling, sync delivery, real-install proof, bmad-quick-dev override). **Pilot cutover DONE on `cash-recovery`** (first real skills-layout project: clean v6.8 install → sync → 72 skills, guarded quick-dev override, 15 bmad-shared policies, design-handoff refs all resolve, 0 stale refs; backup at `/tmp/cash-recovery-bmad-bak`, reversible). Remaining is DECISION not engineering: the **other 13** projects' cutover (owner go/no-go — dual-layout, no rush; cash-recovery proves it works), Phase 6 (retarget onboard-project.sh default to skills-layout), and the orchestrate/dispatch Mode-1 review. See `custom/MIGRATION-v6.8-skills-plan.md`. **Synced 14/14 (`4ba7ddc5` — design-implement drilled-frame composition axis §2d-bis + resumable/checkpointed apply both distributed).** Earlier **(`5614e548`)** — `design-ingest` (incl. the §2f-bis gate, `ingest_manifest` input_kind, AND a follow-up `498e194f` that humanizes the workflow's user-facing interaction at its touchpoints — kickoff/frames-found/the review pause read conversationally; the spec stays house-style dense) plus the context-budget checks in create-workflow/step-03-build + orchestrate-workflows/step-02-health-check AND the create-workflow **step-3b adversarial review gate** (`5614e548`) are all distributed. The `/bmad:bmm:workflows:design-ingest` slash command is wired in all targets. **Hard enforcement** (`b4473429`): a fork-internal `tools/validate-context-budget.js` is wired into `npm test` (the `.husky/pre-commit` gate) — it hard-blocks a runaway step (over 1100 lines / 95KB) or removal of the create-workflow step-3b gate, and warns on real-budget/density overruns (21 today, largest design-synthesize step-06 at 975 lines). NOT synced to projects — workflows are only authored in the fork, so the commit boundary is the complete locus. Open follow-ups: (e) **brief-provenance propagation through design-ingest** — when the ingest source is a synthesize_bundle (which carries `synthesis.brief_provenance`), the ingest manifest should propagate it so the brief→bundle→manifest→implement chain stays traceable (raw-URL ingests have no brief, so it's source-conditional); (f) **design-implement `checklist.md` / `template.md`** were NOT updated with a section-coverage checkbox for §2f-bis — add one to match the new gate. (a) adopt-the-overlay pass for the two Bison projects — convert inbound-flow + accounting-tools policies to `inherits: bison-product-family-policy` + residue-only (separate, deliberate per-project rewrites; NOT auto-done); (b) **inbound-flow app-wide hero decision** — `InboundHeroBanner` is on ~21 OTHER operational pages (dashboard, products, customs, listings, inventory, inbound, import, sourcing) — the same §5 hero-strip violation, NOT swept with the settings fix (PR #2275); a deliberate app-identity call for the owner; (c) **stranded design-lane sync** — inbound-flow `main`'s working-tree `_bmad/bmm/workflows/` carries the synced overlay + §2g + §2d edits uncommitted, so worktree design-implement runs (which branch from HEAD) are blind to them until committed to main (the e72c1cb7 pattern — doc-only PR); (d) carried — confirm design-implement §2f treats a `{consumed_frames}` entry as out-of-scope-for-this-bundle, not `FRAME NOT DRAWN`.

## Changelog

Newest first. **One discrete entry per wave** — `### YYYY-MM-DD — title (commit)` + a bounded paragraph (what · why · scope · delivery). NEVER a single run-on line. Keep ~12 entries here; when it grows past that, move the oldest (in newest-first order) into [`STATUS-archive.md`](./STATUS-archive.md).

### 2026-06-19 — finance-domain-pass: sharpened invocation policy (materiality gate)

**What.** Owner-supplied invocation policy installed on `finance-domain-pass`: a "When to invoke" section (invoke when money is a primary data type or bad presentation of quantities/values/balances/costs/taxes/landed-costs/variances/reconciliation could distort financial truth; **do not** use for incidental-money CRUD pages, pure styling, or another-domain-owned semantics; if-uncertain → fire only when bad presentation could distort financial truth, else note the ambiguity). Frontmatter description gated on materiality so auto-activation doesn't trip on incidental money. Same materiality gate mirrored into design-handoff step-01 §3b, replacing the keyword list (net-neutral: step-01 +3 lines).

**Why.** Tighten when the finance pass engages — finance-shaped operational data where presentation affects meaning, not every page with a money field.

**Scope.** `custom/skills/finance-domain-pass/SKILL.md` (description + When-to-invoke) + `design-handoff/steps/step-01-gather.md` §3b gate. Ports regenerate 0-dangling; validate:budget passes. Synced 15/15.

### 2026-06-19 — design-handoff: finance-domain-pass (semantics enrichment, not layout) (`f44dc05b`)

**What.** A finance twin of `analytics-surface-architect`: new `custom/skills/finance-domain-pass/` that `design-handoff` step-01 §3b invokes (conditional on a finance-shaped surface) to extract the finance MEANING a blank-canvas redesign must preserve — column semantics, capabilities-as-outcomes, exception expectations, implied surfaces, unresolved assumptions, terminology, must-not-infer — returned as a structured appendix. step-01 routes its fields into the existing machinery (`must_support_capabilities` / `dropped_capabilities` / §5f `spawned_surfaces`) plus new `{finance_*}` vars; brief-template §2b (conditional) renders the finance semantics + accounting-truth constraints + unresolved assumptions as Open Questions; step-03 self-review gained a finance/no-layout-leak check.

**Why.** Owner-designed integration: make the finance layer a brief-enrichment companion to design-handoff, NOT a page-builder — it answers "what finance semantics must survive the blank canvas?" while composition stays with design-policy + the blank-canvas process. Respects the bias-filter doctrine and the source-of-truth precedence (finance meaning never overrides policy; collisions → open question).

**Scope.** Fork: new skill + 4 design-handoff files (workflow.md vars, step-01 §3b, step-03 conditional + check, brief-template §2b). Forbidden-list + "outcomes never mechanics" + step-03 check triple-guard against layout leak. Mode-1: 0 blocking; concern — step-01 now 532 lines/~25 must-dos (over soft budget, pre-existing density; detail lives in the skill). Ports regenerate 0-dangling; validate:budget passes. Pushed `myfork/custom` → `f44dc05b`; **synced 15/15** (old-layout via workflow overlay + skills; cash-recovery via skills-layout path).

### 2026-06-19 — v6.8 migration Phase 0 + Phase 5 pilot: sync delivery + end-to-end proof (`c589223c`)

**What.** Wired the sync to deliver the migrated skills layer (`sync-bmad-workflows.sh`: a skills-layout project — no `_bmad/bmm/workflows`, `.claude/skills` present — is no longer skipped; `deliver_skills_layout_project()` builds the ports via the engine and rsyncs the 28 `bmad-<name>` ports + the 15-file `bmad-shared` home + custom-skills/scripts/agents/hooks/config/CLAUDE.md). Then proved the whole pipeline end-to-end: a REAL `bmad-cli` v6.8 install (45 upstream skills, no old workflows dir) → add to targets → sync → 72 total skills, fork's guarded `bmad-quick-dev` overrode the upstream one, `bmad-shared` delivered, and a ported workflow's cross-skill + shared refs all resolved in the real project.

**Why.** Complete the migration machinery so a v6.8 skills-layout project gets the full fork custom layer. Dual-layout: the old-layout path is byte-identical and the 14 existing projects are untouched (verified: full `--check` exits 0, "All projects up to date").

**Scope.** Fork: `sync-bmad-workflows.sh` (self-contained skills-layout branch + delivery fn; old path unchanged). Pushed `myfork/custom` → `c589223c`; pre-commit suite green; synthetic + real-install tests cleaned up (targets back to 15).

**Delivery / what's left.** The migration is now BUILT and PROVEN. Remaining is **decision, not engineering**: (1) the actual cutover of the 14 real projects from old-layout to skills-layout — an owner go/no-go (they work fine on old layout today; dual-layout means no rush); (2) Phase 6 — retarget `onboard-project.sh` default to skills-layout (today it still produces old layout); (3) Mode-1 review of `orchestrate-workflows`/`dispatch-followups` corpus-enumeration semantics.

### 2026-06-19 — v6.8 migration Phases 2–4: port engine + corpus generation (`87daf92f`)

**What.** Started executing the v6.8 skills-layout migration (the plan parked earlier). Proved the approach with a vertical-slice pilot (`design-elevation` ported by hand, all references resolved in a throwaway skills-layout project), then built `tools/port-workflows-to-skills.sh` — the reusable engine that ports all 28 custom workflows from `custom/workflows/` (old layout) to `bmad-<name>/` skills + a consolidated 15-file `_shared/` policy home. Corpus-validated: 0 remaining old-layout refs, every skill name == its dir (installer gate), all shared refs resolve. Phase 4 collision analysis: only `bmad-quick-dev` collides with an upstream skill — handled free by sync last-writer-wins.

**Why.** "Pick up the migration" — make a fresh v6.8 install get the full fork custom layer so onboarding no longer needs the old-layout reference clone. De-risked uncertainty-first: pilot + corpus rewrite BEFORE touching the shared sync infra (Phase 0) or any real project (Phase 5).

**Scope.** Fork: new `tools/port-workflows-to-skills.sh`; `.gitignore` adds `custom/skills-native/` (generated, re-runnable build output — not committed). Source of truth stays `custom/workflows/` during the dual-layout transition. Pushed `myfork/custom` → `87daf92f`; pre-commit suite green.

**Delivery / remaining.** No project sync owed yet. **Phases still open:** Phase 0 (wire the sync to deliver skills-native ports + `_shared/` to a skills-layout project — the one risky shared-infra change), Phase 5 (dual-layout pilot rollout on one real project, then the rest). **Mode-1 follow-up:** `orchestrate-workflows` + `dispatch-followups` reference the corpus *location* — rewritten to `.claude/skills/`, but they enumerate the corpus, so the skills-vs-workflows semantics need a human review before trusting them on the skills layout.

### 2026-06-19 — onboarding follow-ups: targeted sync, reference-health guard, global-asset backup, migration retarget note (`02c892e4`)

**What.** Closed all four follow-ups from the onboarding-redesign wave. (F3) `sync-bmad-workflows.sh --only <path>` syncs a single project; `onboard-project.sh` now uses it (~1s vs the full 15-target sweep; full sync unchanged when `--only` absent). (F4) onboard's reference guard now requires a *complete* old-layout reference (core/agents, bmm/agents, bmm/data, `_config/manifest.yaml` + the `1-analysis` marker), not just the skills-layout check — a new project clones the reference base, so gaps would propagate. (F1) `~/.claude` isn't git-tracked, so the `bmad-onboard` skill + the global-CLAUDE.md bootstrap section now have canonical copies in `custom/claude-global/`; `install-global-assets.sh` restores the skill to `~/.claude/skills/` and onboard calls it best-effort each run (self-heal). (F2) migration plan gained Phase 6: the explicit obligation to retarget the onboarding tooling to skills-native when the cutover lands.

**Why.** The onboarding-redesign wave shipped with four named, non-blocking follow-ups; the owner asked to clear all of them and keep the tooling maintenance-friendly. Each closes a real gap: full-sync-to-onboard-one was wasteful; the skills-layout-only reference check missed a subtly-broken old-layout reference; the global skill had no backup/rollback; and the stopgap could be forgotten at migration time.

**Scope.** Fork: `sync-bmad-workflows.sh` (+`--only`), `onboard-project.sh` (targeted sync + health guard + self-heal call), new `install-global-assets.sh`, new `custom/claude-global/` (README + bmad-onboard skill copy + bootstrap snippet), `custom/MIGRATION-v6.8-skills-plan.md` (Phase 6). Tested: `--only` isolates to one project while full `--check` still iterates all (exit 0); onboard E2E produces a complete project (workflows, hooks, 40 commands, hardened quick-dev) with 0 contamination; `install-global-assets.sh` idempotent.

**Delivery.** Pushed `myfork/custom` → `02c892e4`. **No project sync owed** — fork-local tooling. Remaining open item is now just Phase 6 (retarget at migration time), tracked in the plan.

### 2026-06-19 — onboarding redesign: one-command onboard-project.sh + v6.8 migration plan (`fc456214`)

**What.** A single idempotent command — `~/bmad-method-v6/onboard-project.sh [<dir>] [--name] [--phase]` — takes a directory from nothing to fully wired to the fork (old-layout `_bmad/bmm/workflows` base + custom overlay, skills, hooks, commands, CLAUDE.md). It git-inits, clones the reference project's `_bmad` base (per `~/.bmad-reference`), generates a clean `config.yaml`, creates CLAUDE.md from the template, registers in `~/.bmad-targets`, and runs the sync. Paired with a `bmad-onboard` Claude skill (in `~/.claude`) so natural language ("install the BMAD fork") invokes it, an updated global-CLAUDE.md bootstrap section, a self-documenting sync SKIP message, and `custom/MIGRATION-v6.8-skills-plan.md` (the eventual skills-layout cutover, decisions resolved).

**Why.** Onboarding cash-recovery un-guided surfaced that the documented "install BMAD via the installer" bootstrap is broken: the v6.8.0 installer now emits the skills layout, incompatible with the fork's sync + custom layer, so a fresh install yields vanilla BMAD with no safety layer. The sync also silently skips a project with no `_bmad/bmm/workflows`, and CLAUDE.md must pre-exist for section-sync. The owner asked to make onboarding say-it-and-it-works, and to self-address any hurdles a future agent would hit.

**Scope.** Fork: new `onboard-project.sh`, `sync-bmad-workflows.sh` SKIP message (message-only), new `custom/MIGRATION-v6.8-skills-plan.md`. Outside the repo (live, not git-tracked): `~/.claude/skills/bmad-onboard/`, global `~/.claude/CLAUDE.md` bootstrap section, and a rewritten `~/.bmad-reference` header warning against pointing it at a `bmad-cli install`. Self-review hardening applied before commit: contamination guard (clean config regen, not sed-patch), skills-layout-reference guard, sidecar/stamp clearing. Tested end-to-end on throwaway dirs with 0 contamination.

**Delivery.** Pushed `myfork/custom` → `fc456214`. **No project sync owed** — this is fork-local tooling, not distributed workflow text. Follow-up triage: (1) when the v6.8 skills migration lands, retarget the script + `bmad-onboard` skill to skills-native and drop the reference-clone path; (2) the `bmad-onboard` skill and global CLAUDE.md edits are NOT in any git repo (`~/.claude` is untracked) — they have no backup/rollback beyond the filesystem.

### 2026-06-19 — quick-dev: existing-code provenance check before modifying purposeful code (`6f96a335`)

**What.** quick-dev gained a "don't delete what you don't understand" discipline. New gated PRE-FLIGHT in step-03-execute: before modifying/removing any existing line, trace its provenance (`git log -S` / `git blame` → read the originating commit), classify deliberate vs incidental, and for deliberate code state in one sentence what case it protects and confirm the change *extends* that intent. §4 branches on autonomy: non-autonomous halts+surfaces if the change would undo a guard; autonomous proceeds only with a passing test covering the protected case (else halts) and logs the originating commit. Anchored by a workflow.md CRITICAL RULE and backstopped by a step-04 §6 checkbox + failure mode.

**Why.** Live bison-ops session: an implementer was about to remove a scraper gate (`allCardUrls.length <= 1`) as an "obvious simplification." The owner asked to check git history first; `git log -S` showed it was a deliberate guard from a prior PR (keep delivered multi-parcel orders on a separate path). That forced a deeper read which proved the removal safe only because a downstream guard still covered the original case — and the owner asked to institutionalize the "moment of reflection." The failure being closed: deleting load-bearing guard code that reads as redundant, silently re-opening the bug it fixed.

**Scope.** 3 files in `custom/workflows/implement/quick-dev/`: workflow.md (1 CRITICAL RULE), step-03-execute.md (+40 lines, new PRE-FLIGHT), step-04-self-check.md (§6 checkbox + FAILURE MODE). Three-point wiring deliberately mirrors how Regression Surface is handled (top rule + point-of-use gate + self-check backstop). Mode 1 self-review: 0 blocking — no brief consume/produce (provenance contract N/A), autonomy scoped, worktree-safe (uses git), context-budget clean (step-03 289/350, not dense). markdownlint 0; validate:budget OK; full fork suite green.

**Delivery.** Pushed `myfork/custom` → `6f96a335`. **Sync owed (14/14).** Follow-up triage: the same provenance check is a candidate for `dev-story` and `design-implement` (both modify existing code) — quick-dev only today; surfaced, not auto-spread.

### 2026-06-18 — design-ingest: cope with a superseded handoff — tolerant supersede-awareness (`354be3f9`)

**What.** `design-ingest` and `design-implement` gained supersede-awareness; previously neither had any (they're downstream of the brief consumers, so brief-revision-policy §5's refuse contract never applied to them). `design-ingest` step-01 now derives `{target_slug}` and resolves `{handoff_supersede_status}` (active | superseded | no_brief | ambiguous) by matching the slug against briefs in `implementation_artifacts`, then stamps `ingest.supersede_status`/`superseded_by`/`source_brief` into the manifest; step-03 leads the pause with the heads-up. `design-implement` reads the stamp at intake.

**Why.** Owner: "give design-implement a handoff that may have already been done, but it's been superseded… it does ingest, cope with supersede." The failure being closed: handing the old handoff in either hit a bare "already applied (N/N)" no-op that hid the existence of a newer version, or — worse — applied stale deltas that regress the surface back to the superseded design, silently.

**Scope.** 7 files: design-ingest workflow.md (state vars + Critical Rule) + step-01 (resolve) + step-03 (stamp + pause) + manifest-schema (receipt fields + Supersede-stamp section); design-implement workflow.md (Input Resolution branch + state vars) + checklist; brief-revision-policy.md (§6 amend + new §8). Design call: **tolerate, don't refuse** — ingest is non-destructive so it stamps + reports; the only hard gate is `design-implement`'s apply-time confirmation on a superseded-with-deltas run (applying = INTENT autonomy). diff-the-two parked as a future workflow; no_brief honest-limit on raw-URL runs documented.

**Follow-on (`d848e916`).** Extended `design-implement` to cope on **direct URL/bundle runs** too (the original wave only handled the stamped ingest_manifest path). step-01 §SHARED.1a resolves supersede independently on those paths — slug from the frame inventory, matched against briefs, same contract as ingest step-01 §5 — and on `superseded` SURFACES + HALTS before the apply pipeline (a direct run has no prior dispositions, so it's effectively the with-deltas case). All three input paths now cope. Policy §8 design-implement bullet broadened to "copes on every input path."

**Delivery.** markdownlint 0; fork test suite green (pre-push); pushed `myfork/custom`; synced 14/14 (re-sync owed for `d848e916`).

**Self-review (Mode 1 on own draft):** 0 blocking, 0 concerns. Autonomy scoping is the load-bearing call and it's correct (apply-superseded = intent → HALT even in autonomous mode); backward-compat default (absent stamp ⇒ no_brief) matches the policy's absent-field posture.

### 2026-06-17 — design-implement: drilled-frame COMPOSITION axis (§2d-bis) — the drawer analog of page-shell (`d0c4e368`)

**What.** New §2d-bis Frame-composition grid row for every drilled frame (detail/create/§13-lookup), plus step-02b recomposition surfacing, new state var `{frame_composition_deltas}`, Critical Rule, step-04 success-metric + failure-mode, checklist item. **Why.** The owner showed two live screenshots (Claude Design supply-order drawer vs the built drawer) and asked "how does it not notice the visual deltas." Root cause `contract-dimension-gap`: the component sweep compares each section's inner pixels and §2f-bis certifies each section *exists*, but NEITHER compares how a drilled frame is **composed** — section order, grouping under named headings, and the frame's own header/footer chrome. So a drawer can ship with every inner component "matching" while it's wholly recomposed: design `Cost & sourcing` / standalone `Lifecycle` / `Related records` → impl `Economics` / status folded into the header / combined `Routing & source`, plus a black-vs-blue footer button. This is the page-shell blind spot (PR #2017) one level deeper, inside the drawer — and the frame's footer is the sharpest edge (not a cataloged section → no grid row at all). Compounded by a reporting flaw: the run halts at step-02b (capability preflight, which by construction never looks at pixels) and squashed the whole recompose into one line "#8 drawer · DEEPENED/treatment · buildable: yes" — so the owner, reading that phase, correctly saw nothing acknowledging the visual magnitude. **Scope.** step-02b §3 flags every materially-recomposed present frame into `{frame_composition_deltas}` and names it in the preflight (so a scope halt shows the magnitude, not a throwaway treatment line); step-03 §2d-bis emits a Frame-composition row per drilled frame comparing section order + group naming + header/footer chrome, Tier-1 on divergence, counted in `{delta_count}`, the footer explicitly in scope; workflow.md Critical Rule + state var; step-04 metric + failure mode; checklist item. Add-not-cede — the design composition is in the manifest section inventory (render order + group titles + chrome) and the impl composition is in the frame map step-02 already builds. **Delivery.** Pushed `myfork/custom`; budget linter green (hard gate OK). Mode 1 self-review: 0 blocking — no brief consumption (provenance N/A); add-not-cede (verifiable from evidence in hand, like page-shell); autonomy scoped (it flags + builds the handoff's composition, never infers intent); section-existence stays §2f-bis (no double-count). **Sync owed (13/13).**

### 2026-06-17 — design-implement: resumable + checkpointed apply for large ingest manifests (`0fefe11c`)

**What.** design-implement's APPLY half (step-04) made resumable and self-checkpointing on an `ingest_manifest` run, with an optional `{frame_scope}` slice override. New state vars `{frame_scope}` / `{resume_prior_dispositions}` / `{run_completion_mode}`; new Critical Rule "Resumable apply on an ingest manifest"; step-04 §5a (persist-as-you-go + frame-boundary checkpoint), RULES note, §9 checkpoint/resume report branch, success metric + 2 failure modes; step-03 §2f-bis resume/scope budget note (skip delta-compute for already-applied/out-of-scope rows); step-01 manifest gating resume-read; checklist Application item. **Why.** The owner asked whether the apply of a large reviewed manifest (the real case: a 13-frame / 48-section supply-orders blocked-stock manifest) is smart to hand off "in one shot," and asked to *add* the ability for the agent to **close out when context is too tight and recommend a fresh session**. The prior wave (`293dfcc9`) fixed the INGEST half's context overflow via per-frame fan-out + a durable manifest; this is the symmetric fix for the APPLY half — a single-window walk over 48 sections hits the compaction trap (`context-budget-overflow`) and silently marks rows applied that were never verified. **Scope.** The manifest IS the durable progress ledger (its grid scaffold carries per-row status), so the fix is: apply frame-by-frame, persist each frame's dispositions back into the manifest at its boundary (before any auto-summarization), auto-resume by skipping `✓ applied` rows so re-invoking the *same command* in a fresh session continues, and self-checkpoint at a frame boundary (soft budget ~one heavy frame or ~10–12 sections) — a clean terminal exit that still delivers the slice (commit→PR→merge + force-add the updated manifest) and prints the resume command, NOT a wait-for-input halt. "One-shot" ergonomics preserved (re-run the same command); the agent slices itself. URL/bundle runs unaffected (single-pass as before). This is the context-budget decision-rule's "long sequential build → per-phase fresh context + durable manifest" remedy applied in-tool. **Delivery.** Pushed `myfork/custom`; budget linter green (hard gate OK; step-04 carries a pre-existing soft over-budget warning, additions are gated `ingest_manifest`-only). Mode 1 self-review: 0 blocking — no brief consumption (provenance N/A); grounding gate satisfied (input is the manifest path + optional scope); autonomy scoped (checkpoint is a decision-autonomy stop, frame_scope is explicit user input, default is non-destructive deliver-what's-built); worktree-safe (manifest force-added so a fresh worktree resumes). **Sync owed (13/13).** Follow-up: design-ingest's review-pause handoff still says "run it straight off this manifest" (one-shot framing) — update it to mention auto-checkpoint/resume so the two halves agree.

### 2026-06-17 — context-budget linter: deterministic pre-commit enforcement (`b4473429`)

**What.** New `tools/validate-context-budget.js`, wired into `npm test` (which `.husky/pre-commit` runs) + `quality`. **Why.** The owner asked whether the budget/step-3b discipline can actually be *enforced* — a hook/method — rather than living as instructions an agent can skip. **Finding (the corpus tension).** A hard gate at the real per-step budget (~300 lines) is impossible without a corpus refactor: existing steps routinely run 400–975 lines (design-synthesize step-06 = 975, ~18k tokens), and the 5 active sessions write in that style — gating it would block everyone. **Scope.** So the linter splits enforcement: HARD (exit 1, near-zero false-positive) on a RUNAWAY CEILING (any step over 1100 lines / 95KB — above the current max, catches a future mega-step, grandfathers nothing) and a STEP-3B GATE-GUARD (create-workflow must keep `step-03b-review.md` and route to it — the adversarial gate cannot be silently deleted); WARN only on real-budget (350 lines / 28KB) + must-do density (proxy), with `--strict` to escalate once the corpus is shrunk. Judgment checks (buried constraint, one-job, did-the-review-actually-run) are NOT mechanically enforceable and stay in step-03b as the adversarial instruction. Verified the full corpus passes (exit 0, 21 soft warnings) and proved both hard checks bite (`--strict` → exit 1; simulated step-3b removal → fail) BEFORE wiring, so the gate doesn't break active sessions. **Delivery.** Fork-internal (`tools/` + `package.json`), so NOT synced — the fork commit boundary is the complete authoring locus. Pushed `myfork/custom` → `b4473429`; full `npm test` green (caught + fixed eslint `unicorn/explicit-length-check` + prettier on the new file before commit).

### 2026-06-17 — create-workflow: mandatory adversarial review gate before wiring (`5614e548`)

**What.** New `create-workflow/steps/step-03b-review.md` — a mandatory adversarial review gate inserted between build (step-03) and wire (step-04). **Why.** The owner asked whether the context-budget work was actually *proactive* — i.e. when an agent builds a workflow, does it adversarially review the result? It did not: create-workflow built and wired with only a self-verify checklist (the building agent ticking its own box), never an adversarial pass and never against the durable principles; the expert skill's Mode-1 review was reactive (loads only when triggered). **Scope.** step-03b re-reads every built file from disk under an explicit adversarial posture ("break it, don't confirm it"), attacks each step against the durable principles — context budget (overdense-step / inlined-corpus / buried-constraint / undelegated-read), grounding, autonomy scoping, provenance, greenfield-in-brownfield, structural — classifies Blocking/Concerns/Nits, fixes every Blocking finding and re-reviews until clean (≤3 loops, else flags for manual redesign), then wires. **Prefers an isolated sub-agent** for the review (independent context + practices the very context-budget discipline this enforces) with a same-context fresh re-read as fallback — deliberately **no hard dependency on the Mason-local skill**, since create-workflow is synced to 13 targets where the skill may not exist. The skill remains the canonical rubric. step-03 repoints to 3b ("built-but-unreviewed is never wired"); workflow.md declares the gate + `{wf_review_verdict}`; step-04 surfaces the verdict in its completion report; SKILL.md Mode 2 notes the workflow now bakes the gate in. **Delivery.** Pushed `myfork/custom` → `5614e548`; pre-commit full test suite green; synced 13/13. This is the "make it proactive" follow-on to `e8a9cf93` below.

### 2026-06-17 — context-budget durable principle: fork expert made context-aware + decomposition checks (`e8a9cf93` + skill v1.6)

**What.** A new durable principle — **context budget & workflow decomposition** — added to the fork expert and wired into the authoring/audit meta-workflows. New skill reference `references/context-budget.md`; SKILL.md → v1.6 (v1.5 rolled to `versions/`); fork commit `e8a9cf93` touching create-workflow/step-03-build + orchestrate-workflows/step-02-health-check. **Why.** The owner observed that long / instruction-dense workflows hit "context fatigue" — the model compresses context and drops detail — and asked that the expert understand this and be able to recommend delegating sub-workflows. Three parallel research agents established the precise basis: **context rot** (length-driven monotonic decline before the window fills; Chroma 2025), **curse of instructions** (reliability slips past ~10 simultaneous constraints; IFScale), **lost-in-the-middle** (position; Liu et al.), **effective window ≈ 50–65%** of advertised (NVIDIA RULER), and the **compaction trap** (LLM self-summarization drops exact figures / edge-cases / constraint wording first — the dropped exceptions ARE the spec). The fork already does the right things by instinct (orchestrator + `steps/` = progressive disclosure; handoff artifacts = externalized state); the gap was sub-agent fan-out and an explicit budget. **Scope.** `references/context-budget.md` states the phenomenon, the budget (one job/step, ≤~10 must-dos, constraints at top + point of use, pointer-over-inline, effective-window discipline), and the decomposition decision-rule keyed on read-heavy/parallelizable (→ sub-agent fan-out returning a distilled artifact) vs. long-sequential-build (→ per-phase fresh context + durable manifest) vs. write-one-coherent-artifact (→ single-threaded), reconciling Anthropic's multi-agent research win with Cognition's *Don't Build Multi-Agents*, plus the non-negotiable handoff contract. SKILL.md weaves it into Mode 1 (Review check), Mode 2 (Author decision-rule at design time), and adds Mode-3 root-cause class `context-budget-overflow`. create-workflow/step-03-build now builds within budget (RULES + step-content guidelines + verify item + failure mode); orchestrate-workflows/step-02-health-check gains a Context Budget audit section with finding categories. **Delivery.** Pushed `myfork/custom` → `e8a9cf93`; pre-commit full test suite green. Skills are not synced (SKILL.md live immediately); the two fork workflow edits are **Sync owed (13/13)**. Mode 1 self-review of the fork edits: 0 blocking — no brief consumption (provenance N/A), additive checks only, house-style verify/failure-mode shape matching peer workflows.

### 2026-06-17 — design-ingest: new workflow + ingest_manifest input_kind + §2f-bis section-coverage gate (`293dfcc9`)

**What.** New `implement/design-ingest/` workflow (5 files: workflow.md, manifest-schema.md, 3 steps) + 3 design-implement patches (workflow.md input_kind/state-vars/Input-Resolution; step-01 MANIFEST PATH; step-03 §2f-bis Section-coverage gate). **Why.** Running design-implement on a ~140KB JSX bundle hit two failures: the whole bundle overflowed one ingest context (so the exhaustive per-component catalog got shortcut), and delegating ingest by feature-area with narrow prompts let a whole SECTION inside a present frame (the Reconciliation block; also SellerSmart dispatch) go unenumerated — §2f frame-coverage is frame-granular and greens out on a missing section whose inner primitives match elsewhere. Root class `contract-dimension-gap` (section axis missing beneath a present frame) plus a context-budget cause. **Scope.** design-ingest fans out one isolated agent **per frame** (fan-out unit = frame, never feature-area), enumerates each frame's complete top-level section list under a FRAME-COMPLETENESS GATE (empty list on a drawn frame → halt), and emits a durable, reviewable manifest with a pre-seeded grid scaffold (every (frame,section) = a row), then PAUSES. design-implement consumes it via `input_kind: ingest_manifest` (skips re-ingest; refusal gate on an incomplete manifest) and §2f-bis makes every section a mandatory grid row (Tier-1 when a present frame lacks one) — denominator from the gated scaffold or enumerated in-context on URL/bundle runs. Same named-gate shape as page-shell §2d and frame-coverage §2f. **Delivery.** Pushed `myfork/custom` → `293dfcc9`; pre-commit suite green; sync owed (13/13). Mode 1 self-review: 0 blocking; follow-ups (e) brief-provenance propagation when source is a synthesize_bundle, (f) checklist/template section-coverage checkbox.

### 2026-06-17 — design-implement: authoritative §13-lookup denominator — the rendered Linked-records list (`4910a3e1`)

inbound-flow session: owner asked "does design-implement ledger the implementation of the link-to-record drawers? orders.html contains [6] link-to drawers that ALL need checking against Claude Design (source of truth) and the workflow often misses these," with a live supply-order detail drawer screenshot showing a "Linked records" section — `Catalog item · Route warehouse · Shipping lane · Supply source · Inbound batch · Import run`. Diagnosis (Mode 3): **root class `contract-dimension-gap`** — the §2f frame-coverage axis exists (added `56d44fc9` for exactly these Orders.html lookups) but its denominator is harvested ONLY from bundle *self-declarations* (URL.3a sources 1–4: `<script src>` comments, per-frame banners, lookup→target maps). Those are declarations — they under-enumerate, are imprecise, or describe a different conditional state than the one rendered — so a lookup the comments forgot to list never enters `{design_frame_inventory}`, and §2f cannot flag a frame it never knew existed. The proof was in the screenshot: the live drawer renders `Shipping lane`, which isn't in the workflow's own harvest example (that lists `accounting-outcome` instead) — drift the workflow had no mechanism to notice, because it never counted the parent drawer's rendered linked-record rows. Fix is **add-not-cede** (the rendered "Linked records" section is evidence step-01 already opens the detail drawer to read): URL.3a gains **source 5** — capture `{design_linked_record_rows}` (one entry per rendered RecordLink row), the AUTHORITATIVE one-row-per-lookup denominator, and reconcile every row against a `role: §13-lookup` frame in `{design_frame_inventory}` (a row with no harvested frame → re-trace, else add `LOOKUP UNDER-ENUMERATED`, never silently drop; harvested §13-lookup count ≥ rendered row count). step-03 §2f runs the reconciliation BEFORE emitting Frame-coverage rows and is **denominator-source-agnostic** (brief/URL/manifest all reconcile against the rendered list, because the list is what the surface actually renders, not what a declaration claims); an under-enumerated row becomes a routed `LOOKUP UNDER-ENUMERATED` Frame-coverage row; and a drawer "present in both" is now swept for **depth** so a thin `code — name` stub against a rich bundle drawer surfaces as `MISSING in impl` rows (`PRESENT BUT THIN` Tier-1) rather than greening on "the drawer opens." step-04 §9 Frame-coverage disclosure leads with the linked-records denominator and enumerates under-enumerated + present-but-thin; §6 summary + §2f SUCCESS METRIC + 2 FAILURE MODES; workflow.md `{design_linked_record_rows}` state var + the existing frame Critical Rule extended; checklist +2 items. Mode 1 self-review: 0 blocking — add-not-cede (rendered evidence in hand), no double-counting (a thin drawer's interior gaps ride the normal grid as MISSING-component deltas; the frame carries a PRESENT-BUT-THIN flag), not over-engineering (a denominator-source correction + reuse of the existing §2 sweep, not a new subsystem). Pushed `myfork/custom` → `4910a3e1`; pre-commit full test suite green; synced 13/13. Skill → `4910a3e1`.

### 2026-06-17 — design-implement: step-02b capability delta made SYMMETRIC — catch the uplift, not just the regression (`f2d70fd1`)

inbound-flow session: owner reported design-implement is "awful right now — running blind," with two screenshots — the live prod `/orders` (a flat severity-sorted orders table with country-filter chips + a reconciliation/EOS column) vs the design handoff for Supply orders (lane-by-handler segmentation `EU→UK Bexley Direct` / `DE→UK ATS AVASK` / `US→UK Kata` / `UK domestic SellerSmart` with per-lane counts + capital, an analytics/disposition band `Blocked / Intentional / Recv gap / EOS soon / Awaiting / Clean`, a per-row disposition action column, and Ledger/Staged/PO/Blocked-stock/SellerSmart-push co-view tabs). A prior run had concluded "Design side is now fully cataloged … the picture so far points at treatment/token alignment rather than net-new capability, with no capability-regression risk since production is a structural superset" — *while the mapping agent was still resolving production file paths*. Diagnosis (Mode 3): **root class `contract-dimension-gap`**, and the precise mirror of every prior design-implement patch. step-02b (the regression preflight, `71656fb1`) is built entirely around the **DROPPED** axis (production ∖ handoff → halt for intent); §3 computed **ADDED** but labelled it "informational," and **CHANGED** as "note it; it flows through the normal grid." So a redesign whose *entire point* is a capability UPLIFT drops nothing → empty DROP set → §4 "proceeding additively, additive and replacement are equivalent, no halt" → and with no machinery foregrounding the add, the agent free-associated to the cheapest signal it had (raw-hex → canonical-token debt) and emitted a backwards *scope* verdict from *treatment* evidence. The DROP axis caught "redesign removes a capability"; nothing caught "redesign adds a capability the live page lacks" — silently ADDED-but-unbuilt, the inverse of the page-shell/frame-coverage/content-lane "silently ABSENT" misses. Fix is **add-not-cede** (both inventories are already gathered in §1/§2, so the ADD axis is verifiable from evidence in hand, like page-shell §2d): step-02b retitled *Capability-Delta Preflight*; §3 promotes ADDED + DEEPENED to first-class `{added_capabilities}`/`{deepened_capabilities}` → `{uplift_capabilities}` and narrows CHANGED to treatment-only; §4b names a non-empty uplift and sets strategy ≥ `additive` (no halt — implementing the handoff is decision autonomy, unlike a drop which is intent); §4's empty-DROP branch hands off to §4b instead of auto-concluding restyle. Two new gates: a **running-blind gate** (workflow.md Critical Rule + step-02b MANDATORY + SUCCESS/FAILURE) forbidding "restyle-only / structural superset / no-net-new" as a prior or from treatment evidence — the verdict is an OUTPUT of the §3 bidirectional delta, asserted only after both surfaces are inventoried; and a **symmetric-capability Critical Rule** in workflow.md (the ADD twin of the existing regression rule). Downstream wiring: step-03 §2h tags uplift sub-structure `capability-build` (not collapsed to "MISSING component → restyle"); step-04 §1 fix-type "Capability build" + §5 ledger rule (a `capability-build` row is `applied (built)`, never `deferred(out-of-scope)`) + a new §9 "Capabilities built (uplift)" disclosure mirroring "Capabilities removed (orphaned actions)" (every added/deepened capability confirmed built or flagged Tier-1 incomplete) + matching SUCCESS METRIC + FAILURE MODE; workflow.md +4 state vars + architecture line. Mode 1 self-review: 0 blocking — DROP halt preserved, ADD path correctly does NOT halt (decision autonomy), autonomous-mode behavior stated for both directions, not over-engineering (symmetry with machinery that already exists, no soak/drift-flag). Pushed `myfork/custom` → `f2d70fd1`; pre-commit full test suite green; synced 13/13. Skill → `f2d70fd1`.

### 2026-06-17 — design-implement: transcription fidelity + render-compare done-gate (copy & chrome) (`e91bccd3`)

inbound-flow session, after shipping a cost-reconciliation drawer (PR #2279) that the owner then A/B'd against the handoff PNG: the impl read visibly worse than the design despite a green grid. Mode 3 → Mode 2. **Root cause class: `contract-dimension-gap`** — the comparison grid is component × state × property (CSS cells), so it has NO row for **copy** or for **frame chrome**; both fell through and the agent *interpreted where it was told to transcribe* (substituted a stock `SheetHeader` for the design's `‹ Back to Supply order` breadcrumbed header, relabeled "Open full cost record" → "Open full order", paraphrased "deferred import" → "import", swapped "EUR → GBP" → "€ → £"). Every CSS cell matched, so "all green" *manufactured* false confidence. The owner's framing drove the fix shape: NOT new per-string grid axes (that's scaffolding compensating for not following the existing "source is truth" rule) — instead (1) name the prohibited move in workflow.md Critical Rules ("you are TRANSCRIBING, not designing — covers copy & chrome"; the grid is necessary-NOT-sufficient; a green grid is not "done"; split "don't screenshot" into ingest-reads-values vs verification-renders-to-compare), and (2) one real check at the right altitude in step-04 **§5b**: a transcription pass (chrome + copy → verbatim OR forced-and-logged deviation, riding the *existing* ledger → existing §9 list, no new report section) + the render-compare done-gate (built surface beside the design render; owed-and-routed to `verify`/design-review when unrenderable). Add-not-cede because copy & chrome are verifiable from the design source — a static source-vs-source string diff catches them with no render (the floor; render-compare is the ideal). Matching SUCCESS METRIC + FAILURE MODE echoes; 2 checklist lines. Mode 1 self-review: 0 blocking; tightens autonomy scoping (paraphrasing copy is *intent* autonomy, now explicitly unlicensed). Pushed `myfork/custom` → `e91bccd3`; pre-commit suite green; synced 13/13. Skill → `e91bccd3`.

### 2026-06-17 — design-implement: regression-surface preflight (step-02b) — handoff-vs-production capability delta + strategy gate (`71656fb1`)

inbound-flow session: owner asked for "a new way of entering" design-implement when handed a handoff URL — Claude should proactively "check what was changed from this handoff relative to production," because the new handoff "may have dropped (which of the five to restore — routing, linked records, full economics/cost-recon, dual-status header, activity timeline) relative to production," and offer the user an implementation choice (additive / partially additive / replacement-in-styling). Mode 2. The need is real and recurring: design-implement treats the handoff as the spec and would faithfully reproduce a redesign's *omissions* — and the component sweep greens out on a dropped whole-capability because its inner primitives (pills, money cells, RecordLinks) exist elsewhere in the impl. There was a post-hoc backstop (step-04 §9 "Capabilities removed (orphaned actions)") but it fires mechanically AFTER the apply on now-zero-caller actions; the owner wants it PROACTIVE, at intake, capability-level, with a strategy choice. Built a new **step-02b-regression-surface** between map (step-02) and grid (step-03): §1 inventories `{production_capabilities}` (feature-level — routing/sub-surfaces, §13 linked records, economics/cost-recon, composite status/header, activity/audit, bulk actions/filters, wired mutations — from step-02's outputs), §2 inventories `{handoff_capabilities}` (`{design_components}` + `{design_frame_inventory}` + brief §7), §3 computes `{dropped_capabilities}` (prod minus handoff). Non-empty → **HALT** with a regression report + a 4-way strategy menu (`restyle-only` keep-all · `additive` · `partial` per-capability · `replacement`), recommendation leading non-destructive. The keep/drop decision is *intent* → halt-by-default; autonomous mode defaults to restyle-only and discloses, never silently replaces (intent autonomy out of scope). `{capability_dispositions}` then constrains downstream: step-03 §2h flags `capability-protected` rows (a grid row that would delete a kept capability), step-04 disposes them `deferred(capability-protected)` and the §9 report states the chosen strategy + every kept/dropped capability, with dropped capabilities routed to the existing §9 orphaned-action confirmation. Guards: skips cleanly on a brand-new surface (no production to regress); does NOT mis-score an undrawn-but-promised frame (brief §7 / `{design_frame_inventory}`) as dropped — ties into the recent §2f frame-coverage work to avoid a false-positive against itself. step-01 now announces the check up front (the proactive "I'm going to check what changed vs production" UX the owner described). Wiring: step-02 COMPLETION → step-02b → step-03; workflow.md +5 state vars + a Critical Rule + the sequence line; checklist Step-2b section. Mode 1 self-review: 0 blocking. Pushed `myfork/custom` → `71656fb1`; pre-push suite green; synced 13/13. Skill bumped → `71656fb1`. **Refinement (`80ad60da`, same session):** owner asked the gate "lean into advisory mode, as opposed to 'can the user list this'." §4 reworked from a bare strategy menu (whose `partial` made the user assemble the keep/drop list) to an **advisory**: a per-capability KEEP / SAFE-TO-DROP verdict with grounded reasons + one recommended plan presented as a decision to approve-or-adjust ("I'll apply this unless you'd rather change it"). §5 records `partial` as the AI-proposed advised mix (AI-authored, user-confirmed); new FAILURE MODE bans offloading the keep/drop analysis to the user; step-01 announce + workflow.md Critical Rule aligned. Validated by a simulated live run on the user's `Supply Order Detail - Blocked-stock build` handoff — production drawer (`supply-orders-drawer.tsx`, 1998 lines) inventoried 11 capability groups (dual-status header · §15 economics/cost-recon · 8 §13 linked-record drawers · activity timeline · routing + §7 raw-exchange audit · …); the owner's anticipated dropped-five mapped cleanly to the report. (WebFetch couldn't pull the >10MB bundle — real step-01 uses curl; the sim grounded the handoff side on the frame name + owner's stated scope.) Synced 13/13. Skill → `80ad60da`.

### 2026-06-17 — design-implement: page-shell §2d catches ancestor-injected chrome (hero/banner) (`7f8af1ca`, inbound-flow PR #2275)

inbound-flow session: owner pasted two screenshots — the live prod `/settings/sku-format` (a tall photographic hero banner "Settings · Configure workspace preferences and account security" above the worklist) vs the Claude Design handoff (no hero, straight to the "SKU format" heading) — "design implement is sloppy!" The prior design-implement run had declared the page shipped-and-matching. Diagnosis (Mode 3): root class `contract-dimension-gap`, the page-shell axis (§2d, added `1febc8ce`) modeled the container's *dimensions* (width/centering/padding) but not its *chrome*. The hero is injected by `settings/layout.tsx` (`<InboundHeroBanner>`) — an ancestor layout, above `{children}` — so (a) the component sweep is blind (the bundle renders the frame standalone → no hero in `{design_components}` to diff), and (b) §2d didn't look for it, so every component row greened and a §5 hard failure ("Hero strips… above working tables") shipped. Settled with the owner that this is **add-not-cede**, not the §2e policy cede: the band is visible in the *same layout files the §1a wrapper-chain walk already reads* (the walk that finds `max-w-[1440px]` also sees `<InboundHeroBanner>`), so design-implement can name it directly. Fix: step-02 §1a gains a 4th capture point → `{impl_page_shell}.injected_chrome` (hero/banner/masthead/promo a wrapper renders above children, each `in_design_frame: bool`); step-03 §2d emits an injected-chrome page-shell row, Tier-1 when absent from the design frame (design frame authoritative; "absent from the bundle" is explicitly NOT license to keep an injected hero); checklist + workflow.md state var updated. No new §9 section — it flows through the normal Tier-1 delta ledger. Discovery during the fix: `InboundHeroBanner` is on ~22 pages app-wide (settings + ~21 operational pages) — a systemic §5 divergence; only the settings layout was fixed (one edit clears all four settings sub-pages and matches the design), the rest flagged as a deliberate app-identity decision, NOT swept. Live fix delivered: inbound-flow **PR #2275** (worktree `fix/settings-hero-banner`, tsc + 6050 tests + build green, squash-merged → Railway auto-deploy). Mode 1 self-review of the §2d add: 0 blocking. Pushed `myfork/custom` → `7f8af1ca`; pre-push suite green; synced 13/13. Skill bumped → `7f8af1ca`. Follow-ups: app-wide hero decision (the ~21 other pages); the stranded `_bmad` design-lane sync owes a doc-only commit to inbound-flow main so worktree runs see §2g/§2d.

### 2026-06-17 — design-implement: token-provenance disclose-and-cede (per-screen vs canonical tokens) (`b4ad51cb`)

inbound-flow session: owner pasted a live `design-impl` run (cost-recon worktree) that asserted "Tokens map ~1:1," hit a counterexample (`--status-success-text` / `--status-warning-text` / `--subtle-foreground` absent from `tokens.css`), then *resolved* it by finding `--status-*-text` defined in `supply-orders.css` lines 34–50 and concluding "the bundle was generated from this exact CSS, so the mapping is effectively 1:1." Owner pushed back on whether the "canonical-only" restriction is even good. Worked it through: canonical-only is a good design-*system* north star (a status colour is a shared concept; the v2 hue-160→148 retune repainted every surface from one `tokens.css` edit) but a **bad hard gate** inside design-implement — (a) it's not design-implement's lane (the token resolves fine at runtime; promote-to-canonical is token *architecture*, design-review's call — same cede-vs-add boundary the §2e policy-cede settled), and (b) "per-screen = debt" is too coarse: a shared-semantic token (status/colour/type) in a per-screen file is debt, a local layout constant is fine. So the fix is **disclose-and-cede, never gate**, a sibling of the content-lane (§2c) and policy-conformance (§2e) cedes. Root class `contract-dimension-gap`: the token comparison was missing the *provenance* axis (it checked existence, not which layer it resolved from). step-02 §5 now resolves the value AND tags `{impl_token_provenance}` (`scope: canonical|per-screen` against `docs/design-policy.md` §8's `tokens.css`+`@theme` surface; `semantic_class: shared-semantic|local-constant`) + "resolve every token before any 1:1 claim" (the snippet's literal bug — `--subtle-foreground` was folded into 1:1 while still unresolved); step-03 §2g emits one `NON-CANONICAL TOKEN` disclosure row per shared-semantic per-screen token (not a delta, not in `{delta_count}`, not gated; `local-constant` per-screen tokens NOT flagged to avoid disclosure spam); step-04 §9 gains a mandatory "Token provenance (non-canonical)" section + SUCCESS/FAILURE rows; checklist + workflow.md state-var registry (`{token_noncanonical_count}`). Mode 1 self-review: 0 blocking (legitimate add-where-verifiable + cede-where-unowned; the provenance grep is evidence step-02 §5 already gathers). Pushed `myfork/custom` → `b4ad51cb`; pre-push suite green; synced 13/13. Skill bumped → `b4ad51cb`. Note: the cost-recon worktree session is running the pre-§2g copy — restart it to pick up the disclose-and-cede.

### 2026-06-17 — create-design-policy: Bison product-family policy overlay + `inherits:` mechanism (`3141456f`)

inbound-flow session: owner asked to "template the bulk of the design policy for a reusable policy for new projects" and explicitly "decipher what's unique to project and what is template." Mode 5/2 (explain → author). The key finding reframed the task: a blank greenfield template already existed (`create-design-policy/design-policy-template.md`, 16 placeholder sections) and `shared/design-standards.md` already held the universal AI-fingerprint substance — so the *missing* layer was the **product-family** tier, the exact "bulk" the owner keeps re-porting by hand (inbound-flow v11 was itself a verbatim port of accounting-tools v11). The decipher is **three** tiers, not two, and inbound-flow's own change_log already authored the answer: every entry tags `Propagation (§9)` as "shared product-family value → propagate to accounting-tools" vs "inbound-flow-only, do NOT propagate." Read off those tags: **universal** (anti-AI fingerprints, table-first, status mechanism) → `design-standards.md`; **product-family** (corporate register, status system + 4-colour hierarchy, monochrome-primary/Inter/13px, money/VAT + 2dp + derived-ROI proof, relational expand-in-context linking, API + comingled per-source provenance, sorting/chronology, positive-assertion floor) → the new overlay; **project-unique** (multi-handler warehouse-handler topology, product imagery, Amazon/FBA exemplar content, concrete tokens/routes) → stays in each project policy. Built `shared/bison-product-family-policy.md` (§A–§L + §Z) by lifting the propagate-tagged prose in source-agnostic form, with §Z enumerating the tier-3 residue so a new project knows exactly what it must still author. Wired inheritance without breaking the workflow's project-agnostic contract: template `inherits:` frontmatter + family-overlay precedence tier; `workflow.md` inherit-don't-re-port CRITICAL RULE + `{family_overlay}` state var; `step-04 §2b` residue-only authoring path. Overlay lives in `shared/` (distributes everywhere) but is inert unless a project declares `inherits: bison-product-family-policy`. Mode 1 self-review of the overlay: 0 blocking; 1 caught + fixed pre-commit (§I positive-assertion YAML carried stale inbound-flow section numbers §L/§M and pointed money's relational link at §K instead of §J — corrected to the overlay's own A–L scheme). Pushed `myfork/custom` → `3141456f`; pre-push full test suite green. Skill bumped → `3141456f`. **Sync owed (13/13).** Follow-up: a deliberate per-project adopt-the-overlay rewrite for inbound-flow + accounting-tools (convert to `inherits:` + residue-only) — not auto-done, since it's a large destructive rewrite of two live policies.

### 2026-06-12 — design-implement: §2f frame-coverage gets a URL-path denominator (the bundle's own declared lookup frames) (`56d44fc9`)

inbound-flow session: owner reported "design-implement is no longer catching all deltas when I hand over a bundle and run the flow — every time it fails to catch the detail pages that are there in the 'link to records (lookups)'", with a live Claude Design URL (`open_file=Orders.html`). Diagnosis (Mode 3), grounded by fetching the actual bundle: `Orders.html` consumes five §13 expand-in-context lookup frames inside the Supply Order Detail Drawer — `warehouse-lookup` / `inbound-batch-lookup` / `import-run-lookup` / `accounting-outcome-lookup` (supply-order/lookups.jsx banners) plus `catalog` + `supply-source` (catalog-record/) — declared in the bundle's own HTML comments ("… lookups consumed"), jsx per-frame banners, and the `app.jsx` lookup→target map. Root class `contract-dimension-gap` (URL-path twin of the §2f axis): §2f's two contract sources are the **brief §7** and the **synthesize-bundle manifest** — and a raw Claude Design URL run has NEITHER. So §2f had no denominator, emitted zero Frame-coverage rows, and the lookup drawers fell out silently: their inner primitives (Pill, Money, RecordLink) are shared and match somewhere in the impl, so the component sweep greened out while the whole drawer shipped unbuilt or inferred-thin. step-04 §9's Frame-coverage disclosure was gated on "whenever the brief enumerated a §7 Surface Inventory" — no brief → section legitimately skipped → false-green. Add-not-cede (the bundle declares its frames; reading them is evidence already in hand, same posture as page-shell §2d): step-01 **URL.3a** captures `{design_frame_inventory}` (primary + drilled drawer + each §13 lookup, from traced modules/comments + per-frame banners + lookup→target maps + sibling standalone `<frame>.html`, opening any linked standalone frame into `{design_components}`); step-03 §2f resolves the contract by **three-source precedence** — brief §7 → bundle frame inventory (URL) → manifest (bundle) → needs-human-confirm — with the URL-path frames `drawn:true` so an impl lacking the drawer is `FRAME MISSING in impl` (Tier-1); step-04 §9 + SUCCESS/FAILURE + checklist generalize the disclosure gate beyond brief §7 ("no brief" is not "no contract"); workflow.md gains the state var + a Critical Rule. Mode 1 self-review: 0 blocking. Pushed `myfork/custom` → `56d44fc9`; synced 13/13 (also distributing the previously-owed `e72c1cb7` gate). Skill bumped → `56d44fc9`. Follow-up (carried): `{consumed_frames}` disposition in §2f.

### 2026-06-12 — design-implement: gate the §7 frame-coverage axis + deliver the stranded design-lane sync (`e72c1cb7`, PR #2071)

inbound-flow session: owner pasted a prior `design-implement` run on the `/orders` bundle that declared "Deltas applied: 0 — green," then pushed back with "brief's §7 Surface Inventory lists 9 frames, each a required deliverable." Per-frame cross-check found 5 of 9 frames built, 4 (inbound-batch / import / shipping-lane / comms-case lookup drawers) designed-in-the-bundle but unbuilt in impl. Diagnosis (Mode 3) had **two layers**. Layer 1 — root cause of *this* run — `worktree-sync-drift`: the §2f Frame-coverage axis (and §2d/§2e, and the §7 render/scoping waves) were synced into inbound-flow's main working tree but **never committed**; `EnterWorktree` branches from HEAD, so the worktreed run loaded the pre-§2f copy (`git show HEAD:…/step-03-build-grid.md | grep -c Frame-coverage` → 0) and was structurally blind to a whole unbuilt frame. Fix: committed the stranded design-lane sync to `main` (PR #2071, doc-only admin-merge) — §2f now in committed HEAD, worktree runs see it. Layer 2 — latent even post-sync — `silent-partial-implementation`: §2f's emission was never *enforced* (no checklist row, no mandatory §9 report section), so an agent could still sweep only the found frames and green out. Fix mirrors the existing content-lane / capabilities-removed gates: checklist gains the §2f row; step-04 §9 gains a mandatory **"Frame coverage (brief §7)"** section enumerating every promised frame as built / missing-in-impl (Tier-1) / not-drawn (routed); SUCCESS METRICS + FAILURE MODES codify "the §7 list, not the found-frame set, is the denominator for a green claim." Mode 1 self-review of the gate: 0 blocking (legitimate *add*, not a faked policy check — §2f is verifiable from evidence the workflow already holds). Pushed `myfork/custom` → `e72c1cb7`. Skill bumped → `e72c1cb7`. **`sync bmad` owed** to distribute the gate to all 13 targets. Follow-up (carried from `22406ca7`): `{consumed_frames}` disposition in §2f.

### 2026-06-12 — design-synthesize + design-handoff: draw §7 lookup-drawer frames; redirect lookup-drawer handoffs (`22406ca7`)

inbound-flow session: owner reported the small §13 "link to record" lookup drawers (a thin catalog record drawer subtitled "Catalog record for ASIN"; a supply-source lookup drawer that shipped as a hand-rolled key/value stub violating §15 — bare `€245.71` no GBP/VAT basis, inert Sell ASIN, inert gate) "getting completely missed by Claude Design," AND "whenever I call for a handoff the handoff purpose gets rejected." Diagnosis (Mode 3): the `3a88eee3` wave shipped the §7 enumeration (handoff) + the §2f verification (design-implement) but left the *render* half and the *redirect* half as explicit follow-ups — and they were never done. Root class `silent-partial-implementation`: the §7 Surface Inventory contract was complete, but **design-synthesize never consumed it** — step-01 §7 derived `{screens}` only from frontmatter `route`/`routes`, never the brief BODY's §7 table, so the enumerated lookup-drawer frames never became screens, never rendered, and design-implement §2f then inferred them thin. Symptom 2 was partly correct-by-design (a lookup drawer is owned by the relation, never its own handoff — destination-vs-relationship) but the rejection routed nowhere. Fix, two halves: **(A) render** — design-synthesize step-01 §7a populates `{screens}` from the §7 Surface Inventory (frame-name-keyed verbatim, the contract key that travels brief→`<frame>.html`→§2f), with Gate 1f halting if any §7 frame would go undrawn (silent-partial guard mirroring §2f at the render step) + `{consumed_frames}` accounting for "consume from sibling brief" rows + a pre-§7-brief announced blind-spot; step-04 composes a `drawer-over-{parent}` frame as the §13 drawer open over its parent (§15 basis-complete, §2a richness floor); workflow.md state vars document it. **(B) redirect** — design-handoff step-01 §2a detects a §13 lookup-drawer target and REDIRECTS it (not a bounce) to the matching next command — re-synthesize the parent brief (now §7-aware), material-revise the parent, or redesign the owning surface — preserving the multiple-active-brief invariant; autonomous mode does not override (intent violation). The infra was already multi-screen (step-04/05/07 loop `{screens}`), so the render fix was one missing wire, not new machinery. Mode 1 self-review: 0 blocking. Pushed `myfork/custom`; synced 13/13. Skill bumped → `22406ca7`. Follow-up: verify design-implement §2f treats a `{consumed_frames}` row as out-of-scope-for-this-bundle, not `FRAME NOT DRAWN`.

### 2026-06-12 — design-handoff: spawned drawers scoped in as required deliverable frames (`3a88eee3`)

User reported poor detail-page drawers (financial data presented sloppily, thin warehouse lookup, an `ROI`-bearing catalog drawer subtitled "Catalog record for ASIN") — "especially when the handoff specified a page and not a specific detail page." Root class `contract-dimension-gap`, 5th instance (after state / multiplicity / content-lane / page-shell): handoff enumerated the scoped PAGE but never the surfaces it spawns at runtime — the drilled **detail drawer** and the §13 expand-in-context **lookup drawers**. The pipeline is non-interpretive (Claude Design draws only §7's frames; design-implement matches only bundle frames), so an un-enumerated drawer is never drawn and then INFERRED downstream → thin, unformalised (bare `€60` with no GBP/VAT basis — a `docs/design-policy.md` §15 violation that ships because the drawer never entered the design pipeline as a frame; lookup drawers stubbed at code/type/status). The taxonomy that settled the scoping: **destination vs relationship** — lookup drawers are owned by the *relation* (§2a, never a separate handoff), the drilled drawer is an inline spawned-surface frame by default, fresh handoff only on graduation to a deep `/[id]` route. Verifiable from evidence the workflows already hold, so the fix adds real rows, not a cede: workflow.md Deliverable-Completeness Principle ("if you want it built well, it must be drawn") + `{spawned_surfaces}`; step-01 §5f derives the frames from page_mode + composition + linked_records (richness floor, depth-1); brief §7 leads with a frame-name-keyed Surface Inventory table; §2a richness floor; step-03 substitution + checklist guard; design-implement §2f Frame-coverage rows (a brief-promised frame absent from the bundle = `FRAME NOT DRAWN` → routed, not inferred). No inbound-flow policy change — §15/§13 already mandate the content quality; the gap was the drawer never being drawn. Mode 1: 0 blocking. Pushed `myfork/custom`; synced 13/13. Skill bumped → `3a88eee3`. Follow-ups: design-synthesize (terminal-native render path) should honor §7 frames; design-implement step-01-ingest could capture the brief §7 frame list explicitly rather than §2f re-reading it.

### 2026-06-12 — design-implement: the bundle is a generated proposal, the spec is the policy (`9a0a1089`)

User pulled a second real sheet (`Accounting Import v2`) and asked "is the README gened by Claude Design?" — yes. The bundle AND its README are generated *from* `docs/design-policy.md` and can violate it (that bundle shipped a banned colored-glow `@keyframes ai-pulse` + no `prefers-reduced-motion`). So the bundle is authoritative for **treatment** only, never policy conformance. **(1)** step-01 re-points `{design_layout_constraints}` to read the layout rule from `docs/design-policy.md` FIRST (authoritative); README + wrapper demoted to corroboration with an `authoritative` flag + a no-policy fallback. **(2)** new step-03 §2e + step-04 §9 + a workflow.md Critical Rule explicitly CEDE policy-conformance (prohibitions/tone/motion/iconography) + behavior wiring to design-review / design-review-pr / verify — not a faked grep. Page-shell stays a check (statically verifiable against policy + impl); the rest is ceded. Mode 1: 0 blocking, 1 nit fixed. Pushed `myfork/custom`; synced 13/13. Skill bumped → `9a0a1089`.

### 2026-06-11 — design-implement gains the page-shell axis (`1febc8ce`)

The comparison grid is component × state × property, but the page CONTAINER's width/centering is owned by the wrapper + ancestor layout (no component) and the bundle renders full-bleed standalone — so a nested `max-width:1280px` cap inside the layout's `max-w-[1440px]` rendered `/orders` narrow + centered (PR #2017) while every component CSS matched (grid all-green). Root class `contract-dimension-gap` — 4th instance after state / multiplicity / content-lane. Fix: `{design_layout_constraints}` + `{impl_page_shell}` state vars; step-01 capture (both ingest paths); step-02 §1a resolves the EFFECTIVE width after every nested cap; step-03 §2d emits one always-present Page-shell row (width/centering mismatch = Tier-1). Pushed `myfork/custom`; synced 13/13.

> Earlier waves (2026-06-10 and before) are preserved verbatim — as the original log — in [`STATUS-archive.md`](./STATUS-archive.md).

---

## Versioning

- **Upstream BMAD tracked:** v6.8.0 (`origin/main`)
- **Fork base snapshot:** v6.7.x branch point
- **Commits behind upstream:** 3 (as of 2026-06-12)
- **Commits ahead of upstream:** 258
- **Last upstream sync:** `upgrade-bmad.sh` 2026-06-01 (clean)
- **Last fork commit:** see `## Changelog` top entry (self-maintaining)

## Shipped Features

Foundational capabilities, shipped. The wave-by-wave feature history (what landed each session) lives in `## Changelog` + [`STATUS-archive.md`](./STATUS-archive.md); this is the durable capability set, not the running log.

**Provenance & safety**

- [x] Brief provenance contract — 11-field frontmatter + 6 intake checks (`401e2d57`)
- [x] design-handoff predecessor + supersession logic (brief-revision-policy)
- [x] Quick-dev grounding gate + autonomy scoping (decision vs intent) (`1979c48f`)
- [x] `project_phase` config — greenfield | brownfield | mixed (`0dd2e1ca`)

**Sync & infra**

- [x] `sync-bmad-workflows.sh` + `--worktree` single-worktree flag (`647c14e7`)
- [x] worktree-portability rule (every producer resolves `{project-root}`)
- [x] `bmad-deploy` stale-checkout guard + source-vs-deployed drift gate

**Design pipeline**

- [x] design-handoff / design-artifact-loop / design-synthesize / design-tuning — intake checks + two-lane evidence model
- [x] design-review + design-review-pr — live + PR-time policy §13 / archetype enforcement
- [x] design-implement — comparison-grid axes: state · multiplicity · content-lane · page-shell · policy-as-spec
- [x] design-elevation (scope-expansion) + maintenance-triage (shape gate)

**Analytics presentation family**

- [x] analytics-surface-architect (shape) · analytics-rigor (depth) · decision-analysis (capital decisions)
- [x] archetype taxonomy incl. `waterfall`; design-handoff §5e surface hierarchy

**Verify lane (detect + route)**

- [x] data-quality-audit · scrape-coverage-audit · webhook-contract-check (+ charter) · relational-coherence-audit

**Agents**

- [x] custom-agents lane + create-agent: Vera (data-integrity) · Wren (relational-coherence)

## Designed but Not Yet Shipped

- [ ] **Quick-dev split into `spec-dev` and `direct-dev` entry points** — current `quick-dev` carries both modes behind one entry; split would make the safety boundary explicit at the entry point
- [ ] **`tech-spec` workflow** — lightweight artifact between `maintenance-triage` and dev, for brownfield work that needs more than direct instructions but less than a full PRD
- [ ] **Full brownfield maintenance pipeline** — `maintenance-triage → tech-spec → spec-dev` (1 of 3 surfaces shipped)

## In-Flight Work

- `worktree-feat+brief-revision-policy` in **accounting-tools** (not the fork) — design-tuning step revisions + new directories under `_bmad/bmm/workflows/design/`. These will need to be lifted back into the fork's `custom/workflows/` once stable. **Important:** edits in the project will be overwritten on next sync; the canonical edit point is `~/bmad-method-v6/custom/workflows/`.

## Known Drift Across the 13 Projects

Audit pending. Run `~/bmad-method-v6/sync-bmad-workflows.sh --check` to populate. Active sync targets:

- comms_dashboard, brand-source-finder, inbound-flow, accounting-tools, bison-ops, image-pipeline, bison-website, amazon-removal-assistant, taylor_work, accounting_api_backend, otp_manager, wera-catalog, amazon-lead-generator

## Upstream Items Under Evaluation

25 upstream commits behind. Per-commit triage not done yet. Reconciliation difficulty likely high in any area the fork has restructured (design pipeline, quick-dev, sync layer). Low-risk absorbs likely in areas the fork has not touched (core agents, party-mode, brainstorming, retro).

- v6.8.0 release → undecided → triage pending

## Recent Decisions Worth Remembering

- 2026-05-28: Shipped `project_phase` config and `maintenance-triage` workflow together. `project_phase` is the gating mechanism for routing brownfield work through `maintenance-triage` instead of greenfield-biased entry points.
- 2026-05-27: Shipped `sync --worktree` flag to resolve worktree-portability P0. Worktree creation no longer requires a full multi-project sync.
- 2026-05-?: Brief provenance contract finalized (11 fields + 6 intake checks). Hand-edits to material_revision briefs are now forbidden by intake check 5; clarifications remain allowed.
- 2026-05-?: Quick-dev grounding gate redefined autonomous_mode — decision autonomy (file choice, pattern selection) allowed; intent autonomy (inferring what user meant) forbidden.
