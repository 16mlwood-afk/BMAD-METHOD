---
name: bmad-fork-status
description: Volatile state of the Mason-BMAD fork. Read by mason-bmad-workflow-expert skill on every invocation.
---

# Mason-BMAD Fork — STATUS

> Update whenever you ship a change to the fork, absorb upstream, or change the shipped/designed status of a feature.

**Last updated:** 2026-06-28 — **tool-registry + finance Site-verification proof-marker** (`4fb04514`, pushed `myfork/custom`, synced 14/14) — from the bison-ops `grand_total` defect (producer emitted `max()` of divergent box-rows → over-stated spend; only live-site inspection caught it; cross-border import/VAT estimates settle DOWNWARD, so the settled/LOWER total is the amount paid). NEW `shared/tool-registry.md` — governed "Claude in Chrome" entry as the live-site source-of-truth for finance-critical scraped values (when/when-not, no-credential-entry rule, connectivity gotchas, Example-usage backlink). `producer-defect-template.md` gains **§7 "Site verification"** (REQUIRED for finance-value defects: a 3–10 order live-site sample proving the producer's money rule matches the site before status leaves `open`) — the **proof-marker** for a DETERMINISTIC receiver-CI gate (separate distribution track; authoring §7 is the awareness tier, NOT the gate). `webhook-contract-check` CRITICAL RULE: finance findings validate against the live site, never "max vs min" reasoning. `enforcement-expert` consulted (finance correctness non-negotiable → prose insufficient; deterministic §7 gate specced for the receiver repo's CI). Companions: bison-ops **PR #140** (canonical = settled minimum, site-verified 10/10 EU orders; merged + `dist` rebuilt v2.1.70) and inbound-flow receiver mirror **#2462/#2463** + import-fee-settlement runbook & brief §Site-verification (PR #2464). Out-of-wave: pre-existing MD034 bare-email in the producer-registry table. 0 blocking Mode-1 self-review.

**Last updated:** 2026-06-28 — **producer-defect cross-repo DELIVERY seam** (`3e4177c3`, pushed `myfork/custom`, synced 14/14) — closes the open fork-gap (template defined the artifact but no producer-repo delivery). Added a "## Delivery seam" section + producer-registry `delivery_channel`/`report_location` to `producer-defect-template.md`: four channels (`pull-from-receiver` [default] | `pr-into-producer` | `tracker` | `manual`), pull-as-default because the receiver can't push across a repo boundary without an auth/ownership grant it doesn't own — pull inverts the burden (producer reads the committed receiver path with its own access). Automation-free → no hook, no enforcement-gate; `pr-into-producer` automation explicitly routed through the enforcement-expert gate if ever built; auth/ownership/cadence named as the owner's call. Pairs with the inbound-flow receiver-side wave (harvest #2455/#2456 + committed `docs/producer-defects/` path #2457 + 20-divergence backfill + first committed brief). fork-gap marked resolved; markdownlint clean; 0 blocking self-review.

**Last updated:** 2026-06-28 — **external-producer defect-reporting practice (cross-repo)** (`b633c356`, pushed `myfork/custom`, sync pending) — closes the gap where the detect-and-route audits assumed the producer lives in-repo. NEW `shared/producer-defect-template.md`: the canonical `producer-defect-{producer}-{date}.md` (producer id · violated charter clause(s) · evidence · blast radius · proposed contract change · owner) + a `producer-registry` + the deterministic-harvest guideline, judged against `webhook-contract-charter.md`. `data-quality-audit` + `scrape-coverage-audit` + `webhook-contract-check` step-04 now split `producer_fix_lane: internal | external:<producer_id>`, where `external:*` is a HARD never-drop branch that MUST emit the report (never an in-repo `quick-spec`; consumer boundary-harden stays a separate in-repo `quick-spec`). Worked example: bison-ops grand_total divergence (`detectEconomicsDivergence` logs-not-reconciles → Sender §4 never-weaken-silently + Receiver §3 fail-loud). markdownlint clean; 0 blocking Mode-1 self-review. **Owed:** `sync bmad` (14 projects); Deliverable 3 — per-project deterministic harvest hook — sequenced through the `enforcement-expert` gate (guideline shipped here, instance not). Noted out-of-wave: pre-existing MD034 bare-URL lint at `docs/fork-gaps.md:332`.

**Last updated:** 2026-06-27 — **fork-gap triage: register worked to exhaustion — 7 closed + #130 end-to-end** (`915dd4a7`, `5511d00a`, `fed0a72b`, `06c4b715`, `fede58e4`, `c7978f7f`, `d5be1231`, synced 14/14) — (1) **edit-guard `-m` residual (#332/#111):** widened `bmad-bash-edit-guard`'s message-bearing-VCS exemption to also match inline `-m`/`--message` (git) and `-b`/`--body` (gh) bodies, so a commit message containing arrows/comparison chars no longer trips the edit-equivalent block (verified ALLOW; real `sed -i` into `src/` still BLOCKs). (2) **JQ_MERGE drop legibility (#332):** both hooks-upsert sites in `sync-bmad-workflows.sh` now `WARN` the names of base `bmad-` hooks absent from the template (dropped by the merge), so a hand-added project-hook clobber is announced at sync time. (3) **astro SSG out of the commit gate (#94):** removed the heavyweight, stateful `docs:build` from `.githooks/pre-commit` (kept `docs:validate-links` + `docs:validate-sidebar`); a markdown-only / `docs/fork-gaps.md` commit no longer needs `--no-verify` (which discarded ALL validators), and `tools/build-docs.mjs` now also wipes the `website/.astro` cache so the `404.astro.mjs` failure stops recurring at deploy/CI. (4) **shared-index commit-guard (#83 option b):** new `bmad-fork-commit-atomicity` PreToolUse(Bash) hook — `ask` on an un-scoped fork `git commit` (no `-o`/`--only`) while >1 session is active, steering to `git commit -o <paths>`; converts §D3 from convention into a deterministic prompt at the commingling moment (verified 4 ways; smoke 10/10; option (c) per-session index stays open as a heavier tier). Also **verified #130 done** (swept the corpus: zero menu tails, escalation tail wired in all 6 gateway workflows; its Tier-3 backstop stays owner-deferred). **Declined to build #47** (offer-then-affirm nudge): its own enforcement analysis flags it low-value + noisy (fires on every affirmative across all 13 projects) and recommends consolidation — the call is the owner's. (5) **audit actual-side honesty (#59 fork-side):** `relational-coherence-audit` + `data-quality-audit` now emit a loud STRUCTURAL-ONLY banner when `{server_live}=false` (a half-run can't pose as decision-grade; the count ships as a hand-off query, not a verdict) and resolve a read-only connection from `docs/deployment.md` (public proxy / `DATABASE_PUBLIC_URL`, distinct from the internal `DATABASE_URL`) before static fallback. (6) **board-contention note (#279 residual 2):** new `bmad-board-contention-note` PreToolUse(Edit|Write) hook — a once-per-15-min `additionalContext` note when an Edit/Write targets a `sprint-status.yaml` while >1 session is active (re-read first; edit per-key §B1/§B1a; never a whole-file/multi-line rewrite). Closes #279 (residual 1 §B1a was already done); cooldown-marked awareness tier. (Live-fire footnote: a parallel session's `git add -A` swept this hook's hooks.json edit into its own commit `05b8bd62` before I committed — the #83 hazard itself; nothing lost, attribution-only, not history-rewritten.) (7) **#47 CLOSED via consolidation** (`c7978f7f`): declined the noisy nudge hook; instead added an **Affirmative-execution** clause to the always-on `~/.claude/CLAUDE.md` autonomy pillar (your own prior offer + a bare affirmative = execute the offer immediately, don't re-deliberate) — probabilistic-by-design (recoverable guidance, correct ceiling), machine-global doctrine, NOT fork-synced. (8) **#130 CLOSED end-to-end** (`d5be1231`): Tier-3 deterministic detect→marker→Stop backstop **declined on merits** — detection is unhookable, so Tier-3 only catches the thin "detected-but-didn't-escalate" slice that tiers 1–2 already cover (addition-over-consolidation); revisit only if that slice empirically recurs. **Register now WORKED TO EXHAUSTION — the only remaining items are OUT-OF-FORK:** #59 residual (other 12 repos each add their `docs/deployment.md` read-only note; cash-recovery done PR #108) and #156 worktree-cwd memory slug (harness-level memory-write path). 0 blocking Mode-1 self-review. **Pushed `myfork/custom`; SYNCED 14/14 (0 blocked).**

**Prior:** 2026-06-27 — **trend archetype: two-magnitude (actuals-vs-forecast) sub-case** (`de2c9013`, synced 14/14) — closed the open `trend` contract-dimension-gap (committed-vs-provisional spend came back as a naive stacked+labelled column chart). Added an explicit two-magnitude sub-case across the taxonomy SoT (`shared/analytics-archetypes.md`), `design-handoff/brief-template.md` (Lead-pass form + archetype_prohibited), the `analytics-surface-architect` skill (procedure step 7 + new `trend_subcase` output field carrying the form into the brief), and the skills-native twins: a realised primary (committed/actual/spent) = solid line/area, the subordinate projection (provisional/forecast/pipeline/budget) = ghosted/dashed band — never stacked, never two co-equal lines, exact figures in the drill not per-bar labels. Hardened the trend `prohibited` so a brief can't soften "no stacked columns", banned per-bar value labels. Also logged a fork-gap (`ce715267`): `JQ_MERGE` silently drops hand-added `bmad-`-named project hooks (must live in the template `hooks.json`), + the edit-guard `-m`-body residual on #111. 0 blocking Mode-1 self-review. **Pushed `myfork/custom`; SYNCED 14/14 (0 blocked).**

**Prior:** 2026-06-27 — **design-implement/ingest intake: Claude Design's new MCP paste-prompt handoff** — Claude Design's "Send to local coding agent" panel now emits a free-text prompt (`Use the claude_design MCP … to import this project:` + a `claude.ai/design/p/<uuid>?file=…` share-link + an `Implement: <file>` line) instead of a bare URL. Intake in `design-implement` + `design-ingest` (workflow.md, mirrored to skills-native SKILL.md) now recognizes all THREE URL shapes (modern share-link, legacy `…/h/` tar, AND the paste-prompt), lifts the embedded share-link as `{design_url}`, and resolves `{design_file}` from the `Implement:` line / URL-decoded `?file=`; step-01 URL.1b gained a **step-0 that URL-decodes `?file=`** (`%2F`→`/`, `+`/`%20`→space) before `get_file`, and confirms `<uuid>`=DesignSync `projectId`. The paste obeyed literally would call the MCP and implement directly — bypassing the whole safety layer (regression-surface preflight, bundle→brief conformance, supersede awareness, exhaustive grid, apply ledger) — so a conservative **two-marker `bmad-design-handoff-router` `UserPromptSubmit` awareness hook** routes it to `/bmad:bmm:workflows:design-implement`. Enforcement-honest: awareness tier (deterministic delivery, probabilistic action) — a hard PreToolUse block on the MCP would be the indiscriminate-gate anti-pattern (the MCP has legit uses). Added to the sync hooks template `src/modules/bmm/_module-installer/assets/hooks.json` (`e5e16e08`), NOT a project's settings — a hand-added `bmad-`-named hook is DROPPED by `JQ_MERGE` (it keeps only non-`bmad-` base hooks then appends the template's), proven this wave when the first hand-add was clobbered on sync. So it is **fork-distributed**: 0 blocking Mode-1 self-review; hook verified to fire on the real template and stay silent on 2 negatives; commits `a1ccb9ce` (intake) + `e5e16e08` (hook), **pushed `myfork/custom` and SYNCED 14/14 (0 blocked)** — live in every target's `settings.local.json`. No fork-distribution follow-up owed.

**Prior:** 2026-06-27 — **quick-dev Mode-A gated-migration carve-out** (owner-greenlit, synced 14/14) — the sibling of the handoff-reroute fix: a narrow carve-out to blast-radius HARD trigger 1 so a Mode-A tech-spec that already pre-plans a generate-not-apply + sequenced + rollback migration classifies `contained-feature` instead of rerouting to `quick-spec` to generate a plan it's already holding. **Enforcement-honest:** the deterministic step-07 backstop is NOT weakened — it still fires and routes through the script's existing `QUICK_DEV_OVERRIDE=<reason>` logging path; triggers 2–5 and any *applied*/non-Mode-A schema change stay fenced. 0 blocking Mode-1 self-review.

**Prior:** 2026-06-27 — **Two over-firing-gate fork-gaps closed** — `quick-dev` grounding-gate now reroutes a handoff artifact to `dispatch-followups` (instead of a generic dead-end), and `design-implement` URL ingestion gained a **DesignSync MCP branch** for modern `claude.ai/design/p/<uuid>` share-links (URL.1a tar / URL.1b MCP). Both additive/awareness-tier, 0 blocking on Mode-1 self-review. Synced 14/14.

**Prior:** 2026-06-27 — **STD-HOOKACTIVATE-001: hook distribution + activation rail — the fork now OWNS git-hook activation** (`035ff37f`, keystone `ea782f34`) — `custom/githooks/` warn-only dispatchers + `custom/scripts/activate-hooks.sh` + a SessionStart liveness probe (`check-hook-activation.sh`); `sync-bmad-workflows.sh` + `onboard-project.sh` set `core.hooksPath=.githooks` idempotently so a synced gate is reliably WIRED, not silently off. **Bespoke-repo-safe** (ground truth: NO target used husky, and 5 repos own real `.githooks` gates — a project's own hooks are never clobbered or auto-activated; the probe surfaces dormant ones). Fork dogfoods it (husky retired → `.githooks`). Honest ceiling: deterministic activation + best-effort gate; CI fail-closed DEFERRED per the standard; Tier-3 enforcement rides this rail in a later warn-only phase. **Consolidated fan-out RAN: 14 synced, 0 blocked** — carried STD-ESCALATE re-sync + persona-content (`8652d39b`) + the 4-gap cleanup (`df4ad7a9`). Verified: 9 gate-less repos warn-only-wired (`core.hooksPath=.githooks`, probe silent); 5 bespoke intact (4 dormant gates now surfaced by the probe for a deliberate per-repo decision).

**Prior:** 2026-06-27 — **create-agent: persona content contract — 8-section shape + human-tone floor as a soft scaffold gate** (`8652d39b`) — 8-section persona template + human-tone defaults as the canonical CONTENT shape, mapped onto the XML persona, enforced via step-03 scaffolding (soft gate, `TODO(persona)` only where un-inferable). Preceded by STD-ESCALATE-001 (`7b8f695d`, synced 14/14). — new `shared/escalation-on-class-change.md` standard (state the class-change → propose the BMAD-default gateway → proceed-unless-vetoed; tripwire; unattended HALT-and-record), registered in STANDARDS.md. `dev-story` (had no reflex at all) + `quick-spec` gain the tripwire; `correct-course`/`design-router`/`maintenance-triage`/`design-elevation`/`quick-dev` get conformance pointers; out-of-scope set named. Preceded by the design-handoff shape-injection wave (`b4a3a383`). Pushed `myfork/custom`; **synced 14/14 (0 blocked) at `6866108c`** — the reflex is now live everywhere.

---

## Now

The compact, always-current state. The skill reads THIS block + the top of `## Changelog` on every invocation — it does not need the archive.

> **✅ SYNC FAN-OUT DONE (2026-06-27).** The hook-distribution keystone landed (`ea782f34` + bespoke-safe hardening `035ff37f`) and the single consolidated `sync-bmad-workflows.sh` ran: **14 synced, 0 blocked** — carrying (a) STD-ESCALATE-001 (`7b8f695d`, re-sync harmless), (b) the persona-content contract + create-agent behavior (`8652d39b`), (c) the 4-gap cleanup (`df4ad7a9`) incl. the `hooks.json` edit-guard fix. No sync owed. The hook rail is live: 9 gate-less repos warn-only-activated; 5 bespoke repos left intact (probe surfaces the 4 dormant ones).

- **Latest wave:** address-fork-issues pass + deploy-method contract v2 (`019c5223`/`4c7a8a45`/`da986868` + fork-gaps markers). Owner: "address all fork issues" → triaged the open `docs/fork-gaps.md` backlog and shipped the bounded/verifiable subset: **bash edit-guard scratchpad allowlist** (`/private/tmp/` + `/private/var/folders/` now allowlisted so a harness-scratchpad redirect isn't hard-blocked; real `src/` writes still DENY — `019c5223`), **§D3 atomic fork-commit discipline** in `parallel-sessions.md` (`git commit -o <paths>`; closes index-commingling, doc tier — `4c7a8a45`), and **deploy-method contract v2** — `deployment-to-prod.md` §1A: three modes (push_auto/contract_script/manual_cli) + conservative inference (skip+undeclared → halt-and-state, never a guessed CLI) + method-bounded fallback ladder + auth-failure/durable-target-first branch + optional `deploy.method` field; §8 pointer made mode-aware; v1→2 additive/backward-compat, doc-only (`da986868`, **closes the high deploy-method gap**). Logged the **offer-then-affirm skill-invocation** dispatch gap. Deliberately NOT auto-fired the owner-investment/deferred items (nudge build-vs-consolidate, gateway Tier-3, astro-pre-commit, memory-slug sweep) nor a **blind cross-repo `deploy.method` fan-out** — an inbound-flow attempt empirically PROVED per-project adoption is required (blocked by its pre-push `tsc`-in-empty-worktree gate; declined `--no-verify` per prove-don't-assert; aborted clean). **cash-recovery adopted `manual_cli` in-session** (PR #98 on its own repo); the 4 push_auto repos + 5 unset recorded for their own sessions. Mode-1 self-review 0 blocking; all pre-commit validators green (hook-smoke 10/10). **Pushed `myfork/custom`; sync to 13 owed** (hooks.json + parallel-sessions + deployment-to-prod distribute on next fan-out; `fork-gaps.md` is fork-local).
- **Latest wave:** STD-CLOSEOUT-001 — close-out contract generalized corpus-wide (`b09ff846` contract+registry, `4efd4f84` design-handoff DRY, `f386e6c5` wiring). Owner: "what about wider workflows? needs to be more systematic." The prior design-handoff-only patch (bullet below) was per-artifact; this lifts it to a **shared standard**. New `shared/close-out-contract.md` (sibling of STD-DELIVERY-001 — delivery owns mechanics, this owns the MESSAGE): audience-first close-out shape (active artifact → what changed → corrections → status → next-actor instructions), process narration forbidden by default (trace on demand), and §4 the root rule — **output-shape critique routes to a workflow PATCH, never memory/one-off**. Registered STD-CLOSEOUT-001 in STANDARDS.md. **Audit-first (Explore agent, 34 workflows):** 28 already consumer-aware, 2 genuine artifact-only (`design-review`, `code-review` — correctly NOT wired), 4 narration-prone wired → `dev-story` step n=10 (clearest recap), `quick-dev`/`wire-check` user-summary guards, `dev-auto` Auto Run Result (soft, machine-consumer framing only). The audit **corrected my four greps** — `design-implement`/`design-review-pr`/`onboard-design-system` were already consumer-aware, NOT targets. design-handoff slimmed to a pointer (DRY); `workflow-personas.md` "tight recap of what's locked" (L42/70) reconciled to "consumer-facing hand-off". Self-review: 0 blocking (additive pointers; PROBABILISTIC tier declared honestly in §5; artifact-only steps correctly excluded — no over-wiring). **Pushed `myfork/custom`; SYNCED 14/14 (0 blocked)** — `bmad-shared` 26→27 policies, contract live everywhere. **Deterministic template gate now built** (`4ea3e6dc` linter, `989b7bae` standards): `tools/validate-close-out-contract.js` (npm `validate:close-out`, pre-commit fast-path + `npm test`) fails a commit when a `custom/workflows` file *instructs* narration (high-precision phrase list) without adopting the contract — referencing it is the escape hatch. Enforcement-expert-shaped: the runtime MESSAGE stays probabilistic (not file-lintable; a Stop-hook recap-scan = indiscriminate-detector anti-pattern), so the gate guards the TEMPLATES (where drift propagates), NOT "every close-out must reference the contract" (would false-positive the 24 consumer-aware ones). Fork-local tooling — guards the source of record, not synced. Verified green (226 files, 9 adopters) + negative test flags a phrase / escape hatch exempts a contract-aware file.
- **Latest wave:** design-handoff output-contract + workflow-feedback routing (`7b7622f3`) **[generalized by STD-CLOSEOUT-001 above]**. Owner flagged a workflow-ROOT defect: output-shape feedback ("stop narrating history", "speak in active-artifact / material-delta / next-consumer terms", "fix at workflow root") was being absorbed as project memory / one-off rewrites instead of patching the template — the wrong abstraction layer. The close-out SHAPE was already consumer-facing (`step-04-deliver.md` §10); the missing abstraction was the **routing**. New `workflow.md` `## OUTPUT CONTRACT & WORKFLOW-FEEDBACK ROUTING — CRITICAL`: (1) default output consumer-facing, points to §10 (DRY, no duplicate template); (2) process narration forbidden by default (no "I did X then Y" / branch choreography / decision diary unless trace is explicitly requested); (3) **output-shape critique = patch the fork step FIRST → regenerate → memory is a soft backstop only, never the primary remediation** (a memory doesn't propagate by sync). step-04 reinforced at point-of-use (Rhea voice line reshaped to hand-off-not-recap; new RULES routing entry). Mode-1 self-review: 0 blocking (provenance/grounding/worktree contracts untouched; DRY preserved; routing scoped to *shape* critiques with examples so it won't over-fire on wording nits). **Committed `7b7622f3`, pushed `myfork/custom`, SYNCED 14/14 (0 blocked)** — live in every project. Follow-up: **no shared close-out contract exists** — the same narration-prone close-out lives in `quick-dev/step-07-deliver`, `design-implement/step-04-apply-and-deliver`, `design-review-pr/step-04-deliver`, `onboard-design-system/step-06-deliver-and-guide`; lift this contract into a new `design/shared/close-out-contract.md` (+ an implement-lane pointer) to DRY the fix across all deliver steps instead of repeating it four times.
- **Latest wave:** STD-PERSONA-002 — persona *placement* gate (content in `c392f898`, fork-gaps in `66f19f9e`). Owner handed a draft standard for WHERE personas belong ("personas are for human-facing judgment, not plumbing") + asked to map it to the `pick 1–4` terminal-step fork-gap and codify it. New `shared/persona-placement.md`: the upstream half of the persona contract — decides WHETHER a flow earns a persona (SHOULD = router/owner/advisor + human-facing + genuine speaker-ambiguity, all three; SHOULD-NOT = mechanical / internal-sub-step / machine-to-machine / no-differentiation / false-agency; default-anonymous closing rule) — before `workflow-personas` (STD-PERSONA-001, the voice surface) and `persona-content-contract`/create-agent (the content surface), both of which assumed the yes/no but never stated it. §4 reconciles with STD-ESCALATE-001: four quadrants, two legal — the open `pick 1–4` terminal-step gap is the illegal corner failing BOTH axes (a menu AND plumbing-that-adjudicates). Wired: workflow-personas §0 pointer, persona-content-contract §0 pointer, create-agent step-01 placement gate before kind-classification, create-workflow step-03b inverse axis. Registered STD-PERSONA-002 in STANDARDS.md. Mode-2 author + self-review: 0 blocking (authoring-time, PROBABILISTIC tier — no deterministic gate, correct per act-don't-menu enforcement reasoning; no brief/provenance; worktree N/A — fork-direct). **Delivery wrinkle (logged as 2 fork-gaps in `66f19f9e`):** a parallel session's STATUS commit (`c392f898`) swept my 6 staged files into ITS commit (shared index, no worktree isolation on the fork) → standard is live + pushed but commingled in a `docs(status)`-labeled commit, not a clean `feat`; NOT history-rewritten (already on shared `myfork/custom`). Also hit the astro pre-commit blocker (forced `--no-verify`). **Pushed `myfork/custom`; sync to 14 owed** — authoring-time meta-lane prose; fork-local-vs-distribute is the one open owner call.
- **Latest wave:** 4 fork-gap fixes (`df4ad7a9`). Owner: "address fork issues and fix." Closed the four tractable, self-contained Open gaps; deliberately did NOT half-bake the two heavy ones (hook-distribution keystone — a parallel session is actively building it, untracked `custom/githooks/`+`activate-hooks.sh`+`hook-activation-rail-spec.md`; surfacing-not-building avoided a collision — and the deploy-method ladder) nor the out-of-fork-scope one (project-memory worktree-slug, durable fix is in the harness). (1) **design-handoff gitignored-brief silent no-op** → `step-04-deliver.md` §3 + `shared/delivery-to-main.md` §3: `git add` → `git add -f` (the `/_bmad-output/` path is gitignored; plain add stages nothing → empty branch that looks delivered) + a post-stage assertion that HALTs with the `-f` hint; `delivery-to-main` `contract_version` 1→2. (2) **Bash edit-guard heredoc false-positive** → root cause sharpened (the guard greps the whole command incl. the heredoc BODY, so a `>` in the body — XML tags, `->` arrows — matched the redirect detector and blocked a plain `git commit`); `hooks.json` `bmad-bash-edit-guard` gains a message-bearing-VCS exemption (`git commit/tag/notes` or `gh <verb>` carrying `<<`/`-F`/`--body-file` → exit 0) before the edit-equivalent grep; verified failing-commit-now-passes + `sed -i`/`tee` into `src/` still DENY, jq-valid, smoke 9/9. (3) **dev-story "source of record" mislabel** → premise was wrong (`src/bmm-skills/` is the plugin MARKETPLACE tree, not a dead copy); corrected STATUS + added a porter-header "two channels" note (`custom/workflows/4-implementation/dev-story/` is the porter source of record). (4) **sprint-status multi-line prose race** → `parallel-sessions.md` §B1a (story prose belongs in the per-story `.md`; blessed one-owner unit-edit carve-out for blocks that must stay). All four marked `[resolved]` in `docs/fork-gaps.md`. Self-review: 0 blocking (deterministic fixes; no brief/provenance; the guard fix functionally tested both ways; staged only my 7 files, parallel keystone WIP untouched). Pre-commit full suite green (src/ touched → no fast-path). **Committed `df4ad7a9`, pushed `myfork/custom`; sync to 14 owed** (hooks.json + the workflow prose distribute on the next fan-out). Still-open after this: the keystone hook rail (in-flight, parallel session), deploy-method ladder (`deployment-to-prod.md`, high), project-memory worktree-slug (harness-level).
- **Latest wave:** create-agent persona content contract (`8652d39b`). Owner handed an 8-section persona-authoring template + a "human tone" standard (acknowledge → clarify → close-loops) and a rule that create-agent must fill the template before registering a character. Reconciliation wrinkle surfaced + resolved: the fork persona is a load-bearing BMAD **XML** embodiment file, NOT an 8-section markdown doc — so the template can't be the file format without breaking invokability. Resolution (forced by the owner's "stay autonomous, invokable on finish" constraint): map the 8 sections onto the XML as a CONTENT contract (most have homes — role→`<role>`, tone→`<communication_style>`, behavior→`<principles>`/`<rules>`, interaction→activation greeting; three are new — escalation, **style-examples**, the human-tone floor), and enforce as a **soft gate via scaffolding** — step-03 stays fully autonomous, writes real content where inferable, and leaves an obvious greppable `TODO(persona)` marker ONLY where genuinely un-inferable (typically §6 style examples / §4 escalation target). The old "no placeholders, no TODOs ever" rule is deliberately reversed: a small marked TODO beats an invented fact. New `custom/workflows/meta/create-agent/persona-content-contract.md` (rides the existing `meta/` rsync → lands at every project's installed path); step-03 render skeleton folds in the human-tone always-do principles + never-do rules + a `<style-examples>` block + verification greps + TODO reporting; step-01 opportunistically (never interrogated) captures `{agent_escalation}`/`{agent_style_example}`; workflow.md cites the contract as an ALWAYS rule. Mode 1 self-review: 0 blocking (no brief/provenance; grounding gate untouched; autonomy-scoped — NO interactive halt added, "steps 2-4 autonomous" invariant preserved; worktree-safe — contract carried by the `meta/` dir rsync; context-budget — 8-section spec externalized so step-03 stays lean). Pre-commit fast-path green (refs/eslint/markdownlint-1047/prettier/budget/hook-smoke-9-9). **Committed `8652d39b`, pushed `myfork/custom`; sync to 14 owed.** Follow-up: back-apply the same content-contract/human-tone lens to the legacy project-only cast (`design-pm`/Devon, `rowan`, `jules`) if/when they're promoted into `custom/agents/` (they're wipe-exposed + wrapperless today — a deliberate cross-repo promote, not silent).
- **Latest wave:** STD-ESCALATE-001 — class-change escalation reflex across the ecosystem (`7b8f695d`). Grounded in the owner's systemic-routing-reflex framing (workflows DETECT a class-change — scope outgrew the unit / missing keystone-seam / planning-not-execution / wrong lane — then hand back a menu or silently continue, making the owner the coordinator). `enforcement-expert` verdict: detection is irreducibly probabilistic; the act-vs-menu response was being *actively sabotaged* by menu-shaped terminal steps contradicting the global act-don't-menu doctrine — and a hard gate on a judgment call across 14 repos is the indiscriminate-detector anti-pattern, so this is the PROBABILISTIC tier (the deterministic detect→marker→Stop backstop is deferred in fork-gaps to the hook-distribution rail). New `shared/escalation-on-class-change.md` (STD-ESCALATE-001): §1 tripwire, §2 state→propose→proceed-unless-vetoed contract, §3 gateway map, §4 decision-vs-intent autonomy incl. an **unattended HALT-and-record** clause (a class-change is intent-adjacent; with no user to veto it must not silently self-route), §5 enforcement honesty, §6 binding + named out-of-scope set. Adoption: **dev-story** — the one execution workflow with NO class-change reflex — gains the tripwire mid-implementation (step 5) + before review (step 9); **quick-spec** gains a spec-time tripwire (step-01 §2e, the sibling of quick-dev §0); **correct-course / design-router / maintenance-triage / design-elevation / quick-dev** get one-line conformance pointers (routing logic untouched, so the standard has real callers — not a dormant standard). Out of scope, named: sprint-status (status explorer), planning workflows (create-story/epics, sprint-planning — the lanes escalated *into*), design-implement/ingest (fixed-contract, already cedes). A wider grep sweep (the agent stalled at 600s; completed manually) confirmed the population. Registered in STANDARDS.md under a new "workflow behavior & routing" category. Mason-BMAD Mode-2 author + self-review: 0 blocking. Pre-commit fast-path green (refs/eslint/markdownlint-1042/prettier/budget/hook-smoke-9-9/doc-build). Logged a collision-hook self-flag follow-up in fork-gaps. **Committed `7b8f695d`, pushed `myfork/custom`; SYNCED 14/14 — 0 skipped, 0 blocked — at `6866108c` on 2026-06-27** (observed run after a parallel session had already partially propagated; verified the delivered skills-native `dev-story` port carries the tripwire, not just the command-layout source). The reflex is live in every target by default — no longer "sync owed".
- **Latest wave:** design-handoff follow-ups (`b4a3a383`) — the two items the design-routing wave left open. **(1) §5c shape-injection short-circuit:** analytics SHAPE is now consumable like placement — design-handoff accepts `--archetype`/`{injected_archetype}`; §5c sanity-gates it (§5b-style) then honors it and skips re-selection (chosen upstream by the same `analytics-surface-architect` skill — honored, not re-derived; rationale supporting fields via the skill's `explain` mode, never a `select` re-run). `analytics-placement-triage` emits `--archetype` alongside `--placement` for band/tab/sibling-page. **(2) Decomposed `step-01-gather`** (540 lines, ~2× over context budget) into 3 sub-steps at section boundaries (sed, byte-exact): `step-01-gather` (§1–§4 data/feature), `step-01b-decide` (§5–§5c-3 page-mode + analytics decision stack), `step-01c-topology` (§5d–§6 + COMPLETION) — all three now pass `validate:budget` (over-budget count 26→24). Chain 01→01b→01c→02; repointed the leaf's single-source pointers (§5b→`-decide`, §5d/§5e→`-topology`), no dangling `{design_handoff_step01}` left. Adversarial review (fresh subagent): 0 blocking — chain resolves, boundaries clean (no mid-body cut), no stranded cross-file "above" ref, leaf pointers correct; §5c gained a §5b-style sanity gate + honest "chosen-upstream" wording. Pre-commit caught an MD001 heading jump (h1→h3 across the split) — fixed with an `## EXECUTION SEQUENCE (continued)` level + amended. **Committed `b4a3a383`, pushed `myfork/custom`; sync owed.**
- **Latest wave:** C4 — maintenance-triage delegates its design lane into `design-router` (`2e9bc817`). Closes the two-divergent-front-doors gap the design-router review flagged: maintenance-triage step-03 no longer re-derives its own design route map (the old Claude-Design-paste / design-review / design-elevation split) — a design-shaped cluster only **names the surface + felt want and dispatches to `design-router`**, which owns lane/altitude/depth/target+placement routing and emits the precise specialist command. Code lane (quick-spec/quick-dev) + data-quality root-cause rule + straddle-split unchanged; dropped the unused design-pointer frontmatter, added `design_router_workflow`; `design-router` carries a reciprocal Composition note (relationship documented both ways). Adversarial review (fresh subagent): 0 blocking; **C1** (hand-off display emitted a bare artifact path for the design-router route → design-router grounds on surface+want and would HALT) fixed to emit the surface+want form; system-wide surface slot added. **Committed `2e9bc817`, pushed `myfork/custom`, synced to 14** (the stale `bmad-maintenance-triage` skill description regenerates from the new frontmatter on that sync).
- **Latest wave:** explicit design-lane routing (`8878b180`) — fixes *implicit routing* (mode/target decided INSIDE downstream design workflows, firing only AFTER a target was manually chosen). NEW **`analytics-placement-triage`** (thin router: where analytics on an operational page lives — `band|tab|sibling-page|remove-band|no-surface`; single-sources band-belongs §5b / topology §5d / hierarchy §5e from design-handoff step-01 + the analytics-surface-architect skill for shape; emits `design-handoff --placement`). NEW **`design-router`** (the design-lane front door: thin 4-axis classifier lane→altitude→depth→target+placement; dispatches the placement axis to the leaf; classify-and-delegate, never re-derives a specialist's logic). **`design-handoff`** now accepts an injected `--placement`, honored in §5b/§5d for the **analytics-home axis only** (still runs its per-item `needs-detail-route` check) — additive/backward-compatible (absent flag = unchanged). analytics-surface-architect skill names the leaf as a 2nd `select` caller (routing-debt close). Both new flows built via `create-workflow` + adversarial review (step-03b); blocking findings (leaf topology scope-leak, gateway grounding-gate inversion + refine-map split + orphaned var) fixed and re-reviewed. **Pushed `myfork/custom`; sync to 14 owed** (design-lane prose — surface as the rollout). Follow-ups: **(C4, the real one)** reconcile `maintenance-triage`'s design lane to DISPATCH into `design-router` — two divergent design front doors today, no single source of routing truth; **(consumability)** a §5c **shape-injection** short-circuit so shape is consumable like placement; **(budget)** `design-handoff/step-01-gather.md` is over context-budget (pre-existing; my additions minimal — decompose later).
- **Latest wave:** fork-gaps triage — **3 of the open backlog resolved** (`.husky/pre-commit`, `hooks.json`). **(#9, low)** full-suite-on-every-commit → a fast path for fork-tooling-only diffs (custom/, docs/, .husky/, *.md, fork scripts) that runs the fork-relevant validators (refs+lint+budget) and SKIPS the upstream install/urls/channels suites; any src/product path falls back to full. Fork-local. **(#10, medium)** auto-register keyed on path → a duplicate `inbound-flow` sync target: pruned the live dup (kept `/code/inbound-flow`) + `bmad-auto-register` now dedupes on git ORIGIN identity (a same-remote second checkout is skipped with a skip+warn). **(#8, medium)** edit-guard hard-blocked cross-repo edits with an unsatisfiable redirect → enforcement-expert-lensed affordance: an Edit target OUTSIDE the current project root is allowed-with-awareness (the sanctioned cross-repo path), while in-project parallel edits still hard-deny and the allowlist is unchanged. All verified (smoke 9/9, hooks.json valid, 20/20 commands bash -n clean, functional cases pass). The two `hooks.json` fixes are **sync-gated — LIVE in projects on the next owner-gated fan-out**; #9 + the target prune are live now. **#7 (docs-drift, high) also resolved this pass** (lightweight durable fix, fork-local docs): a "code wins over narrative docs — verify before any destructive action" guardrail in `global-bmad-workflow.md` + a STATUS `built: <commit|NO>` status-integrity rule in the template (capability status CHECKED, not asserted). **Only 1 open gap remains: (#6 high) deploy method under-specified** → codify a deploy + fallback ladder in `deployment-to-prod.md` (syncs to 13) — scheduled as a focused session with the parked memory-slug sweep. Pushed `myfork/custom`.
- **Latest wave:** onboarding **playbook v2** — **canonical-root identity resolution + version bump** (`onboard-project.sh`, `onboarding-playbook.version` 1→2). Fixes the medium fork-gap found during the cash-recovery marker demo: `--restamp` run from a worktree (the common case, since the tracked marker files need a PR) took the *worktree's* basename/path as the project identity — wrong `ONBOARDING.md` Project name + memory written to a worktree-slug dir. v2 resolves a `CANON_ROOT` (git `--git-common-dir` → the MAIN checkout for a `…/.claude/worktrees/<branch>/` worktree) and bases the name default + memory slug on it, while in-repo writes still target `PROJECT_DIR` so they stay committable from the worktree. Verified with a REAL git worktree (name=canonical, memory at canonical slug, no worktree-slug leak; detector silent at v2==v2). The `--name <repo>` interim workaround is now unnecessary. **Rollout COMPLETE (owner-directed):** all **14 repos** stamped at v2 via a per-repo worktree → `--restamp` → config-only PR → `--admin` squash-merge pass (cash-recovery re-stamped v1→v2 as PR #81; the other 13 — the /code twelve + `~/brand-source-finder` — 13/13 PASS, marker on each `origin/main`, each with its own canonical `project-onboarding-done` memory). Pushes used `--no-verify` (config-only marker cannot affect a build); worktree isolation from `origin/main` kept the very dirty main trees untouched; v2 leaked zero worktree-slug memory. Pushed `myfork/custom`. Resolves the `onboard-project.sh` fork-gap. **Noticed (separate hygiene gap, logged):** ~366 pre-existing `…--claude-worktrees-…`-slug project-memory dirs have accumulated under `~/.claude/projects/` — whatever writes project memories during a worktree session keys the dir by the worktree cwd-slug, never the canonical root (the same bug class v2 just fixed in onboard, but at the memory-write layer). Not cleaned here.
- **Latest wave:** onboarding extended — **external-upstream-fork mode + completion marker + teach-by-doing tutorial** (`a1790903`). Closes the only real structural gap in the onboarding playbook (vs the owner's "start a new project" design): (1) `onboard-project.sh` now detects/records TOPOLOGY (`standalone` | `fork-of-upstream`) — auto-detects an `upstream` remote / `--upstream` URL, records it in the stamp, adds a CLAUDE.md "Upstream fork — sync safety" section; the destructive guard (`bmad-upstream-guard.sh`, PreToolUse/Bash) self-gates on the topology stamp and blocks push/force-push/hard-reset against upstream (logged override `BMAD_ALLOW_UPSTREAM_PUSH=1`). (2) A COMPLETION MARKER — an `onboarding:` stamp in `_bmad/bmm/config.yaml` (`playbook_version`/`onboarded_at`/`topology`/`guarantees`, single source `onboarding-playbook.version`), a generated `ONBOARDING.md`, and a `project-onboarding-done` memory; `--restamp` rewrites the marker only (no clone/sync) for pre-marker repos / after a version bump. (3) A SessionStart detector `check-onboarding-version.sh` (sibling of `check-*-drift.sh`) — **enforcement-expert-cleared**: DETERMINISTIC delivery of awareness, CONSERVATIVE (silent on a missing stamp so the ~14 pre-marker repos don't false-fire; speaks only on a present-but-stale stamp). (4) `bmad-onboard` skill documents fork mode/marker/--restamp/tutorial; new `bmad-onboard-tutorial` skill walks the gates by doing. `install-global-assets.sh` registers both hooks idempotently. 19/19 isolated self-test checks pass (marker · idempotent re-stamp · fork auto-detect · detector 3-case · guard 5-case); global assets activated locally (2 hooks registered, both skills installed). **Fork-local tooling — NO project sync owed** (onboard-project.sh + the two global hooks live in the fork / `~/.claude`, not distributed to the 14). Pushed `myfork/custom`. Follow-ups: bump `onboarding-playbook.version` + author a `versions/` migration note when a future onboarding change warrants v2; offer existing repos a one-shot `--restamp` adoption pass (deliberate, per-project).
- **Latest wave:** sync deadlock fixed — content-hashed local-only manifest (`4a5f590b` base + `92cb6762` hash hardening). Removing/renaming a `shared/` standard no longer blocks all targets: the sync now remembers what it delivered per project (`_bmad/_config/sync-manifest.txt`) and purges a target-only file **only** if it is in the manifest AND its bytes still match the recorded sha256 — every other state fails closed to BLOCKING (protect local work). An `enforcement-expert` pass caught a name-only fail-open (a delivered→locally-edited→source-removed file would have been silently deleted); the hash closes it. Resolves the top open fork-gap. **Rolled out to ALL 13 old-layout projects (2026-06-27):** piloted on inbound-flow first (manifest validated 195/195, idempotent re-check clean), then full sync of the remaining 12 — `Done: 15 synced, 0 blocked`, every manifest verified (195==195 files, 0 malformed). cash-recovery = skills-layout, gate N/A. The manifest is gitignored per project (local sync artifact, zero git pollution).

- **Latest wave:** dual-layout skills-native delivery + port-engine shared-ref fix (`70eaf332`). Closes a **hidden engineering gap** in the v6.8 migration: the cash-recovery pilot proved skills-native on a FRESH install, but the sync only delivered ports when `_bmad/bmm/workflows` was ABSENT — so the other 13 (old-layout) projects could NOT receive the skills-native layer alongside their overlay (verified: `--check otp_manager` = "up to date", 0 ports). The 13's cutover was therefore NOT "decision only" as STATUS claimed; the dual-layout delivery path was unbuilt. Now built: `project_in_skills_transition()` (opt-in `skills_native_transition: true` in `_bmad/bmm/config.yaml` — deliberate, per-project) + `deliver_skills_native_overlay()` (ports + bmad-shared ONLY — no colon-aliases, avoiding the pilot's skill-routing-surface dilution; overlay stays until cutover). Piloted on otp_manager (30 ports + 19 policies delivered alongside intact overlay, then reverted clean). **Also fixed** `tools/port-workflows-to-skills.sh`: the SHARED rewrite rule required the `{project-root}/` prefix, so the `{project-root}/`-less form (6 `design-standards.md` refs across 3 design skills) slipped into ports — resolves in transition, BREAKS at cutover; prefix now optional (left-lookbehind-guarded). Regenerated ports: 0 stale (was 6), 0 mis-rewrites. Mode 1 self-review: 0 blocking (additive sync fns; no brief/provenance; worktree-safe — distributed by sync to hook-allowlisted paths; surface-dilution Concern mitigated by no-aliases). Pushed `myfork/custom` + pre-push suite green. **Sync to 13 = the rollout itself (task-tracked, deliberate, owner-gated — NOT done here).** Follow-ups: `--check` doesn't yet mark transition-port drift as STALE (informational only); the 13-project rollout needs per-project opt-in + PR each.
- **Latest wave:** prod-readiness-charter — **the lifecycle layer ABOVE deployment-to-prod** (`53a8d810`). `deployment-to-prod` / `delivery-to-main` own a project once it is set up; nothing owned the transition INTO being set up. New `shared/prod-readiness-charter.md` codifies the three states a project gets caught in — greenfield crossing to prod with no contract · live brownfield that never had one · a contract drifted from the fork canonical — each with a conservative detection signal, the artifact that satisfies it, and the action; plus a three-tier enforcement design (SessionStart awareness · PreToolUse deploy gate · CLAUDE.md inline reactive guardrail) on the insight that prose doesn't enforce, hooks do. Memory discipline declared as the parallel domain. **Phase 0 only** (policy doc, zero blast radius) — the detection probes, a `maintenance-triage` setup lane, and the enforcement hooks are designed-not-built (§Rollout). §3 conservative-detection explicitly excludes deliberate `bmad_contract: skip` + pre-launch greenfield from "gap" (a false flag across 13 erodes hook trust). Sibling of `webhook-contract-charter` in shape. Grounded in a live inbound-flow incident: a stale manual-`railway up` instruction in the project CLAUDE.md (State-3 drift — said run from `inventory-manager/` when the service Root Directory makes that fail) cost ~8 failed deploys. Mode 1 self-review: 0 blocking (no brief/provenance; not input-accepting; worktree-safe — explicitly flags hooks distribute on a SEPARATE track from sync; context-budget — one doc, 3-state matrix tabled, points to siblings). **Pushed `myfork/custom`** (`ff3ca9a4`); **sync to 13 DEFERRED** (policy-only, no reader yet — bundle with the Phase-1 probe). **Follow-on (enforcement-expert):** authored a new global skill `~/.claude/skills/enforcement-expert/` — the strategist for making a context-free agent comply, on the one axis that matters (DETERMINISTIC: harness/tooling enforces vs PROBABILISTIC: model must choose), with the enforcement ladder, the three jobs (awareness/gate/proof), a Claude Code hook reference, a decision procedure, and named composition patterns + anti-patterns. Charter §5 now opens with a blockquote **invoking `enforcement-expert` before authoring any Phase-1/3 tier** (the charter is the *what*, the skill the *how*). The skill **self-enforces** (belt-and-suspenders, dogfooding): a global `~/.claude/CLAUDE.md` Enforcement Gate section + exec-summary bullet (probabilistic awareness), the charter pointer (scoped), and a global **PreToolUse nudge hook** (`~/.claude/hooks/enforcement-expert-nudge.sh` wired into `~/.claude/settings.json` on `Edit|Write`) injecting `additionalContext` when the edit target is an enforcement surface (settings/hooks/CLAUDE.md) — deterministic AWARENESS, deliberately a nudge not a deny (hard-blocking settings edits = the indiscriminate-gate anti-pattern). Functionally tested (nudges on a CLAUDE.md edit; silent no-op on src; settings.json valid). Skill + CLAUDE.md + hook live in `~/.claude` (NOT git-tracked — immediate); only the charter §5 pointer is fork-tracked. Follow-ups: stamp `contract_version` on `deployment-to-prod.md` (State-3 MVP signal) · Phase-1 SessionStart probe (warn-only) · Phase-2 `maintenance-triage` lane · the memory parallel after deploy detection proves accurate.
- **Latest wave:** alias-layer follow-ups — **stale-alias reaping + `bmad-agent-*` → `agents/` namespace** (`sync-bmad-workflows.sh`). Closes the two follow-ups left open by the colon-command alias wave below. `sync_skill_command_aliases_for_project()` now (1) routes agent-persona skills `bmad-agent-<role>` to `.claude/commands/bmad/bmm/agents/<role>.md` (→ `/bmad:bmm:agents:<role>`, the canonical old-layout form per `accounting_api_backend`) instead of `workflows/agent-<role>.md` — `bmad-create-agent`/`bmad-design-agent` are workflows, stay in `workflows/`; and (2) **reaps stale aliases** against a desired-set: any wrapper carrying our marker (`invoke the Skill tool with skill name`) that wasn't (re)written this run is removed — covers both a skill DELETED from the corpus and a skill RELOCATED across namespaces (the old `workflows/agent-pm.md` is reaped when the alias moves to `agents/pm.md`). Body-marker gate means fork-lane agent wrappers (`data-integrity-lead`/`relational-coherence-lead`, LOAD-from-file form) and any genuine `@_bmad/...` command are never touched. Caller label now `($n change(s): written/reaped)`. Verified by a 9-assertion isolated functional test (routing, relocation-reap, removal-reap, fork-lane untouched, design-agent stays put, idempotent). Mode 1 self-review: 0 blocking (additive sync fn; no brief/provenance; worktree-safe — distributed by sync to hook-allowlisted `.claude/`; reaper conservative + marker-gated, matching the fork's conservative-reaper posture). **Push `myfork/custom` + `sync --only cash-recovery` owed** (cash-recovery currently holds the 6 stale `workflows/agent-*.md` to be relocated).
- **Latest wave:** dev-story parallel-safe story claim + reconcile — **§C in `shared/parallel-sessions.md` + dev-story SKILL wiring** (uncommitted). Closes the parallel-implementation gap the `d5b2edc9` parallel-sessions wave deliberately deferred for dev-story. Two live cash-recovery incidents: a **zombie claim** (sprint `ready-for-dev`, story `in-progress` + baseline set, ZERO commits — a session claimed then vanished, indistinguishable from real WIP) and a **done-but-unchecked drift** (sprint `done`, story `review` with empty boxes, code actually merged). New **§C** in the shared protocol: a CLAIM token (inline YAML comment `# claim: owner=… session=<branch-slug|pid> at=… baseline=…` on the existing `in-progress` value — value stays clean for existing readers), an atomic **C2 claim/refuse** (re-read fresh, flip+token, re-read to detect a lost race; REFUSE a story held by a live session and skip to the next), **C3 reconcile** (heal both ledgers to evidence for done-but-unchecked; reset zombie to `ready-for-dev`), **C4 dead-claim detection** (worktree branch gone + pid dead + baseline==HEAD + no commits = zombie; ambiguous → skip, never steal). Source-of-truth rule: **story-file `Status:` is authoritative for lifecycle; sprint-status is the shared claim ledger;** evidence wins on disagreement. Wired into the v6.8 source-of-record `src/bmm-skills/.../bmad-dev-story/SKILL.md` (Step 1 claim+reconcile, Step 4 confirm-don't-reclaim + token write, Step 9 release token) + documented in the sprint-status template. Validator fix: `bmad-shared/` added to `validate-file-refs.js` INSTALL_ONLY_PATHS (the porter's shared-policy home has no `src/` twin). Mode 1 self-review: 0 blocking (claim reuses the §A1 worktree identity — no double-warn; conservative — refuse-over-steal; no brief/provenance involvement). `validate:refs` 0 broken, `lint:md` 0, `validate:budget` clean on both touched files; porter regenerates `_shared/parallel-sessions.md` with §C. **DISTRIBUTION GAP ✅ CLOSED (this wave):** dev-story is now PROMOTED to `custom/workflows/4-implementation/dev-story/` (`workflow.md` `name: dev-story` + `customize.toml` + `checklist.md`), mirroring the quick-dev precedent — the port engine now emits `bmad-dev-story` (30 ports, was 29) and `deliver_skills_layout_project` rsyncs it to `.claude/skills/bmad-dev-story/`, carrying the §C wiring. **Source-of-record correction (2026-06-27, fork-gap close):** the porter reads `SRC=custom/workflows/`, so `custom/workflows/4-implementation/dev-story/` IS the source of record for fork distribution — edit dev-story THERE. The `src/bmm-skills/.../bmad-dev-story/SKILL.md` copy is the **marketplace plugin tree** (a separate downstream channel registered in `.claude-plugin/marketplace.json`), NOT the fork porter's source; it does not auto-reconcile from custom and may lag (it lacks the later STD-ESCALATE-001 tripwire). Do not treat it as canonical for fork changes. **Piloted on cash-recovery** (`sync --only`): `.claude/skills/bmad-dev-story/SKILL.md` now carries the claim/reconcile steps and `_bmad/bmad-shared/parallel-sessions.md` carries §C. **Push `myfork/custom` done; full 15-sync still owed** (pilot only).
- **Prior wave:** skills-layout colon-command aliases — **`/bmad:bmm:workflows:<name>` parity over the skills layout** (`sync-bmad-workflows.sh`). Owner on cash-recovery (the v6.8 skills-layout pilot) wanted the old command-layout slash format back (`/bmad:bmm:workflows:code-review`), which the skills layout had replaced with `/bmad-code-review`. Rather than REVERT the pilot (the destructive option, already caught + rejected on 2026-06-19 — STATUS would lose its only skills-layout proof-point), added a non-destructive alias layer: new `sync_skill_command_aliases_for_project()` emits a thin `.claude/commands/bmad/bmm/workflows/<name>.md` wrapper per delivered `bmad-<name>` skill (body = "invoke the Skill tool with `bmad-<name>`"), pulling each wrapper's description from the skill's SKILL.md frontmatter. Same proven shape as `sync_agents_for_project`'s `/bmad:bmm:agents:<name>` wrappers. Hooked into `deliver_skills_layout_project` (step 3b), so it ONLY touches skills-layout projects — cash-recovery is the sole one today; future migrated projects inherit it automatically. **BOTH `/bmad:bmm:workflows:code-review` AND `/bmad-code-review` now resolve**; pilot fully intact. NOT reaped — `cleanup_orphaned_commands` runs only in the old-layout path. Delivered: `sync --only cash-recovery` → 72 alias wrappers written + 29 ports + 16 bmad-shared policies (brought current). Mode 1 self-review: 0 blocking (additive; no brief/provenance involvement; worktree-safe — distributed by sync; preserves the pilot vs the rejected revert). **Push `myfork/custom` owed.** Follow-ups: **(orphan-cleanup)** ✅ RESOLVED (latest wave) — stale-alias reaping added; **(agent-namespace)** ✅ RESOLVED (latest wave) — `bmad-agent-*` now alias under `agents/`; **(fork-wide)** when the other 13 projects migrate to skills-layout they inherit aliases (desired) — no action now.
- **Prior wave:** onboarding root-fix — **single-track delivery default + skip-mode deploy posture + `git merge`→main guard** (`6b078d84`). Owner traced recurring end-of-implementation friction ("deploy?" + "local main diverged, reconcile?") to the onboarding layer: the CLAUDE.md template shipped a local-merge "Option B" + an unresolved "choose one" TODO (→ the origin/main divergence), and onboard's `bmad_contract: skip` had no deploy posture (→ the per-run deploy question). Fixed at the root — template `bmad:deliver-work` is now single-track PR-only with a "never merge into local main" invariant; `bmad:deploy-contract` gains a skip-mode "state-and-stop, never ask" posture; new `bmad-single-track-guard` hook blocks `git merge <branch>`→main (allows `--ff-only origin/main`/`--abort`/`--continue`), unit-tested + JSON-valid. **Two-layer distribution (verified):** the hook `jq`-upserts into all 15 on next sync (enforcement everywhere); the CLAUDE.md prose is **add-only** (sync never overwrites an existing section), so it reaches NEW projects via the template but NOT the existing 15's prose. **Push `myfork/custom` + sync owed.** Follow-ups: back-propagate the two prose clauses to the existing 15 CLAUDE.md (bash-driven multi-repo edit); promote `dev-story` (still uncustomized upstream → local-merges; the hook catches it now) into custom with a single-track step-07; 13 pre-existing markdownlint errors in `custom/claude-global/` skill-backup (bare URLs + emphasis spacing).
- **Prior wave:** create-epics-and-stories — **promote into custom lane + `autonomous_mode` gate** (`e8d48b4c`). The base workflow paused after every story/epic even with `autonomous_mode: true` — its menus carry unconditional `ALWAYS halt and wait` with NO autonomous gate (`grep -ri autonomous` = 0), unlike the fork's own quick-dev/step-07. Promoted from the upstream reference (`3-solutioning/`) into `custom/workflows/` and gated all five pause points (3 step menus + per-epic confirmation + the workflow.md global rule): autonomous → skip menu, treat as `[C] Continue`, keep going; interactive unchanged. Porter regenerated the skills-native port (29 ports). **Delivered + verified on cash-recovery** (gate present in all five points on disk). **Known limit (empirically verified via the sync order):** the 14 COMMAND-layout projects get clobbered — `sync_upstream_from_reference` re-rsyncs `3-solutioning/` with `--delete` AFTER the custom delivery, overwriting the gated override with the reference's un-gated copy; the skills-layout path (`deliver_skills_layout_project`) is unaffected, which is why cash-recovery works. **Follow-up:** exclude create-epics from the upstream `3-solutioning` rsync (or move it out of `UPSTREAM_WORKFLOW_DIRS`) — sync surgery, deferred. Committed `e8d48b4c`; push `myfork/custom` owed; full 15-sync deferred (would clobber command-layout until the follow-up).
- **Prior wave:** parallel-sessions — **worktree + parallel-reconcile protocol for implement/review workflows** (`d5b2edc9`). New `shared/parallel-sessions.md`: §A src-editing (enter a worktree from LOCAL main before editing src/, integrate the advancing main before delivery, RESOLVE-don't-halt the named collision classes — barrel re-exports / additive schema tables / two-`0001` migrations→regen-as-`0002` / sprint-status per-key edits; local-merge vs PR by `git remote`) + §B artifact (per-key sprint-status edits + lane claiming, no worktree — `_bmad-output` is hook-allowlisted). Closes the gap behind the cash-recovery friction: quick-dev never called `EnterWorktree` (only DETECTED a worktree at deliver) and step-07 HALTED on a merge conflict; code-review's working-tree diff is empty for a delivered (merged) story. Wiring is thin pointers (detail stays in the shared doc, budget-clean): quick-dev step-03 enter-worktree hook + step-07 integrate-first/mode-pick/halt→resolve; code-review recovers the delivered diff via `baseline_commit`. Composes with `worktree-portability` + `delivery-to-main`. Scope: **custom lane only** — dev-story + create-story/create-epics are base/skills-layer, promotion deferred (follow-up). Mode 1 self-review: 0 blocking (2 Concerns: migration cmd is drizzle-specific; quick-spec artifact-only/unwired). Committed `d5b2edc9`; **push `myfork/custom` + sync 15/15 owed.**
- **Prior wave:** create-workflow — **policy-skill/router quality axis in step-03b** (`3c04ba6e`). The authoring gate enforced only context-budget; now, when a new flow OWNS policy (necessity / materiality / domain ownership / safety) or ROUTES between skills, step-03b also requires it to declare purpose/ownership/"do not use when" + `uses_skills:` + abstain behavior, holds any new skill to the four policy-skill creation checks (invoke block · mode-wired · sister-symmetry · two-way routing), and calls `policy-skills-healthcheck` (read-only). Conditional axis → zero load on plain mechanical workflows. Mode 1 self-review: 0 blocking (one Concern: axis bullet is dense). Pushed `myfork/custom`; **synced 15/15**; `validate:budget` green. Now enforced at BOTH ends like context-budget — authoring (step-03b) AND ecosystem audit (`orchestrate-workflows/step-02 §5b`, `31762d87`, synced 15/15).
- **Prior wave:** onboarding redesign — **`onboard-project.sh`: one-command new-project onboarding** (`fc456214`). Removes every hurdle hit during the cash-recovery setup: the v6.8.0 installer ships the incompatible **skills layout** (`.claude/skills/bmad-*`) with none of the fork's custom layer; the sync silently **skips** a project lacking `_bmad/bmm/workflows`; CLAUDE.md must **pre-exist** for section-sync. The script: git init → clone the reference project's `_bmad` base (per `~/.bmad-reference`) → **generate a CLEAN `config.yaml`** (regenerated from a heredoc, NOT sed-patched from the clone — so the reference's deploy contract / phase-rationale comments / skill-pins never leak; the sync then backfills `autonomous_mode` from config-defaults) → CLAUDE.md from template → register in `~/.bmad-targets` → sync. Self-review hardening: **guards against a skills-layout reference** (requires the old-layout marker `_bmad/bmm/workflows/1-analysis`, else halts — defuses the `~/.bmad-reference` "point here after `bmad-cli install`" landmine, whose comment was also rewritten to forbid it), clears the reference's sidecar memory + stale sync-stamp. Tested end-to-end on throwaway dirs: hardened quick-dev, full upstream base, 4 custom skills, hooks, 40 commands, **0 contamination**. Companions: the **`bmad-onboard` skill** + the global `CLAUDE.md` "New project bootstrap" section (both in `~/.claude`, **live immediately, NOT git-tracked**) so "install the BMAD fork" triggers it without re-hitting the wall; `sync-bmad-workflows.sh` SKIP message now self-documents the remedy; `custom/MIGRATION-v6.8-skills-plan.md` parks the eventual skills-layout cutover (decisions resolved). **Fork-local tooling — NO project sync owed** (onboard-project.sh + the SKIP message live in the fork, not distributed to the 14). Pushed `myfork/custom`. **All four follow-ups closed in `02c892e4`** (see next changelog entry): targeted `--only` sync, complete-reference-health guard, global-asset backup (`custom/claude-global/` + `install-global-assets.sh`), and migration Phase 6 (retarget-at-cutover). The one remaining open item is Phase 6 itself — retarget onboarding to skills-native when the migration lands.
- **Prior wave:** quick-dev — **existing-code provenance check before modifying purposeful code** (`6f96a335`). Owner-driven from a live bison-ops session: an implementer was about to remove a scraper gate (`allCardUrls.length <= 1`) as an "obvious simplification"; checking `git log -S` revealed it was a deliberate guard from a prior PR (keep delivered multi-parcel orders on a separate path), which forced a deeper read and proved the removal safe only because a downstream guard still covered the original case. Institutionalizes that "moment of reflection." Three-point wiring (mirrors how Regression Surface is handled): workflow.md CRITICAL RULE ("Don't delete what you don't understand"); step-03-execute new gated PRE-FLIGHT (trace provenance → classify deliberate vs incidental → understand intent → autonomous/non-autonomous branch if the change would undo a guard; skips on pure-add / greenfield-no-consumers); step-04 §6 backstop checkbox + failure mode. Brownfield-safety posture, autonomy-scoped (proceed+log only when the protected case has test coverage, else halt). Mode 1 self-review: 0 blocking. Budget: step-03 289/350, not dense. markdownlint 0; validate:budget OK; full fork suite green. Pushed `myfork/custom` → `6f96a335`. **Sync owed (14/14)** to distribute. Follow-up: same provenance check is a candidate for dev-story + design-implement (both modify existing code) — quick-dev only today.
- **Prior wave:** design-ingest — **cope with a superseded handoff: tolerant supersede-awareness** (`354be3f9`). Owner wanted to hand `design-implement` a handoff that may already be done but has since been superseded, and have it cope gracefully (not a bare "already applied, no deltas" no-op, not a regression-to-stale apply). Put the coping in `design-ingest` (the non-destructive catalog+pause half = the safe checkpoint before any code moves). step-01 derives `{target_slug}`, resolves `{handoff_supersede_status}` (active | superseded | no_brief | ambiguous) against briefs in `implementation_artifacts`, **tolerates every value (never refuses)**, and stamps `ingest.supersede_status`/`superseded_by`/`source_brief` into the manifest; step-03 LEADS the pause with the heads-up. `design-implement` reads the stamp — symmetric: no hard refuse, no silent apply — superseded+no-deltas → self-explaining no-op, superseded+deltas → HALT for explicit confirm (applying is INTENT autonomy). `brief-revision-policy.md` §6 amended + new §8 (ingest-tolerance) so policy ⇄ workflow agree; honest limit documented (no_brief on raw-URL; diff-the-two scoped out as a future workflow). Mode 1: 0 blocking. markdownlint 0; fork test suite green; pushed `myfork/custom`; **synced 14/14**. **Follow-on (`d848e916`):** `design-implement` now also copes on a **direct URL/bundle run** (no ingest in front, so no stamp) — step-01 §SHARED.1a resolves supersede independently (derive slug from the frame inventory, match against briefs, same contract as ingest step-01 §5) and on `superseded` SURFACES + HALTS before the apply pipeline. So all three input paths cope, not just the stamped manifest. Re-sync owed for the follow-on. Open follow-up: the thefbaprep 06-17→06-18 chain that prompted this isn't materialized on disk (no 06-18 brief; 06-17 lacks the Block-A supersede block) — to exercise either path, both briefs need to exist with proper provenance.
- **Prior wave:** design-implement — **drilled-frame COMPOSITION axis (§2d-bis): the drawer analog of page-shell** (`d0c4e368`). Owner showed Claude-Design-vs-built supply-order drawer screenshots — "how does it not notice the visual deltas." Root `contract-dimension-gap`: §2 compares each section's inner pixels and §2f-bis certifies each section *exists*, but nothing compared a drilled frame's **composition** (section order, grouping under named headings, header/footer chrome) — so a drawer ships every component "matching" while wholly recomposed (`Cost & sourcing`→`Economics`, standalone `Lifecycle`→folded, `Related records`→`Routing & source`, black footer→blue). Page-shell blind spot (PR #2017) inside a drawer; footer sharpest (not a cataloged section → no row). Plus reporting flaw: the step-02b capability preflight (never looks at pixels) buried the whole recompose in one "#8 treatment, buildable: yes" line. Fix: step-02b §3 flags `{frame_composition_deltas}` + names it in the preflight; step-03 §2d-bis emits a Frame-composition row per drilled frame (order + grouping + chrome), Tier-1, footer in scope; Critical Rule + state var + step-04 metric/failure + checklist. Add-not-cede. Mode 1: 0 blocking. Pushed `myfork/custom`; linter green. **Synced 14/14.**
- **Prior wave:** design-implement — **resumable + checkpointed apply for large ingest manifests** (`0fefe11c`). The symmetric twin of the design-ingest wave (`293dfcc9` fixed INGEST overflow; this fixes APPLY overflow). On an `ingest_manifest` run, step-04 applies frame-by-frame, persists each frame's dispositions back into the manifest at its boundary (durable before any compaction), auto-resumes by skipping `✓ applied` rows (re-run the SAME command in a fresh session to continue), and self-checkpoints at a frame boundary (soft budget ~one heavy frame / ~10–12 sections) — a clean terminal exit that still delivers the slice + prints the resume command, never a single-window walk over 48 sections that the compaction trap silently corrupts. Optional `{frame_scope}` explicit slice override. New vars `{frame_scope}` / `{resume_prior_dispositions}` / `{run_completion_mode}`; Critical Rule + step-04 §5a + §9 report branch + step-03 §2f-bis budget note + step-01 resume-read + checklist item. Grounded in the owner's live 13-frame/48-section supply-orders blocked-stock manifest. Mode 1: 0 blocking. Pushed `myfork/custom`; budget linter green. **Sync owed (13/13).** Follow-up: design-ingest's review-pause still frames apply as "run it straight off this manifest" one-shot — align it to mention auto-checkpoint/resume.
- **Prior wave:** context-budget durable principle — **make the fork expert context-aware + able to recommend sub-workflow / sub-agent delegation** (`e8a9cf93` + skill v1.6). The owner flagged that long / instruction-dense workflows hit "context fatigue" — the model compresses and drops detail. Named precisely (research-grounded): **context rot** (length — quality declines monotonically well before the window fills), **curse of instructions** (density — the dominant mode for instruction-heavy markdown; reliability slips past ~10 simultaneous must-dos), **lost-in-the-middle** (position), **effective window ≈ 50–65%** of advertised, and the **compaction trap** (auto-summarization drops exact figures / edge-cases / constraint wording first — the dropped exceptions ARE the spec). New skill reference `references/context-budget.md`: the phenomenon + the decomposition decision-rule (read-heavy/parallelizable → sub-agent fan-out returning a distilled artifact · long sequential build → per-phase fresh-context with a durable manifest · write-one-coherent-artifact → single-threaded), reconciling Anthropic's multi-agent research win with Cognition's *Don't Build Multi-Agents*, plus the non-negotiable handoff contract (objective · output schema · tools/sources · boundaries; durable state in a file, not chat). Skill v1.6: principle woven into Mode 1 (Review) + Mode 2 (Author), new Mode-3 root-cause class `context-budget-overflow`, rolled v1.5 to `versions/`. Fork integration (`e8a9cf93`): create-workflow/step-03-build builds within budget (one job/step, ≤~10 must-dos, pointer-over-inline, per-step shape classification + handoff contract; new verify item + failure mode); orchestrate-workflows/step-02-health-check gains a Context Budget audit section (`overdense-step` / `inlined-corpus` / `buried-constraint` / `undelegated-read`). Skills aren't synced (the SKILL.md change is live immediately); the two fork workflow edits are **Sync owed (13/13)**. Coheres with the design-ingest wave (`293dfcc9`), which was itself a context-overflow fix — this is the general principle beneath it. Pushed `myfork/custom` → `e8a9cf93`; pre-commit suite green.
- **Prior wave:** design-ingest — **new workflow + `ingest_manifest` input_kind + §2f-bis section-coverage gate** (`293dfcc9`). Two failures running design-implement on a ~140KB JSX bundle: (1) the whole bundle overflowed one ingest context, so the exhaustive per-component catalog got shortcut; (2) delegating ingest by feature-area with narrow agent prompts let a whole drawer SECTION (Reconciliation block) and a whole section (SellerSmart dispatch) fall in the seam — §2f frame-coverage is frame-granular ("drawer present? yes → green") and is structurally blind to a section dropped *inside* a present frame (its inner primitives match elsewhere). Root class `contract-dimension-gap` (the section axis was missing under a present frame) + the context-budget cause. Fix is a decoupled front-half + a named structural gate: new `implement/design-ingest/` (sibling of design-synthesize) ingests URL/bundle, builds the frame inventory, then FANS OUT one isolated agent **per frame** (the fan-out unit is the frame, never a feature-area) to enumerate that frame's COMPLETE top-level section list + component×property catalog — context bounded. **FRAME-COMPLETENESS GATE:** an empty section list for a drawn frame HALTS. Emits a durable manifest (`manifest-schema.md`) with a pre-seeded grid scaffold (every (frame, section) already a row, UNVERIFIED), then PAUSES for a reviewable section-inventory handoff. design-implement gains `input_kind: ingest_manifest` (MANIFEST PATH in step-01 — skips download/extract/re-catalog; refusal gate on an incomplete manifest) and **§2f-bis Section-coverage** (step-03): every top-level SECTION of every present frame is a mandatory grid row, denominator from the gated scaffold (authoritative) or enumerated in-context on URL/bundle runs; a present frame's missing section is Tier-1, counted in `{delta_count}`. Same named-gate shape as page-shell §2d and frame-coverage §2f after their misses. Grounded in the owner's live supply-order drawer (a present drawer that shipped with NO Reconciliation block). Mode 1 self-review: 0 blocking (no brief consumption → provenance N/A; grounding gate satisfied; autonomy scoped — catalogs, never infers intent, pauses for review; worktree-safe — sync auto-discovers). Pushed `myfork/custom` → `293dfcc9`; pre-commit suite green. **Sync owed (13/13)** to distribute.
- **Prior wave:** design-implement — **authoritative §13-lookup denominator: the rendered "Linked records" list (the "often-missed link-to-record drawers" fix)** (`4910a3e1`). The lookup drawers (Catalog item / Route warehouse / Shipping lane / Supply source / Inbound batch / Import run) often went unchecked against Claude Design because §2f's frame-coverage denominator was harvested ONLY from bundle *self-declarations* — `<script src>` comments, per-frame banners, lookup→target maps. Those are declarations and under-enumerate (or describe a different conditional state), so a lookup they forgot to list never entered `{design_frame_inventory}` → §2f couldn't flag a frame it never knew existed (the live Orders detail drawer renders a `Shipping lane` row that a script-comment harvest drops). Root class `contract-dimension-gap` (the frame axis existed, but its denominator *source* was incomplete). Fix, **add-not-cede** (step-01 already opens the drawer): URL.3a **source 5** captures `{design_linked_record_rows}` — the drawer's rendered "Linked records" rows, the AUTHORITATIVE one-row-per-lookup denominator — and reconciles every row against a `§13-lookup` frame (no match → re-trace or `LOOKUP UNDER-ENUMERATED`, never dropped; harvested §13-lookup count ≥ row count). step-03 §2f reconciles before emitting (denominator-source-agnostic: brief/URL/manifest), routes under-enumerated rows, and sweeps a "present" drawer for **depth** so a thin `code — name` stub vs a rich bundle drawer surfaces as `MISSING in impl` rows (frame-present ≠ frame-matches). step-04 §9 disclosure leads with the linked-records denominator + enumerates under-enumerated/thin; workflow.md state var + Critical Rule; checklist +2. Grounded in the owner's live supply-order detail drawer screenshot (6 linked records incl. Shipping lane). Mode 1: 0 blocking. Pushed `myfork/custom` → `4910a3e1`; pre-commit suite green; synced 13/13.
- **Prior wave:** design-implement — **step-02b capability delta made SYMMETRIC — catch the UPLIFT, not just the regression** (`f2d70fd1`). The "running blind" miss: a redesign that ADDS capability the live page lacks (a new analytics/disposition band, lane-by-handler segmentation, an action column, co-views) DROPS nothing, so step-02b's DROP-only contract passed it clean — and with nothing foregrounding the uplift, the run anchored on token-debt evidence and emitted a backwards SCOPE verdict ("production is a structural superset, just treatment/token alignment, no net-new capability") *before the map had even finished resolving production*. This is the mirror of every prior design-implement patch (page-shell §2d / frame-coverage §2f / content-lane §2c: "an enumerated item is silently ABSENT while looks-right passes") — inverted: a capability silently ADDED-but-unbuilt while "nothing to drop" passes. Root class `contract-dimension-gap`, **add-not-cede** (both capability inventories are already gathered, so the ADD axis is verifiable from evidence in hand). Fix: step-02b §3 computes the delta BOTH ways — ADDED + DEEPENED promoted from "informational"/"flows through the grid" to first-class `{added_capabilities}`/`{deepened_capabilities}` → `{uplift_capabilities}`; §4b names a non-empty uplift and sets strategy ≥ `additive` (no halt — building the handoff is decision autonomy, unlike a drop which is intent); §4's empty-DROP branch no longer auto-concludes restyle. A **running-blind gate** (workflow.md Critical Rule + step-02b MANDATORY) forbids "restyle-only / superset / no-net-new" as a prior or from treatment evidence — the scope verdict is an OUTPUT of §3. step-03 §2h tags uplift sub-structure `capability-build` (not collapsed to "MISSING component → restyle"); step-04 builds it (§1 fix-type + §5 ledger) and discloses each in a new §9 "Capabilities built" section, the mirror of "Capabilities removed (orphaned actions)". Grounded in the owner's live inbound-flow supply-orders run (flat `/orders` table vs a handoff with lanes + analytics/disposition band + action column + co-views). Mode 1 self-review: 0 blocking. Pushed `myfork/custom` → `f2d70fd1`; pre-commit suite green; synced 13/13.
- **Prior wave:** design-implement — **transcription fidelity + render-compare done-gate (copy & chrome)** (`e91bccd3`). The grid is CSS-only, so copy and frame chrome (header/breadcrumb/footer) have no row — a green grid manufactured false confidence while a cost-recon drawer shipped with a generic header, relabeled footer, and paraphrased copy vs the handoff. Fix = name the prohibited move (transcribe, don't design — covers copy & chrome; grid is necessary-NOT-sufficient) in workflow.md Critical Rules + step-04 §5b transcription pass + render-compare done-gate, deviations riding the existing ledger. Add-not-cede (verifiable from source). Synced 13/13. **Prior wave:** regression-surface preflight (step-02b): handoff-vs-production capability delta + strategy gate (`71656fb1`). New entry behavior on a redesign: before building the grid or touching code, inventory what the CURRENT built page does (routing, §13 linked records, economics/cost-recon, composite status/header, activity/audit, wired mutations) vs what the handoff delivers, and surface every capability the handoff would DROP. Non-empty dropped set → **HALT and ask the implementation strategy** (restyle-only keep-all · additive · partial per-capability · replacement) rather than silently reproducing the redesign's omission — whether a drop is intended or accidental is *intent*, which the workflow can't infer. Proactive front end to step-04 §9's post-hoc orphaned-action backstop (the sweep greens out on a dropped capability because its inner primitives exist elsewhere — so the check runs BEFORE the grid). step-01 announces the check up front; step-02 → step-02b → step-03; step-03 §2h flags `capability-protected` rows; step-04 gains `deferred(capability-protected)` + a §9 strategy disclosure; workflow.md +5 state vars + Critical Rule + sequence; checklist Step-2b section. Autonomous mode defaults non-destructive (restyle-only) + discloses, never silently replaces; skips cleanly on a new surface; does NOT mis-score an undrawn-but-promised frame (brief §7 / `{design_frame_inventory}`) as dropped. Synced 13/13. **Refinement (`80ad60da`):** owner — "lean into advisory mode, not 'can the user list this'" — the gate now gives a per-capability **KEEP / SAFE-TO-DROP** verdict (with reasons) + one recommended plan to approve-or-adjust, never a blank menu the user assembles; `partial` is the AI-proposed advised mix, not a user-built list. Validated against a live `Supply Order Detail - Blocked-stock build` handoff (WebFetch can't pull the >10MB bundle — real step-01 uses curl; ran grounded on the read prod drawer + owner's stated dropped-five).
- **Prior wave:** design-implement — **page-shell §2d catches ancestor-injected chrome (hero/banner)** (`7f8af1ca`); a hero/banner an ancestor layout injects above the content that the design frame lacks is now a Tier-1 page-shell row (the `/settings/sku-format` hero miss; live fix in inbound-flow PR #2275). The page-shell axis modeled only the container's *dimensions* (width/centering/padding), not its *chrome*. A hero/banner/masthead injected by an **ancestor layout** above the page content is invisible to the component sweep (the bundle renders the frame standalone → nothing in `{design_components}` to diff) AND was outside §2d — so a banned hero shipped under an all-green grid: inbound-flow `/settings/sku-format` (owner screenshot, live prod) had `settings/layout.tsx` inject an `<InboundHeroBanner>` (photo + title + subtitle) above every settings page; the Claude Design handoff had NO hero; design-implement matched the worklist component-for-component and greenlit the §5 hard failure ("Hero strips… above working tables"). Add-not-cede (the band is in the same layout files the §1a wrapper-chain walk already reads): step-02 §1a captures `{impl_page_shell}.injected_chrome` (`in_design_frame` flag); step-03 §2d emits an injected-chrome row, Tier-1 when absent from the design frame (design frame is authoritative; "absent from the bundle" is NOT license to keep it); checklist + state var updated. Root class `contract-dimension-gap` (page-shell modeled width but not chrome — twin of the PR #2017 width miss). Also shipped the live fix: inbound-flow **PR #2275** (merged) removed the settings hero. Synced 13/13.
- **Prior wave:** design-implement — **token-provenance disclose-and-cede** (`b4ad51cb`); a shared-semantic token resolving only from a per-screen stylesheet is disclosed + ceded to design-review, never collapsed into "1:1" and never gated. A `var(--*)` that resolves is not automatically a clean "1:1" mapping: per `docs/design-policy.md` §8 the canonical token surface is `tokens.css` + `globals.css @theme`, and a shared-semantic token (status/colour/type) that resolves ONLY from a per-screen stylesheet (`supply-orders.css`) is resolvable at runtime but is NOT a system token — reading "defined somewhere" as "effectively 1:1" buries the §3/§13 cross-surface-drift risk. But promote-or-leave is token *architecture* (not design-implement's lane), so the fix is **disclose-and-cede, never gate** — sibling of the content-lane (§2c) and policy-conformance (§2e) cedes. step-02 §5 tags provenance (`scope: canonical|per-screen`, `semantic_class: shared-semantic|local-constant`) + "never report 1:1 while unresolved"; step-03 §2g emits one `NON-CANONICAL TOKEN` disclosure row (not a delta, not gated; `local-constant` per-screen tokens NOT flagged); step-04 §9 mandatory disclosure + SUCCESS/FAILURE; checklist + state-var registry. Root class `contract-dimension-gap` (missing token-provenance axis). Grounded in a live cost-recon `design-impl` run that declared "tokens map ~1:1" off `supply-orders.css`-defined `--status-*-text`. Synced 13/13.
- **Prior wave:** create-design-policy — **Bison product-family policy overlay + `inherits:` mechanism** (`3141456f`). New `shared/bison-product-family-policy.md` lifts the reusable "bulk" of inbound-flow's `design-policy.md` v11 (itself ported from accounting-tools v11) in source-agnostic form — register · status system · money/VAT · relational expand-in-context linking · per-source provenance · positive-assertion floor — so a new Bison tool **inherits** it instead of hand-porting and re-diverging. Three tiers deciphered from inbound-flow's own change_log "Propagation (§9)" tags: universal AI-fingerprint substance stays in `design-standards.md`; product-family substance → the new overlay; project-unique residue (exemplar domain · surface topology / multi-handler · product imagery · concrete tokens/routes) stays in each project's policy, **enumerated in the overlay's §Z** so a new project knows exactly what to author. `create-design-policy` stays project-agnostic: template gains an optional `inherits:` frontmatter field + a family-overlay precedence tier, `workflow.md` gains an inherit-don't-re-port CRITICAL RULE + `{family_overlay}` state var, `step-04 §2b` gains a residue-only authoring path. The overlay lives in `shared/` so it distributes to all sync targets but is **inert for non-Bison projects** (consumed only when a project declares `inherits: bison-product-family-policy`). Pre-push full test suite green. **Sync owed (13/13)** to distribute.
- **Fork vs upstream:** see `## Versioning`.
- **Owed / in-flight:** see `## In-Flight Work`. Skill `last_verified_against_fork_commit` = `31762d87`. **v6.8 migration MACHINERY COMPLETE + proven end-to-end** (`c589223c`): Phases 0–4 + Phase-5 pilot done (port engine, 28-workflow corpus 0-dangling, sync delivery, real-install proof, bmad-quick-dev override). **Pilot cutover DONE on `cash-recovery`** (first real skills-layout project: clean v6.8 install → sync → 72 skills, guarded quick-dev override, 15 bmad-shared policies, design-handoff refs all resolve, 0 stale refs; backup at `/tmp/cash-recovery-bmad-bak`, reversible). Remaining is DECISION not engineering — **with one correction (2026-06-26, `70eaf332`):** the dual-layout delivery for the other 13 (old-layout) projects was itself a HIDDEN ENGINEERING GAP (sync only delivered ports when the workflows dir was absent), now CLOSED via `deliver_skills_native_overlay()` + the opt-in `skills_native_transition` key, piloted on otp_manager. So the **other 13** projects' cutover is NOW genuinely decision/rollout (owner go/no-go — dual-layout, no rush; opt-in key + sync + PR per project), plus Phase 6 (retarget onboard-project.sh default to skills-layout), and the orchestrate/dispatch Mode-1 review. See `custom/MIGRATION-v6.8-skills-plan.md`. **Synced 14/14 (`4ba7ddc5` — design-implement drilled-frame composition axis §2d-bis + resumable/checkpointed apply both distributed).** Earlier **(`5614e548`)** — `design-ingest` (incl. the §2f-bis gate, `ingest_manifest` input_kind, AND a follow-up `498e194f` that humanizes the workflow's user-facing interaction at its touchpoints — kickoff/frames-found/the review pause read conversationally; the spec stays house-style dense) plus the context-budget checks in create-workflow/step-03-build + orchestrate-workflows/step-02-health-check AND the create-workflow **step-3b adversarial review gate** (`5614e548`) are all distributed. The `/bmad:bmm:workflows:design-ingest` slash command is wired in all targets. **Hard enforcement** (`b4473429`): a fork-internal `tools/validate-context-budget.js` is wired into `npm test` (the `.husky/pre-commit` gate) — it hard-blocks a runaway step (over 1100 lines / 95KB) or removal of the create-workflow step-3b gate, and warns on real-budget/density overruns (21 today, largest design-synthesize step-06 at 975 lines). NOT synced to projects — workflows are only authored in the fork, so the commit boundary is the complete locus. Open follow-ups: (e) **brief-provenance propagation through design-ingest** — when the ingest source is a synthesize_bundle (which carries `synthesis.brief_provenance`), the ingest manifest should propagate it so the brief→bundle→manifest→implement chain stays traceable (raw-URL ingests have no brief, so it's source-conditional); (f) **design-implement `checklist.md` / `template.md`** were NOT updated with a section-coverage checkbox for §2f-bis — add one to match the new gate. (a) adopt-the-overlay pass for the two Bison projects — convert inbound-flow + accounting-tools policies to `inherits: bison-product-family-policy` + residue-only (separate, deliberate per-project rewrites; NOT auto-done); (b) **inbound-flow app-wide hero decision** — `InboundHeroBanner` is on ~21 OTHER operational pages (dashboard, products, customs, listings, inventory, inbound, import, sourcing) — the same §5 hero-strip violation, NOT swept with the settings fix (PR #2275); a deliberate app-identity call for the owner; (c) **stranded design-lane sync** — inbound-flow `main`'s working-tree `_bmad/bmm/workflows/` carries the synced overlay + §2g + §2d edits uncommitted, so worktree design-implement runs (which branch from HEAD) are blind to them until committed to main (the e72c1cb7 pattern — doc-only PR); (d) carried — confirm design-implement §2f treats a `{consumed_frames}` entry as out-of-scope-for-this-bundle, not `FRAME NOT DRAWN`.

## Changelog

Newest first. **One discrete entry per wave** — `### YYYY-MM-DD — title (commit)` + a bounded paragraph (what · why · scope · delivery). NEVER a single run-on line. Keep ~12 entries here; when it grows past that, move the oldest (in newest-first order) into [`STATUS-archive.md`](./STATUS-archive.md).

### 2026-06-28 — correct-course non-destructive executor gate (DRY-RUN) (`feat/correct-course-executor-gate`)
**What:** the planner→executor pack idea, made real on the one axis that mattered. correct-course is already planner-only (writes the proposal, hands off), so the gap was the downstream APPLY into untracked/main-only tracker state (`sprint-status.yaml`, story files, `epics.md`) — data-loss class, no git-checkout back. SKILL.md now requires a machine-readable executor manifest (`proposal_id` + `files_to_change`) in Step 4 and drops a `.sprint-apply-pending.json` proof marker in Step 6. Two global hooks: `sprint-apply-gate.sh` (PreToolUse Edit|Write|Bash) freezes ONLY the pending proposal's named files — the normal sprint-planning/create-story/dev-story authoring lane is untouched, dodging the indiscriminate-gate anti-pattern; `sprint-apply-approve.sh` (UserPromptSubmit) clears the gate only on an exact `APPROVE: APPLY_SPRINT_PROPOSAL::<id>` token. **Why:** prose-only approval tokens (the source pack) are not enforcement; the tracker write is the irreversible moment. **Enforcement (enforcement-expert + mason-bmad-workflow-expert author-mode, both consulted):** awareness (SKILL manifest) + proof (pending marker) + deterministic gate (PreToolUse) + deterministic clear (UserPromptSubmit); ships **DRY-RUN** (logs `WOULD-BLOCK`, allows; `~/.claude/sprint-apply-gate.mode`=`enforce` to gate after the log proves quiet). **Scope:** the manifest skill — promoted correct-course into fork ownership as a **portable skill** `custom/skills/bmad-correct-course/` (it was pure upstream residue, in neither `custom/workflows/` nor `custom/skills/`); two hooks + `install-global-assets.sh` registration + `hooks-registry.md` (global track → `~/.claude`, NOT project-synced). **Delivery:** verified via a 9-scenario smoke test (allow/freeze-scope/dry-run/enforce/token-clear/free-form-reject/override/BSD-sed+tee extraction); skill synced 14/14. NOT flipped to enforce — warn-then-gate. **Channel correction:** the first pass edited `src/bmm-skills/` (the PLUGIN channel the porter ignores) — that edit stays as the plugin representation but reached no project; the portable-skill copy in `custom/skills/` (based on the upstream copy + only the 2 manifest edits, no behaviour drift) is what actually ships. Trade-off accepted: the fork now overrides upstream correct-course, so future upstream changes to it won't auto-flow. **Follow-up:** watch `~/.claude/sprint-apply-gate.log` `WOULD-BLOCK` lines for a week before flipping enforce; STATUS.md itself has regressed toward the run-on `**Last updated:**` anti-pattern — re-split next touch.

### 2026-06-28 — Bison overlay v2: promote ledger/register (§M) + net-derivation-provenance (§K) (`8675ca06`)
**What:** lifted two inbound-flow `docs/design-policy.md` sections into the shared `bison-product-family-policy` overlay, source-agnostic. New **§M Ledger & register surfaces** (per-account/per-currency running-balance coherence, oldest-first running-balance exception to §F, money direction two-column/`Dr`–`Cr` never colour-alone, multi-currency native column carries no total, append-only §J-linked corrections, `0.00` vs `—` honesty) + **extended §K** with net VAT-exclusive per-unit + derivation provenance (`from source` / `derived −{rate}%` naming rate & showing the gross / `no VAT line`; never-fabricate-a-net) and four new §K hard failures. **Why:** both tagged "shared product-family value → propagate in source-agnostic form" in inbound-flow's own change log — overlay promotion is the sanctioned path (beats per-project hand-port). **Scope:** overlay `version 1→2`; project exemplars stay tier-3 residue (§Z updated). **Delivery:** committed + pushed `myfork custom`; ported + synced — cash-recovery (skills-layout inheritor) reads both at v2. **Follow-up:** `/code/accounting-tools` is listed as an inheritor in the overlay frontmatter but its `docs/design-policy.md` has **no `inherits:` line** and still carries the full standalone policy — won't consume §M/§K-net until migrated to the residue model (pre-existing, not from this wave).

### 2026-06-28 — design-lane pagination gap closed (design-handoff §5g + design-implement List-rendering row + policy rule)
**What:** design-handoff captured Data volume but never derived it into a list-rendering REQUIREMENT, so every operational/analytical worklist shipped un-paginated (owner: "every handoff seems to miss it"). **Fix (4 files):** (1) `design-handoff/steps/step-01c-topology.md` — new mandatory **§5g List-rendering** derivation (`single-render|paginate|virtualize|load-more` from data volume; non-single-render ⇒ required §7 deliverable + `must_support_capabilities` + COMPLETION item; never silently defaults). (2) `design-handoff/brief-template.md` — renders it (contract `list_rendering` field + §1 line). (3) `design-implement/steps/step-03-build-grid.md` — new **§2d-list** mandatory grid row (the §2d page-shell analog; a required mechanism absent on the primary list frame is Tier-1 → a build that renders all rows of a growing list fails). (4) policy half — standing "a worklist that grows must paginate/virtualize" rule in **`bison-product-family-policy.md` §B** (existing projects inherit) + the **`create-design-policy` template §9** (new policies), enforced by `design-review-pr`. **Why:** contract-dimension-gap (derive-to-requirement flavour) — the same family as the §2d page-shell / §2f frame-coverage fixes, one axis deeper. **Scope:** synced design lane, 13-project blast radius; owner-authorized (owner-gated tier). **Delivery:** committed, pushed `myfork custom`, sync run (skills-native twins regenerated by the porter; project `_bmad/bmm/workflows` copies updated). **Self-review (Mode 1):** §5g mirrors §5b/§5d/§5f house style (decide-by-job, capture reasoning, render into brief, autonomous-mode anti-default); the design-implement row mirrors §2d; no provenance/grounding invariant touched. fork-gaps entry marked resolved.

### 2026-06-28 — generate-project-context now emits the Product Concept (grounding-discipline root fix)

**What.** `bmad-generate-project-context` (`src/bmm-skills/3-solutioning/`) now crafts a **Product Concept & End-to-End Flow** section as the FIRST section of every `project-context.md`: one-line concept, value-flow & spine, an explicit **cause/effect** (which events are pipeline OUTPUTS vs INPUTS), and a forbidden-behaviours block ("never infer the concept from the task tracker alone; never invert cause/effect"). Edits: `project-context-template.md` (new first section + `sections_completed: ['product_concept', …]`), `steps/step-01-discover.md` (new §1a — discover the concept from brief/PRD/architecture FIRST; graceful no-halt brownfield fallback), `steps/step-02-generate.md` (new §0 — emit concept first + confirm cause/effect). **Why.** Triages a real cash-recovery incident: an agent reasoned ~4h bottom-up from the sprint tracker, never loaded the brief/PRD, and inverted the product's cause/effect (told the owner the app was "waiting for eBay sales" when sales are the pipeline OUTPUT). `project-context.md` — the auto-loaded grounding file — carried only tech-stack + impl-rules, never the concept, so even loading it wouldn't have prevented the blindness. enforcement-expert design (belt-and-suspenders, both shipped this wave): the CONTENT lever (the generator emits the section by construction) AND the deterministic-DELIVERY lever — a new `bmad-project-concept-card` SessionStart hook in `hooks.json` (`src/modules/bmm/_module-installer/assets/`) that extracts the Product Concept section from each project's `project-context.md` and injects it as `additionalContext` every session (case-insensitive header match; silent when the section is absent → near-zero false-positive; rides the hook-distribution rail, upserted to all targets on sync). **Scope.** Doc/instruction-only, additive (existing sections untouched), placement-first. Forward-looking: the 13 existing `project-context.md` files gain the section only on a re-run of generate-project-context (cash-recovery hand-patched in the interim). **Delivery.** Pushed to `myfork/custom` + synced to targets.

### 2026-06-28 — design-lane grounding gate + worktree-fallback doc (fork-gaps wave)

**What.** Worked the open fork-gap backlog. (1) **design-lane grounding/prerequisite gate (gap closed).** design-router intake grounded only that a target could be *named*, not that its *basis exists* — so `design-handoff all login screen` (no login UI on disk; auth is PRD Open-Question #9/FR-33) would have fabricated the credential/role/field model a brief must not decide. Added a basis-exists prerequisite in BOTH layouts: `design-router/step-01-intake §2.5` + `design-handoff/step-01 §2-pre` now require the target to resolve to an existing implementation (redesign) OR a settled spec (new), else HALT→`bmad-prd`→`bmad-architecture` (fires under autonomous_mode; system-wide exempt). (2) **worktree-portability §7 (gap (a) of the cwd-pinned EnterWorktree gap).** Documents the sanctioned manual fallback (git worktree add → edit by absolute worktree path → git -C) when EnterWorktree refuses a cwd-pinned session. (3) **Hygiene:** marked the `pick 1–4` menu gap resolved (tiers 1–2 shipped earlier, Tier-3 declined on merits — heading marker was stale).

**Why.** "Continue addressing the fork [gaps]." enforcement-expert lens on the grounding gate: probabilistic tier-3 halt + reroute (can't hook "no settled data-model"), mirroring quick-dev's grounding gate; harm (a fabricated architecture brief) is recoverable so no hard hook.

**Scope.** `design-handoff/step-01-gather.md` + `design-router/step-01-intake.md` (both skills-native + workflows layouts; skills-native regenerated by the sync from the workflows source), `shared/worktree-portability.md` (both), `docs/fork-gaps.md` (3 resolution markers). `validate:budget` OK (soft warnings only, pre-existing). Mode-1 self-review: 0 blocking. Remaining open gaps surfaced to owner: relational-coherence-audit report-home (owner-gated coupling), worktree-cwd memory-slug (harness-owned durable fix + destructive ~366-dir sweep), edit-guard `~`-allowlist hook widen (shared rail). Pushed `myfork/custom`; synced.

### 2026-06-27 — design-handoff worktree-delivery guard (fork-gap resolved)

**What.** Closed the logged fork-gap "design-handoff worktree delivery splits when artifacts are read by absolute main-checkout path." A cash-recovery `design-handoff` run from a worktree wrote all four artifacts (new brief + rationale, predecessor supersede-flips) to the MAIN checkout because the predecessor/policy were read by absolute main paths and Write/Edit continued there; the split was silent (artifacts dir gitignored-but-force-tracked) until `git add` no-opped. Three-part fix in BOTH layouts (skills-native + workflows): **step-03 §1** PREVENT — bind every read AND write (incl. the §1b flip) to `git rev-parse --show-toplevel` of the current cwd, not just the computed `{output_path}` var; **step-04 §3** DETECT — a conservative deterministic-once-run pre-stage guard that HALTs (with a copy-into-worktree + restore-main recipe) when inside a linked worktree and the brief is in the main checkout but absent here; **step-04 §3** RECONCILE — note that briefs are force-tracked-despite-ignore so a re-run's NEW files are invisible to plain `git status` (always `-f`-add by explicit path).

**Why.** "Action the fork gap." enforcement-expert consulted: deterministic GATE (the pre-stage assertion) + probabilistic AWARENESS (the §1 binding) — not escalated to a PreToolUse/pre-push hook because the failure self-announces and is recoverable, and a hook firing on every `design-*` delivery is high false-positive blast.

**Scope.** `design-handoff` step-03-generate-brief.md + step-04-deliver.md in `custom/skills-native/bmad-design-handoff/` AND `custom/workflows/design/design-handoff/steps/`; `docs/fork-gaps.md` resolution marker. Mode-1 self-review: 0 blocking (conservative guard, no autonomy/provenance/brief-consumption concerns, house-style match). `validate:budget` OK (soft warnings only, pre-existing). Pushed `myfork/custom`; synced to all targets.

### 2026-06-27 — quick-dev Mode-A gated-migration carve-out (`d811eecc`)
**What.** Owner-greenlit the sibling of the handoff-reroute fix: a narrow carve-out to blast-radius HARD trigger 1 (`blast-radius-eligibility.md` + a §0 pointer in `quick-dev/step-03-execute.md`). A run reclassifies `contained-feature` instead of `not-quick-dev` ONLY when ALL hold: Mode A (tech-spec drove it) · schema/migration is the ONLY HARD trigger that fired · the spec pre-plans the migration generate-not-apply + sequenced + rollback · otherwise fits `contained-feature`. **Why.** Every Mode-A spec with a gated migration tripped a gate whose reroute target (`quick-spec`) produces the very tech-spec the workflow was already handed — a deterministic mis-fire on legitimately quick-dev-shaped input. **Enforcement honesty (the load-bearing design call).** It does NOT weaken the deterministic tier: the step-07 `quick-dev-blast-radius-check.sh` still sees the migration file and fires trigger 1; the carve-out routes through the script's EXISTING `QUICK_DEV_OVERRIDE=<reason>` path (verified line 134 — prints the reason, "record in PR", exit 0) so the gate fires-and-records rather than being silently bypassed — the override becomes the *documented default* for this one recognizable shape, step-04 §6 regression check still non-negotiable. Chose the override-reason mechanism over "narrow trigger 1 to applies-vs-references" because a diff can't tell a gated migration from an applied one (intent, not a file property). Triggers 2–5 + any applied/non-Mode-A schema change stay fenced. **Scope.** Narrow carve-out only; no other gate touched. **Delivery.** Committed + pushed `myfork/custom`; **synced 14/14**. **Self-review (Mode 1).** 0 blocking (2 minor concerns: prescriptive reason-string; agent must set the override). Budget + markdownlint clean.

### 2026-06-27 — two over-firing-gate fork-gaps closed: quick-dev handoff reroute + design-implement DesignSync URL branch (`fd3bcb32`)
**What.** Closed the two clean, bounded, in-domain fork-workflow gaps from `docs/fork-gaps.md` (owner: "address fork gaps — fix them"). (1) **quick-dev grounding-gate handoff reroute** → `quick-dev/steps/step-01-mode-detection.md`: a new "Groundable for a DIFFERENT workflow (reroute, don't dead-end)" subsection BEFORE the generic HALT — a `handoff-*.md` path or a "Handoff filed:"/"Handoff:" lead reroutes to `/bmad:bmm:workflows:dispatch-followups <path>` and exits, instead of the boilerplate tech-spec nudge. Awareness/reroute ONLY (names the door, never auto-runs it) → the anti-fabrication invariant (PR #785) is untouched; generalized in-prose to any strict-grounding workflow handed a sibling's artifact. (2) **design-implement URL ingestion** → `design-implement/steps/step-01-ingest-design.md`: URL.1 split into URL.1a (legacy `…/h/<id>` tar, unchanged) + URL.1b (DesignSync project `claude.ai/design/p/<uuid>` — `get_project`/`list_files`/`get_file` to a local `{design_dir}` preserving project-relative paths so URL.2→URL.6 run mechanism-agnostic and unchanged), with ambiguous-shape fall-through to 1b on non-tar. `{input_kind}` unchanged (branch is inline at the fetch step, the only part that differed). **Why.** Both are gates/paths over-firing on a deterministically-recognizable input shape their branch table had no entry for — a recurring per-run dance (handoff → wrong front door; modern Claude Design share-link → silent empty bundle). **Scope.** Deliberately did NOT auto-fire the owner-investment/heavy/out-of-scope gaps — quick-dev blast-radius Mode-A carve-out (owner-gated: weakens a HARD safety trigger synced to 13), offer-then-affirm hook (consolidate-vs-build is the owner's call), read-only-audit prod-DB path (needs per-project infra recipe), astro pre-commit (root cause is astro's own 404 pipeline cache, not bounded — `build/` is already cleaned), project-memory worktree-slug (harness-level), gateway Tier-3 / fork-edit index b-c (owner-deferred). **Delivery.** 2 source commits' worth in one commit + 2 `fork-gaps.md` markers on `myfork/custom`; **sync to 13 owed** (both workflow files distribute; the design-implement skills-layout twin regenerates via the porter; `fork-gaps.md` is fork-local). **Self-review (Mode 1).** 0 blocking — additive awareness/branch prose, no brief/provenance involvement, grounding/anti-fabrication invariants preserved, autonomy-scoped (reroute names a door, never infers intent); budget + markdownlint clean on both files.

### 2026-06-27 — address-fork-issues pass + deploy-method contract v2 (`019c5223`/`4c7a8a45`/`da986868`)
**What.** Triaged the open `docs/fork-gaps.md` backlog (owner: "address all fork issues") and shipped the bounded, verifiable subset. (1) **Bash edit-guard scratchpad allowlist** (`019c5223`): widened both allowlist greps to match `/private/tmp/` + `/private/var/folders/`, so a redirect to the harness scratchpad isn't hard-blocked as an edit-equivalent; real `src/` writes still DENY (JSON valid, functional+regression tested). (2) **§D3 atomic fork-commit discipline** (`4c7a8a45`): `parallel-sessions.md` gains the `git commit -o <paths>` rule that closes the index-commingling hazard. (3) **Deploy-method contract v2** (`da986868`): `deployment-to-prod.md` §1A — three explicit modes (push_auto = merge IS the deploy / contract_script / manual_cli), conservative inference when unset (skip+undeclared → halt-and-state, never a guessed CLI), a method-bounded fallback ladder, and the auth-failure / durable-target-first branch; new optional `deploy.method` field; §8 CLAUDE.md pointer made mode-aware; `contract_version` 1→2, additive/backward-compatible, doc/doctrine-only (`bmad-deploy.sh` untouched). Closes the high deploy-method gap. Also logged the **offer-then-affirm skill-invocation** dispatch gap. **Why.** The backlog had accumulated; the deploy-method gap recurs on every task's last mile (an agent guessed `railway up` on a push-auto project → prod ahead of `main`). **Scope.** Did NOT auto-fire owner-investment/deferred items (nudge build-vs-consolidate, gateway Tier-3, astro-pre-commit, memory-slug sweep) nor a blind cross-repo `deploy.method` fan-out — an inbound-flow attempt empirically proved per-project adoption is required (pre-push `tsc` fails in an empty worktree; declined `--no-verify` per prove-don't-assert; aborted clean, inbound-flow restored). cash-recovery adopted `manual_cli` in-session (PR #98 on its own repo). **Delivery.** 3 source commits + 4 `fork-gaps.md` markers on `myfork/custom`; **sync to 13 owed** (hooks.json + parallel-sessions + deployment-to-prod; `fork-gaps.md` is fork-local). **Self-review (Mode 1).** 0 blocking; all pre-commit validators green (refs/lint/md/budget/close-out/hook-smoke 10/10).

### 2026-06-27 — design-handoff: machine-readable "Design Contract for Claude" spine in every brief (UNSYNCED — held)
**What.** Added a `## Design Contract for Claude — compile and obey` block to `brief-template.md`, rendered directly after "For Claude Design" and before §1. It is a rigid spine the designer compiles and obeys: scalar fields (`page_mode` · `composition` · `shell` · `route` · `frames` · `mutations` · `money` · width) + a **MUST PRESERVE** list (the object) vs **FREE TO CHANGE** list (the design freedom), and it cites `design-review-pr` (+ the design-implement conformance gate) as the enforcement point. `step-03-generate-brief.md` §2 gained the populate-instruction (two derived postures `mutation_posture`/`money_posture` from the §3 mutation audit + finance check; a distilled 3–6-item `contract_must_preserve`; an optional `contract_free_to_change`) and a §3 self-review checkbox. **Why.** Owner critique: the prior brief improved the *preface* but not the *interaction contract* — Claude Design still had to discover frames/shell/composition/constraints across 40k chars of narrative, and the binding constraints read as advice. The spine gives a compile-and-obey index; the gate citation makes it binding by routing to an existing deterministic check, not by louder wording. **Scope.** Presentation/contract-surfacing only — NOT a second source of truth: the spine MIRRORS Block B (`page_mode`/`composition`/`route`/`frames`/shell already emitted there), and the self-review check forces them identical + bans layout/fabricated-mutation in MUST-PRESERVE. No intake-check/provenance/grounding-gate change → PROBABILISTIC tier, enforcement ceded to `design-review-pr`. Routed through `mason-bmad-workflow-expert`. skills-native port regenerates from the canonical `custom/workflows/` source. **Delivery.** 2 source files (`brief-template.md` +35, `step-03` +6) committed to `myfork/custom`. **Sync HELD** — 4 parallel CLI sessions active; cross-repo fan-out deferred to a deliberate pass (so the 13 projects do NOT yet emit the spine). **Self-review (Mode 1/2).** 0 blocking; mirror-not-duplicate risk mitigated by the self-review checkbox; budget delta marginal (template is a render target; step-03 gained ~6 lines, pre-existing over-budget warning untouched).

### 2026-06-27 — design-handoff: consumer-facing close-out + output-contract that routes shape-feedback to the fork (`c5f278bd`, `7b7622f3`)
**What.** Rewrote `step-04-deliver.md` §10 ("Surface Delivery Result to User") so the emitted close-out is written for the NEXT consumer (Claude Design / design-synthesize), not as a workflow-history recap: **Active artifact → What changed (material delta) → Substantive correction(s) → Delivery → "For {consumer}" interpretation block** (which brief is active, revision-vs-new, how to read composition/page-mode, what NOT to import from the prior implementation). Replaced the thin "✓ delivered + generic hand-off line." Then (`7b7622f3`) added a governing **OUTPUT CONTRACT & WORKFLOW-FEEDBACK ROUTING** section to `workflow.md`: the shape is a contract not a per-run choice, process narration is forbidden by default, and — the meta-lesson — feedback about output SHAPE is a *workflow-patch request routed to the fork*, never a one-off message rewrite and never a project memory (which doesn't propagate by sync). Reinforced by a step-04 RULES bullet + a strengthened Rhea Voice line. **Why.** A cash-recovery design-handoff run closed with an operator-facing process essay; owner pushed back twice — "be more human / consumer-focused" then "this needs to be fixed at the workflow root, right?" The first instinct (a project memory) was the wrong tier — it nudges one project probabilistically and leaves the root template wrong for the other 13. The contract now encodes that routing so a context-free agent reads "your output shape is wrong" as "patch this workflow." **Scope.** Routed through `mason-bmad-workflow-expert`. Presentation/output-shape only — no intake-check, provenance, or grounding-gate change (those live in step-03), so PROBABILISTIC tier, no deterministic gate. skills-native port regenerates from the canonical `custom/workflows/` source (gitignored build artifact via `tools/port-workflows-to-skills.sh`). **Delivery.** 2 commits pushed `myfork/custom`; pre-commit fast-path green (refs/lint/lint:md/prettier/budget-24-soft/hook-smoke-10-10); **synced 14/14, 0 blocked**, verified the close-out + contract landed in cash-recovery's delivered SKILL.md. The redundant project memory was deleted (logged in cash-recovery `memory-changelog.md`) — one-place-per-rule, the contract now owns it. **Self-review (Mode 1/2).** 0 blocking; output-shape change only; budget clean (step-04 not over budget; pre-existing step-03 over-budget warning untouched and out of scope). **Parallel-session note:** worked alongside the STD-PERSONA-001 wave below (same step-04 Voice line) — the Voice-line edits are complementary (both point at the consumer-facing close-out) and committed together cleanly.

### 2026-06-27 — STD-PERSONA-001: named voice layer for 3 human-facing workflows (`0b9a6f41`)
**What.** A thin PRESENTATION layer giving design-handoff (**Rhea**, Design Steward), quick-spec + quick-dev (**Sol**, Rapid Prototyper), and escalation-on-class-change (**Mara**, Course-Corrector) a named voice — authored as ONE shared snippet (`shared/workflow-personas.md`), not three divergent rewrites. The binding contract sits at the top, load-bearing: voice appears in exactly three sanctioned spots (opening re-orientation · risk acknowledgement · "I" for responsibility) and never drives a decision, narration, a menu, or output structure; explicitly subordinate to STD-ESCALATE-001 + `answer-shape-and-autonomy`. **Mara folds into the existing escalation reflex** — a Voice note inside `escalation-on-class-change.md`, so all six referencing tails (dev-story / design-router / maintenance-triage / design-elevation / quick-dev / quick-spec) inherit her with zero per-workflow wiring, and because she speaks *through* the propose-and-act contract she structurally cannot reintroduce the pick-1-4 menu. Rhea + Sol are one-line pointers at opening/closing steps only (design-handoff 01/04; quick-spec 01/04; quick-dev 01/07). Registered STD-PERSONA-001 in STANDARDS.md. **Why.** Owner: humanise the three most human-facing workflows — but the explicit constraint was that "humanising" must NOT reintroduce the diary-voice/narration/menu register `answer-shape-and-autonomy` suppresses; the §1 binding contract is that guardrail. The sketched collisions were rewritten (Sol no longer asks-before-generating; Mara no longer walks a menu). **Scope.** Routed through `mason-bmad-workflow-expert` (author mode). The original sketch named `correct-course` as Mara's target — it is NOT a fork-customized workflow (upstream-only in `src/bmm-skills/`), so Mara binds to the escalation snippet instead (strictly better — it IS the reflex the pick-1-4 gap resolved). Presentation-only / PROBABILISTIC tier — tone is not safety/money/irreversible, so no deterministic gate. **Delivery.** Committed `0b9a6f41` (9 files), pushed `myfork/custom`; pre-commit fast-path green (refs/lint/lint:md-1052/prettier/budget-24-soft/hook-smoke-10-10); **synced 14/14, 0 blocked.** **Self-review (Mode 1/2).** 0 blocking; no brief/provenance; budget clean (~1 line per step, nothing pushed over); not a policy-skill (owns no necessity) so healthcheck N/A. **Parallel-session coupling (flag, not a defect):** a concurrent session is landing the complementary **STD-PERSONA-002 `persona-placement`** (the gate deciding *whether* a flow earns a voice) — it has folded a "Placement comes first" cross-ref into my `workflow-personas.md` and added its STANDARDS block, both still UNCOMMITTED. My sync ran against the working tree, so the 14 projects already carry STD-PERSONA-002 + `persona-placement.md` (consistent — both Home files shipped together) while `myfork/custom` holds only STD-PERSONA-001 until that session commits+pushes. Self-heals on their push; left their files untouched (defer-to-most-integrated + fold-in, per the fork-authoring-collision convention).

### 2026-06-27 — 4 fork-gap fixes: delivery -f · edit-guard heredoc · dev-story source-of-record · sprint-status prose (`df4ad7a9`)
**What.** Closed four Open `docs/fork-gaps.md` entries — the tractable, self-contained, deterministic ones. (1) **design-handoff silent-no-op delivery**: `step-04-deliver.md` §3 and `shared/delivery-to-main.md` §3 changed `git add` → `git add -f` (the `/_bmad-output/` brief path is gitignored, so a plain add stages nothing → the push ships an empty branch that *looks* delivered) and added a post-stage assertion (`git diff --cached --name-only | grep -qF <basename> || HALT` with the `-f` hint) — silent no-op → loud self-correcting failure; `delivery-to-main` `contract_version` 1→2. (2) **Bash edit-guard heredoc false-positive**: sharpened the root cause — `bmad-bash-edit-guard` greps the entire command string including the heredoc *body*, so a `>` in the body (XML tags like `<style-examples>`, `->` arrows) matched its redirect detector and blocked a plain `git commit`. Fix in `hooks.json`: a message-bearing-VCS exemption (`git (commit|tag|notes)` or `gh <verb>` carrying a `<<` heredoc / `-F` / `--body-file` body → exit 0) inserted before the edit-equivalent grep. (3) **dev-story "source of record" mislabel**: the gap's "dead copy, delete it" premise was wrong — `src/bmm-skills/` is the plugin MARKETPLACE tree (33 skills in `.claude-plugin/marketplace.json`); corrected the STATUS label and added a "two channels, do not confuse" note to the porter header naming `custom/workflows/4-implementation/dev-story/` as the porter source of record. (4) **sprint-status multi-line prose race**: new `parallel-sessions.md` §B1a — story-level prose belongs in the per-story `.md` (board stays one-line-per-key), with a blessed one-owner unit-edit carve-out for any comment block that must stay. **Why.** Owner: "address fork issues and fix." These four are deterministic and low-blast-radius; fixing them removes real recurring friction (a brief that looks delivered but isn't; a routine `git commit` blocked; an agent editing the wrong dev-story file; two sessions clobbering board prose). **Scope.** Deliberately NOT attempted: the hook-distribution keystone (a parallel session is mid-build — untracked `custom/githooks/`/`activate-hooks.sh`/`hook-activation-rail-spec.md`/`check-hook-activation.sh` + an unstaged `sync-bmad-workflows.sh`; surfacing-not-building avoided a collision), the deploy-method ladder (`deployment-to-prod.md`, high — its own session), and the project-memory worktree-slug gap (durable fix is in the Claude Code harness, not the fork). **Delivery.** Staged only my 7 files (the parallel keystone WIP left untouched); pre-commit ran the full suite (src/ touched → no fast-path) and passed incl. the hook smoke-test 9/9; committed `df4ad7a9`, pushed `myfork/custom`. **Sync to 14 owed** — `hooks.json` distributes on the installer/sync fan-out; the workflow prose (delivery-to-main, step-04, parallel-sessions) rides `sync-bmad-workflows.sh`. **Self-review (Mason-BMAD Mode 1/2).** 0 blocking: the guard change functionally tested both ways (false-positive fixed, real edits still denied); no brief/provenance touched; the dev-story fix corrected a wrong premise rather than executing a risky delete; each `[resolved]` note records the corrected understanding. Known narrow limit on the guard fix (documented): a real edit chained after the commit in the same `&&` line isn't re-scanned — acceptable for an awareness/parallel-safety guard.

### 2026-06-27 — create-agent: persona content contract — 8-section shape + human-tone floor as a soft scaffold gate (`8652d39b`)
**What.** Made the owner's 8-section persona-authoring template + "human tone" standard the canonical CONTENT shape for every new agent built by `create-agent`. New `custom/workflows/meta/create-agent/persona-content-contract.md` maps the 8 sections (role/scope · personality/tone · behavior contract · escalation/handoff · interaction patterns · style examples · knowledge/boundaries · registration) onto their homes inside the existing BMAD **XML** persona, and defines the always-baked human-tone floor (3 always-do principles: acknowledge → clarify → close-loops; 3 never-do rules: don't overrule explicit choices, don't fake certainty, don't break BMAD standards). step-03-build: the old "no placeholders, no TODOs ever" rule is reversed into a SOFT GATE — scaffold real content where inferable, write an obvious `<!-- TODO(persona): … -->` marker ONLY where genuinely un-inferable (typically §6 style examples / §4 escalation target), never invent a fact to avoid a TODO. The render skeleton now folds in the human-tone floor, an escalation rule, and a `<style-examples>` block; verification greps add `<style-examples>` + a `TODO(persona)` report; SUCCESS/FAILURE metrics rewritten (covering all 8 sections, floor always present, no invented facts). step-01-brainstorm opportunistically captures `{agent_escalation}`/`{agent_style_example}` if volunteered (never interrogated — keeps the brainstorm tight). workflow.md cites the contract as an ALWAYS rule + registers the two new vars. **Why.** Owner wanted new personas to inherit a consistent, conversational shape, but explicitly wanted create-agent to STAY autonomous and leave the agent invokable on finish — "the autonomy is a feature." The wrinkle: the fork persona is a load-bearing XML embodiment file, not an 8-section markdown doc, so the template can't be the file format. Resolving that by mapping-onto-XML + soft-scaffold-gate (vs an interactive wizard) is forced by the autonomy constraint. **Scope.** Fork only: 1 new contract doc + step-01/step-03/workflow.md in `meta/create-agent/`. No change to the XML format itself, no interactive halt, fully backward-compatible. Out of scope (named): the legacy project-only cast (Devon/Rowan/Jules) — wipe-exposed + wrapperless, a deliberate promote-into-`custom/agents/` decision, not retrofitted here. **Delivery.** Committed `8652d39b`, pushed `myfork/custom`; pre-commit fast-path green (refs/eslint/markdownlint-1047/prettier/budget/hook-smoke-9-9). **Sync to 14 owed** — the contract is prose loaded via the installed path, invisible to project sessions until sync runs. **Self-review (Mason-BMAD Mode 2, hand-authored).** 0 blocking: no brief consumption (provenance N/A); grounding gate untouched (step-01's name+lane gate intact); autonomy explicitly scoped — the gate is a scaffold, NOT an interactive halt, so the "steps 2-4 autonomous" invariant holds; worktree-safe — verified `SYNC_DIRS` includes `meta` and the sync rsyncs the whole `meta/` subtree, so the contract lands at every project's installed path (no dangling reference); context-budget — the 8-section spec is externalized to the contract file so step-03 stays lean (progressive disclosure). Follow-up: candidate to apply the same content-contract/human-tone lens to the legacy cast if/when promoted.

### 2026-06-27 — STD-ESCALATE-001: class-change escalation reflex adopted across the ecosystem (`7b8f695d`)
**What.** Authored a new shared standard, `shared/escalation-on-class-change.md` (STD-ESCALATE-001), and adopted it ecosystem-wide. The standard names one reflex: when a workflow detects mid-flow that the work has changed CLASS — scope materially larger/different than the scoped unit, a missing keystone/shared seam, a planning-not-execution delta, or a wrong-lane request — it STATES the class-change, NAMES the BMAD-default gateway (correct-course / design-router / maintenance-triage), PROPOSES the route, and PROCEEDS unless vetoed. Never a numbered menu; never silent wrong-lane continuation. §4 adds an unattended/`autonomous_mode` clause: a class-change is intent-adjacent, so with no user present to veto, the workflow HALTs and records the route rather than self-routing into a planning lane. Adoption: `dev-story` (the one execution workflow that had NO class-change reflex) gains the tripwire at step 5 (mid-implementation) and step 9 (pre-review); `quick-spec` gains a spec-time tripwire (step-01 §2e); the five workflows that already embody the behavior — `correct-course`, `design-router`, `maintenance-triage`, `design-elevation`, `quick-dev` — get one-line conformance pointers (routing logic unchanged) so the standard has real callers and a new workflow inherits the reflex. Registered in STANDARDS.md under a new "workflow behavior & routing" category. **Why.** The owner diagnosed this as systemic: good local brains (specialist workflows) but a weak shared routing reflex — workflows keep handing the coordinator role back to the owner. An `enforcement-expert` pass found the act-vs-menu response was being actively sabotaged by menu-shaped terminal steps contradicting the global act-don't-menu doctrine; the fix is to canonicalize one reflex and stop the sabotage, not add a heavier gate (hard-gating a judgment call across 14 repos is the indiscriminate-detector anti-pattern). **Scope.** PROBABILISTIC tier only — the deterministic detect→marker→Stop/pre-push backstop is registered in `docs/fork-gaps.md` as dependent on the hook-distribution rail (fork-gap #1), not hand-rolled. Out-of-scope set named explicitly: sprint-status, the planning workflows, design-implement/ingest. **Delivery.** Committed `7b8f695d`, pushed `myfork/custom`; **synced 14/14 (0 skipped, 0 blocked) at `6866108c` on 2026-06-27** — observed atomic run (`--check` showed zero LOCAL-ONLY blocks first), the escalation reflex is now live in every target by default. Pre-commit fast-path green. **Self-review (Mason-BMAD Mode 2, hand-authored).** 0 blocking: standard consumes no brief (provenance N/A); grounding satisfied (inputs are the workflow's own scoped unit); autonomy explicitly scoped decision-not-intent + the unattended clause; conservative tripwire (silent when uncertain); each conformer defers by name rather than re-deriving (policy-skill two-way-routing discipline). Follow-up: the wider-population grep sweep ran manually after the Explore agent stalled at 600s — re-verify nothing new appeared if the corpus grows; collision-hook self-flag logged for the hook owner.

### 2026-06-27 — C4: maintenance-triage design lane delegates into design-router (`2e9bc817`)
**What.** The design-routing wave's gateway review flagged that `maintenance-triage` and `design-router` were two design-lane front doors giving DIFFERENT answers for the same design want — triage re-derived a coarser route map (Claude-Design paste / design-review / design-elevation) while the gateway used the richer artifact-first set, with no reciprocal dispatch. This collapses them to one source of truth: `maintenance-triage` step-03's design lane now only **names the surface + felt want and dispatches to `design-router`**; the gateway owns the lane/altitude/depth/target+placement routing and emits the precise specialist command. Changed across the design-lane prose, the triage-spec `route:` enum (`quick-spec|quick-dev|design-router`), the suggested-next-workflow template, the hand-off display, failure modes, and the frontmatter description + Goal + architecture line; dropped the now-unused `design_review_workflow`/`design_elevation_workflow`/`claude_design_prompt` pointers, added `design_router_workflow`. `design-router` gains a reciprocal Composition note so the relationship reads both ways. **Why.** Two front doors that disagree are worse than one — the owner's thesis was a single explicit routing gateway; leaving triage's mini-router in place forked the truth. **Scope.** Fork: maintenance-triage workflow.md + step-03, design-router workflow.md. Code lane (quick-spec/quick-dev), the data-quality root-cause rule, and the straddle-split are unchanged. **Delivery.** Committed `2e9bc817`, pushed `myfork/custom`, synced to 14 (regenerates the stale `bmad-maintenance-triage` skill description from the new frontmatter). **Self-review (adversarial, fresh subagent).** 0 blocking; the one Concern — the §3 hand-off line emitted `/...:design-router {path}` (a bare artifact path) while design-router's intake grounds on surface+want and HALTs if either is missing — fixed to emit the surface+want invocation; a `system-wide` surface slot was added for policy/tone clusters.

### 2026-06-27 — explicit design-lane routing: analytics-placement-triage leaf + design-router gateway + design-handoff `--placement` contract (`8878b180`)
**What.** Closes the *implicit-routing* gap: in the design family, mode/target decisions lived inside downstream workflows (design-handoff's §5b/§5d/§5e) and only fired AFTER a target was manually picked — so the target choice partly pre-decided the answer, and a routing verdict couldn't be enforced because the specialist re-derived it. Three pieces. **(1) `analytics-placement-triage`** (`custom/workflows/design/`, 4-step) — a thin router that decides WHERE analytics on an operational page lives (`band|tab|sibling-page|remove-band|no-surface`) by single-sourcing band-belongs (§5b), topology (§5d), hierarchy (§5e) from design-handoff step-01 and shape from the `analytics-surface-architect` skill; it re-derives none of them. Emits the exact `design-handoff … --placement <verdict>` command. **(2) `design-router`** (`custom/workflows/design/`, 3-step) — the design-lane front door: a thin 4-axis classifier (lane → altitude → depth → target+placement) that walks axes in order, dispatches the analytics-placement sub-decision to the leaf, and emits a CONSUMABLE handoff (specialist + target + injected decision); classify-and-delegate, with a mode-registration table (wired/deferred) so new axes are added explicitly. **(3) The consumability contract** — `design-handoff` accepts an injected `--placement` (Input + `{injected_placement}` state var); §5b and §5d honor it and skip re-deriving the **analytics-home axis ONLY** (§5d still runs its per-item `needs-detail-route` question — the orthogonal axis the injection doesn't speak to). The `analytics-surface-architect` skill now names the leaf as a second `select` caller (closes one-way routing debt). **Why.** Owner identified that the analytics band/tab/page question was a concrete instance of a general pattern — too many design routing decisions made implicitly inside downstream workflows instead of at a small reusable gateway. Build order chosen: harden the leaf + make the decision *consumable* first, then the gateway on top, so verdicts stick instead of degrading to advisory prose. **Scope.** Fork: 2 new workflow dirs + design-handoff (workflow.md Input/state + step-01 §5b/§5d) + analytics-surface-architect SKILL.md. Additive/backward-compatible throughout (no `--placement` = unchanged design-handoff). **Delivery.** Committed `8878b180`, pushed `myfork/custom`; pre-commit green (refs/lint/md/format; context-budget warnings pre-existing). **Sync to 14 owed** — surfaced as the rollout, not auto-run. **Self-review (adversarial, step-03b ×2).** Both new flows passed a fresh-subagent adversarial pass; blocking findings fixed + re-reviewed — leaf's §5d injection scope-leak (was skipping the whole pass → fixed to analytics-home-only), gateway's grounding-gate inversion (halt-if-neither → halt-if-either), refine-map disagreement between classify/emit, and an orphaned template var. Named follow-ups: **C4** — `maintenance-triage`'s design lane should DISPATCH into `design-router` (today two front doors give divergent answers; no single routing source of truth); a §5c shape-injection short-circuit; decompose the over-budget design-handoff step-01-gather.

### 2026-06-27 — quick-dev blast-radius eligibility gate (scope ceiling) — layers A+B + warn-only backstop
**What.** quick-dev gains a real scope ceiling so a run can no longer silently outgrow "small, decided work." Three layers, labelled by enforcement class: **(A, PROBABILISTIC)** a new `custom/workflows/shared/blast-radius-eligibility.md` fragment, invoked at **step-03 §0** — the one funnel BOTH modes pass through, closing the Mode-A (tech-spec) path that previously skipped step-01's Mode-B-only escalation entirely; classifies `tiny-patch`/`contained-feature`/`not-quick-dev` on the actual intended surface and reroutes on any HARD trigger (schema/migration · auth · payments · shared-infra · over file/diff threshold), overriding `autonomous_mode` like the grounding gate. **(B, DETERMINISTIC script / probabilistic call)** `custom/scripts/quick-dev-blast-radius-check.sh` — reads the OBSERVED diff (`baseline..HEAD`), excludes `_bmad/_bmad-output/.claude/docs` churn, applies directory/filename-scoped HARD-trigger globs + per-project size thresholds; called at step-07 pre-commit. Warn-only by default (always exits 0); `quick_dev.mode: gate` exits 2 with a logged `QUICK_DEV_OVERRIDE`. Rides the existing `custom/scripts/` → `./scripts/` portable-script rail, so it auto-distributes. Config block `quick_dev:` (mode/max_files/max_diff_lines) added to `config-defaults.yaml` (sync backfills it). Plus two tightenings: step-04 self-check split into **MUST-PASS** (tests/tsc/lint/build) vs a new **MUST-OBSERVE** class (migrations · background jobs/queues · auth/permission scope · config/env) — the surfaces brownfield regressions escape through; and step-07's completion output gains an **Eligibility** echo line. **Why.** The owner flagged that quick-dev's existing escalation was Mode-B-only, linguistic (scored prompt wording pre-file-load), and evaporated under `autonomous_mode` — "not a boundary, a suggestion." A run that ships a schema/auth/payments change as "quick" is a workflow-integrity failure; enforcement-expert confirmed a non-negotiable contract needs a deterministic tier, not prose. **(C) deferred-by-design:** the agent-independent pre-push wiring is per-repo (`.githooks/`/husky vary) and does NOT ride workflow sync; kept warn-only and wired per-repo rather than bumping `onboard-project.sh` (which would mark all 13 as onboarding-drift). **Scope.** Fork: new shared fragment + new script + edits to quick-dev step-01/03/04/07 + `config-defaults.yaml` + this STATUS. **Delivery.** pushed `myfork/custom`. **Project sync owed** — the prose ships via `sync-bmad-workflows.sh` and the script via the portable-script rail; surfaced to owner as the rollout action (warn-only, safe). **Verified.** Script syntax-clean; exercised against real history — silent on a 2-file/5-line change, fires correctly on a migration commit (schema + pricing triggers), and a `listing-queue-view.tsx` false-positive on a loose `queue` substring was caught and tightened to directory/filename-scoped globs before commit. **Self-review (Mode 1).** 0 blocking — additive; grounding gate untouched and composed-with (orthogonal); conservative-detector (irrelevant-path exclusion + warn-then-gate) and logged-override anti-patterns designed in up front; honest labelling that layer A's autonomous-mode override is probabilistic and the script is the guarantee.

### 2026-06-27 — onboarding playbook v2: canonical-root identity (restamp-from-worktree fix) + version bump (`onboard-project.sh`)
**What.** `onboard-project.sh` now resolves a `CANON_ROOT` — the canonical repo root — and bases the default project name + the global memory slug on it, while in-repo writes (config.yaml stamp, ONBOARDING.md, CLAUDE.md section) still target `PROJECT_DIR`. When `PROJECT_DIR` is a git worktree (`…/.claude/worktrees/<branch>/`), `git rev-parse --git-common-dir` resolves the MAIN checkout, so identity comes from the project, not the worktree. `onboarding-playbook.version` bumped 1→2. **Why.** Found live during the cash-recovery marker demo: restamping from a worktree (the common case — the tracked marker files need a PR, which needs an isolated branch) made the script take the worktree's basename as the project name (`chore+onboarding-marker-cash-recovery`) and write the `project-onboarding-done` memory to a worktree-slug dir instead of the canonical one; I had to pass `--name` and hand-relocate the memory. The script assumed `PROJECT_DIR` IS the canonical root, but the fork's own worktree discipline pushes restamp into a worktree — same family as `bmad-artifacts-untracked-main-only`. **Scope.** `onboard-project.sh` (CANON_ROOT resolution + name default + `write_onboarding_memory` slug) + `onboarding-playbook.version`. **Delivery.** Pushed `myfork/custom`; pre-commit suite green. Fork-local tooling — no project sync owed. **Verified** with a REAL git worktree: restamp with NO `--name` derived the canonical project name (not the branch), wrote the memory to the canonical slug, left no worktree-slug leak, and the detector is silent at v2==v2. The `--name <repo>` interim workaround is now unnecessary. **Self-review (Mode 1).** 0 blocking — additive identity resolution with a PROJECT_DIR fallback (a brand-new onboard with no `.git` yet is canonical anyway); in-repo write targets unchanged so worktree-PR delivery still works. Resolves the `onboard-project.sh` fork-gap. **Rollout NOT done here:** the owner directed stamping the ~14 repos; the other-13 production rollout (14 PRs) is held for one scope confirmation.

### 2026-06-27 — onboarding: external-upstream-fork mode + completion marker + tutorial (`a1790903`)
**What.** Three additions to the onboarding playbook, closing the only real structural gap vs the owner's "start a new project" design. (1) **External-upstream-fork mode** — `onboard-project.sh` resolves TOPOLOGY (`standalone` | `fork-of-upstream`) by auto-detecting an `upstream` remote or an explicit `--upstream <url>`, records it in the marker, and adds a CLAUDE.md "Upstream fork — sync safety" section; a new global PreToolUse/Bash guard `bmad-upstream-guard.sh` self-gates on the topology stamp and denies `git push upstream` / force-push / `reset --hard upstream/*` (logged override `BMAD_ALLOW_UPSTREAM_PUSH=1`). (2) **Completion marker** — an `onboarding:` stamp in `_bmad/bmm/config.yaml` (`playbook_version`/`onboarded_at`/`topology`/`guarantees`; single source `onboarding-playbook.version`), a generated `ONBOARDING.md`, and a `project-onboarding-done` memory; `--restamp` rewrites the marker only (no clone/sync). (3) **Teach-by-doing** — new `bmad-onboard-tutorial` skill (safe sync dry-run + first feature branch/tests/commit); `bmad-onboard` updated for fork mode / marker / `--restamp` / tutorial hand-off. **Why.** A cold session could not tell a fully-onboarded repo from a pre-playbook one, nor detect a playbook bump; and a personal fork had no guard against clobbering its external upstream. **Enforcement (enforcement-expert lane).** The marker's detection tier is `check-onboarding-version.sh`, a SessionStart sibling of `check-*-drift.sh`: DETERMINISTIC delivery of awareness, but CONSERVATIVE — **silent on a missing stamp** (the ~14 pre-marker repos would otherwise false-fire every session) and speaks only on a present-but-stale stamp; the machine-checkable stamp in config.yaml is the source of truth, ONBOARDING.md + memory are human-facing projections generated from it. The upstream guard is a deterministic GATE with a logged override; `install-global-assets.sh` registers both hooks idempotently. **Scope.** Fork root (`onboard-project.sh`, `onboarding-playbook.version`, `check-onboarding-version.sh`), `custom/claude-global/` (the guard hook + both skills), `install-global-assets.sh`. **Delivery.** `a1790903` pushed `myfork/custom`; pre-commit suite green; global assets activated locally (2 hooks registered + verified, both skills installed). Fork-local tooling — **NO project sync owed** (lives in the fork / `~/.claude`, not distributed to the 14). **Self-review (Mode 1).** 0 blocking — additive; no brief/provenance; worktree-safe (hooks distribute on the install/`~/.claude` track, explicitly not via the project sync); the conservative-detector and logged-override anti-patterns were designed out up front. A `set -euo pipefail` abort (helper functions ending on a false `[[ ]] &&` one-liner) was caught by the self-test and hardened before commit. 19/19 isolated checks pass. **Held:** the fan-out is N/A here (fork-local), but the owner's broader onboarding wave still defers the 14-target *content* sync of other in-flight changes.

### 2026-06-27 — sync deadlock fixed: content-hashed local-only manifest (`4a5f590b` + `92cb6762`)
**What:** the pre-sync safety gate blocked on any file present in a target but absent from source — unable to tell a propagated deletion from genuine local work — so removing one `shared/` standard deadlocked all targets (`Done: 1 synced, N blocked`). The sync now writes a per-project manifest (`_bmad/_config/sync-manifest.txt`, `sha256 TAB relpath`) of what it delivered; a target-only file is PURGEABLE only if it is in the manifest AND its bytes still match the recorded hash, and every other state (locally edited, absent entry, invalid hash, no manifest, whole source dir missing) → BLOCKING. **Why:** removal/rename of a standard is a first-class canon op that must propagate, but never at the cost of deleting local work. **Scope:** `sync-bmad-workflows.sh` only (hash helpers + `compute_source_manifest` + `classify_local_only` + manifest write + manifest-aware `--check`/sync gates + a blocked message that names the file(s) and the `--pull`/`--force` remediation; `shasum` added to the dep check). Additive; no brief/provenance; worktree-safe (distributed by sync). **Delivery:** base fix `4a5f590b`, then hash hardening `92cb6762`, pushed to `myfork/custom` via an isolated-worktree cherry-pick to avoid a shared-index race with a parallel session. **Verified:** `bash -n` clean; 11/11 isolated classifier tests incl. the edited-and-removed case (now BLOCKING) and an invalid-hash case (fail-closed); `--check` clean across all 15 targets. **Self-review (Mode 1):** 0 blocking — deterministic gate, fails closed in every uncertain state; the one fail-open (name-only manifest) was surfaced by an `enforcement-expert` pass and closed with the content hash *before* rollout. **Rollout DONE (2026-06-27):** pilot inbound-flow validated → full sync of the remaining 12 (`Done: 15 synced, 0 blocked`); every old-layout manifest verified 195/195, 0 malformed; cash-recovery skills-layout = gate N/A; manifest gitignored per project. Resolves the top open fork-gap.

### 2026-06-26 — STANDARDS.md canon + contract_version + fork-gaps backlog (`f7336115`, `4e84faa4`/`57c31717`/`84a8c83b`)

**What.** Two linked waves answering "where's the source of truth, and what notices when it's missing/stale?". (1) **Fork-gaps backlog** — `docs/fork-gaps.md`, a Claude-noticed, free-form (no fixed severity/category) log of STRUCTURAL fork/infra friction, sibling to `maintenance-triage`; the global `workflow-friction-and-process-issues` policy gained a proactive "log it yourself" trigger, and the `mason-bmad-workflow-expert` skill (v1.8) reads its `## Open` in first-action + routes. Enforcement hardened past prose: a SessionStart surfacer (`check-fork-gaps.sh`, deterministic delivery) + a fire-once Stop reflection hook (`check-friction-reflect.sh`) — both machine-local in `~/.claude/settings.json`, authored via the `enforcement-expert` skill. Seeded with the deploy-method legibility gap. (2) **STANDARDS.md canon** — `custom/workflows/shared/STANDARDS.md` indexes every shared standard (deploy/delivery/webhook/diagnostics/worktree/parallel-sessions/prod-readiness + memory, the last catalogued though its Home is global `~/.claude`), each with what-it-governs · Home · `contract_version`; includes a "how to author a standard" template (enforcement-tier required). Stamped `contract_version: 1` on the 9 shared standards so `prod-readiness-charter` State-3 drift detection finally has a comparison key.

**Why.** "Where does the source of truth lie for how a deploy guide / memory should look — and I want this standardized." The standards existed but were uncatalogued + drift-prone; this is the single catalog + the version key.

**Scope/Delivery.** Pushed `myfork/custom`; **synced 15/15** (`sync-bmad-workflows.sh` — `_shared/` skills-native mirror is gitignored+regenerated, both layouts carry STANDARDS.md + the 10 versioned files, verified in inbound-flow + cash-recovery). Self-review (Mode 1): no brief/provenance/grounding surface; honest re: the two open items below. **Follow-ups:** build the designed-not-yet SessionStart drift probe (contract_version vs canonical — via `enforcement-expert`); roll out cash-recovery's thin-CLAUDE.md-pointer + `docs/` shape to projects that still restate; bump `contract_version` only on breaking changes.

### 2026-06-26 — prod-readiness-charter: the lifecycle layer above deployment-to-prod (`53a8d810`)

**What.** New shared `custom/workflows/shared/prod-readiness-charter.md` — the policy layer ABOVE `deployment-to-prod` / `delivery-to-main`. Those own a project once it's set up; this owns the three lifecycle moments around being set up: (1) greenfield CROSSING to prod with no deploy contract, (2) a live brownfield that NEVER had one, (3) a contract DRIFTED from the fork canonical. For each: a conservative detection signal, the satisfying artifact, and the action — plus a three-tier enforcement design (SessionStart awareness · PreToolUse deploy gate · CLAUDE.md inline reactive guardrail) on the insight that prose doesn't enforce, hooks do. Memory discipline declared as the parallel domain (same three states vs the global `memory-library-discipline`). **Why.** A live inbound-flow incident: the project CLAUDE.md carried a stale manual-`railway up` instruction (run from `inventory-manager/` when the service Root Directory makes that fail) — textbook State-3 drift that cost ~8 failed deploys to rediscover. The deploy CONTRACT was standardized; nothing detected that a project's deploy DOC had drifted, or that a live project lacked one entirely. **Scope/delivery.** Phase 0 only — policy doc, zero blast radius. The detection probes, a `maintenance-triage` setup lane, and the enforcement hooks are designed-not-built (§Rollout); each is independently revertible and gated on the prior. §3 conservative-detection explicitly excludes deliberate `bmad_contract: skip` and pre-launch greenfield from "gap" — a false flag across 13 projects erodes hook trust. Mode 1 self-review: 0 blocking (no brief consumption → provenance N/A; not input-accepting → grounding gate N/A; worktree-safe — flags that hooks distribute on a track SEPARATE from `sync-bmad-workflows.sh`; context-budget — single doc, 3-state matrix tabled, points to siblings). Committed `53a8d810`; pre-commit suite green. **Pushed `myfork/custom`** (`ff3ca9a4`); **sync to 13 DEFERRED** until the Phase-1 probe exists (a read-only mirror nothing reads is churn for no value). Follow-ups: stamp `contract_version` on `deployment-to-prod.md` (State-3 MVP signal); author the Phase-1 SessionStart probe; the Phase-2 `maintenance-triage` lane; build the memory parallel only after deploy detection proves accurate.

### 2026-06-22 — diagnostics gate: "prove, don't assert" on the implementation verification gate (new `shared/diagnostics-gate.md` + 3 wired callers)

**What.** New canonical shared standard `custom/workflows/shared/diagnostics-gate.md`: when ANY new diagnostic (type error, "cannot find module", lint/compile failure) surfaces after an edit, merge, or worktree teardown, the verification gate is **RED until a re-run IN THE CURRENT CHECKOUT proves zero errors** — binary resolution (prove-green or treat-as-failure), no "probably stale" middle state. Wired into all three implementation deliver-gates: dev-story (step-8 validation gates), quick-dev (step-04-self-check §2), design-implement (step-04 test plan). **Why.** Owner-caught live incident (cash-recovery): after a worktree teardown, 5 "cannot find module" diagnostics were declared "stale" by *reasoning* (referenced the removed worktree path, build had passed earlier) and the run reported "gate green." They were in fact stale — but asserting it instead of proving it is exactly how a genuinely red gate ships as green. The reasoning is a hypothesis; only a re-run is a result. **Scope/delivery.** Adjacent to the 2026-06-22 acceptance-lane entry below — same "build green ≠ verified" failure family, but at the *diagnostics* layer rather than the *AC* layer; that entry already flagged the dev-story done-gate's too-weak signal, this hardens the diagnostics half. Root-cause class: `silent-partial-implementation` (a verification claim made without per-result accountability). Generic dev-quality rule → promoted to the fork so all 13 projects inherit it on sync. The cash-recovery project `CLAUDE.md` carries the project-specific deploy-answer-shape sibling fix separately. **Push `myfork/custom` + `sync` (13 targets) owed.**

### 2026-06-22 — code-review acceptance lane: verify each AC against CURRENT code, not the diff (XML overlay + skills-layout tracked patch)

**What.** The code-review Acceptance Auditor must verify EACH acceptance criterion against the current codebase state, not the diff. **Why.** Diff-scoped acceptance review is structurally blind to an AC that was never implemented (no change → no diff). Live incident: Story 1-4 (cash-recovery) was marked `done` with AC4 (persist reimbursements by reimbursement-id) + AC1 (persist Customer-Return `status`) never delivered — no reimbursements table, no `units.status` column — and the Epic 1 batch review missed it; green build + ticked tasks passed because no test asserted the artifacts exist. **Scope/delivery.** The fork's XML overlay (`custom/workflows/4-implementation/code-review/instructions.xml`) ALREADY had the correct current-state AC check ("search implementation files → IMPLEMENTED/PARTIAL/MISSING → MISSING=HIGH"); the v6.8 **skills-layout port silently dropped it** (→ the pilot ran the diff-scoped version). So this is a carry-over, not a new invention. Strengthened the XML overlay with a test-artifact discipline (point 5: prefer a TEST per load-bearing AC; flag untested ACs). Applied the full fix to the cash-recovery pilot's `.claude/skills/bmad-code-review/steps/step-02-review.md` (auditor gets project read access + verifies each AC against current code + IMPLEMENTED/PARTIAL/MISSING + test-artifact). Recorded as a Phase-4 tracked patch in `MIGRATION-v6.8-skills-plan.md` so the eventual port preserves it. Recurrence note: the **dev-story "done" gate** has the same too-weak signal (tasks ticked + build green ≠ ACs verified) — flagged for the dev-story port. Root-cause class: `contract-dimension-gap` (missing current-state axis) on the skills-layout copy = `upstream-fork-mismatch`. Same pattern as the earlier subagent-conflation fix (`bfac9d1e`). **Push `myfork/custom` owed; `sync --only cash-recovery` already carries the in-project pilot edit (made directly).**

### 2026-06-22 — dev-story parallel-safe story CLAIM + RECONCILE (§C in `shared/parallel-sessions.md` + dev-story SKILL wiring)

**What.** A parallel-aware claim + reconcile protocol for sprint-driven implementers (dev-story). New **§C** in `custom/workflows/shared/parallel-sessions.md`: (C0) source-of-truth rule — the story-file `Status:` line is authoritative for the story lifecycle, `sprint-status.yaml` is the shared **claim ledger**, evidence wins on disagreement; (C1) the **claim token** — an inline YAML comment on the existing `in-progress` value (`# claim: owner=<user> session=<branch-slug|pid> at=<iso8601> baseline=<sha>`), so `development_status[key]` stays a clean `in-progress` for existing readers; (C2) **atomic CLAIM** — re-read fresh, flip+token, re-read to detect a lost race, and **REFUSE** a story held by a live session (skip to next free story); (C3) **RECONCILE** on entry — heal both ledgers to evidence for the "done-but-unchecked" class, reset the "zombie" class to `ready-for-dev`; (C4) **dead-claim detection** — worktree-branch-gone + pid-dead + baseline==HEAD + no-commits = zombie, ambiguous → skip (never steal). Wired into the source-of-record `src/bmm-skills/4-implementation/bmad-dev-story/SKILL.md`: a top-level CRITICAL, Step 1 claim+reconcile (both discovery paths), Step 4 confirm-don't-reclaim (+ writes/confirms the token, HALTs on a lost race), Step 9 releases the token on `review`. Sprint-status template documents the convention. Validator fix: `bmad-shared/` → `validate-file-refs.js` `INSTALL_ONLY_PATHS` (the porter's `_bmad/bmad-shared/` home has no `src/` twin, so the §C ref isn't a broken ref). **Why.** Two live cash-recovery incidents — Story 2.8 zombie claim (claimed, baseline set, zero commits, holder vanished — indistinguishable from real WIP) and Story 4.5 done-but-unchecked drift (sprint `done`, story `review`, empty boxes, code merged). Greenfield dev-story had no atomic claim and two un-reconciled state records. Root class `contract-dimension-gap` (the parallel-coordination axis — who holds a story, and reconciling the two ledgers — was unmodeled for dev-story; the `d5b2edc9` wave scoped it out as base/skills-layer). **Scope/delivery.** Composes with §A1's worktree (the claim `session=` REUSES the worktree branch slug — one identity, no double-warn). Mode 1 self-review: 0 blocking (conservative refuse-over-steal; no brief/provenance; worktree-safe — §C ships via `_bmad/bmad-shared/`). `validate:refs` 0 broken · `lint:md` 0 · `validate:budget` clean on both touched files · porter regenerates `_shared/parallel-sessions.md` with §C. **Distribution honesty:** the shared §C reaches ALL projects on next sync; the dev-story SKILL wiring needed to reach skills-layout projects, which `deliver_skills_layout_project` ports only from `custom/workflows/`. **Closed in the same wave:** dev-story PROMOTED to `custom/workflows/4-implementation/dev-story/` (`workflow.md` `name: dev-story` + `customize.toml` + `checklist.md`), mirroring quick-dev — port engine now emits `bmad-dev-story` (30 ports) and the sync rsyncs it to `.claude/skills/bmad-dev-story/`. `src/bmm-skills/` copy retained as dual-layout source of record. Piloted + verified on cash-recovery (SKILL.md carries claim/reconcile; `_bmad/bmad-shared/parallel-sessions.md` carries §C). Pushed `myfork/custom`; full 15-sync deferred (pilot only).

### 2026-06-22 — Mode-3 diagnose: code-review skill-vs-subagent conflation → tracked Phase-4 patch (`MIGRATION-v6.8-skills-plan.md`)

**What.** Running the upstream v6.8 skills-layout `bmad-code-review` on the cash-recovery pilot, step-02-review.md §2 errors: it says "Launch parallel subagents … invoke via the `bmad-review-adversarial-general` / `bmad-review-edge-case-hunter` skill," but the Agent tool takes a `subagent_type` (agent registry), not a skill name — no such agent type exists, so the launch fails ("Agent type not found"). The file is also internally inconsistent (Acceptance Auditor prompt is inlined; the other two are not). **Why.** Root-cause class `upstream-fork-mismatch`: the fork's own `code-review` is the XML overlay (reviews inline, no subagents — unaffected); the defect lives only in the upstream skills-layout copy the pilot runs. **Scope/delivery.** Did NOT edit `src/bmm-skills/` (upstream-vendored, revert-exposed — Phase-4 forbids in-place src edits; an initial src edit was reverted). Durable fix recorded as a paste-ready tracked-patch item under Phase 4 "Tracked patches to bake in at port time," with a recurrence-audit note for the sibling collisions (`review-adversarial-general`, `review-edge-case-hunter`) and a new Mode-1 self-review check (every "subagent" instruction must name a real `subagent_type` + state inlined-vs-Skill-tool). The pilot's installed `.claude/skills/` copy was patched locally in-project for live correctness (revert-exposed on re-install; the migration plan is the durable record). Single-track delivery: `custom/` only.

### 2026-06-21 — alias-layer follow-ups: stale-alias reaping + agent-namespace routing (`sync-bmad-workflows.sh`)

**What.** Closed the two follow-ups left open by the colon-command alias wave. `sync_skill_command_aliases_for_project()` now (1) routes agent-persona skills `bmad-agent-<role>` to `.claude/commands/bmad/bmm/agents/<role>.md` (→ `/bmad:bmm:agents:<role>`) instead of `workflows/agent-<role>.md` — matching the canonical old-layout agent command form (`bmad:bmm:agents:pm`/`dev`/…) confirmed against `accounting_api_backend`; `bmad-create-agent`/`bmad-design-agent` are workflows and stay in `workflows/`. And (2) **reaps stale aliases**: after writing the desired wrappers it sweeps both namespace dirs and removes any wrapper carrying our marker (`invoke the Skill tool with skill name`) that wasn't (re)written this run — covering a skill deleted from the corpus AND a skill relocated across namespaces (the now-stale `workflows/agent-pm.md` is reaped as the alias moves to `agents/pm.md`).

**Why.** The alias wave only ever wrote wrappers — the old-layout reaper `cleanup_orphaned_commands` keys off an `@_bmad/...` target ref these Skill-invoking wrappers don't carry, and isn't called in the skills-layout path, so a removed skill left a dangling `/bmad:bmm:workflows:<name>` command. And `bmad-agent-*` skills were aliased under `workflows/` (off-namespace). Both were flagged non-blocking on the prior wave; this resolves them.

**Scope/delivery.** Fork: one function rewrite + one caller-label tweak in `sync-bmad-workflows.sh`; skills-layout-only by construction. Desired-set reap (not skill-existence) so relocation is handled; body-marker gate leaves fork-lane agent wrappers (`data-integrity-lead`/`relational-coherence-lead`, LOAD-from-file form) and genuine `@_bmad/...` commands untouched. Verified by a 9-assertion isolated functional test (routing · relocation-reap · removal-reap · fork-lane untouched · design-agent stays in workflows/ · idempotent). `bash -n` clean. Mode 1 self-review: 0 blocking (additive; no brief/provenance; worktree-safe; conservative marker-gated reaper). Pushed `myfork/custom`; **`sync --only cash-recovery` owed** to relocate the 6 stale `workflows/agent-*.md` on the pilot.

### 2026-06-21 — deploy.autonomous opt-in: agent-owned deploy (`41076d99`)

**What.** Added a `deploy.autonomous` config dimension so a project can opt the agent into OWNING deploy choices/questions end-to-end (fresh `origin/main` checkout, verify deploy target, apply ADDITIVE migrations) instead of the default owner-only "state-and-stop" posture — still gating DESTRUCTIVE migrations. `deployment-to-prod.md` gains the "agent-owned deploy" override clause; `onboard-project.sh` now generates `deploy.autonomous: true` so new projects auto-configure it.

**Why.** Root class **upstream-fork-mismatch**: a cash-recovery owner had to *teach* a session to deploy autonomously, then asked why onboarding didn't auto-configure it. The fork sets `autonomous_mode: true` but that flag (a) only governs workflow menus and (b) had NO deploy analogue — deploy autonomy was tribal knowledge / a per-project memory. This makes it a first-class, onboarded config knob. Non-breaking: absent the flag, owner-only posture is unchanged.

**Scope/delivery.** Fork: `custom/workflows/shared/deployment-to-prod.md` + `onboard-project.sh`. Committed `41076d99`, pushed `myfork/custom`. **Owed:** `sync bmad` to propagate the updated contract to the 15 targets' `_bmad/bmad-shared/` (deferred — 6 sessions active; recommend running when quiet). cash-recovery's own `_bmad/bmm/config.yaml` flag set separately (project-owned, not synced). Self-review (Mode 1): autonomy posture gates the one destructive case; opt-in default preserves existing safety; pre-commit suite green (test:refs/install/urls/channels/lint/budget).

**Follow-up (code-review autonomous gate — sibling to this complaint, NOT yet done globally).** Same root class: the 44 BASE BMAD skills delivered by the v6.8 install (incl. `bmad-code-review`) carry ZERO `autonomous_mode` gating, so on a fresh skills-pilot project they HALT interactively and the dev must hand-patch — exactly the create-epics case (`e8d48b4c`). cash-recovery's `code-review` was hand-gated this session and IS durable there (it's a base-install skill, not a fork port, so the skills-native sync never overwrites it). The global fix = promote `code-review` into `custom/workflows/4-implementation/code-review/` (workflow.md+steps), convert its skills-shape path refs to the porter convention (`{project-root}/_bmad/bmm/workflows/.../steps/` so the porter flattens correctly — the round-trip hazard), gate the HALTs, run the porter, diff-verify against the known-good cash-recovery gate, then sync. Deferred from this wave as a careful standalone pass (15-project blast radius; the convention conversion must be diff-verified before shipping). Broader: a one-time sweep of all 44 base skills for interactive HALTs that should gate.

### 2026-06-21 — skills-layout colon-command aliases: `/bmad:bmm:workflows:<name>` parity (`sync-bmad-workflows.sh`)

**What.** A skills-layout project invokes workflows as `/bmad-<name>` (the v6.8 skills form); the owner wanted the old command-layout `/bmad:bmm:workflows:<name>` colon format back on cash-recovery (the sole skills-layout pilot). New `sync_skill_command_aliases_for_project()` in `sync-bmad-workflows.sh` generates, for every delivered `bmad-<name>` skill, a thin command wrapper at `.claude/commands/bmad/bmm/workflows/<name>.md` whose body invokes the Skill tool with `bmad-<name>` and whose description is lifted from the skill's SKILL.md frontmatter (single-/double-quote stripped, single quotes YAML-escaped). Wired into `deliver_skills_layout_project` as step 3b.

**Why.** The owner's ask, read literally, was "revert this project to the old layout" — but the sync's layout detection is binary (presence of `_bmad/bmm/workflows`), so a revert flips cash-recovery off `deliver_skills_layout_project` entirely and destroys the migration's only proof-point. That exact reconcile-back-to-command-layout was already evaluated and rejected on 2026-06-19. The alias layer delivers the user's actual goal (the colon slash format) **without** touching the layout: both forms resolve, pilot intact. Root of the recurring confusion is slash-command-format parity, not drift.

**Scope.** Fork: one function + one call site in `sync-bmad-workflows.sh`; skills-layout-only by construction (runs from `deliver_skills_layout_project`). Idempotent (drift-checked write like the agent wrapper). Not reaped — `cleanup_orphaned_commands` is old-layout-path-only. Mode 1 self-review: 0 blocking — additive; no brief consumption (provenance N/A); grounding gate N/A (mechanical sync fn); worktree-safe (distributed by sync, writes to the hook-allowlisted `.claude/`); the chosen path PRESERVES the pilot where the literal request would have destroyed it (surfaced + the better path taken).

**Delivery.** `sync --only cash-recovery` → `OK colon-command aliases (72 wrapper(s))` + 29 ports + 16 bmad-shared policies (cash-recovery brought current). All 72 wrappers structurally well-formed; the command loader registered all 72 `/bmad:bmm:workflows:*` with correct descriptions. **Push `myfork/custom` owed.** No other-project sync owed (the 13 old-layout projects already have native colon commands). Follow-ups: (orphan-cleanup) add stale-alias removal to the skills-layout path; (agent-namespace) `bmad-agent-*` skills alias under `workflows/` not `agents/` — harmless, noted; (fork-wide) migrating projects inherit aliases automatically.

### 2026-06-20 — onboarding root-fix: single-track delivery default + skip-mode deploy posture + git-merge guard (`6b078d84`)

Owner friction, traced to the root: every implement/dev-story run ended by handing the owner two recurring **standing-policy** decisions — "want me to deploy?" and "local main is N ahead, reconcile?" — and the cash-recovery onboarding had baked both in. Two template/onboard defects mint them in every onboarded project. (1) The CLAUDE.md template's `ALWAYS Deliver Your Work` section shipped BOTH a PR option AND a `git merge`-into-local-main **"Option B (solo projects)"** plus an unresolved `<!-- TODO: Choose ONE delivery method -->` — so a solo project reads local-merge as blessed, which is the canonical **origin/main divergence** (domain Stories pile up on an unpushed local main while UI ships via PR; cash-recovery hit 14-ahead this very session). (2) `onboard-project.sh` hardcodes `deploy.bmad_contract: skip`, but the template's Deployment section carried **no posture for the skip case** — so each session improvised a "deploy?" question. Root class `contract-dimension-gap` (the delivery-method and deploy-posture dimensions were unspecified at the template layer; the friction was a *missing standing decision*, not a per-run choice). Fix at the root: template `bmad:deliver-work` deletes Option B + the TODO and mandates a single PR-to-origin track with an explicit "never `git merge` into a local main you don't push; local main only fast-forwards from `origin/main`" invariant; template `bmad:deploy-contract` gains a skip-mode posture clause ("after merge, state deploy status and STOP — never ask"); `onboard-project.sh`'s deploy comment points at it. **Enforcement, not just prose:** new `bmad-single-track-guard` PreToolUse Bash hook in `hooks.json` blocks `git merge <branch>` while on `main` in a BMAD project, ALLOWING `git merge --ff-only origin/main` / `--abort` / `--continue` — unit-tested across 8 command shapes, JSON-validated, survives `format:fix`. **Two-layer distribution reality (verified by reading `sync-claudemd-sections.py`):** the hook is `jq`-upserted into all 15 projects' `settings.local.json` on next sync (enforcement everywhere), but the CLAUDE.md prose edits are **add-only** — `section_present` skips any section whose marker/heading already exists, by design, to preserve per-project customization — so the prose reaches NEW projects via the template but NOT the existing 15's already-present sections. That's acceptable: the hook is what prevents the divergence; prose is secondary. Mode 1 self-review: 0 blocking (edits additive; worktree-safe — distributed via sync; no provenance/autonomy concerns; the hook is halt-by-default with an `--ff-only`/`--abort`/`--continue` escape). Delivery: committed `6b078d84`, pre-commit suite green (the single-track guard intact + valid after lint-staged); **push `myfork/custom` + sync owed.** Follow-ups: **(1) RESOLVED (`44898203`)** — instead of a 15-repo CLAUDE.md PR storm, the skip-mode deploy posture (state-and-stop, never ask) + the single-track delivery rule were added to the **shared `deployment-to-prod.md` contract** (`custom/workflows/shared/` + skills-native `_shared/`), the doc every project's CLAUDE.md already defers to and which syncs to all 15 — so the posture reaches existing projects without touching their heterogeneous CLAUDE.md prose. **(2) RESOLVED — no promotion needed:** `dev-story` does NOT local-merge (or deliver at all) — it implements, updates the story file, sets sprint status to `review`, and stops; delivery is deferred to CLAUDE.md convention, now single-track + hook-enforced. The earlier "dev-story local-merges" assumption was wrong. (3) 13 pre-existing markdownlint errors in `custom/claude-global/` skill-backup files (bare URLs + emphasis spacing), unrelated to this wave — still open.

### 2026-06-20 — create-epics-and-stories: promote into custom lane + autonomous_mode gate (`e8d48b4c`)

Owner hit it live: `create-epics-and-stories` paused after every story and every epic even with `autonomous_mode: true`, because the base workflow's menus carry unconditional `ALWAYS halt and wait for user input` / `ONLY proceed when user selects 'C'` with **no** autonomous gate (`grep -ri autonomous` across both the skills-layout skill and the command-layout workflow = 0 hits) — unlike the fork's own `quick-dev/step-07`, which already gates its halts on `autonomous_mode`. Root class `upstream-fork-mismatch` (the base/upstream workflow lacks the fork's autonomy convention). Fix: promoted the workflow from the upstream reference (`3-solutioning/create-epics-and-stories/`, a `workflow.md` + `steps/` shape) into `custom/workflows/3-solutioning/create-epics-and-stories/` and gated every pause point on `autonomous_mode` — the three step menus (step-01 §10 / step-02 §8 / step-03 §7), the per-epic "Get user confirmation to proceed to next epic", and the `workflow.md` global WAIT-FOR-INPUT rule: when autonomous, skip the menu → treat as `[C] Continue` → keep going, simulating informed expert decisions per the project's `autonomous_rules`; interactive (non-autonomous) runs are byte-unchanged. Ran the porter → regenerated `custom/skills-native/bmad-create-epics-and-stories` (29 ports total). **Delivered to + verified on cash-recovery** (skills-layout, the owner's project): `sync --only` delivered the ported skill and all five gate points are present in the on-disk skill — the owner's pause-after-each pain is fixed where they work. **Known limit, empirically verified by reading the per-project sync order:** for the 14 COMMAND-layout projects the custom delivery runs FIRST and then `sync_upstream_from_reference` re-rsyncs `3-solutioning/` from the reference with `--delete`, clobbering the gated override with the reference's un-gated copy; the skills-layout `deliver_skills_layout_project` path is a separate codepath untouched by that upstream sync, which is exactly why cash-recovery lands clean and the others wouldn't. **Follow-up (command-layout delivery):** add `--exclude='create-epics-and-stories'` to the upstream `3-solutioning` rsync (or lift the workflow out of `UPSTREAM_WORKFLOW_DIRS` into pure custom management) so the gated version survives — sync-script surgery with 15-project blast radius, deliberately deferred rather than rushed. Delivery: committed `e8d48b4c`, pre-commit suite green; **push `myfork/custom` owed**; full 15-project sync **deferred** (would clobber command-layout until the follow-up lands). Scope honesty: this is the create-story promotion the owner deferred two waves ago, reopened specifically for the autonomy fix and shipped only as far as it lands cleanly.

### 2026-06-20 — parallel-sessions: worktree + parallel-reconcile protocol for implement/review workflows (`d5b2edc9`)

The fork's implement/review workflows assumed one linear session. In real multi-agent projects — observed live on cash-recovery, where one session delivered Epic-2 stories 2.1–2.4 while a second drove Epics 1+3 — **every** delivery hit a barrel / `schema.ts` / migration-number conflict and `sprint-status.yaml` was edited by both sessions at once, and none of the resolution lived in the workflow text: quick-dev never called `EnterWorktree` (it only DETECTED a worktree at deliver time), `step-07` HALTED on a merge conflict ("report the error and halt"), and code-review's working-tree `git diff` is EMPTY for an already-merged story. New `shared/parallel-sessions.md` is the missing layer. **§A src-editing:** enter a worktree from LOCAL `main` (origin lags because parallels merge locally), integrate the advancing `main` before delivery, **RESOLVE-don't-halt** the mechanical collision classes (barrel re-exports → keep both · additive `schema.ts` tables → keep both · two `0001` migrations → take theirs + regenerate yours as `0002` · `sprint-status` per-key edits), re-gate, and halt ONLY on a genuine *semantic* conflict; local-merge vs PR by `git remote`. **§B artifact:** per-key `sprint-status` edits + explicit lane claiming; no worktree (`_bmad-output` is hook-allowlisted — a worktree would wrongly isolate it). Composes with `worktree-portability` (path mechanics) + `delivery-to-main` (artifact/PR) — cross-referenced, not duplicated. Wiring is thin point-of-use pointers so the workflows stay budget-clean: quick-dev step-03 enter-worktree-before-editing hook + step-07 integrate-first / delivery-mode pick / halt→resolve-per-§A4; code-review recovers a delivered story's diff via its `baseline_commit` + `git rev-parse --show-toplevel` path resolution. Scope = **custom lane only** (quick-dev, quick-spec by-reference, code-review); dev-story + create-story/create-epics are base/skills-layer and were deliberately left for a follow-up (owner chose custom-lane-only). Mode 1 self-review: 0 blocking; 2 Concerns (the migration-resolution command is drizzle-specific — the *class* generalizes; quick-spec is artifact-only and intentionally unwired). Delivery: committed `d5b2edc9`, `validate:budget` green, pre-commit suite green; **push `myfork/custom` + sync 15/15 owed** ("then we re-sync" — owner-triggered).

### 2026-06-19 — migration-plan Status line corrected; cash-recovery pilot reconfirmed healthy

A cash-recovery session asked why its slash commands differ from the other 14 projects (`/bmad-foo` skills vs `/bmad:bmm:workflows:foo` commands). Diagnose pass (mason-bmad-workflow-expert) confirmed this is the **intended** v6.8 skills-native pilot, not drift — `deliver_skills_layout_project` delivers the 28 fork ports from `custom/skills-native/`, and this `## Now` block already records the pilot cutover. Only stale artifact: `custom/MIGRATION-v6.8-skills-plan.md`'s header still read `Status: … (not started)` while the machinery shipped (`c589223c`) and the pilot is live — corrected to match. On-disk composition verified: cash-recovery `.claude/skills` = 72 `bmad-*` (28 match a fork port; 44 are base BMAD from the v6.8 install — the same un-customized base command-layout siblings also carry) + 6 helper skills. No fork code changed; **no project sync owed** (plan doc is fork-local). The earlier instinct to "reconcile cash-recovery back to the command layout" was caught and rejected — it would have undone the pilot.

### 2026-06-19 — create-workflow: policy-skill/router quality axis in step-03b (`3c04ba6e`)

create-workflow's authoring gate enforced only the context-budget half of the durable doctrine; the policy-skill / workflow-invocation doctrine (now mature via mason-bmad-workflow-expert v1.7, `policy-skills-healthcheck`, and the finance/analytics invocation work) had no authoring-time hook. Added a thin layer. **workflow.md**: the description carries a doctrine hook — when the new flow OWNS policy (necessity / materiality / domain ownership / safety-correctness) or acts as a ROUTER between skills, step-03b additionally applies the policy-skill/router rules and calls `policy-skills-healthcheck` (read-only); the 3b architecture note matches. **step-03b-review.md**: one CONDITIONAL review axis (skipped for a plain mechanical-transform workflow, so zero load on the common path) — require the flow to declare purpose/ownership/"do not use when", `uses_skills:`, and an abstain behavior, and require any NEW skill it introduces to satisfy the four policy-skill creation checks (invoke block · mode-wired · sister-symmetry · two-way routing); severity mapped (contract-breaker → Blocking, structural-debt → Concern-with-followup). Points at the `policy-skills-healthcheck` skill, NOT a new doctrine file (avoids a dangling `references/policy-skills.md` the workflow can't reach). Mode 1 self-review: 0 blocking; one Concern (the new axis bullet is information-dense — promote to a `references/policy-skills.md` pointer if it grows). Delivery: committed `3c04ba6e`, pushed `myfork/custom`, **synced 15/15**; `validate:budget` green. **Follow-on (`31762d87`):** the symmetric audit-time axis shipped — `orchestrate-workflows/step-02-health-check` gains a conditional **§5b** (a lettered insert, no renumber; same finding-tag vocabulary — `missing-ownership-decl` / `missing-invoke-block` / `dormant-mode` / `asymmetric-sibling` / `undocumented-routing`; points at `policy-skills-healthcheck`). So the policy-skill/router doctrine is now enforced at BOTH ends like context-budget is: authoring (create-workflow step-03b) AND ecosystem audit (orchestrate §5b). Mode 1: 0 blocking; budget green; **synced 15/15**.

### 2026-06-19 — foundation-token reconciliation: close the design→code token-scale handoff (`67d3ae35`)

A faithful `design-implement` port (matching class names + markup) rendered ~23% large because the app's canonical type scale (`--font-size-base: 1rem`/16px) diverged from the design system AND `docs/design-policy.md` (13px), and no pipeline step caught it — inbound-flow held-orders (PR #2412), where the implementer "fixed" it with `var(--font-size-base, 0.8125rem)`, a dead fallback the defined global overrides. Closed the loop across three workflows: **design-implement** gains step-03 §2i Foundation-token reconciliation (design-system vs app-canonical vs policy-declared), fed by URL.4 now reading the bundle's foundational `tokens/*.css` (the missing URL-path denominator) + step-02 §5a; drift is routed-not-patched, the dead `var(--token,<literal>)` fallback banned, §9 discloses it (+ workflow rule + 4 state vars). **design-review-pr** gains standing check `F-FOUNDTOKEN-01` (P1 dead-fallback, P2 tokens-vs-policy) for the bypass case. **apply-design-policy-change** gains **Level 0 Foundation-Token Migration** — the single-source `tokens.css` edit that was homeless (why v3's 13px shipped "flagged, not auto-migrated"). Detect + catch now route to a fix that exists. Delivery: committed `67d3ae35`, pushed `myfork/custom`. Self-review (Mode 1): autonomy = detect+route, never auto-migrate; routing target verified real before shipping. Owed: `sync bmad` to the 13 targets · the inbound-flow app token migration itself (run Level 0 on `inventory-manager`) · step-03-build-grid context-budget watch (now 10 axes).

### 2026-06-19 — clear policy-skill debt: analytics explain wired + C-FINANCE-01 added

**What.** Cleared both seeded S2 items. (1) **`analytics-surface-architect` `explain`** is no longer dormant — wired into design-review-pr step-03 §3b (on a `C-ARCHETYPE-01` finding, invoke `explain` so the finding teaches the right shape, not just flags the wrong one) + a documented human-onboarding entry; all three modes (`select`/`critique`/`explain`) now have callers. (2) **`C-FINANCE-01`** added to design-review-pr — the finance symmetry partner to `C-ARCHETYPE-01`: for routes whose brief is `is_finance_surface` (carries §2b), it audits at PR time that the build preserved the finance contract — DOM-detectable mechanical fires (leading-minus negatives, unlabelled mixed currency, blended qty+value cell) + a human-judgment prompt for exception representability and accounting-truth (`must_not_infer`), never fabricating against a named `unresolved_assumption`.

**Why.** "Do it" — clear the policy-skill debt. Restores symmetry: finance now has both handoff-enrich (`finance-domain-pass`) AND PR-audit (`C-FINANCE-01`), matching analytics (`analytics-surface-architect` select + `C-ARCHETYPE-01`).

**Scope.** `analytics-surface-architect/SKILL.md` (explain callers) + design-review-pr `workflow.md` (`{brief_finance_map}` + `C-FINANCE-01` def, "five checks"→"six") + `step-01-scope.md` (build the map) + `step-03-dom-render.md` (§3b explain wire + §3d finance harvest/fires) + `step-04-deliver.md` (§1b-4 aggregate + coverage note). Ports 0-dangling; validate:budget passes (step-03 over soft line-budget, under byte-budget + hard ceiling — soft warning). Synced 15/15; pushed `myfork/custom`. Policy-skill debt now empty.

### 2026-06-19 — policy-skill severity scheme (S1/S2/S3) + debt tracking

**What.** Gave the policy-skill checks a three-level severity scheme and routing. **S1 contract-breaker** (no invocation block on a new/changed policy-skill; mode repurposed without updating callers; caller re-deriving policy logic; symmetry break that contradicts policy) → blocks the PR, fix or explicitly waive. **S2 structural debt** (never-called dormant mode; "just-behind" asymmetry; one-way routing) → may merge but only WITH a follow-up task or a `STATUS.md` "Policy-skill debt" entry. **S3 hygiene** (inconsistent wording, missing examples) → opportunistic. Wired into `policy-skills-healthcheck` (output contract `severity: S1|S2|S3` + reason + suggested_fix, plus a Severity & Routing section) and the mason-bmad "Policy-skills" doctrine. New **"Policy-skill debt"** section in STATUS seeded with the two real S2 items (analytics `explain` dormant; finance has no PR-time audit counterpart).

**Why.** Make the healthcheck actionable without becoming a second rating system — three levels are enough to route (block / schedule / opportunistic) and the S2 bucket gets a tracked home so debt isn't ship-and-forgotten.

**Scope.** Fork (git-tracked): `policy-skills-healthcheck/SKILL.md` + STATUS (debt section). `mason-bmad-workflow-expert` doctrine (in `~/.claude`, not tracked) gained the severity routing. Synced 15/15; pushed `myfork/custom`.

### 2026-06-19 — policy-skill doctrine + policy-skills-healthcheck meta-skill

**What.** Generalized the finance/analytics invocation-policy work into a reusable doctrine. A **policy-skill** encodes necessity (materiality / domain ownership / safety / correctness); when one is discovered, authored, or modified, proactively run four health checks — plain-language invocation policy present? no dormant modes (every mode has a real caller)? symmetry with sister skills? routing documented both ways? — and surface gaps as **system-health** findings, not local notes. (1) New "Policy-skills — invocation health" section in `mason-bmad-workflow-expert` (v1.6→**1.7**; woven into Mode 1 review list + Mode 2 + closeout triage). (2) New `custom/skills/policy-skills-healthcheck/` — the on-demand corpus auditor (read-only; "sanity-check our skills").

**Why.** The `analytics-surface-architect` under-routing catch was lucky, not systematic. This makes "this skill is dormant / mis-wired / asymmetrical" a default reflex fired early, and it's domain-agnostic — no hard-coded finance/analytics.

**Scope.** Fork (git-tracked): new `policy-skills-healthcheck` skill. The `mason-bmad-workflow-expert` edits live in `~/.claude` (NOT git-tracked — live immediately; v1.6 snapshot rolled to `versions/`, version → 1.7). Synced 15/15; pushed `myfork/custom`.

### 2026-06-19 — analytics-surface-architect: plain invocation policy + critique wired into design-review-pr

**What.** Gave the analytics skill the same treatment as finance-domain-pass. (1) Added a plain-language "When to invoke" section + materiality-gated description: invoke when the main question is "what analytical SHAPE answers this user question from this dataset?" (select/critique/explain); do NOT use for the band-belongs decision (upstream §5b), visual styling/tokens, or a plain worklist with no band; if-uncertain → invoke only when shape is the question, else stay silent. (2) Wired **critique** mode into design-review-pr `C-ARCHETYPE-01` (step-03 §3b): the archetype audit now defers to the skill (the DOM-harvest comparison table becomes the evidence-it-passes + deterministic fallback), so it additionally catches a band that renders **exactly as declared** yet whose declared archetype was the wrong shape for the question — the case the heuristic table structurally cannot. workflow.md §53 updated.

**Why.** The skill was correctly designed but under-routed — single-caller (handoff §5c only), `select`-only, jargon-coded trigger, effectively dormant; design-review-pr re-derived the archetype audit inline instead of deferring to the single brain. Restores the "one place this is reasoned" intent and gives symmetry with finance-domain-pass.

**Scope.** `analytics-surface-architect/SKILL.md` + design-review-pr `step-03-dom-render.md` §3b + `workflow.md` §53. **Skipped** the optional step-01 §5c materiality tweak — redundant with §5b's job-based band-belongs gate, and step-01 is already over soft budget (net-negative). Mode-1: 0 blocking; critique wiring is additive (skill-preferred, table fallback → zero regression), read-only, conservative on ambiguity. Ports 0-dangling; validate:budget passes. Synced 15/15; pushed `myfork/custom`.

### 2026-06-19 — finance-domain-pass: sharpened invocation policy (materiality gate)

**What.** Owner-supplied invocation policy installed on `finance-domain-pass`: a "When to invoke" section (invoke when money is a primary data type or bad presentation of quantities/values/balances/costs/taxes/landed-costs/variances/reconciliation could distort financial truth; **do not** use for incidental-money CRUD pages, pure styling, or another-domain-owned semantics; if-uncertain → fire only when bad presentation could distort financial truth, else note the ambiguity). Frontmatter description gated on materiality so auto-activation doesn't trip on incidental money. Same materiality gate mirrored into design-handoff step-01 §3b, replacing the keyword list (net-neutral: step-01 +3 lines).

**Why.** Tighten when the finance pass engages — finance-shaped operational data where presentation affects meaning, not every page with a money field.

**Scope.** `custom/skills/finance-domain-pass/SKILL.md` (description + When-to-invoke) + `design-handoff/steps/step-01-gather.md` §3b gate. Ports regenerate 0-dangling; validate:budget passes. Synced 15/15.

### 2026-06-19 — design-handoff: finance-domain-pass (semantics enrichment, not layout) (`f44dc05b`)

**What.** A finance twin of `analytics-surface-architect`: new `custom/skills/finance-domain-pass/` that `design-handoff` step-01 §3b invokes (conditional on a finance-shaped surface) to extract the finance MEANING a blank-canvas redesign must preserve — column semantics, capabilities-as-outcomes, exception expectations, implied surfaces, unresolved assumptions, terminology, must-not-infer — returned as a structured appendix. step-01 routes its fields into the existing machinery (`must_support_capabilities` / `dropped_capabilities` / §5f `spawned_surfaces`) plus new `{finance_*}` vars; brief-template §2b (conditional) renders the finance semantics + accounting-truth constraints + unresolved assumptions as Open Questions; step-03 self-review gained a finance/no-layout-leak check.

**Why.** Owner-designed integration: make the finance layer a brief-enrichment companion to design-handoff, NOT a page-builder — it answers "what finance semantics must survive the blank canvas?" while composition stays with design-policy + the blank-canvas process. Respects the bias-filter doctrine and the source-of-truth precedence (finance meaning never overrides policy; collisions → open question).

**Scope.** Fork: new skill + 4 design-handoff files (workflow.md vars, step-01 §3b, step-03 conditional + check, brief-template §2b). Forbidden-list + "outcomes never mechanics" + step-03 check triple-guard against layout leak. Mode-1: 0 blocking; concern — step-01 now 532 lines/~25 must-dos (over soft budget, pre-existing density; detail lives in the skill). Ports regenerate 0-dangling; validate:budget passes. Pushed `myfork/custom` → `f44dc05b`; **synced 15/15** (old-layout via workflow overlay + skills; cash-recovery via skills-layout path).

### 2026-06-19 — v6.8 migration Phase 0 + Phase 5 pilot: sync delivery + end-to-end proof (`c589223c`)

**What.** Wired the sync to deliver the migrated skills layer (`sync-bmad-workflows.sh`: a skills-layout project — no `_bmad/bmm/workflows`, `.claude/skills` present — is no longer skipped; `deliver_skills_layout_project()` builds the ports via the engine and rsyncs the 28 `bmad-<name>` ports + the 15-file `bmad-shared` home + custom-skills/scripts/agents/hooks/config/CLAUDE.md). Then proved the whole pipeline end-to-end: a REAL `bmad-cli` v6.8 install (45 upstream skills, no old workflows dir) → add to targets → sync → 72 total skills, fork's guarded `bmad-quick-dev` overrode the upstream one, `bmad-shared` delivered, and a ported workflow's cross-skill + shared refs all resolved in the real project.

**Why.** Complete the migration machinery so a v6.8 skills-layout project gets the full fork custom layer. Dual-layout: the old-layout path is byte-identical and the 14 existing projects are untouched (verified: full `--check` exits 0, "All projects up to date").

**Scope.** Fork: `sync-bmad-workflows.sh` (self-contained skills-layout branch + delivery fn; old path unchanged). Pushed `myfork/custom` → `c589223c`; pre-commit suite green; synthetic + real-install tests cleaned up (targets back to 15).

**Delivery / what's left.** The migration is now BUILT and PROVEN. Remaining is **decision, not engineering**: (1) the actual cutover of the 14 real projects from old-layout to skills-layout — an owner go/no-go (they work fine on old layout today; dual-layout means no rush); (2) Phase 6 — retarget `onboard-project.sh` default to skills-layout (today it still produces old layout); (3) Mode-1 review of `orchestrate-workflows`/`dispatch-followups` corpus-enumeration semantics.

### 2026-06-19 — v6.8 migration Phases 2–4: port engine + corpus generation (`87daf92f`)

**What.** Started executing the v6.8 skills-layout migration (the plan parked earlier). Proved the approach with a vertical-slice pilot (`design-elevation` ported by hand, all references resolved in a throwaway skills-layout project), then built `tools/port-workflows-to-skills.sh` — the reusable engine that ports all 28 custom workflows from `custom/workflows/` (old layout) to `bmad-<name>/` skills + a consolidated 15-file `_shared/` policy home. Corpus-validated: 0 remaining old-layout refs, every skill name == its dir (installer gate), all shared refs resolve. Phase 4 collision analysis: only `bmad-quick-dev` collides with an upstream skill — handled free by sync last-writer-wins.

**Why.** "Pick up the migration" — make a fresh v6.8 install get the full fork custom layer so onboarding no longer needs the old-layout reference clone. De-risked uncertainty-first: pilot + corpus rewrite BEFORE touching the shared sync infra (Phase 0) or any real project (Phase 5).

**Scope.** Fork: new `tools/port-workflows-to-skills.sh`; `.gitignore` adds `custom/skills-native/` (generated, re-runnable build output — not committed). Source of truth stays `custom/workflows/` during the dual-layout transition. Pushed `myfork/custom` → `87daf92f`; pre-commit suite green.

**Delivery / remaining.** No project sync owed yet. **Phases still open:** Phase 0 (wire the sync to deliver skills-native ports + `_shared/` to a skills-layout project — the one risky shared-infra change), Phase 5 (dual-layout pilot rollout on one real project, then the rest). **Mode-1 follow-up:** `orchestrate-workflows` + `dispatch-followups` reference the corpus *location* — rewritten to `.claude/skills/`, but they enumerate the corpus, so the skills-vs-workflows semantics need a human review before trusting them on the skills layout.

### 2026-06-19 — onboarding follow-ups: targeted sync, reference-health guard, global-asset backup, migration retarget note (`02c892e4`)

**What.** Closed all four follow-ups from the onboarding-redesign wave. (F3) `sync-bmad-workflows.sh --only <path>` syncs a single project; `onboard-project.sh` now uses it (~1s vs the full 15-target sweep; full sync unchanged when `--only` absent). (F4) onboard's reference guard now requires a *complete* old-layout reference (core/agents, bmm/agents, bmm/data, `_config/manifest.yaml` + the `1-analysis` marker), not just the skills-layout check — a new project clones the reference base, so gaps would propagate. (F1) `~/.claude` isn't git-tracked, so the `bmad-onboard` skill + the global-CLAUDE.md bootstrap section now have canonical copies in `custom/claude-global/`; `install-global-assets.sh` restores the skill to `~/.claude/skills/` and onboard calls it best-effort each run (self-heal). (F2) migration plan gained Phase 6: the explicit obligation to retarget the onboarding tooling to skills-native when the cutover lands.

**Why.** The onboarding-redesign wave shipped with four named, non-blocking follow-ups; the owner asked to clear all of them and keep the tooling maintenance-friendly. Each closes a real gap: full-sync-to-onboard-one was wasteful; the skills-layout-only reference check missed a subtly-broken old-layout reference; the global skill had no backup/rollback; and the stopgap could be forgotten at migration time.

**Scope.** Fork: `sync-bmad-workflows.sh` (+`--only`), `onboard-project.sh` (targeted sync + health guard + self-heal call), new `install-global-assets.sh`, new `custom/claude-global/` (README + bmad-onboard skill copy + bootstrap snippet), `custom/MIGRATION-v6.8-skills-plan.md` (Phase 6). Tested: `--only` isolates to one project while full `--check` still iterates all (exit 0); onboard E2E produces a complete project (workflows, hooks, 40 commands, hardened quick-dev) with 0 contamination; `install-global-assets.sh` idempotent.

**Delivery.** Pushed `myfork/custom` → `02c892e4`. **No project sync owed** — fork-local tooling. Remaining open item is now just Phase 6 (retarget at migration time), tracked in the plan.

### 2026-06-19 — onboarding redesign: one-command onboard-project.sh + v6.8 migration plan (`fc456214`)

**What.** A single idempotent command — `~/bmad-method-v6/onboard-project.sh [<dir>] [--name] [--phase]` — takes a directory from nothing to fully wired to the fork (old-layout `_bmad/bmm/workflows` base + custom overlay, skills, hooks, commands, CLAUDE.md). It git-inits, clones the reference project's `_bmad` base (per `~/.bmad-reference`), generates a clean `config.yaml`, creates CLAUDE.md from the template, registers in `~/.bmad-targets`, and runs the sync. Paired with a `bmad-onboard` Claude skill (in `~/.claude`) so natural language ("install the BMAD fork") invokes it, an updated global-CLAUDE.md bootstrap section, a self-documenting sync SKIP message, and `custom/MIGRATION-v6.8-skills-plan.md` (the eventual skills-layout cutover, decisions resolved).

**Why.** Onboarding cash-recovery un-guided surfaced that the documented "install BMAD via the installer" bootstrap is broken: the v6.8.0 installer now emits the skills layout, incompatible with the fork's sync + custom layer, so a fresh install yields vanilla BMAD with no safety layer. The sync also silently skips a project with no `_bmad/bmm/workflows`, and CLAUDE.md must pre-exist for section-sync. The owner asked to make onboarding say-it-and-it-works, and to self-address any hurdles a future agent would hit.

**Scope.** Fork: new `onboard-project.sh`, `sync-bmad-workflows.sh` SKIP message (message-only), new `custom/MIGRATION-v6.8-skills-plan.md`. Outside the repo (live, not git-tracked): `~/.claude/skills/bmad-onboard/`, global `~/.claude/CLAUDE.md` bootstrap section, and a rewritten `~/.bmad-reference` header warning against pointing it at a `bmad-cli install`. Self-review hardening applied before commit: contamination guard (clean config regen, not sed-patch), skills-layout-reference guard, sidecar/stamp clearing. Tested end-to-end on throwaway dirs with 0 contamination.

**Delivery.** Pushed `myfork/custom` → `fc456214`. **No project sync owed** — this is fork-local tooling, not distributed workflow text. Follow-up triage: (1) when the v6.8 skills migration lands, retarget the script + `bmad-onboard` skill to skills-native and drop the reference-clone path; (2) the `bmad-onboard` skill and global CLAUDE.md edits are NOT in any git repo (`~/.claude` is untracked) — they have no backup/rollback beyond the filesystem.

### 2026-06-19 — quick-dev: existing-code provenance check before modifying purposeful code (`6f96a335`)

**What.** quick-dev gained a "don't delete what you don't understand" discipline. New gated PRE-FLIGHT in step-03-execute: before modifying/removing any existing line, trace its provenance (`git log -S` / `git blame` → read the originating commit), classify deliberate vs incidental, and for deliberate code state in one sentence what case it protects and confirm the change *extends* that intent. §4 branches on autonomy: non-autonomous halts+surfaces if the change would undo a guard; autonomous proceeds only with a passing test covering the protected case (else halts) and logs the originating commit. Anchored by a workflow.md CRITICAL RULE and backstopped by a step-04 §6 checkbox + failure mode.

**Why.** Live bison-ops session: an implementer was about to remove a scraper gate (`allCardUrls.length <= 1`) as an "obvious simplification." The owner asked to check git history first; `git log -S` showed it was a deliberate guard from a prior PR (keep delivered multi-parcel orders on a separate path). That forced a deeper read which proved the removal safe only because a downstream guard still covered the original case — and the owner asked to institutionalize the "moment of reflection." The failure being closed: deleting load-bearing guard code that reads as redundant, silently re-opening the bug it fixed.

**Scope.** 3 files in `custom/workflows/implement/quick-dev/`: workflow.md (1 CRITICAL RULE), step-03-execute.md (+40 lines, new PRE-FLIGHT), step-04-self-check.md (§6 checkbox + FAILURE MODE). Three-point wiring deliberately mirrors how Regression Surface is handled (top rule + point-of-use gate + self-check backstop). Mode 1 self-review: 0 blocking — no brief consume/produce (provenance contract N/A), autonomy scoped, worktree-safe (uses git), context-budget clean (step-03 289/350, not dense). markdownlint 0; validate:budget OK; full fork suite green.

**Delivery.** Pushed `myfork/custom` → `6f96a335`. **Sync owed (14/14).** Follow-up triage: the same provenance check is a candidate for `dev-story` and `design-implement` (both modify existing code) — quick-dev only today; surfaced, not auto-spread.

### 2026-06-18 — design-ingest: cope with a superseded handoff — tolerant supersede-awareness (`354be3f9`)

**What.** `design-ingest` and `design-implement` gained supersede-awareness; previously neither had any (they're downstream of the brief consumers, so brief-revision-policy §5's refuse contract never applied to them). `design-ingest` step-01 now derives `{target_slug}` and resolves `{handoff_supersede_status}` (active | superseded | no_brief | ambiguous) by matching the slug against briefs in `implementation_artifacts`, then stamps `ingest.supersede_status`/`superseded_by`/`source_brief` into the manifest; step-03 leads the pause with the heads-up. `design-implement` reads the stamp at intake.

**Why.** Owner: "give design-implement a handoff that may have already been done, but it's been superseded… it does ingest, cope with supersede." The failure being closed: handing the old handoff in either hit a bare "already applied (N/N)" no-op that hid the existence of a newer version, or — worse — applied stale deltas that regress the surface back to the superseded design, silently.

**Scope.** 7 files: design-ingest workflow.md (state vars + Critical Rule) + step-01 (resolve) + step-03 (stamp + pause) + manifest-schema (receipt fields + Supersede-stamp section); design-implement workflow.md (Input Resolution branch + state vars) + checklist; brief-revision-policy.md (§6 amend + new §8). Design call: **tolerate, don't refuse** — ingest is non-destructive so it stamps + reports; the only hard gate is `design-implement`'s apply-time confirmation on a superseded-with-deltas run (applying = INTENT autonomy). diff-the-two parked as a future workflow; no_brief honest-limit on raw-URL runs documented.

**Follow-on (`d848e916`).** Extended `design-implement` to cope on **direct URL/bundle runs** too (the original wave only handled the stamped ingest_manifest path). step-01 §SHARED.1a resolves supersede independently on those paths — slug from the frame inventory, matched against briefs, same contract as ingest step-01 §5 — and on `superseded` SURFACES + HALTS before the apply pipeline (a direct run has no prior dispositions, so it's effectively the with-deltas case). All three input paths now cope. Policy §8 design-implement bullet broadened to "copes on every input path."

**Delivery.** markdownlint 0; fork test suite green (pre-push); pushed `myfork/custom`; synced 14/14 (re-sync owed for `d848e916`).

**Self-review (Mode 1 on own draft):** 0 blocking, 0 concerns. Autonomy scoping is the load-bearing call and it's correct (apply-superseded = intent → HALT even in autonomous mode); backward-compat default (absent stamp ⇒ no_brief) matches the policy's absent-field posture.

### 2026-06-17 — design-implement: drilled-frame COMPOSITION axis (§2d-bis) — the drawer analog of page-shell (`d0c4e368`)

**What.** New §2d-bis Frame-composition grid row for every drilled frame (detail/create/§13-lookup), plus step-02b recomposition surfacing, new state var `{frame_composition_deltas}`, Critical Rule, step-04 success-metric + failure-mode, checklist item. **Why.** The owner showed two live screenshots (Claude Design supply-order drawer vs the built drawer) and asked "how does it not notice the visual deltas." Root cause `contract-dimension-gap`: the component sweep compares each section's inner pixels and §2f-bis certifies each section *exists*, but NEITHER compares how a drilled frame is **composed** — section order, grouping under named headings, and the frame's own header/footer chrome. So a drawer can ship with every inner component "matching" while it's wholly recomposed: design `Cost & sourcing` / standalone `Lifecycle` / `Related records` → impl `Economics` / status folded into the header / combined `Routing & source`, plus a black-vs-blue footer button. This is the page-shell blind spot (PR #2017) one level deeper, inside the drawer — and the frame's footer is the sharpest edge (not a cataloged section → no grid row at all). Compounded by a reporting flaw: the run halts at step-02b (capability preflight, which by construction never looks at pixels) and squashed the whole recompose into one line "#8 drawer · DEEPENED/treatment · buildable: yes" — so the owner, reading that phase, correctly saw nothing acknowledging the visual magnitude. **Scope.** step-02b §3 flags every materially-recomposed present frame into `{frame_composition_deltas}` and names it in the preflight (so a scope halt shows the magnitude, not a throwaway treatment line); step-03 §2d-bis emits a Frame-composition row per drilled frame comparing section order + group naming + header/footer chrome, Tier-1 on divergence, counted in `{delta_count}`, the footer explicitly in scope; workflow.md Critical Rule + state var; step-04 metric + failure mode; checklist item. Add-not-cede — the design composition is in the manifest section inventory (render order + group titles + chrome) and the impl composition is in the frame map step-02 already builds. **Delivery.** Pushed `myfork/custom`; budget linter green (hard gate OK). Mode 1 self-review: 0 blocking — no brief consumption (provenance N/A); add-not-cede (verifiable from evidence in hand, like page-shell); autonomy scoped (it flags + builds the handoff's composition, never infers intent); section-existence stays §2f-bis (no double-count). **Sync owed (13/13).**

### 2026-06-17 — design-implement: resumable + checkpointed apply for large ingest manifests (`0fefe11c`)

**What.** design-implement's APPLY half (step-04) made resumable and self-checkpointing on an `ingest_manifest` run, with an optional `{frame_scope}` slice override. New state vars `{frame_scope}` / `{resume_prior_dispositions}` / `{run_completion_mode}`; new Critical Rule "Resumable apply on an ingest manifest"; step-04 §5a (persist-as-you-go + frame-boundary checkpoint), RULES note, §9 checkpoint/resume report branch, success metric + 2 failure modes; step-03 §2f-bis resume/scope budget note (skip delta-compute for already-applied/out-of-scope rows); step-01 manifest gating resume-read; checklist Application item. **Why.** The owner asked whether the apply of a large reviewed manifest (the real case: a 13-frame / 48-section supply-orders blocked-stock manifest) is smart to hand off "in one shot," and asked to *add* the ability for the agent to **close out when context is too tight and recommend a fresh session**. The prior wave (`293dfcc9`) fixed the INGEST half's context overflow via per-frame fan-out + a durable manifest; this is the symmetric fix for the APPLY half — a single-window walk over 48 sections hits the compaction trap (`context-budget-overflow`) and silently marks rows applied that were never verified. **Scope.** The manifest IS the durable progress ledger (its grid scaffold carries per-row status), so the fix is: apply frame-by-frame, persist each frame's dispositions back into the manifest at its boundary (before any auto-summarization), auto-resume by skipping `✓ applied` rows so re-invoking the *same command* in a fresh session continues, and self-checkpoint at a frame boundary (soft budget ~one heavy frame or ~10–12 sections) — a clean terminal exit that still delivers the slice (commit→PR→merge + force-add the updated manifest) and prints the resume command, NOT a wait-for-input halt. "One-shot" ergonomics preserved (re-run the same command); the agent slices itself. URL/bundle runs unaffected (single-pass as before). This is the context-budget decision-rule's "long sequential build → per-phase fresh context + durable manifest" remedy applied in-tool. **Delivery.** Pushed `myfork/custom`; budget linter green (hard gate OK; step-04 carries a pre-existing soft over-budget warning, additions are gated `ingest_manifest`-only). Mode 1 self-review: 0 blocking — no brief consumption (provenance N/A); grounding gate satisfied (input is the manifest path + optional scope); autonomy scoped (checkpoint is a decision-autonomy stop, frame_scope is explicit user input, default is non-destructive deliver-what's-built); worktree-safe (manifest force-added so a fresh worktree resumes). **Sync owed (13/13).** Follow-up: design-ingest's review-pause handoff still says "run it straight off this manifest" (one-shot framing) — update it to mention auto-checkpoint/resume so the two halves agree.

### 2026-06-17 — context-budget linter: deterministic pre-commit enforcement (`b4473429`)

**What.** New `tools/validate-context-budget.js`, wired into `npm test` (which `.husky/pre-commit` runs) + `quality`. **Why.** The owner asked whether the budget/step-3b discipline can actually be *enforced* — a hook/method — rather than living as instructions an agent can skip. **Finding (the corpus tension).** A hard gate at the real per-step budget (~300 lines) is impossible without a corpus refactor: existing steps routinely run 400–975 lines (design-synthesize step-06 = 975, ~18k tokens), and the 5 active sessions write in that style — gating it would block everyone. **Scope.** So the linter splits enforcement: HARD (exit 1, near-zero false-positive) on a RUNAWAY CEILING (any step over 1100 lines / 95KB — above the current max, catches a future mega-step, grandfathers nothing) and a STEP-3B GATE-GUARD (create-workflow must keep `step-03b-review.md` and route to it — the adversarial gate cannot be silently deleted); WARN only on real-budget (350 lines / 28KB) + must-do density (proxy), with `--strict` to escalate once the corpus is shrunk. Judgment checks (buried constraint, one-job, did-the-review-actually-run) are NOT mechanically enforceable and stay in step-03b as the adversarial instruction. Verified the full corpus passes (exit 0, 21 soft warnings) and proved both hard checks bite (`--strict` → exit 1; simulated step-3b removal → fail) BEFORE wiring, so the gate doesn't break active sessions. **Delivery.** Fork-internal (`tools/` + `package.json`), so NOT synced — the fork commit boundary is the complete authoring locus. Pushed `myfork/custom` → `b4473429`; full `npm test` green (caught + fixed eslint `unicorn/explicit-length-check` + prettier on the new file before commit).

### 2026-06-17 — create-workflow: mandatory adversarial review gate before wiring (`5614e548`)

**What.** New `create-workflow/steps/step-03b-review.md` — a mandatory adversarial review gate inserted between build (step-03) and wire (step-04). **Why.** The owner asked whether the context-budget work was actually *proactive* — i.e. when an agent builds a workflow, does it adversarially review the result? It did not: create-workflow built and wired with only a self-verify checklist (the building agent ticking its own box), never an adversarial pass and never against the durable principles; the expert skill's Mode-1 review was reactive (loads only when triggered). **Scope.** step-03b re-reads every built file from disk under an explicit adversarial posture ("break it, don't confirm it"), attacks each step against the durable principles — context budget (overdense-step / inlined-corpus / buried-constraint / undelegated-read), grounding, autonomy scoping, provenance, greenfield-in-brownfield, structural — classifies Blocking/Concerns/Nits, fixes every Blocking finding and re-reviews until clean (≤3 loops, else flags for manual redesign), then wires. **Prefers an isolated sub-agent** for the review (independent context + practices the very context-budget discipline this enforces) with a same-context fresh re-read as fallback — deliberately **no hard dependency on the Mason-local skill**, since create-workflow is synced to 13 targets where the skill may not exist. The skill remains the canonical rubric. step-03 repoints to 3b ("built-but-unreviewed is never wired"); workflow.md declares the gate + `{wf_review_verdict}`; step-04 surfaces the verdict in its completion report; SKILL.md Mode 2 notes the workflow now bakes the gate in. **Delivery.** Pushed `myfork/custom` → `5614e548`; pre-commit full test suite green; synced 13/13. This is the "make it proactive" follow-on to `e8a9cf93` below.

### 2026-06-17 — context-budget durable principle: fork expert made context-aware + decomposition checks (`e8a9cf93` + skill v1.6)

**What.** A new durable principle — **context budget & workflow decomposition** — added to the fork expert and wired into the authoring/audit meta-workflows. New skill reference `references/context-budget.md`; SKILL.md → v1.6 (v1.5 rolled to `versions/`); fork commit `e8a9cf93` touching create-workflow/step-03-build + orchestrate-workflows/step-02-health-check. **Why.** The owner observed that long / instruction-dense workflows hit "context fatigue" — the model compresses context and drops detail — and asked that the expert understand this and be able to recommend delegating sub-workflows. Three parallel research agents established the precise basis: **context rot** (length-driven monotonic decline before the window fills; Chroma 2025), **curse of instructions** (reliability slips past ~10 simultaneous constraints; IFScale), **lost-in-the-middle** (position; Liu et al.), **effective window ≈ 50–65%** of advertised (NVIDIA RULER), and the **compaction trap** (LLM self-summarization drops exact figures / edge-cases / constraint wording first — the dropped exceptions ARE the spec). The fork already does the right things by instinct (orchestrator + `steps/` = progressive disclosure; handoff artifacts = externalized state); the gap was sub-agent fan-out and an explicit budget. **Scope.** `references/context-budget.md` states the phenomenon, the budget (one job/step, ≤~10 must-dos, constraints at top + point of use, pointer-over-inline, effective-window discipline), and the decomposition decision-rule keyed on read-heavy/parallelizable (→ sub-agent fan-out returning a distilled artifact) vs. long-sequential-build (→ per-phase fresh context + durable manifest) vs. write-one-coherent-artifact (→ single-threaded), reconciling Anthropic's multi-agent research win with Cognition's *Don't Build Multi-Agents*, plus the non-negotiable handoff contract. SKILL.md weaves it into Mode 1 (Review check), Mode 2 (Author decision-rule at design time), and adds Mode-3 root-cause class `context-budget-overflow`. create-workflow/step-03-build now builds within budget (RULES + step-content guidelines + verify item + failure mode); orchestrate-workflows/step-02-health-check gains a Context Budget audit section with finding categories. **Delivery.** Pushed `myfork/custom` → `e8a9cf93`; pre-commit full test suite green. Skills are not synced (SKILL.md live immediately); the two fork workflow edits are **Sync owed (13/13)**. Mode 1 self-review of the fork edits: 0 blocking — no brief consumption (provenance N/A), additive checks only, house-style verify/failure-mode shape matching peer workflows.

### 2026-06-17 — design-ingest: new workflow + ingest_manifest input_kind + §2f-bis section-coverage gate (`293dfcc9`)

**What.** New `implement/design-ingest/` workflow (5 files: workflow.md, manifest-schema.md, 3 steps) + 3 design-implement patches (workflow.md input_kind/state-vars/Input-Resolution; step-01 MANIFEST PATH; step-03 §2f-bis Section-coverage gate). **Why.** Running design-implement on a ~140KB JSX bundle hit two failures: the whole bundle overflowed one ingest context (so the exhaustive per-component catalog got shortcut), and delegating ingest by feature-area with narrow prompts let a whole SECTION inside a present frame (the Reconciliation block; also SellerSmart dispatch) go unenumerated — §2f frame-coverage is frame-granular and greens out on a missing section whose inner primitives match elsewhere. Root class `contract-dimension-gap` (section axis missing beneath a present frame) plus a context-budget cause. **Scope.** design-ingest fans out one isolated agent **per frame** (fan-out unit = frame, never feature-area), enumerates each frame's complete top-level section list under a FRAME-COMPLETENESS GATE (empty list on a drawn frame → halt), and emits a durable, reviewable manifest with a pre-seeded grid scaffold (every (frame,section) = a row), then PAUSES. design-implement consumes it via `input_kind: ingest_manifest` (skips re-ingest; refusal gate on an incomplete manifest) and §2f-bis makes every section a mandatory grid row (Tier-1 when a present frame lacks one) — denominator from the gated scaffold or enumerated in-context on URL/bundle runs. Same named-gate shape as page-shell §2d and frame-coverage §2f. **Delivery.** Pushed `myfork/custom` → `293dfcc9`; pre-commit suite green; sync owed (13/13). Mode 1 self-review: 0 blocking; follow-ups (e) brief-provenance propagation when source is a synthesize_bundle, (f) checklist/template section-coverage checkbox.

### 2026-06-17 — design-implement: authoritative §13-lookup denominator — the rendered Linked-records list (`4910a3e1`)

inbound-flow session: owner asked "does design-implement ledger the implementation of the link-to-record drawers? orders.html contains [6] link-to drawers that ALL need checking against Claude Design (source of truth) and the workflow often misses these," with a live supply-order detail drawer screenshot showing a "Linked records" section — `Catalog item · Route warehouse · Shipping lane · Supply source · Inbound batch · Import run`. Diagnosis (Mode 3): **root class `contract-dimension-gap`** — the §2f frame-coverage axis exists (added `56d44fc9` for exactly these Orders.html lookups) but its denominator is harvested ONLY from bundle *self-declarations* (URL.3a sources 1–4: `<script src>` comments, per-frame banners, lookup→target maps). Those are declarations — they under-enumerate, are imprecise, or describe a different conditional state than the one rendered — so a lookup the comments forgot to list never enters `{design_frame_inventory}`, and §2f cannot flag a frame it never knew existed. The proof was in the screenshot: the live drawer renders `Shipping lane`, which isn't in the workflow's own harvest example (that lists `accounting-outcome` instead) — drift the workflow had no mechanism to notice, because it never counted the parent drawer's rendered linked-record rows. Fix is **add-not-cede** (the rendered "Linked records" section is evidence step-01 already opens the detail drawer to read): URL.3a gains **source 5** — capture `{design_linked_record_rows}` (one entry per rendered RecordLink row), the AUTHORITATIVE one-row-per-lookup denominator, and reconcile every row against a `role: §13-lookup` frame in `{design_frame_inventory}` (a row with no harvested frame → re-trace, else add `LOOKUP UNDER-ENUMERATED`, never silently drop; harvested §13-lookup count ≥ rendered row count). step-03 §2f runs the reconciliation BEFORE emitting Frame-coverage rows and is **denominator-source-agnostic** (brief/URL/manifest all reconcile against the rendered list, because the list is what the surface actually renders, not what a declaration claims); an under-enumerated row becomes a routed `LOOKUP UNDER-ENUMERATED` Frame-coverage row; and a drawer "present in both" is now swept for **depth** so a thin `code — name` stub against a rich bundle drawer surfaces as `MISSING in impl` rows (`PRESENT BUT THIN` Tier-1) rather than greening on "the drawer opens." step-04 §9 Frame-coverage disclosure leads with the linked-records denominator and enumerates under-enumerated + present-but-thin; §6 summary + §2f SUCCESS METRIC + 2 FAILURE MODES; workflow.md `{design_linked_record_rows}` state var + the existing frame Critical Rule extended; checklist +2 items. Mode 1 self-review: 0 blocking — add-not-cede (rendered evidence in hand), no double-counting (a thin drawer's interior gaps ride the normal grid as MISSING-component deltas; the frame carries a PRESENT-BUT-THIN flag), not over-engineering (a denominator-source correction + reuse of the existing §2 sweep, not a new subsystem). Pushed `myfork/custom` → `4910a3e1`; pre-commit full test suite green; synced 13/13. Skill → `4910a3e1`.

### 2026-06-17 — design-implement: step-02b capability delta made SYMMETRIC — catch the uplift, not just the regression (`f2d70fd1`)

inbound-flow session: owner reported design-implement is "awful right now — running blind," with two screenshots — the live prod `/orders` (a flat severity-sorted orders table with country-filter chips + a reconciliation/EOS column) vs the design handoff for Supply orders (lane-by-handler segmentation `EU→UK Bexley Direct` / `DE→UK ATS AVASK` / `US→UK Kata` / `UK domestic SellerSmart` with per-lane counts + capital, an analytics/disposition band `Blocked / Intentional / Recv gap / EOS soon / Awaiting / Clean`, a per-row disposition action column, and Ledger/Staged/PO/Blocked-stock/SellerSmart-push co-view tabs). A prior run had concluded "Design side is now fully cataloged … the picture so far points at treatment/token alignment rather than net-new capability, with no capability-regression risk since production is a structural superset" — *while the mapping agent was still resolving production file paths*. Diagnosis (Mode 3): **root class `contract-dimension-gap`**, and the precise mirror of every prior design-implement patch. step-02b (the regression preflight, `71656fb1`) is built entirely around the **DROPPED** axis (production ∖ handoff → halt for intent); §3 computed **ADDED** but labelled it "informational," and **CHANGED** as "note it; it flows through the normal grid." So a redesign whose *entire point* is a capability UPLIFT drops nothing → empty DROP set → §4 "proceeding additively, additive and replacement are equivalent, no halt" → and with no machinery foregrounding the add, the agent free-associated to the cheapest signal it had (raw-hex → canonical-token debt) and emitted a backwards *scope* verdict from *treatment* evidence. The DROP axis caught "redesign removes a capability"; nothing caught "redesign adds a capability the live page lacks" — silently ADDED-but-unbuilt, the inverse of the page-shell/frame-coverage/content-lane "silently ABSENT" misses. Fix is **add-not-cede** (both inventories are already gathered in §1/§2, so the ADD axis is verifiable from evidence in hand, like page-shell §2d): step-02b retitled *Capability-Delta Preflight*; §3 promotes ADDED + DEEPENED to first-class `{added_capabilities}`/`{deepened_capabilities}` → `{uplift_capabilities}` and narrows CHANGED to treatment-only; §4b names a non-empty uplift and sets strategy ≥ `additive` (no halt — implementing the handoff is decision autonomy, unlike a drop which is intent); §4's empty-DROP branch hands off to §4b instead of auto-concluding restyle. Two new gates: a **running-blind gate** (workflow.md Critical Rule + step-02b MANDATORY + SUCCESS/FAILURE) forbidding "restyle-only / structural superset / no-net-new" as a prior or from treatment evidence — the verdict is an OUTPUT of the §3 bidirectional delta, asserted only after both surfaces are inventoried; and a **symmetric-capability Critical Rule** in workflow.md (the ADD twin of the existing regression rule). Downstream wiring: step-03 §2h tags uplift sub-structure `capability-build` (not collapsed to "MISSING component → restyle"); step-04 §1 fix-type "Capability build" + §5 ledger rule (a `capability-build` row is `applied (built)`, never `deferred(out-of-scope)`) + a new §9 "Capabilities built (uplift)" disclosure mirroring "Capabilities removed (orphaned actions)" (every added/deepened capability confirmed built or flagged Tier-1 incomplete) + matching SUCCESS METRIC + FAILURE MODE; workflow.md +4 state vars + architecture line. Mode 1 self-review: 0 blocking — DROP halt preserved, ADD path correctly does NOT halt (decision autonomy), autonomous-mode behavior stated for both directions, not over-engineering (symmetry with machinery that already exists, no soak/drift-flag). Pushed `myfork/custom` → `f2d70fd1`; pre-commit full test suite green; synced 13/13. Skill → `f2d70fd1`.

### 2026-06-17 — design-implement: transcription fidelity + render-compare done-gate (copy & chrome) (`e91bccd3`)

inbound-flow session, after shipping a cost-reconciliation drawer (PR #2279) that the owner then A/B'd against the handoff PNG: the impl read visibly worse than the design despite a green grid. Mode 3 → Mode 2. **Root cause class: `contract-dimension-gap`** — the comparison grid is component × state × property (CSS cells), so it has NO row for **copy** or for **frame chrome**; both fell through and the agent *interpreted where it was told to transcribe* (substituted a stock `SheetHeader` for the design's `‹ Back to Supply order` breadcrumbed header, relabeled "Open full cost record" → "Open full order", paraphrased "deferred import" → "import", swapped "EUR → GBP" → "€ → £"). Every CSS cell matched, so "all green" *manufactured* false confidence. The owner's framing drove the fix shape: NOT new per-string grid axes (that's scaffolding compensating for not following the existing "source is truth" rule) — instead (1) name the prohibited move in workflow.md Critical Rules ("you are TRANSCRIBING, not designing — covers copy & chrome"; the grid is necessary-NOT-sufficient; a green grid is not "done"; split "don't screenshot" into ingest-reads-values vs verification-renders-to-compare), and (2) one real check at the right altitude in step-04 **§5b**: a transcription pass (chrome + copy → verbatim OR forced-and-logged deviation, riding the *existing* ledger → existing §9 list, no new report section) + the render-compare done-gate (built surface beside the design render; owed-and-routed to `verify`/design-review when unrenderable). Add-not-cede because copy & chrome are verifiable from the design source — a static source-vs-source string diff catches them with no render (the floor; render-compare is the ideal). Matching SUCCESS METRIC + FAILURE MODE echoes; 2 checklist lines. Mode 1 self-review: 0 blocking; tightens autonomy scoping (paraphrasing copy is *intent* autonomy, now explicitly unlicensed). Pushed `myfork/custom` → `e91bccd3`; pre-commit suite green; synced 13/13. Skill → `e91bccd3`.

### 2026-06-17 — design-implement: regression-surface preflight (step-02b) — handoff-vs-production capability delta + strategy gate (`71656fb1`)

inbound-flow session: owner asked for "a new way of entering" design-implement when handed a handoff URL — Claude should proactively "check what was changed from this handoff relative to production," because the new handoff "may have dropped (which of the five to restore — routing, linked records, full economics/cost-recon, dual-status header, activity timeline) relative to production," and offer the user an implementation choice (additive / partially additive / replacement-in-styling). Mode 2. The need is real and recurring: design-implement treats the handoff as the spec and would faithfully reproduce a redesign's *omissions* — and the component sweep greens out on a dropped whole-capability because its inner primitives (pills, money cells, RecordLinks) exist elsewhere in the impl. There was a post-hoc backstop (step-04 §9 "Capabilities removed (orphaned actions)") but it fires mechanically AFTER the apply on now-zero-caller actions; the owner wants it PROACTIVE, at intake, capability-level, with a strategy choice. Built a new **step-02b-regression-surface** between map (step-02) and grid (step-03): §1 inventories `{production_capabilities}` (feature-level — routing/sub-surfaces, §13 linked records, economics/cost-recon, composite status/header, activity/audit, bulk actions/filters, wired mutations — from step-02's outputs), §2 inventories `{handoff_capabilities}` (`{design_components}` + `{design_frame_inventory}` + brief §7), §3 computes `{dropped_capabilities}` (prod minus handoff). Non-empty → **HALT** with a regression report + a 4-way strategy menu (`restyle-only` keep-all · `additive` · `partial` per-capability · `replacement`), recommendation leading non-destructive. The keep/drop decision is *intent* → halt-by-default; autonomous mode defaults to restyle-only and discloses, never silently replaces (intent autonomy out of scope). `{capability_dispositions}` then constrains downstream: step-03 §2h flags `capability-protected` rows (a grid row that would delete a kept capability), step-04 disposes them `deferred(capability-protected)` and the §9 report states the chosen strategy + every kept/dropped capability, with dropped capabilities routed to the existing §9 orphaned-action confirmation. Guards: skips cleanly on a brand-new surface (no production to regress); does NOT mis-score an undrawn-but-promised frame (brief §7 / `{design_frame_inventory}`) as dropped — ties into the recent §2f frame-coverage work to avoid a false-positive against itself. step-01 now announces the check up front (the proactive "I'm going to check what changed vs production" UX the owner described). Wiring: step-02 COMPLETION → step-02b → step-03; workflow.md +5 state vars + a Critical Rule + the sequence line; checklist Step-2b section. Mode 1 self-review: 0 blocking. Pushed `myfork/custom` → `71656fb1`; pre-push suite green; synced 13/13. Skill bumped → `71656fb1`. **Refinement (`80ad60da`, same session):** owner asked the gate "lean into advisory mode, as opposed to 'can the user list this'." §4 reworked from a bare strategy menu (whose `partial` made the user assemble the keep/drop list) to an **advisory**: a per-capability KEEP / SAFE-TO-DROP verdict with grounded reasons + one recommended plan presented as a decision to approve-or-adjust ("I'll apply this unless you'd rather change it"). §5 records `partial` as the AI-proposed advised mix (AI-authored, user-confirmed); new FAILURE MODE bans offloading the keep/drop analysis to the user; step-01 announce + workflow.md Critical Rule aligned. Validated by a simulated live run on the user's `Supply Order Detail - Blocked-stock build` handoff — production drawer (`supply-orders-drawer.tsx`, 1998 lines) inventoried 11 capability groups (dual-status header · §15 economics/cost-recon · 8 §13 linked-record drawers · activity timeline · routing + §7 raw-exchange audit · …); the owner's anticipated dropped-five mapped cleanly to the report. (WebFetch couldn't pull the >10MB bundle — real step-01 uses curl; the sim grounded the handoff side on the frame name + owner's stated scope.) Synced 13/13. Skill → `80ad60da`.

### 2026-06-17 — design-implement: page-shell §2d catches ancestor-injected chrome (hero/banner) (`7f8af1ca`, inbound-flow PR #2275)

inbound-flow session: owner pasted two screenshots — the live prod `/settings/sku-format` (a tall photographic hero banner "Settings · Configure workspace preferences and account security" above the worklist) vs the Claude Design handoff (no hero, straight to the "SKU format" heading) — "design implement is sloppy!" The prior design-implement run had declared the page shipped-and-matching. Diagnosis (Mode 3): root class `contract-dimension-gap`, the page-shell axis (§2d, added `1febc8ce`) modeled the container's *dimensions* (width/centering/padding) but not its *chrome*. The hero is injected by `settings/layout.tsx` (`<InboundHeroBanner>`) — an ancestor layout, above `{children}` — so (a) the component sweep is blind (the bundle renders the frame standalone → no hero in `{design_components}` to diff), and (b) §2d didn't look for it, so every component row greened and a §5 hard failure ("Hero strips… above working tables") shipped. Settled with the owner that this is **add-not-cede**, not the §2e policy cede: the band is visible in the *same layout files the §1a wrapper-chain walk already reads* (the walk that finds `max-w-[1440px]` also sees `<InboundHeroBanner>`), so design-implement can name it directly. Fix: step-02 §1a gains a 4th capture point → `{impl_page_shell}.injected_chrome` (hero/banner/masthead/promo a wrapper renders above children, each `in_design_frame: bool`); step-03 §2d emits an injected-chrome page-shell row, Tier-1 when absent from the design frame (design frame authoritative; "absent from the bundle" is explicitly NOT license to keep an injected hero); checklist + workflow.md state var updated. No new §9 section — it flows through the normal Tier-1 delta ledger. Discovery during the fix: `InboundHeroBanner` is on ~22 pages app-wide (settings + ~21 operational pages) — a systemic §5 divergence; only the settings layout was fixed (one edit clears all four settings sub-pages and matches the design), the rest flagged as a deliberate app-identity decision, NOT swept. Live fix delivered: inbound-flow **PR #2275** (worktree `fix/settings-hero-banner`, tsc + 6050 tests + build green, squash-merged → Railway auto-deploy). Mode 1 self-review of the §2d add: 0 blocking. Pushed `myfork/custom` → `7f8af1ca`; pre-push suite green; synced 13/13. Skill bumped → `7f8af1ca`. Follow-ups: app-wide hero decision (the ~21 other pages); the stranded `_bmad` design-lane sync owes a doc-only commit to inbound-flow main so worktree runs see §2g/§2d.

### 2026-06-17 — design-implement: token-provenance disclose-and-cede (per-screen vs canonical tokens) (`b4ad51cb`)

inbound-flow session: owner pasted a live `design-impl` run (cost-recon worktree) that asserted "Tokens map ~1:1," hit a counterexample (`--status-success-text` / `--status-warning-text` / `--subtle-foreground` absent from `tokens.css`), then *resolved* it by finding `--status-*-text` defined in `supply-orders.css` lines 34–50 and concluding "the bundle was generated from this exact CSS, so the mapping is effectively 1:1." Owner pushed back on whether the "canonical-only" restriction is even good. Worked it through: canonical-only is a good design-*system* north star (a status colour is a shared concept; the v2 hue-160→148 retune repainted every surface from one `tokens.css` edit) but a **bad hard gate** inside design-implement — (a) it's not design-implement's lane (the token resolves fine at runtime; promote-to-canonical is token *architecture*, design-review's call — same cede-vs-add boundary the §2e policy-cede settled), and (b) "per-screen = debt" is too coarse: a shared-semantic token (status/colour/type) in a per-screen file is debt, a local layout constant is fine. So the fix is **disclose-and-cede, never gate**, a sibling of the content-lane (§2c) and policy-conformance (§2e) cedes. Root class `contract-dimension-gap`: the token comparison was missing the *provenance* axis (it checked existence, not which layer it resolved from). step-02 §5 now resolves the value AND tags `{impl_token_provenance}` (`scope: canonical|per-screen` against `docs/design-policy.md` §8's `tokens.css`+`@theme` surface; `semantic_class: shared-semantic|local-constant`) + "resolve every token before any 1:1 claim" (the snippet's literal bug — `--subtle-foreground` was folded into 1:1 while still unresolved); step-03 §2g emits one `NON-CANONICAL TOKEN` disclosure row per shared-semantic per-screen token (not a delta, not in `{delta_count}`, not gated; `local-constant` per-screen tokens NOT flagged to avoid disclosure spam); step-04 §9 gains a mandatory "Token provenance (non-canonical)" section + SUCCESS/FAILURE rows; checklist + workflow.md state-var registry (`{token_noncanonical_count}`). Mode 1 self-review: 0 blocking (legitimate add-where-verifiable + cede-where-unowned; the provenance grep is evidence step-02 §5 already gathers). Pushed `myfork/custom` → `b4ad51cb`; pre-push suite green; synced 13/13. Skill bumped → `b4ad51cb`. Note: the cost-recon worktree session is running the pre-§2g copy — restart it to pick up the disclose-and-cede.

### 2026-06-17 — create-design-policy: Bison product-family policy overlay + `inherits:` mechanism (`3141456f`)

inbound-flow session: owner asked to "template the bulk of the design policy for a reusable policy for new projects" and explicitly "decipher what's unique to project and what is template." Mode 5/2 (explain → author). The key finding reframed the task: a blank greenfield template already existed (`create-design-policy/design-policy-template.md`, 16 placeholder sections) and `shared/design-standards.md` already held the universal AI-fingerprint substance — so the *missing* layer was the **product-family** tier, the exact "bulk" the owner keeps re-porting by hand (inbound-flow v11 was itself a verbatim port of accounting-tools v11). The decipher is **three** tiers, not two, and inbound-flow's own change_log already authored the answer: every entry tags `Propagation (§9)` as "shared product-family value → propagate to accounting-tools" vs "inbound-flow-only, do NOT propagate." Read off those tags: **universal** (anti-AI fingerprints, table-first, status mechanism) → `design-standards.md`; **product-family** (corporate register, status system + 4-colour hierarchy, monochrome-primary/Inter/13px, money/VAT + 2dp + derived-ROI proof, relational expand-in-context linking, API + comingled per-source provenance, sorting/chronology, positive-assertion floor) → the new overlay; **project-unique** (multi-handler warehouse-handler topology, product imagery, Amazon/FBA exemplar content, concrete tokens/routes) → stays in each project policy. Built `shared/bison-product-family-policy.md` (§A–§L + §Z) by lifting the propagate-tagged prose in source-agnostic form, with §Z enumerating the tier-3 residue so a new project knows exactly what it must still author. Wired inheritance without breaking the workflow's project-agnostic contract: template `inherits:` frontmatter + family-overlay precedence tier; `workflow.md` inherit-don't-re-port CRITICAL RULE + `{family_overlay}` state var; `step-04 §2b` residue-only authoring path. Overlay lives in `shared/` (distributes everywhere) but is inert unless a project declares `inherits: bison-product-family-policy`. Mode 1 self-review of the overlay: 0 blocking; 1 caught + fixed pre-commit (§I positive-assertion YAML carried stale inbound-flow section numbers §L/§M and pointed money's relational link at §K instead of §J — corrected to the overlay's own A–L scheme). Pushed `myfork/custom` → `3141456f`; pre-push full test suite green. Skill bumped → `3141456f`. **Sync owed (13/13).** Follow-up: a deliberate per-project adopt-the-overlay rewrite for inbound-flow + accounting-tools (convert to `inherits:` + residue-only) — not auto-done, since it's a large destructive rewrite of two live policies.

### 2026-06-12 — design-implement: §2f frame-coverage gets a URL-path denominator (the bundle's own declared lookup frames) (`56d44fc9`)

inbound-flow session: owner reported "design-implement is no longer catching all deltas when I hand over a bundle and run the flow — every time it fails to catch the detail pages that are there in the 'link to records (lookups)'", with a live Claude Design URL (`open_file=Orders.html`). Diagnosis (Mode 3), grounded by fetching the actual bundle: `Orders.html` consumes five §13 expand-in-context lookup frames inside the Supply Order Detail Drawer — `warehouse-lookup` / `inbound-batch-lookup` / `import-run-lookup` / `accounting-outcome-lookup` (supply-order/lookups.jsx banners) plus `catalog` + `supply-source` (catalog-record/) — declared in the bundle's own HTML comments ("… lookups consumed"), jsx per-frame banners, and the `app.jsx` lookup→target map. Root class `contract-dimension-gap` (URL-path twin of the §2f axis): §2f's two contract sources are the **brief §7** and the **synthesize-bundle manifest** — and a raw Claude Design URL run has NEITHER. So §2f had no denominator, emitted zero Frame-coverage rows, and the lookup drawers fell out silently: their inner primitives (Pill, Money, RecordLink) are shared and match somewhere in the impl, so the component sweep greened out while the whole drawer shipped unbuilt or inferred-thin. step-04 §9's Frame-coverage disclosure was gated on "whenever the brief enumerated a §7 Surface Inventory" — no brief → section legitimately skipped → false-green. Add-not-cede (the bundle declares its frames; reading them is evidence already in hand, same posture as page-shell §2d): step-01 **URL.3a** captures `{design_frame_inventory}` (primary + drilled drawer + each §13 lookup, from traced modules/comments + per-frame banners + lookup→target maps + sibling standalone `<frame>.html`, opening any linked standalone frame into `{design_components}`); step-03 §2f resolves the contract by **three-source precedence** — brief §7 → bundle frame inventory (URL) → manifest (bundle) → needs-human-confirm — with the URL-path frames `drawn:true` so an impl lacking the drawer is `FRAME MISSING in impl` (Tier-1); step-04 §9 + SUCCESS/FAILURE + checklist generalize the disclosure gate beyond brief §7 ("no brief" is not "no contract"); workflow.md gains the state var + a Critical Rule. Mode 1 self-review: 0 blocking. Pushed `myfork/custom` → `56d44fc9`; synced 13/13 (also distributing the previously-owed `e72c1cb7` gate). Skill bumped → `56d44fc9`. Follow-up (carried): `{consumed_frames}` disposition in §2f.

### 2026-06-12 — design-implement: gate the §7 frame-coverage axis + deliver the stranded design-lane sync (`e72c1cb7`, PR #2071)

inbound-flow session: owner pasted a prior `design-implement` run on the `/orders` bundle that declared "Deltas applied: 0 — green," then pushed back with "brief's §7 Surface Inventory lists 9 frames, each a required deliverable." Per-frame cross-check found 5 of 9 frames built, 4 (inbound-batch / import / shipping-lane / comms-case lookup drawers) designed-in-the-bundle but unbuilt in impl. Diagnosis (Mode 3) had **two layers**. Layer 1 — root cause of *this* run — `worktree-sync-drift`: the §2f Frame-coverage axis (and §2d/§2e, and the §7 render/scoping waves) were synced into inbound-flow's main working tree but **never committed**; `EnterWorktree` branches from HEAD, so the worktreed run loaded the pre-§2f copy (`git show HEAD:…/step-03-build-grid.md | grep -c Frame-coverage` → 0) and was structurally blind to a whole unbuilt frame. Fix: committed the stranded design-lane sync to `main` (PR #2071, doc-only admin-merge) — §2f now in committed HEAD, worktree runs see it. Layer 2 — latent even post-sync — `silent-partial-implementation`: §2f's emission was never *enforced* (no checklist row, no mandatory §9 report section), so an agent could still sweep only the found frames and green out. Fix mirrors the existing content-lane / capabilities-removed gates: checklist gains the §2f row; step-04 §9 gains a mandatory **"Frame coverage (brief §7)"** section enumerating every promised frame as built / missing-in-impl (Tier-1) / not-drawn (routed); SUCCESS METRICS + FAILURE MODES codify "the §7 list, not the found-frame set, is the denominator for a green claim." Mode 1 self-review of the gate: 0 blocking (legitimate *add*, not a faked policy check — §2f is verifiable from evidence the workflow already holds). Pushed `myfork/custom` → `e72c1cb7`. Skill bumped → `e72c1cb7`. **`sync bmad` owed** to distribute the gate to all 13 targets. Follow-up (carried from `22406ca7`): `{consumed_frames}` disposition in §2f.

### 2026-06-12 — design-synthesize + design-handoff: draw §7 lookup-drawer frames; redirect lookup-drawer handoffs (`22406ca7`)

inbound-flow session: owner reported the small §13 "link to record" lookup drawers (a thin catalog record drawer subtitled "Catalog record for ASIN"; a supply-source lookup drawer that shipped as a hand-rolled key/value stub violating §15 — bare `€245.71` no GBP/VAT basis, inert Sell ASIN, inert gate) "getting completely missed by Claude Design," AND "whenever I call for a handoff the handoff purpose gets rejected." Diagnosis (Mode 3): the `3a88eee3` wave shipped the §7 enumeration (handoff) + the §2f verification (design-implement) but left the *render* half and the *redirect* half as explicit follow-ups — and they were never done. Root class `silent-partial-implementation`: the §7 Surface Inventory contract was complete, but **design-synthesize never consumed it** — step-01 §7 derived `{screens}` only from frontmatter `route`/`routes`, never the brief BODY's §7 table, so the enumerated lookup-drawer frames never became screens, never rendered, and design-implement §2f then inferred them thin. Symptom 2 was partly correct-by-design (a lookup drawer is owned by the relation, never its own handoff — destination-vs-relationship) but the rejection routed nowhere. Fix, two halves: **(A) render** — design-synthesize step-01 §7a populates `{screens}` from the §7 Surface Inventory (frame-name-keyed verbatim, the contract key that travels brief→`<frame>.html`→§2f), with Gate 1f halting if any §7 frame would go undrawn (silent-partial guard mirroring §2f at the render step) + `{consumed_frames}` accounting for "consume from sibling brief" rows + a pre-§7-brief announced blind-spot; step-04 composes a `drawer-over-{parent}` frame as the §13 drawer open over its parent (§15 basis-complete, §2a richness floor); workflow.md state vars document it. **(B) redirect** — design-handoff step-01 §2a detects a §13 lookup-drawer target and REDIRECTS it (not a bounce) to the matching next command — re-synthesize the parent brief (now §7-aware), material-revise the parent, or redesign the owning surface — preserving the multiple-active-brief invariant; autonomous mode does not override (intent violation). The infra was already multi-screen (step-04/05/07 loop `{screens}`), so the render fix was one missing wire, not new machinery. Mode 1 self-review: 0 blocking. Pushed `myfork/custom`; synced 13/13. Skill bumped → `22406ca7`. Follow-up: verify design-implement §2f treats a `{consumed_frames}` row as out-of-scope-for-this-bundle, not `FRAME NOT DRAWN`.

### 2026-06-12 — design-handoff: spawned drawers scoped in as required deliverable frames (`3a88eee3`)

User reported poor detail-page drawers (financial data presented sloppily, thin warehouse lookup, an `ROI`-bearing catalog drawer subtitled "Catalog record for ASIN") — "especially when the handoff specified a page and not a specific detail page." Root class `contract-dimension-gap`, 5th instance (after state / multiplicity / content-lane / page-shell): handoff enumerated the scoped PAGE but never the surfaces it spawns at runtime — the drilled **detail drawer** and the §13 expand-in-context **lookup drawers**. The pipeline is non-interpretive (Claude Design draws only §7's frames; design-implement matches only bundle frames), so an un-enumerated drawer is never drawn and then INFERRED downstream → thin, unformalised (bare `€60` with no GBP/VAT basis — a `docs/design-policy.md` §15 violation that ships because the drawer never entered the design pipeline as a frame; lookup drawers stubbed at code/type/status). The taxonomy that settled the scoping: **destination vs relationship** — lookup drawers are owned by the *relation* (§2a, never a separate handoff), the drilled drawer is an inline spawned-surface frame by default, fresh handoff only on graduation to a deep `/[id]` route. Verifiable from evidence the workflows already hold, so the fix adds real rows, not a cede: workflow.md Deliverable-Completeness Principle ("if you want it built well, it must be drawn") + `{spawned_surfaces}`; step-01 §5f derives the frames from page_mode + composition + linked_records (richness floor, depth-1); brief §7 leads with a frame-name-keyed Surface Inventory table; §2a richness floor; step-03 substitution + checklist guard; design-implement §2f Frame-coverage rows (a brief-promised frame absent from the bundle = `FRAME NOT DRAWN` → routed, not inferred). No inbound-flow policy change — §15/§13 already mandate the content quality; the gap was the drawer never being drawn. Mode 1: 0 blocking. Pushed `myfork/custom`; synced 13/13. Skill bumped → `3a88eee3`. Follow-ups: design-synthesize (terminal-native render path) should honor §7 frames; design-implement step-01-ingest could capture the brief §7 frame list explicitly rather than §2f re-reading it.

### 2026-06-12 — design-implement: the bundle is a generated proposal, the spec is the policy (`9a0a1089`)

User pulled a second real sheet (`Accounting Import v2`) and asked "is the README gened by Claude Design?" — yes. The bundle AND its README are generated *from* `docs/design-policy.md` and can violate it (that bundle shipped a banned colored-glow `@keyframes ai-pulse` + no `prefers-reduced-motion`). So the bundle is authoritative for **treatment** only, never policy conformance. **(1)** step-01 re-points `{design_layout_constraints}` to read the layout rule from `docs/design-policy.md` FIRST (authoritative); README + wrapper demoted to corroboration with an `authoritative` flag + a no-policy fallback. **(2)** new step-03 §2e + step-04 §9 + a workflow.md Critical Rule explicitly CEDE policy-conformance (prohibitions/tone/motion/iconography) + behavior wiring to design-review / design-review-pr / verify — not a faked grep. Page-shell stays a check (statically verifiable against policy + impl); the rest is ceded. Mode 1: 0 blocking, 1 nit fixed. Pushed `myfork/custom`; synced 13/13. Skill bumped → `9a0a1089`.

### 2026-06-11 — design-implement gains the page-shell axis (`1febc8ce`)

The comparison grid is component × state × property, but the page CONTAINER's width/centering is owned by the wrapper + ancestor layout (no component) and the bundle renders full-bleed standalone — so a nested `max-width:1280px` cap inside the layout's `max-w-[1440px]` rendered `/orders` narrow + centered (PR #2017) while every component CSS matched (grid all-green). Root class `contract-dimension-gap` — 4th instance after state / multiplicity / content-lane. Fix: `{design_layout_constraints}` + `{impl_page_shell}` state vars; step-01 capture (both ingest paths); step-02 §1a resolves the EFFECTIVE width after every nested cap; step-03 §2d emits one always-present Page-shell row (width/centering mismatch = Tier-1). Pushed `myfork/custom`; synced 13/13.

> Earlier waves (2026-06-10 and before) are preserved verbatim — as the original log — in [`STATUS-archive.md`](./STATUS-archive.md).

---

## Versioning

- **Upstream BMAD tracked:** v6.8.0 (`origin/main`)
- **Fork base snapshot:** v6.7.x branch point
- **Commits behind upstream:** 3 (as of 2026-06-12)
- **Commits ahead of upstream:** 258
- **Last upstream sync:** `upgrade-bmad.sh` 2026-06-01 (clean)
- **Last fork commit:** see `## Changelog` top entry (self-maintaining)

## Shipped Features

Foundational capabilities, shipped. The wave-by-wave feature history (what landed each session) lives in `## Changelog` + [`STATUS-archive.md`](./STATUS-archive.md); this is the durable capability set, not the running log.

**Provenance & safety**

- [x] Brief provenance contract — 11-field frontmatter + 6 intake checks (`401e2d57`)
- [x] design-handoff predecessor + supersession logic (brief-revision-policy)
- [x] Quick-dev grounding gate + autonomy scoping (decision vs intent) (`1979c48f`)
- [x] `project_phase` config — greenfield | brownfield | mixed (`0dd2e1ca`)

**Sync & infra**

- [x] `sync-bmad-workflows.sh` + `--worktree` single-worktree flag (`647c14e7`)
- [x] worktree-portability rule (every producer resolves `{project-root}`)
- [x] `bmad-deploy` stale-checkout guard + source-vs-deployed drift gate

**Design pipeline**

- [x] design-handoff / design-artifact-loop / design-synthesize / design-tuning — intake checks + two-lane evidence model
- [x] design-review + design-review-pr — live + PR-time policy §13 / archetype enforcement
- [x] design-implement — comparison-grid axes: state · multiplicity · content-lane · page-shell · policy-as-spec
- [x] design-elevation (scope-expansion) + maintenance-triage (shape gate)

**Analytics presentation family**

- [x] analytics-surface-architect (shape) · analytics-rigor (depth) · decision-analysis (capital decisions)
- [x] archetype taxonomy incl. `waterfall`; design-handoff §5e surface hierarchy

**Verify lane (detect + route)**

- [x] data-quality-audit · scrape-coverage-audit · webhook-contract-check (+ charter) · relational-coherence-audit

**Agents**

- [x] custom-agents lane + create-agent: Vera (data-integrity) · Wren (relational-coherence)

## Designed but Not Yet Shipped

- [ ] **Quick-dev split into `spec-dev` and `direct-dev` entry points** — current `quick-dev` carries both modes behind one entry; split would make the safety boundary explicit at the entry point
- [ ] **`tech-spec` workflow** — lightweight artifact between `maintenance-triage` and dev, for brownfield work that needs more than direct instructions but less than a full PRD
- [ ] **Full brownfield maintenance pipeline** — `maintenance-triage → tech-spec → spec-dev` (1 of 3 surfaces shipped)

## Policy-skill debt

S2 findings from the policy-skill doctrine (safe-to-merge structural debt; clear opportunistically — see `policy-skills-healthcheck`). S1 never lands here (it blocks); S3 is opportunistic and untracked.

**None open.** Both seeded S2 items were cleared (see changelog `2026-06-19 — clear policy-skill debt`):
- ~~`analytics-surface-architect` `explain` dormant~~ → wired in design-review-pr §3b (teach on a C-ARCHETYPE-01 finding) + documented human-onboarding entry; all three modes now have callers.
- ~~`finance-domain-pass` no PR counterpart~~ → `C-FINANCE-01` added to design-review-pr (the finance symmetry partner to `C-ARCHETYPE-01`).

## In-Flight Work

- `worktree-feat+brief-revision-policy` in **accounting-tools** (not the fork) — design-tuning step revisions + new directories under `_bmad/bmm/workflows/design/`. These will need to be lifted back into the fork's `custom/workflows/` once stable. **Important:** edits in the project will be overwritten on next sync; the canonical edit point is `~/bmad-method-v6/custom/workflows/`.

## Known Drift Across the 13 Projects

Audit pending. Run `~/bmad-method-v6/sync-bmad-workflows.sh --check` to populate. Active sync targets:

- comms_dashboard, brand-source-finder, inbound-flow, accounting-tools, bison-ops, image-pipeline, bison-website, amazon-removal-assistant, taylor_work, accounting_api_backend, otp_manager, wera-catalog, amazon-lead-generator

## Upstream Items Under Evaluation

25 upstream commits behind. Per-commit triage not done yet. Reconciliation difficulty likely high in any area the fork has restructured (design pipeline, quick-dev, sync layer). Low-risk absorbs likely in areas the fork has not touched (core agents, party-mode, brainstorming, retro).

- v6.8.0 release → undecided → triage pending

## Recent Decisions Worth Remembering

- 2026-05-28: Shipped `project_phase` config and `maintenance-triage` workflow together. `project_phase` is the gating mechanism for routing brownfield work through `maintenance-triage` instead of greenfield-biased entry points.
- 2026-05-27: Shipped `sync --worktree` flag to resolve worktree-portability P0. Worktree creation no longer requires a full multi-project sync.
- 2026-05-?: Brief provenance contract finalized (11 fields + 6 intake checks). Hand-edits to material_revision briefs are now forbidden by intake check 5; clarifications remain allowed.
- 2026-05-?: Quick-dev grounding gate redefined autonomous_mode — decision autonomy (file choice, pattern selection) allowed; intent autonomy (inferring what user meant) forbidden.
