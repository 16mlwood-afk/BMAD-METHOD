# .githooks/gates.d/design-brief-completeness.conf — FORK-owned gate drop-in (STD-HOOKACTIVATE-001).
#
# Fork gates register HERE (not in the project-owned gates.conf) so a new fork
# gate retrofits the whole fleet through sync alone — gates.d/*.conf is always
# distributed (gap 496), whereas gates.conf is create-only to protect per-repo
# gate choices. Same format as gates.conf: <event>\t<command-relative-to-repo-root>.
# A command whose script is ABSENT in a repo is silently skipped by the dispatcher.
#
# event       command
pre-commit    .githooks/check-design-brief-completeness.sh   # design-handoff brief §7/frames contract (Phase-1 WARN-only)
