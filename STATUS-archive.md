# Mason-BMAD Fork — STATUS · Archive

> Complete verbatim wave log, moved out of STATUS.md's hot-read path. The skill reads STATUS.md's `## Now` + recent `## Changelog` on every invocation and reads this archive only on demand. The newest waves are rendered as discrete entries in STATUS.md; everything below is the original run-on log, preserved as-is.

## Changelog (archived — full verbatim log, newest-first)

**Last updated:** 2026-06-12 by inbound-flow session — **design-implement: the bundle is a generated PROPOSAL, the spec is `docs/design-policy.md`** `commit 9a0a1089` — follow-up correcting the page-shell wave below. User pulled a second real sheet (`Accounting Import v2`) to back the "what else are we dropping" claim, then asked "the readme is gened by Claude Design right" — **yes**, and it reframes the fix. The bundle README states its own provenance ("Trust hierarchy: `docs/design-policy.md` > … policy-first; foundations derived from the policy; scaffolded from the seed"), so it is a **generated, lossy, sometimes self-violated echo** of the project policy — proven by the `Accounting Import v2` bundle itself shipping a banned **colored-glow `@keyframes ai-pulse`** + **no `prefers-reduced-motion`**, both forbidden by the very `docs/design-policy.md` (v8, L50 full-width rule; v3/§5 colored-glow+motion bans) it was generated from. Consequence: my page-shell step-01 capture was pointed at the wrong source (the generated README), and a "prohibition lane" inside a bundle-diff would be a check that lies (matching a policy-violating bundle scores green). Correction: **(1)** step-01 re-points `{design_layout_constraints}` to read the layout rule from `docs/design-policy.md` **FIRST (authoritative)**, README+wrapper demoted to corroboration with an `authoritative` flag + a no-policy fallback (mark needs-confirmation, don't assert); step-03 §2d uses the policy-authoritative value. **(2)** New step-03 **§2e Ceded-dimensions** + step-04 §9 disclosure + a workflow.md Critical Rule: design-implement owns **treatment + structure + page-shell** and explicitly **CEDES policy-conformance (prohibitions/tone/motion/iconography) AND behavior wiring (drawer-stack/Esc/mutation/live-SSE/sort-filter)** to `design-review` / `design-review-pr` (which already enforce the policy) and `verify` (live behavior) — the content-lane disclosure model, not a faked grep. The principle: the bundle is authoritative for the pixels it proposes, never for policy it can itself break; the page-shell stays a CHECK (statically verifiable against policy+impl), the rest is CEDED (unverifiable from a generated bundle / needs live evidence). Self-reviewed Mode 1: 0 blocking, 1 nit fixed (a malformed `verify` command path in step-04). Fork pre-commit suite green; pushed `myfork/custom`; **synced 13/13, 0 blocked.** Skill `last_verified_against_fork_commit` → `9a0a1089`. **Prior wave (2026-06-11): design-implement gains the PAGE-SHELL axis** `commit 1febc8ce` — user (after a `/orders` redesign shipped narrow + centered): "how did design-implement miss this" → then "update the workflow." Root-cause class **contract-dimension-gap**, the fourth named instance after state (`PR #827`), implementation-multiplicity (the forked pill), and content-lane (`7a09717c`): the comparison grid is component × state × property, but the page CONTAINER's own width/centering/padding is owned by the page wrapper + ancestor layout (not any component), AND the Claude Design bundle renders its root `.app` full-bleed standalone, so **no per-component row could ever see a nested `max-width` cap** — inbound-flow `/orders` (PR #2017) nested an inner `max-width:1280` inside the layout's `max-w-[1440px]`, rendered narrow+centered against a README that said "full-width within the content container", and every component CSS matched ⇒ grid all-green. Unlike content-lane (CEDED — unverifiable from a mock bundle), the page-shell IS verifiable from evidence the workflow already holds (README prose + the full-bleed wrapper + the impl wrapper chain), so the fix **adds a real row**, not a cede. Axis threaded end-to-end: `workflow.md` `{design_layout_constraints}`+`{impl_page_shell}` state vars + a Critical Rule; **step-01** captures `{design_layout_constraints}` from README layout prose + the bundle wrapper width (URL.2) with a **SHARED note so the synthesize-bundle path — which has no README — populates it from the wrapper HTML + manifest `page_mode`**; **step-02 §1a** walks the wrapper chain OUTWARD through every ancestor layout and resolves the EFFECTIVE width (tightest nested cap wins) into `{impl_page_shell}`, noting sibling-page convention; **step-03 §2d** ALWAYS emits exactly one Page-shell row (width/centering mismatch = **Tier-1**; silence + full-bleed wrapper still scores; surface — don't auto-spread — a house-convention divergence) + Tier-1 list + success-metric + failure-mode; `template.md` Page-Shell section + `checklist.md` per-phase items. Self-reviewed Mode 1: **0 blocking, 1 concern caught + fixed** (first draft captured `{design_layout_constraints}` only in the URL path's README section → the bundle path would leave it empty; closed via the SHARED convergence note so both ingest paths populate it). Produces/consumes no brief ⇒ 6 intake checks + provenance N/A; decision-autonomy only; sync-safe fork edit. Fork pre-commit suite (markdownlint + full test run) green; pushed `myfork/custom`. **Synced 13/13 projects, 0 blocked.** Skill `last_verified_against_fork_commit` bumped `472fab1b`→`1febc8ce` and the `contract-dimension-gap` root-cause-class instance list extended with the page-shell axis (+ the verifiable-so-add vs unverifiable-so-cede contrast). The `/orders` page-shell fix itself shipped separately as inbound-flow **PR #2017** (the live bug the workflow change prevents recurring). **Prior wave (2026-06-10): relational-coherence-audit HUMANIZED as Wren + a maintained home + a linkage-aware handoff** `commits 49b771fd`+`472fab1b` — user: "humanise and assign a 'person' to the relational-coherence audit + a safe place to store and maintain the files it produces; the handoff human should be aware of page linkage and use them when making handoffs; the two characters should converse." Four moves. **(1) Wren — Relational Coherence Lead** (`custom/agents/relational-coherence-lead.md`, `/bmad:bmm:agents:relational-coherence-lead`): the cast member who owns the §13 linkage GRAPH across a surface set, sibling to **Vera** (data-integrity lead). Grounds the surface set from plain English, runs the audit, maintains the edge map, routes torn edges — re-design→Rowan/`design-handoff`, mechanical→`quick-spec`/`quick-dev`, out-of-scope→named-not-failed. Modeled on Vera's structure (grounding gate, menu-handlers, detect+route-never-edit). **(2) Maintained home `docs/relational-coherence/`** — `relational-edges.yaml` (hand-maintained edge+co-view map, source of truth) + `reports/` (dated history beside the map version walked against), both git-tracked + deploy-irrelevant, OUT of transient `implementation-artifacts/`. workflow.md/step-02/step-04 read+write the home (legacy flat-path fallback + "move it" route); `relational-edges.template.yaml` header repointed. **(3) Linkage-aware handoff** — `design-handoff` step-01 now SEEDS the §2a linked-records inventory FROM the maintained map + latest report before deriving from scratch (the map is authoritative since it carries the derived edges the schema can't see — the SellerSmart warehouse push; a routed handoff carries its §2a seed verbatim; an omitted reference routes "declare it" back, no-guessed-edges both sides). **(4) The two converse** — `wren-and-rowan-on-the-seam.md`: a dialogue encoding the audit→handoff seam (torn edge handed forward WITH its §2a context, consumed by the linkage-aware handoff). **KEY DISCOVERY (skill is STALE):** the `mason-bmad-workflow-expert` skill's "Authoring an Agent" section claims *"the fork has no `custom/agents/`"* and *"sync does NOT generate wrappers for custom agents"* — BOTH NOW FALSE. `custom/agents/` exists (Vera, now Wren) and `sync_agents_for_project` mirrors the persona AND generates the `.claude/commands/` wrapper. The "durable fix (not yet built)" the skill describes HAS SHIPPED. The skill section needs correction + `last_verified` bump. **Delivery:** fork pushed `myfork/custom` (both commits, pre-commit suite green). inbound-flow **PR #1992 MERGED** (focused: Wren persona+wrapper + the new relational-coherence-audit workflow dir + `docs/relational-coherence/` scaffold seeded with the canonical warehouse edge + queue⇄triage co-view; tsc+vitest 5697 green; `_bmad/`+`docs/` only, deploy-irrelevant, no deploy). Self-reviewed Mode 1: **0 blocking** (persona + read-only audit produce/consume no brief ⇒ 6 intake checks + provenance N/A; the design-handoff edit is producer-side body, decision-autonomy only, sync-safe fork edit; no-guessed-edges carried into both the audit and the brief). **Follow-ups (NAMED):** (1) **skill correction owed** — fix "Authoring an Agent" (custom/agents + wrapper-gen shipped) + bump `last_verified_against_fork_commit` to HEAD; (2) **Rowan PERSONA linkage rule is BLOCKED** — the project `_bmad/bmm/agents/rowan-product-designer.md` is sync-managed (hook refuses the edit) but Rowan has NO fork source (the design cast Devon/Rowan/Jules is project-local + wipe-exposed). The functional requirement is met by the design-handoff WORKFLOW seed (move 3) + Wren's hand-forward + the conversation; making the PERSONA itself linkage-aware durably requires promoting the design cast to `custom/agents/` — which DISTRIBUTES the cast to 12 other projects on next full sync = a deliberate decision for the user, NOT taken unprompted; (3) **full `sync bmad` OWED** to distribute Wren (+ new wrapper) and the design-handoff linkage-seed to all 13 projects — this PR only delivered to inbound-flow, and the design-handoff seed + the pre-existing fork-ahead `_bmad/` drift still sit uncommitted on inbound-flow main. **Prior wave: relational-coherence-audit gains the `co-view` relation kind (same-record sibling surfaces)** `commit 618fb56f` — user (designing the Listings Queue ↔ Upload Triage seam with Claude Design): "we have two extremely similar pages but they do not communicate at all" + "I thought the coherence skill would catch this." It couldn't: the audit modeled ONLY foreign-record §13 edges (FK + declared derived edges between DIFFERENT record types), so two surfaces over the SAME record type partitioned by status (`/listings/queue` all-statuses ⇄ `/listings/queue/triage` failure-tail) fell through all three gates — not an FK, not declared, and §13's "record owned by another surface" clause never fires on a sibling that isn't foreign. Root-cause class **contract-dimension-gap** (the same-record co-view axis missing from the relation-kind contract; sibling to the cross-PAGE linking axis `f4bf10e8` added, one level up again). Fix: a declared `co_views:` section (in `relational-edges.template.yaml`) + **CO-VIEW CHECKS CV1–CV5** (bidirectional per-row link · count reconciliation · consistent handler-split IA · shared status vocabulary · no orphaned partition) threaded through workflow.md + step-01 (single-owner **carve-out**: a co-viewed record legitimately has a `master` + `partition` co-viewers, NOT rival owners — must not be flagged dual-owner; an undeclared sibling routes "declare a `co_view`," never conjured) + step-02 (load `co_views` into `{expected_graph}`; scope-gated on ≥2 surfaces in the set) + step-03 (walk CV1–CV5, distinct from the §13 lookup checks) + step-04 (classify/route: seam-as-IA `no-row-link`/`ia-divergence` → `design-handoff` spanning BOTH surfaces; seam-as-mechanism `count-drift`/`vocabulary-drift`/`orphaned-partition` → `quick-dev`). Additive/non-breaking (no `co_views:` key ⇒ identical behavior). Self-reviewed Mode 1: **0 blocking** (read-only, no brief ⇒ 6 intake checks + provenance N/A; declared-not-inferred inherits no-guessed-edges; every CV verdict gets a disposition row = silent-partial guard; all five checks verifiable from the audit's evidence source ⇒ no dimension ceded). Fork pre-commit suite green; pushed `myfork/custom`. **Project side:** inbound-flow `docs/relational-edges.yaml` declares the queue⇄triage co-view (PR #1977); the re-run on the BUILT pages catches it — **C1: `no-row-link` + `count-drift` + `vocabulary-drift` + `orphaned-partition`** live, plus a **forthcoming `ia-divergence`** when the queue's per-handler redesign ships and triage stays flat. Skill `last_verified_against_fork_commit` bumped `91c8b334` → `618fb56f`. **Synced 13/13 projects** 2026-06-10, 0 blocked (verified CO-VIEW CHECKS block landed in accounting-tools + comms_dashboard). **Follow-ups:** (1) the triage v8 `design-handoff` (the CV3 seam) fires after the queue layout locks so triage INHERITS the per-handler split rather than designing it twice; (2) `design-review` (live-page audit) is a candidate host for the co-view checks at audit-time, complementing this standing-audit home. **Prior wave: design-lane digestibility wave + brief-provenance "ten"→ELEVEN ruling REVERSED** `commits 6b2632a5`+`91c8b334` — user ran `orchestrate-workflows` on the design lane (LLM-digestibility lens) → "do 1". **(1) design-handoff step-03 brief-template extraction:** the 768-line step (a thin procedural shell wrapping a ~580-line embedded brief template) was split — new `design-handoff/brief-template.md` holds the brief shape verbatim (inside a `​```​markdown` fence, as it was in-step) + a 189-line shell that points to it. **Provably lossless** — 3 empty diffs vs the original from git (template body 581 lines byte-for-byte, head 1–93, tail §3-Self-Review→Success-Metrics). `commit 6b2632a5`. **(2) brief-revision-policy restored a DROPPED Block-A field:** schematizing the provenance block surfaced a `contract-dimension-gap` — §2 asserts "11 fields" in three places AND §1a references `policy_version_required`, but the §2 YAML example/table enumerated only **10**. The producer (`brief-template.md:37`, `workflow.md:93`) DOES emit `policy_version_required` (the policy-version-drift pin). Restored it to the §2 example+table → enumeration now matches the count (11), the producer, and §1a. `commit 91c8b334`. **(3) RULING REVERSED — the prior wave's "ten is authoritative; the prose's 'eleven' is the long-known miscount" claim (recorded in the *Prior wave* line directly below) was WRONG and has been reversed.** Its premise ("ten is what `design-handoff` emits") was false — the producer emits **eleven**; the team verified against the stale §2 *example*, not the producer. Corrected `references/brief-provenance.md` §9 + the field table + every count ref to **eleven**, preserving the real nuance the old ruling half-reached for: `policy_version_required` is backward-compat-defaulted (§1a: absent⇒`0`, not a halt), so the Block-A enumeration = **11** while the strict halt-on-missing set = the **10 lifecycle fields**. Skill `last_verified_against_fork_commit` bumped → `91c8b334`. **Synced 13/13 projects** 2026-06-10 (verified inbound-flow: step-03=189 lines, `brief-template.md` present, policy = 11 Block-A fields). Digestibility queue remainder deliberately NOT done this wave (lower-value/higher-risk): thin the 2 oversized routers (`design-synthesize` 636 / `design-artifact-loop` 497 → `NOTES.md`), one-line the project `design-policy` change_log, and a `step-06-self-critique` safe-partial (rubric/detector-table extraction ONLY — the file is the lane's most *coupled*, so a blind file-split risks the untestable synthesis quality gate). Audit report: `_bmad-output/implementation-artifacts/orchestration-tuning-2026-06-10-design-lane-digestibility.md` (inbound-flow, local/gitignored). Prior wave: **multi-handler enforcement triangle completed + fork-expert skill re-verified & bumped + bison-ops sync unblocked** `commit c189f16a` — follow-up wave to the multi-handler change below; user: "do all" (four named follow-ups). **(1) `design-review` live-audit check** (`commit c189f16a`): §5 Compare gains a project-policy-gated multi-handler-split check — a live surface that **interleaves records from ≥2 operational handlers into one undifferentiated list** (instead of tabs / per-handler segments / sibling routes) is a `hard failure`. Kept **design-agnostic** (references the project policy for what counts as a "handler"; no-op where no such rule exists). Completes the **enforcement triangle**: design-time (`design-handoff` §5d) + PR-time (`design-review-pr` §12) + live-audit (`design-review` §5). Fork pre-push green; pushed `myfork/custom`. **(2) skill re-verified + SHA bumped `155cdca2`→`c189f16a`:** unlike the two prior waves that deliberately deferred, this wave **actually re-verified the load-bearing references against HEAD** before bumping — `brief-provenance.md`'s 10 fields (`target_slug` + 9; the policy prose's "eleven" is the long-known miscount the ref already documents and works around) + 6 intake checks (Check 1–6) confirmed against `brief-revision-policy.md`; grounding-gate + autonomy-scoping present in `quick-dev`; sync script present; `project_phase` greenfield/brownfield/mixed lifecycle intact. Honest bump (drift was ~230 commits of distance, not factual staleness — the cited invariants hold). **(3) bison-ops sync unblocked (was the recurring block across every prior wave):** the `--pull` diff review revealed bison-ops is **far behind, not ahead** — committing the pull would have DELETED 3148 fork lines incl. today's `relational-coherence-audit` (`f4bf10e8`) + `create-agent`. Discarded the pull, ran `--force`. Result: **13 synced, 0 blocked**; the `--force` run **self-injected the 3 missing CLAUDE.md sections** (Cross-Repo-Edits / Deployment-BMAD-contract / CI-Health-Check) into bison-ops via its `<!-- bmad:* -->` upsert anchors — the prior normal-sync "VERIFY FAIL" was a side-effect of the workflow block, now cleared. (bison-ops's synced `_bmad/`+CLAUDE.md sit uncommitted in its working tree — normal post-sync state; its own session commits them, not cross-committed from here.) **(4) Listings Queue brief §4c — confirmed, deliberately NOT edited:** the brief has no formal §4c verdict (predates v7; `policy_version_required: 6`) but is **already substantively compliant** — §1 mandates working the queue per-warehouse "*instead of a single undifferentiated list*", §5 mandates the segmenting lens + explicit never-dropped "Unrouted" group, §2 keeps route-warehouse vs SellerSmart-dispatch handlers distinct. Hand-adding the tag would flip `revision_mode → manual_minor_revision` while `change_class: material_revision` — manufacturing the **Check-5 forbidden "material-change-via-hand-edit" combination** on a merged brief — so **noted, not added** (next `design-handoff` regen emits §4c via the new §5d trigger; §12 enforces the split at PR-time regardless). Wave self-review: the only edit-equivalents the guard blocked (cross-repo `cat >>` to bison-ops CLAUDE.md; in-project brief edit) were *correctly* routed — the sync did the CLAUDE.md inject, the brief was left alone. **Follow-ups:** (1) skill `version` not bumped (no public-shape change — only the verification SHA moved); (2) `design-policy` §13 v5 §9's accounting-tools propagation of the linked-record mandate still owed by that project's next session (unchanged). Prior wave (2026-06-10, same session): **design-handoff §5d gains a generic multi-handler split trigger + inbound-flow design-policy v7 multi-handler rule** `commit 8c59ea10` — user (via `modify-design-policy`): "any surface whose records span more than one warehouse handler (route/dispatch warehouse, lane, or fulfilment provider — SellerSmart, TheFBAPrep DE, SmartPrepCentre US) must be split by view — per-handler tabs or per-handler routes — and **never** a single merged front-end list … co-residence in the data layer is fine; this governs **presentation only, not storage** … design-handoff §5d must detect the condition and recommend the split." **Policy side** (inbound-flow `docs/design-policy.md` v6→v7, PR #1961 merged): new **§6 "Multi-handler surfaces"** subsection (≥2-handler trigger, any number ≥2 → `needs-tab-views`/`needs-sibling-route`, never a merged list; presentation-only); one **§5 hard failure** (a merged front-end list interleaving ≥2 handlers — coverage-only add, six-category §5 audit unaffected); one **§12 contract-critical positive assertion** so `design-review-pr` enforces the split. **Fork side** (`design-handoff` step-01-gather §5d Surface Topology Assessment): adds a **5th question** — does the surface span ≥2 operational handlers the project policy requires be split? Kept **design-agnostic** — it references the **project** `design-policy.md` for what counts as a "handler" and is an explicit no-op where no such rule exists; ≥2 handlers is a **forcing condition** (verdict cannot be `single-page-appropriate` as a merged list). **Self-reviewed Mode 1: 1 BLOCKING caught + fixed** — the first draft hardcoded `warehouse handler`/SellerSmart/TheFBAPrep DE/SmartPrepCentre US into the **shared** §5d, violating the "shared BMAD design workflows remain design-agnostic — project-specific decisions live in the policy" invariant (accounting-tools must not inherit inbound-flow provider names); rewritten to policy-reference. Produces/consumes no brief → 6 intake checks + 10-field provenance N/A; decision-autonomy only (recommends split for operator confirm, never infers intent); sync-safe (fork edit, not project `_bmad/`). Fork pre-push suite green; pushed `myfork/custom`. **Synced 12/13 projects 2026-06-10** (bison-ops pre-existing CLAUDE.md-section block, unrelated; inbound-flow §5d marker verified). **Downstream — `apply-design-policy-change` NOT fired (expert call, autonomous mode):** the only impacted surface, the **Listings Queue**, is ALREADY mid-redesign with `design-brief-listings-queue-2026-06-10b.md` (PR #1957, "warehouse-routing lens") which implements the per-handler split — a route-warehouse grouping/segmentation lens ("clear one warehouse's intake at a time instead of a single undifferentiated list") + an explicit never-dropped "Unrouted" group. The page is actioned, not flagged-and-abandoned; firing the apply-workflow would emit a duplicate competing brief (forbidden by no-overengineering). The Listings Queue carries TWO handlers per row (route warehouse inbound + SellerSmart dispatch outbound) — the canonical multi-handler surface this rule governs, and its brief + the new policy now agree. **Follow-ups:** (1) skill `last_verified_against_fork_commit` STILL `155cdca2`, now further behind HEAD `8c59ea10` — deliberately NOT bumped (no re-verification of the skill's referenced checks this wave; same honesty discipline as the relational-coherence-audit wave); (2) recurrence: `design-review` (live-page audit) is a candidate host for a multi-handler-merge check at audit-time, complementing the §12 `design-review-pr` enforcement at PR-time. Prior wave (2026-06-10): **NEW `verify/relational-coherence-audit` workflow — the cross-surface §13 linkage-graph auditor** `commit f4bf10e8` — user: "I wish to evaluate the coherency/linkage across one or multiple pages... handoff is 1 input page only... listing queue should WITHOUT a doubt have a link to the warehouse page." `design-handoff` / `design-review` / `design-review-pr` all enforce §13 on ONE surface in isolation; none holds the cross-surface GRAPH, so §13's hardest clauses — "no relationship visible from one side only", "a page passes only if it coheres with AND links to its sibling records" — were stated-but-enforced-by-nothing. Root-cause class **contract-dimension-gap**: the cross-page linking axis, one level up from `155cdca2` (which added the linking axis WITHIN a page). The catch no per-page check can structurally make — an absent-but-required link leaves no diff, no failing assertion, no symptom; only an independent expectation (a schema FK or a declared edge) reveals it (canonical case: `/listings/queue` shows warehouse-derived dispatch state but never links the warehouse record, and every per-page check stays green). **Two-evidence contract** (the `webhook-contract-check` shape): EXPECTED = Drizzle FKs + a declared `relational-edges.yaml` for the derived/correlated edges the schema can't encode (the SellerSmart push is an `EXISTS`, not an FK); ACTUAL = live source/DOM; the finding lives in the join (displayed-but-not-linked), and **"displayed?" is decided from the page, not the schema** — the cede that kills the false-positive flood. Load-bearing split → opposite lanes: **missing-required-link** → `design-handoff` (re-design, flagged material-revision so it supersedes not duplicates); **unresolved-lookup / drift / loud-affordance / round-trip** → `quick-spec`/`quick-dev`; **out-of-scope-candidate** (schema edge never displayed) → named not failed; **edge-map gap** → declare + re-run (P1). Read-only, detect+route, never edits — joins the `verify/` audit family (data-quality-audit, scrape-coverage-audit, webhook-contract-check). 5 files (workflow.md + 4 steps + `relational-edges.template.yaml`, project-copied to `docs/`, never sync-overwritten). Self-reviewed Mode 1: **0 blocking** (1 nit fixed — step-02 accepts `relation_kind: fk-with-lookups` to match the template); produces/consumes no brief so the 6 intake checks + 10-field provenance are N/A; grounding gate + autonomy scoping + silent-partial guard present. Fork pre-commit suite green; pushed `myfork/custom`. **13-project `sync bmad` OWED** — new workflow → a new `/bmad:bmm:workflows:relational-coherence-audit` wrapper generated on sync. **Follow-ups:** (1) the `mason-bmad-workflow-expert` skill is **230 commits behind** (`last_verified 155cdca2` vs HEAD `f4bf10e8`) — re-verification owed, deliberately NOT bumped here (bumping without re-verifying its referenced checks would be dishonest); (2) no project has a `docs/relational-edges.yaml` yet, so until one exists every run is FK-only and must announce that P1 blind spot — inbound-flow's should be seeded first (the warehouse edge is in the template); (3) recurrence: `design-review` (live-page audit) is a candidate host for the same graph check at audit-time. Prior wave (2026-06-09): **design-handoff carries the §13 linked-records mandate into the brief** `commit 155cdca2` — user: "policy emphasis on the link-to-record lane — handoff does not seem to give it the space it needs." design-policy §13 v5 (commit `ec4272e0`, same day) promoted cross-surface linking from a coherence *check* to a contract-critical **§12 positive assertion that `design-review-pr` hard-fails**: every on-screen value that IS a record owned by another surface (ASIN/SKU/order/batch/shipment/supplier/customs-entry/listing) must be a navigable lookup/link, not inert text. design-handoff was **silent** on it — §2 Domain Data captured relations *descriptively* (a "Relationships:" line + FK notes), the brief deliberately strips the existing layout (blank canvas) so the one place the linking was visible is gone, and the regenerated design defaulted to inert duplicated text → **failed review on the way back in**. Root-cause class **contract-dimension-gap** (the relational-linking axis missing from the brief contract). Fix: step-01 **§3a Linked-records inventory** derives `{linked_records_inventory}` mechanically from the data surface (foreign reference · owning surface+route · drill target = drawer + "Open full {sibling} →" · inline read-only lookups; empty ONLY for a true leaf surface) + COMPLETION; step-03 **§2a Linked records & lookups** renders it as a labeled lane (mirrors §4b/§4c — render-iff-non-empty, full omission otherwise) with the §13 quiet-styling guardrail (never button/CTA/pill) + the round-trip review-test; self-review item + success metric so a shared-data surface is never handed off silent on §13. Honesty: a value no surface owns is a plain value, not a fabricated link. Self-reviewed Mode 1: **0 blocking** (producer-side body content → 6 intake checks + 10-field provenance N/A; decision-autonomy only — derives from schema FKs, no intent inference; sync-safe — fork edit not project `_bmad/`). markdownlint + full fork pre-push suite green; pushed `myfork/custom`. **Synced 12/13 projects 2026-06-09** (bison-ops pre-existing CLAUDE.md-section block — recurring, unrelated; inbound-flow markers verified: §3a, `{linked_records_inventory}`, brief §2a). **Follow-ups:** (1) design-policy §13 v5 §9 already flags the linked-record mandate SHOULD propagate to **accounting-tools** — that project's next session applies it (not auto-edited here); (2) existing shared-data surfaces that render foreign records as inert text now diverge from policy and route to **apply-design-policy-change** (per §13 v5's own downstream note); (3) `design-review-pr` already enforces §13/§12 at PR time — this closes the producer end so the two ends meet. Prior wave (same day): **decision-analysis — the executive/quant-desk layer above rigor** `commit 169520f4` — user asked "is it executive data-analyst level or not?"; honest answer was NO: `analytics-rigor` reaches *senior-analyst* grade (honest ranges/base-rates/deciding-fields/gaps) but is descriptive — a capital-commitment surface (buy/reorder/sizing) needs a decision-theoretic rung (model the outcome distribution, size to the loss tail, name the breakeven driver). New `decision-analysis` skill (project-local + hand-apply fallback) wired as the third layer — **stack is now shape → depth → decision** (archetype/rigor/decision). Eight decision moves; narrowly gated on a new `{is_capital_decision}` (stricter than `{has_decision_numbers}` — dashboards/coverage/status commit nothing → no §4e, `C-DECISION-01` no-op). Wired: handoff §5c-3 invoke + brief §4e + rationale §3c + `C-DECISION-01` (5th review check, reads brief §4e, conservative — hard finding only on an unsized BUY/PASS or a confident probability the brief flagged un-modellable) + taxonomy cross-link. Model-honesty gate (paramount): un-modellable decision → honest `single-scenario` + VOI gap, NEVER a fabricated outcome distribution. Mode 1: 0 blocking; pre-push green; synced 12/13 (bison-ops pre-existing block). Worked proof: in-browser Monte-Carlo demo (`/tmp/inbound-flow-probabilistic.html`). Residual: skill is project-local (not fork-distributed; `71a961e9` class). Prior wave (same day): **analytics-rigor gate broadened to decision surfaces (follow-up to `796fd990`)** `commit 3e994a6f` — user asked "does the handoff call it appropriately?" and it did NOT for the motivating case: rigor was gated on `{has_analytics_band}`, but step-03's self-check asserted detail pages carry no band → rigor never fired for lead-detail (a detail buy surface). Plus the worst offenders (verdict/econ numbers) lived in the §4a hero, outside the band §4b·F covered; not per-surface; and the line-673 detail-no-band absolute contradicted §5e. Root-cause class `contract-dimension-gap`. Fix: new `{has_decision_numbers}` gate (has_analytics_band OR a detail/analytical decision surface; not CRUD), per-surface; rigor lifted §4b·F → surface-level brief **§4d** (covers band AND record/hero figures); `C-RIGOR-01` reads brief §4d not rationale §3b (so bandless decision surfaces, which have no rationale, are still enforced); line-673 reconciled (detail CAN carry analytics per §5e; must not carry a dashboard). Honesty gate preserved. Mode 1: 0 blocking; pre-push green; synced 12/13 (bison-ops pre-existing block). Prior wave (same day): **analytics-rigor — the DEPTH axis of data presentation** `commit 796fd990` — the analytics presentation family governed SHAPE (`analytics-surface-architect` + taxonomy) and PIXELS (`design-policy`) but nothing governed DEPTH, so a correctly-shaped, policy-clean band could ship every figure as a naked point estimate, no base rate, no trend magnitude, no one-line read (correct and useless). User-surfaced example: the inbound-flow lead-detail buy surface — polished, compliant, schoolboy-grade analysis. Root-cause class `contract-dimension-gap` (the missing depth axis of the presentation contract). New `analytics-rigor` skill (eight rigor moves: lead-with-read · no-naked-number · sensitivity · base-rate · deciding-field · trend-magnitude · rank-by-impact · missing-vs-weak), wired across all three layers so depth is BUILT (brief §4b·F), RECORDED (rationale §3b), and ENFORCED (`C-RIGOR-01`, the depth counterpart to `C-ARCHETYPE-01`). Honesty gate (paramount): uncomputable uncertainty/base-rate → surfaced data gap, NEVER a fabricated interval; `C-RIGOR-01` is conservative (P1 only on the unambiguous naked-number case, else a precise human-judgment prompt) and never flags a named gap as a defect. Self-reviewed Mode 1: 0 blocking (rationale out of scope for `brief-revision-policy.md`; brief §4b·F is body not frontmatter; worktree-safe via documented fallback). `validate:refs` + markdownlint + full pre-push suite green; pushed `myfork/custom`. **Synced 12/13 projects 2026-06-09** (bison-ops blocked on its pre-existing CLAUDE.md-section drift — same recurring block as every prior wave, unrelated to this change; verified §5c-2/§4b·F/C-RIGOR-01/§3b/taxonomy markers landed in inbound-flow). Residual: `analytics-rigor` is project-local (not fork-distributed) — same drift class as `analytics-surface-architect` (`71a961e9` follow-up); other 12 projects use the hand-apply fallback. Prior wave: 2026-06-08 by inbound-flow session — **capability-drop hardening (design-handoff mutation-derivation audit + `{dropped_capabilities}` end-of-run disclosure · design-implement orphaned-action disclosure)** `commit 6b09432a` — design-handoff DERIVED the data shape from the schema but RECALLED capabilities, and the anti-bias "don't read the current UI" rule discouraged the inspection that would surface a non-obvious one. The ingest audit `16e7eeab` closed the record-CREATION subclass; this closes the sibling EXISTING-record-mutation subclass (resolve/remap/override/re-run/reprice). Canonical silent loss (user-surfaced): the EOS batch-detail EAN→ASIN remap — `overrideWholesaleAsinAction` left with zero callers after the funnel redesign deleted the components that called it. Root-cause class `contract-dimension-gap`. Fix: step-01 §3 **mutation-derivation audit** (grep the current surface's components for every action they invoke — verbs NOT layout, anti-bias preserved — each maps to `must_support_capabilities` OR a logged `{dropped_capabilities}` drop); workflow.md + step-01 COMPLETION add the state var; step-03 renders drops in brief §1 "Deliberately not carried forward" + **MANDATORY end-of-run user disclosure** in §5 + self-review item. design-implement step-04 §9 gains a **mandatory "Capabilities removed (orphaned actions)"** report when the apply deletes/replaces components (grep for now-zero-caller actions among those the removed files called) — the grid-driven apply ledger can't catch it because the lost capability was never a grid row. Self-reviewed Mode 1: 0 blocking. Fork pre-push suite green. Pushed `myfork/custom`; 12/13 `sync bmad` 2026-06-08 (bison-ops pre-existing CLAUDE.md drift). **Follow-ups:** (1) optional brief frontmatter flag for `{dropped_capabilities}` if `brief-revision-policy` should validate it (currently body-only, like `must_support_capabilities`); (2) the EOS batch-detail remap itself is being restored via `design-brief-eos-batch-error-resolution-2026-06-08.md` (PR #1882, merged). Prior wave: 2026-06-08 by inbound-flow session (**design-handoff §5e Analytics Surface Hierarchy — rank multiple co-resident surfaces** `commit 3e1be5ca` — the handoff could pick the shape of ONE analytics band (§5c) and split a page across routes (§5d), but had no step to rank MULTIPLE co-resident analytics surfaces on one page → briefs came back with no hierarchy, designs rendered the flat equal-weight panel-stack that policy §6/§5 ban, review caught it after the fact. Surfaced by a legacy Keepa-style product detail page (price + sales-rank + competition + live-stock + buy-box-ownership = 5 charts at equal weight, an archive not an argument). Same root pattern as the waterfall gap: the handoff's analytics reasoning was one-band-shaped; this adds the missing axis (surface hierarchy). Fix: step-01-gather §5e (NEW) — gated on ≥2 distinct analytics surfaces, fires regardless of page_mode INCLUDING detail (overrides the §5b/step-03 "a single record has no aggregate dimension" absolute that suppressed analytics-rich detail pages — the exact Keepa failure), ranks hero/supporting/drill by the page's PRIMARY question, ground-or-flag reuses §5d (can't name one hero → needs-tab-views/needs-sibling-route); §5c runs once per surface on multi-surface pages; step-03 §4b.0 (NEW) renders the ranked hierarchy before per-surface specs; step-03 detail-mode "no analytics band" softened to carve out research/monitoring detail pages. Operationalizes existing policy (§6 "one or two lead charts + supporting"); does not invent a rule. Pushed to `myfork/custom`; synced to inbound-flow + others 2026-06-08 (bison-ops blocked on pre-existing CLAUDE.md drift). Follow-on flagged (not built): design-review-pr `C-AHIER-01` to enforce the declared hierarchy at PR time. Prior 2026-06-08: (**`waterfall` analytics archetype — reconciliation-bridge split out of `flow`** `commit 71a961e9` — the archetype taxonomy had eight shapes and `flow` was overloaded to cover BOTH attrition-funnels AND running-total reconciliations, so a dataset shaped `opening − Δ − Δ = closing` (EOS batch-detail conversion run, 642 → −131/−41/−82/−57 → 331 winners, headlined by a single £4,790 figure) could only map to `flow`, whose prescribed form is "a funnel with drop deltas" — the architect selected `flow` correctly and the synthesizer rendered a funnel correctly, producing a funnel wearing a waterfall's data (deltas double-encoded as bar segments AND reason chips; the corporate bridge shape unreachable for lack of a name). Root-cause class contract-dimension-gap — missing dimension lived INSIDE the archetype vocabulary, not the §5c wiring (already correct). Fix: new `waterfall` archetype + `flow`-vs-`waterfall` disambiguation (independent gates/survival-rate → flow; one population reconciled by signed deltas → waterfall) with a question-driven tie-break; `brief-revision-policy.md` adds `waterfall` to the `analytics_archetype` allowed-values contract; design-handoff step-01/step-03/workflow.md bump enums + "eight"→"nine" + a waterfall form hint; design-review-pr C-ARCHETYPE-01 gains a `waterfall` row. Pushed to `myfork/custom`; **13-project `sync bmad` OWED** (workflow text, no new wrapper). Residual: the project-local `analytics-surface-architect` SKILL.md enum still lists eight (taxonomy-wins makes it functionally correct post-sync, output-contract enum stale) — skill is not fork-managed, a known drift class. Prior 2026-06-08: (**design-tuning craft/legibility checks + PASS-WITH-ISSUES gate** `commit 7f9fb748` — design-tuning auto-approved a Prep Partners Hub design on iter-1 (clean PASS) that the user then found riddled with craft defects. Root-cause class: contract-dimension-gap — the PASS gate was binary on §5 hard-failures, so issue-severity craft had no enumerated check and could not block approval; the reviewer also substituted holistic reasoning for the §1b/§2 grids. Three blind spots had ZERO check: status over-encoding by repetition (same severity repainted border+pill+colored-text+colored-count on one row — the 4-color cap counts breadth of hues, not per-row repetition), decorative element redundant with adjacent text (a per-row micro-bar restating its own caption — fails §5 "what job does this do?"), and label collision / label↔severity-wording / icon↔meaning mismatch. Fix: step-02 §1b role catch-all extended (status-indicator repetition test + analytics-viz per-row redundancy test); new §2 "Craft & legibility" table (issue-severity); §7 splits PASS into PASS-CLEAN vs PASS-WITH-ISSUES via a new {has_unresolved_issues} gate; step-03 §5 gains an APPROVED-WITH-ISSUES template with a mandatory Issues-to-resolve block, §5b brand-identity feedback scoped to PASS-CLEAN only, §6 summary/next-step updated; checklist.md gains matching role-keyed boxes. Pushed to `myfork/custom`; 12/13-project sync ran 2026-06-08 (bison-ops blocked on pre-existing CLAUDE.md drift, unrelated). Prior 2026-06-08: (**design-handoff §5d surface topology assessment** `commit 7e366b34` — adds a reasoning step (not a checklist) to step-01-gather.md §5d that uses gathered evidence — data model depth, capability breadth, user jobs, existing routes — to assess whether a feature's scope fits one route or warrants multiple surfaces. Four verdict classes: `single-page-appropriate | needs-detail-route | needs-tab-views | needs-sibling-route`. Verdict drives a new §4c template block in step-03 (rendered only when non-trivial) and a self-review checklist item. The step asks the LLM to reason from evidence rather than check thresholds — aware of right-drawer capacity, full-page detail ceiling, sibling-route sub-entities, and tab-as-mode-switch vs tab-as-chrome-overflow. Pushed to `myfork/custom`; 12/13-project sync ran 2026-06-08 (bison-ops blocked on pre-existing CLAUDE.md drift). Prior 2026-06-08: (**design-handoff ingest/entry-point hardening** `commit 16e7eeab` — two design-handoff runs on the same leads surface both silently dropped the wholesale upload action because the gather step asked about mutations but not how data *enters* the system. Root-cause class: `contract-dimension-gap` (the check was "capabilities not dropped" but the gap was "capability never added to must_support_capabilities"). Fix: step-01-gather.md §3 API Surface gets an explicit ingest/entry-point audit prompt; step-03-generate-brief.md §3 self-review gets a dedicated checklist item that is distinct from the existing must-support-capabilities check. Pushed to `myfork/custom`; 12/13-project sync ran 2026-06-08 (bison-ops blocked on pre-existing CLAUDE.md drift, unrelated). Corrected brief `design-brief-leads-2026-06-08.md` shipped to inbound-flow via PR #1859 (merged); 06-06 brief marked superseded.). Prior 2026-06-06: (**webhook-contract-charter** `commit 1142471a` — declarative contract-of-record companion to the `webhook-contract-check` enforcer; charter authored in `workflows/shared/`, enforcer now cites it as its source of truth; pushed `myfork/custom` + full 13-project sync 2026-06-06, 13 synced / 0 blocked. ALSO earlier this day: design-tuning **check-surface scoping** `commit 0539fd7a` + `d43830e8` + `b0d5a196` — element-type-scoping gaps in step-02-analyze, both surfaced by a user catching defects the workflow scored clean on a TheFBAPrep Audit History design: (1) the §1b/§2 band & "Dashboard card grid" check keyed only on per-tile FRAMING (border/fill/shadow), so a band whose content is big summary-count figures (a stat/KPI row) passed once the framing came off — added a content-keyed clause + framing-independent litmus ("delete the band and move its numbers to the inline summary line; if you lose only numbers and no visualization, it was a stat row → hard failure", routed through the §2 AI-fingerprint row so severity is forced hard-failure); (2) the §2 color-cap check was scoped to badges, so status color in a 3-segment progress bar (pink awaiting-delivery / orange awaiting-receipt / green received = a rainbow stage mapping + off-palette pink/orange) passed while the pills read clean — broadened to count status color across ALL state-encoding elements (progress/meter/segment/dot/series/strip) + off-palette/rainbow/same-state-different-tone sub-checks. Then a FULL audit of design-tuning (user: "we identified 2 gaps in 30 minutes") found the SAME defect throughout and produced the structural fix `b0d5a196`: §1b re-keyed by semantic ROLE (status-indicator / CTA / identifier / analytics-viz / surface-fill / detail-drawer) with a catch-all so a rule is inspected wherever its role appears, not only on named elements (retroactively covers the drawer-on-operational §7 gap, CTAs outside detail mode, status color on non-badge elements); explicit severity routing so no §1b finding silently defaults to "issue"; and 4 §5 hard-failures that had ZERO check (grep-confirmed) added — emoji-as-UI-icon, sidebar-in-feature-page, status meaning↔tone mismatch (red reserved for genuine failures), uppercase tracking-wide. checklist.md de-drifted from other-project specifics (emerald/Inter/ring-inset) into a project-agnostic role-keyed companion. Root-cause class throughout: contract-dimension-gap (check surface scoped by element TYPE). **All three commits pushed to `myfork/custom`; full 13-project `sync-bmad-workflows.sh` RAN 2026-06-06 — 13 synced, 0 blocked.** Sibling carry-over `commit 86e737cd`: design-review-pr step-03-dom-render role-keyed — S-STATUS-03 broadened to count status color across ALL state-encoding elements (`distinctStatusColorsAllEls`, not just badges), new S-STATUS-09 (per-bar rainbow stage mapping ≥3 hues), L-LAYOUT-03 broadened with `bigNumberBlocksAbove` (unframed big-number stat rows), C-ARCHETYPE-01 stat-row-in-disguise across all archetypes (the `bigNumbers` harvest was wired only to single-metric). design-implement assessed and deliberately NOT patched — it is a *fidelity* tool (matches the design bundle), has no band/policy/color-cap check, so role-keying it is a category error; catching a bad design is upstream (design-tuning/review) and it already cedes the content lane to them. Wave commits: 0539fd7a + d43830e8 + b0d5a196 + 86e737cd + `81568ed9` (the 3 remaining audit follow-ups: icon-cluster / colored-icon-circle check, centered-card-on-gray §5 row, and a §11 dropdown role+check) — all pushed and all distributed by three 2026-06-06 13-project syncs (verified in inbound-flow). **design-tuning audit fully closed.** Only residual: heuristic-threshold tuning of design-review-pr's new DOM harvests (`bigNumberBlocksAbove` fontSize≥22, `segBars`/`rainbowBars` selectors) after they run against a real PR or two — conservative by design, may need widening.). Prior 2026-06-02: inbound-flow session — design-implement **content-lane boundary** `commit 7a09717c` — the grid is treatment-only and compares against a mock-data bundle, so formatter/enum-driven canonical-identifier cells (marketplace/supplier/ASIN/…) whose value is wrong only on a real-data variant the bundle never contained were shipping invisibly: bundle `UK→UK` matches impl `UK→UK` while live leaks `amazon_us`. Fix cedes the content lane rather than faking certification — step-02 §3d tags value_source, step-03 §2c marks such cells CONTENT-LANE-UNVERIFIED (not a delta), step-04 §5/§9 adds `deferred(content-lane)` + a mandatory "Content-lane verification owed (live page)" report section routing to design-review §13(a) / design-tuning §2b. Root-cause class: contract-dimension-gap (dimension CEDED, not checked). **Pushed to `myfork/custom`; 13-project `sync bmad` still owed** — design-implement is an existing workflow so no new slash-command wrapper, sync just refreshes the workflow text.). Prior same day: NEW `verify/data-quality-audit` workflow `commit d9f099a6` — the detection front door that feeds the `data-quality` root-cause rule `72849dae`; pushed to `myfork/custom` + full 13-project sync, `/bmad:bmm:workflows:data-quality-audit` wrapper generated everywhere). Prior 2026-06-01 (design-elevation step-04 shape-axis routing + Claude Design prompt route `commit 657913cd`; upgrade-bmad.sh cleared the 2-commit upstream drift). Prior same day: worktree-aware discovery: design-tuning `84b31972` + follow-up across design-synthesize/artifact-loop/elevation `commit efdfa028`). Prior same day: design-handoff detail interaction-verb composition fit + capability completeness `commit 20738ae9` (**not yet pushed/synced** — author-only that wave). Prior same day: design-handoff composition-fit check / policy-default bias guard `commit c33a3b24`. Prior same day: accounting-tools session (design-handoff analytics presentation rationale companion artifact `commit 705bb912`). **A full 13-project `sync-bmad-workflows.sh` ran on 2026-06-01 — this distributed this wave AND cleared the prior broad-sync backlog** (analytics-archetype wave `29f6c713`/`077b5fe9`/`e8b5ef7a`, data-quality `72849dae`, design-implement multiplicity/apply-ledger axes, bmad-deploy auth pre-flight). Prior (2026-05-31): data-quality root-cause `72849dae` + CLAUDE.md adapter PR #946; operational-analytics-band reframe `e8b5ef7a`, design-review-pr C-ARCHETYPE-01 `077b5fe9`, design-handoff archetype axis `29f6c713`.

## Shipped Features (archived 2026-06-12 — verbatim, pre-trim)

## Shipped Features

- [x] **design-handoff §13 linked-records mandate carried into the brief (the link-to-record lane)** — `custom/workflows/design/design-handoff/steps/step-01-gather.md` (§3a Linked-records inventory + `{linked_records_inventory}` COMPLETION field) + `step-03-generate-brief.md` (AVAILABLE STATE input + brief **§2a Linked records & lookups** + self-review item + success metric) (commit `155cdca2`, 2026-06-09). **Surfaced by the user:** "policy emphasis on the link-to-record lane — handoff does not seem to give it the space it needs." design-policy **§13 v5** (`ec4272e0`, same day) promoted cross-surface linking from a coherence *check* to a contract-critical **§12 positive assertion `design-review-pr` hard-fails** — every on-screen value that IS a record owned by another surface (ASIN/SKU/order/batch/shipment/supplier/customs-entry/listing) must be a navigable lookup/link (Airtable *behavior*: click foreign reference → drill; pull related fields as read-only lookups — never Airtable's *form*: no blue link pills/CTA/grid chrome, links stay quiet per §4). design-handoff carried it **nowhere**: §2 Domain Data held relations *descriptively* (a "Relationships:" line + FK notes in the Notes column), the brief deliberately strips the existing layout (blank-canvas principle) so the one place the linking was visible disappears, and a blank-canvas redesign defaults foreign references to **inert duplicated text** → fails `design-review-pr` §13 review-test on the way back in. The two natural homes both missed it — §2 is descriptive (a fact, not a mandate), §4c Surface Topology is page-to-page route-splitting (wrong granularity: value-to-record vs page-to-page). Root-cause class **contract-dimension-gap** (the relational-linking axis absent from the brief contract; same shape as the analytics-rigor/decision waves). **Fix:** step-01 **§3a** derives `{linked_records_inventory}` mechanically from the just-mapped data surface (start from §3-step-4 flattened FKs/natural keys) — per entry: foreign reference · owning surface+route · drill target (row/value → §7 detail drawer **+** "Open full {sibling} →") · inline read-only lookups resolved from the canonical record (never re-keyed); **empty ONLY for a true leaf surface** (empty-by-omission is the silent failure the step exists to prevent). step-03 **§2a** renders the inventory as a labeled lane (mirrors §4b/§4c — render-iff-non-empty, full omission with no placeholder otherwise) carrying the §13 quiet-styling guardrail + round-trip review-test; a self-review checklist item + a success metric ensure a shared-data surface is never handed off silent on §13. **Honesty gate:** a value no surface owns yet is a plain value, not a fabricated link target (mirrors `no-silent-fallbacks` / the rigor & decision honesty gates). Self-reviewed Mode 1: **0 blocking** (producer-side brief *body* → 6 intake checks + 10-field provenance N/A — `{linked_records_inventory}` is step-state, Block A/B untouched; decision-autonomy only — derives from schema FKs, no intent inference; sync-safe — fork `custom/workflows/` edit, not the project `_bmad/` copy; no `workflow.md` brief-section manifest to keep in sync; step-03b's internal "§2a" is a different namespace, no collision). markdownlint + full fork pre-push suite green; pushed `myfork/custom`. **Synced 12/13 projects 2026-06-09** (bison-ops pre-existing CLAUDE.md-section block; inbound-flow markers verified: §3a, `{linked_records_inventory}`, brief §2a, self-review + metric). **Follow-ups:** (1) §13 v5's own §9 note flags the linked-record mandate SHOULD propagate to **accounting-tools** — that project's next session applies it (not auto-edited here); (2) existing shared-data surfaces rendering foreign records as inert text now diverge from policy → route to **apply-design-policy-change** (per §13 v5 downstream note); (3) consider a `design-review-pr` capture of brief §2a (a `{brief_linked_records_map}`, the §13 sibling of `{brief_archetype_map}`/`{brief_rigor_map}`) so the PR enforcer reads the declared linking rather than re-deriving — `design-review-pr` already enforces §13/§12 intrinsically, so this is an optional tightening, not a gap.
- [x] **decision-analysis — the executive/quant-desk layer above rigor (capital-commitment surfaces only)** — new `decision-analysis` skill (project `.claude/skills/`, hand-apply fallback) wired across `custom/workflows/design/shared/analytics-archetypes.md` (shape→depth→**decision** cross-link) + `shared/analytics-rationale.md` (§3c decision spec + self-check) + `design-handoff/steps/step-01-gather.md` (§5c-3 new `{is_capital_decision}` gate + invoke + COMPLETION) + `step-03-generate-brief.md` (brief **§4e** Decision analysis + self-check) + `step-03b-emit-rationale.md` (§3c emission) + `design-review-pr/` (workflow.md **`C-DECISION-01`** 5th intrinsic check + `{brief_decision_map}` + step-01 §7 capture + step-04 §1b-3 + coverage) (commit `169520f4`, 2026-06-09). **Surfaced by the user's question "is it executive data-analyst level or not?"** — honest answer was no: `analytics-rigor` gets a surface to *senior-analyst* grade (honest ranges, base rates, deciding fields, named gaps) but stops at descriptive. A surface that **commits a scarce resource under uncertainty** (a buy/reorder/sizing decision) needs a decision-theoretic rung: model the outcome distribution, size to the loss tail, name the breakeven driver. Root-cause framing: the rigor skill is a *presentation-honesty* discipline, not a *decision-analysis* one — so its ceiling is one rung below executive by design; the fix is a third skill, not a bigger rigor. **The stack is now shape → depth → decision:** archetype picks the shape, rigor makes every figure honest, decision-analysis turns honest figures into a modelled, sized bet. Eight decision moves (frame the bet · model the outcome distribution P(success)/EV/P10/P90 · size to the loss tail · breakeven driver · compute the reference class/outside view · weight the decision-relevant regime · value-of-information · asymmetry). **Narrowly gated** — `{is_capital_decision}` is stricter than `{has_decision_numbers}`: a dashboard/coverage/status surface carries decision numbers (§4d) but commits nothing, so it has **no** §4e and `C-DECISION-01` is a silent no-op. **Model-honesty gate (paramount, mirrors rigor's no-fabricated-interval):** an un-modellable decision (key probabilistic input missing AND no computable reference class) ships as an honest `single-scenario` read + a named VOI gap, **never** a fabricated outcome distribution — a confident fake P(success) is the worse failure; `C-DECISION-01`'s hard finding fires precisely on an unsized BUY/PASS or a confident probability the brief flagged `single-scenario`. Self-reviewed Mode 1: **0 blocking** (rationale §3c out of scope for `brief-revision-policy.md`; brief §4e is body not frontmatter; worktree-safe via documented fallback; C-DECISION-01 mirrors the C-RIGOR idiom). markdownlint + full fork pre-push suite green; pushed `myfork/custom`. **Synced 12/13 projects 2026-06-09** (bison-ops pre-existing CLAUDE.md-section block; inbound-flow markers verified: §5c-3/`{is_capital_decision}`, brief §4e, C-DECISION-01). **Follow-ups:** (1) the `decision-analysis` SKILL.md (like `analytics-rigor`) is **project-local, NOT fork-distributed** — same drift/wipe-exposure class flagged in `71a961e9`; the other 12 projects use the hand-apply fallback; a fork-synced skill lane for all three presentation skills (architect/rigor/decision) is the durable fix; (2) `analytics-rigor` SKILL.md's "composes with" section does not yet name `decision-analysis` as the next rung (cosmetic cross-ref; the fork wiring already chains them); (3) the worked proof is the in-browser Monte-Carlo demo (`/tmp/inbound-flow-probabilistic.html`) — productionising it as the lead-detail decision panel is the open implementation step.
- [x] **analytics-rigor gate broadened — decision surfaces, not just bands (follow-up to `796fd990`)** — `custom/workflows/design/design-handoff/steps/step-01-gather.md` (§5c-2 regated on new `{has_decision_numbers}`, per-surface, COMPLETION) + `step-03-generate-brief.md` (§4b·F removed → surface-level **§4d Analytic depth**; §4b self-check reverted to 5 subsections + new §4d self-check; line-673 detail-no-band absolute reconciled with §5e) + `design-review-pr/` (workflow.md `{brief_rigor_map}` + `C-RIGOR-01` now read **brief §4d** not rationale §3b; step-01 §7 + step-04 §1b-2 + coverage) + `shared/analytics-rationale.md` (§3b reframed as reasoning record, §4d is the enforced contract) (commit `3e994a6f`, 2026-06-09). **Surfaced by the user asking "does the handoff call it appropriately?" — it didn't, for the motivating case.** `796fd990` gated rigor on `{has_analytics_band}`, but step-03's own self-check asserted *"a `detail` page must NOT carry an analytics band"* → `has_analytics_band=false` on detail pages → **§5c-2 never fired for lead-detail (a detail buy surface), the exact surface the skill was built for.** Three gaps + one pre-existing contradiction: (A) **gate** too narrow/contradicted; (B) **scope seam** — §4b·F covered only band figures, but the worst offenders (verdict `+£840`, econ `ROI 42%`) live in the §4a record/hero, outside the band; (C) **not per-surface** while §5c is; (D) the line-673 absolute contradicted §5e (analytics-rich detail carve-out) — drift the §5e wave (`3e1be5ca`) left behind. Root-cause class **contract-dimension-gap** (rigor scoped to the *band* dimension; "carries decision-bearing numbers" is the broader dimension). **Fix:** new `{has_decision_numbers}` gate (`has_analytics_band` OR a detail/analytical decision/verdict/KPI surface; NOT pure data-entry/CRUD), fired per-surface; rigor lifted from §4b·F to a **surface-level brief §4d** covering every decision figure wherever it sits (band values AND record/hero/verdict); `C-RIGOR-01` reads the **brief §4d** (not rationale §3b) so a **bandless decision surface — which has no rationale — is still enforced**; line-673 corrected to "a detail page CAN carry analytics per §5e; what it must not carry is a *dashboard*." Honesty gate preserved (data gaps surfaced, never fabricated). Self-reviewed Mode 1: **0 blocking**. markdownlint + full fork pre-push suite green; pushed `myfork/custom`. **Synced 12/13 projects 2026-06-09** (bison-ops pre-existing CLAUDE.md-section block; inbound-flow markers verified: `{has_decision_numbers}`, §4d, line-673 reconciled, C-RIGOR→§4d). **Follow-ups:** carry forward the three from `796fd990` (project-local skill distribution; no DOM harvest; lead-detail itself unredesigned) — all unchanged by this fix.
- [x] **analytics-rigor — the DEPTH axis of data presentation (shape vs depth)** — new `analytics-rigor` skill (project `.claude/skills/`, hand-apply fallback) wired across `custom/workflows/design/shared/analytics-archetypes.md` (shape-vs-depth cross-link) + `shared/analytics-rationale.md` (§3b rigor spec + self-check) + `design-handoff/steps/step-01-gather.md` (§5c-2 invokes the skill + COMPLETION capture) + `step-03-generate-brief.md` (brief §4b·F analytic depth + six-subsection self-check) + `step-03b-emit-rationale.md` (§3b emission) + `design-review-pr/` (workflow.md `C-RIGOR-01` intrinsic check + `{brief_rigor_map}` + step-01 §7 capture + step-04 §1b-2 eval + coverage) (commit `796fd990`, 2026-06-09). The analytics presentation family governed **shape** (`analytics-surface-architect` + taxonomy) and **pixels** (`design-policy`) but had **no member governing depth** — so a correctly-shaped, policy-clean band could ship every figure as a naked point estimate, no base rate, no trend magnitude, no one-line read (*correct and useless*). **Real example (user-surfaced):** the inbound-flow lead-detail buy surface — polished, compliant, schoolboy-grade analysis (six side-by-side widgets, every number bare, "share of *stock*" charted where "share of *sales*" decides, risk/opportunity as unranked free-text). Root-cause class **contract-dimension-gap** — the missing dimension was the depth axis of the presentation contract. **Fix:** `analytics-rigor` = the eight rigor moves (lead with the read · no naked decision number · sensitivity to drivers · base rate · deciding-field-not-handy · trend magnitude+dispersion · rank-by-impact · missing-vs-weak), wired across all three layers so depth is **built** (brief §4b·F), **recorded** (rationale §3b), and **enforced** (`C-RIGOR-01`, the depth counterpart to `C-ARCHETYPE-01`) — no specified-but-not-built or enforced-but-not-built gap. **Honesty gate (paramount, mirrors ground-or-flag / `no-silent-fallbacks`):** an uncomputable uncertainty/base-rate becomes a surfaced **data gap**, NEVER a fabricated interval — false precision is the worse failure; `C-RIGOR-01` never flags a named data gap as a defect, and is conservative (P1 only on the unambiguous naked-number case, else a precise human-judgment prompt). Self-reviewed Mode 1: **0 blocking** (rationale artifact is out of scope for `brief-revision-policy.md` → 6 intake checks + 10-field provenance N/A; brief §4b·F is body not frontmatter; worktree-safe via documented fallback; decision-autonomy only — refuses to fabricate rather than infer). `validate:refs` + markdownlint + full fork pre-push suite green; pushed `myfork/custom`. **Synced 12/13 projects 2026-06-09** (bison-ops blocked on pre-existing CLAUDE.md-section drift — recurring, unrelated; inbound-flow markers verified). Taxonomy + handoff + review-pr are workflow files projects load via `_bmad/bmm/workflows/`; no new wrapper, all existing workflows. **Follow-ups:** (1) `analytics-rigor` is project-local (inbound-flow `.claude/skills/`), NOT fork-distributed — same wipe-exposure/drift class as `analytics-surface-architect` (per the `71a961e9` follow-up) and the custom-agent lane; the durable fix is a fork-synced skill lane for both presentation skills; until then the other 12 projects use the hand-apply fallback; (2) no `analytics-rigor` DOM harvest in `design-review-pr` step-03 — `C-RIGOR-01`'s hard finding reads the diff/render, deliberately conservative (a "number with no adjacent uncertainty" harvest is false-positive-prone); add later only if PR-time depth enforcement proves too soft; (3) the lead-detail surface that triggered this is unchanged — a fresh design-handoff/review now produces a rigor spec for it (user deferred the leads redesign itself).
- [x] **Capability-drop hardening — derive capabilities (not recall), log + surface deliberate drops** — `custom/workflows/design/design-handoff/steps/step-01-gather.md` (§3 mutation-derivation audit + COMPLETION) + `workflow.md` (`{dropped_capabilities}` state var) + `steps/step-03-generate-brief.md` (brief §1 "Deliberately not carried forward" + §5 mandatory end-of-run disclosure + self-review item) + `custom/workflows/implement/design-implement/steps/step-04-apply-and-deliver.md` (§9 "Capabilities removed (orphaned actions)" report + metric + failure-mode) (commit `6b09432a`, 2026-06-08). **Root-cause class `contract-dimension-gap`** (the third in the design-handoff capability-capture line, after ingest `16e7eeab`). design-handoff DERIVES the data shape mechanically from the DB schema, so no field is missed — but capabilities were **recalled**, and the anti-bias "never read the current UI layout" rule discouraged the one inspection that surfaces a non-obvious capability. The ingest audit closed the record-CREATION subclass; this closes the sibling **existing-record-mutation** subclass (resolve/remap/override/re-run/reprice). **Real incident (user-surfaced):** the EOS batch-detail EAN→ASIN remap — `overrideWholesaleAsinAction` left with **zero callers** after the funnel redesign (`design-implement`) deleted `batch-row-detail-panel`/`batch-errors-table` (which hosted the catalogue-search→override remap + reason-grouped triage); the brief never named "remap" as a capability (it named "re-run enrichment"), so it fell through, and the implement step deleted the files without auditing the actions they called. **Fix (two layers):** (1) **design-handoff** step-01 §3 mutation-derivation audit — `grep` the current surface's components for every server action they invoke (verbs NOT layout → anti-bias preserved), each must resolve to a `{must_support_capabilities}` entry OR a logged `{dropped_capabilities}` drop (`capability·backing_action·reason: relocated|obsolete|out-of-scope-by-design`); step-03 renders drops in brief §1 + a **MANDATORY end-of-run user disclosure** (§5, vetoable) + self-review item. (2) **design-implement** step-04 §9 — a **mandatory "Capabilities removed (orphaned actions)"** report whenever the apply deletes/replaces components: grep for now-zero-caller actions among those the removed files called and disclose them. The grid-driven apply ledger **cannot** catch this because the lost capability was never a grid row — the orphaned-action grep is the backstop. Self-reviewed Mode 1: **0 blocking** (no brief consume/produce → 6 intake checks + 10-field provenance N/A; anti-bias preserved — captures verbs not layout; decision-autonomy only). Fork pre-push suite green. **Synced to inbound-flow + 11 others 2026-06-08** (bison-ops blocked on its pre-existing CLAUDE.md drift). **Follow-ups:** (1) consider a `brief-revision-policy` frontmatter flag for `{dropped_capabilities}` if it should be validated at intake (currently brief-body only, like `must_support_capabilities`); (2) the EOS remap itself is being restored via `design-brief-eos-batch-error-resolution-2026-06-08.md` (inbound-flow PR #1882, merged) — the new audit would have flagged it at handoff time; (3) `design-review-pr` has no orphaned-capability check (it audits a rendered page, not a diff that deletes files) — a `quick-dev`/`design-implement`-side concern, already covered by the step-04 §9 disclosure.
- [x] **design-handoff §5e Analytics Surface Hierarchy — rank multiple co-resident analytics surfaces** — `custom/workflows/design/design-handoff/steps/step-01-gather.md` (§5e NEW + §5c per-surface note + COMPLETION fields) + `step-03-generate-brief.md` (§4b.0 hierarchy block + detail-mode carve-out) (commit `3e1be5ca`, 2026-06-08). The handoff reasoned about analytics as ONE band per page: §5b (does *a* band belong), §5c (what shape is *the* band), §5a (the page's *one* primary composition), §5d (split across *routes*). Nothing ranked **multiple co-resident analytics surfaces** on a single page — so a page with several legitimate charts came back with no hierarchy and rendered as the flat equal-weight panel-stack policy §6/§5 explicitly ban, leaving design-review to catch it after the fact. **Real incident:** a legacy Keepa-style product detail page — buy-box-history (trend) + sales-rank (trend) + competition (trend) + competitor-stock (composition/ranking) + buy-box-ownership (composition/ranking), five panels at equal weight = a data archive, not an analyst's argument. **Two compounding causes:** (1) no ranking step; (2) step-03's detail-mode rule flatly asserted *"No analytics band: a single record has no aggregate dimension"* — an ABSOLUTE that actively suppresses analytics-rich detail pages (a product carries genuine aggregates: price/rank over time, competitor share). **Fix:** §5e (NEW) gates on ≥2 distinct analytics surfaces, **fires regardless of `page_mode` including `detail`**, and ranks each surface `hero` (1–2, full weight) / `supporting` (compact sparkline-strip) / `drill` (collapsed) **by the page's primary question, not the legacy render**; ground-or-flag **reuses §5d** (can't name one hero → `needs-tab-views`/`needs-sibling-route` — "if you can't rank it to one hero, it may not be one page"). §5c now runs once per surface on multi-surface pages; COMPLETION gains `{analytics_hierarchy}`/`{hierarchy_rationale}`/`{analytics_surface_inventory}`/`{hierarchy_unresolved}`. step-03 §4b.0 (NEW) renders the ranked hierarchy before the per-surface A–E specs (written hero→supporting→drill); the detail-mode "no analytics band" absolute softened to carve out the research/monitoring exception that routes into §5e. **Operationalizes existing policy** (§6 "the visual lead must be one or two restrained charts… supporting tables") — does not invent a rule; the principle existed, the handoff just never carried it into the brief. Third instance of the same root pattern (waterfall vocabulary → multi-surface ranking): the handoff's analytics reasoning kept being one-band-shaped. Self-reviewed Mode 1: **0 blocking** (no brief consume/produce; grounding gate satisfied via primary-question + §5d escape hatch; decision-autonomy only). Fork pre-push suite green. **Synced to inbound-flow + others 2026-06-08** (bison-ops blocked on its pre-existing CLAUDE.md drift). **Follow-ups:** (1) `design-review-pr` **`C-AHIER-01`** — verify the *rendered* hierarchy matches the *declared* one (hero visually dominant, supporting demoted), the analytics-hierarchy sibling of `C-ARCHETYPE-01`; recommended fast-follow so the decision is enforced at PR time, not just specified; (2) decide whether `{analytics_hierarchy}` warrants a brief frontmatter flag (`analytics_multi_surface: true`) for `brief-revision-policy` validation, or stays §4b body content (currently body); (3) the legacy Keepa page that surfaced this is unchanged — a fresh handoff now produces a ranked brief for it.
- [x] **`waterfall` analytics archetype — reconciliation-bridge split out of `flow`** — `custom/workflows/design/shared/analytics-archetypes.md` (new archetype + disambiguation) + `shared/brief-revision-policy.md` (allowed-values contract) + `design-handoff/steps/step-01-gather.md` + `step-03-generate-brief.md` + `workflow.md` (enums/count/form-hint) + `design-review-pr/steps/step-03-dom-render.md` (C-ARCHETYPE-01 row) (commit `71a961e9`, 2026-06-08). The archetype taxonomy had **eight** shapes and `flow` was overloaded to cover BOTH attrition-funnels AND running-total reconciliations. A dataset shaped `opening − Δ − Δ = closing` (real incident: inbound-flow EOS batch-detail conversion run, 642 uploaded → −131/−41/−82/−57 → 331 ranked winners, headlined by a single £4,790 outcome figure) could only map to `flow`, whose prescribed form is *"a funnel with drop deltas"* — so `analytics-surface-architect` selected `flow` **correctly** and design-synthesize rendered a funnel **correctly**, producing a funnel wearing a waterfall's data: the reconciling deltas double-encoded as both amber bar-segments AND separate reason chips, and the corporate-native bridge shape unreachable because it had no name. Root-cause class **contract-dimension-gap** — the missing dimension lived INSIDE the archetype vocabulary, not the §5c wiring (that was already correct; my first-pass diagnosis of "skill is orphaned" was wrong and was corrected by reading the files). **Fix:** new `waterfall` archetype (anchored opening/closing bars + signed labelled steps that reconcile; reasons live in the step's drill, not a duplicate chip) + a `flow`-vs-`waterfall` disambiguation (independent sequential gates where survival-rate-per-gate is the story → `flow`; one population reconciled to a closing total by signed deltas → `waterfall`) with a **question-driven tie-break** when both fit (single reconciled headline number = the point → lead waterfall; which-gate-leaks-and-why = the point → lead flow) — deliberately NOT swapping one default for another. Plumbed through every closed enum so a brief can actually emit it and the enforcer can check it. Self-reviewed Mode 1: **0 blocking** (no brief consume/produce → 6 intake checks + 10-field provenance N/A; additive to the taxonomy; worktree-safe). Fork pre-push test suite green. **13-project `sync bmad` OWED** (taxonomy + handoff text are workflow files projects load via `_bmad/bmm/workflows/` — invisible to project sessions until sync; no new wrapper, all existing workflows). **Follow-ups:** (1) the project-local `analytics-surface-architect` **SKILL.md enum still lists eight** (line 3 description, line 16 "eight archetypes", line 38 output-contract enum) — trust-hierarchy "taxonomy wins" makes it functionally correct after sync, but the output-contract enum is stale and the skill is **not fork-managed** (same wipe-exposure/drift class as the custom-agent lane); promoting the skill (or at least its archetype enum) into a fork-synced lane is the durable fix; (2) the EOS batch-detail screen that triggered this is still rendered as a funnel — re-running design-handoff/design-review on it now selects `waterfall`; (3) `design-synthesize` has no archetype-specific synthesis guidance for `waterfall` yet (it reads the taxonomy form, which is now sufficient, but a worked bridge example could be added if reuse demands).
- [x] **Vera (data-integrity-lead) — interactive front-door agent + NEW custom-agent sync lane** — `custom/agents/data-integrity-lead.md` (NEW persona) + `sync-bmad-workflows.sh` (`sync_agents_for_project` + 3 call sites) (commit `0cd582d4`, 2026-06-06). The data-integrity workflow family (`scrape-coverage-audit` / `webhook-contract-check` / `data-quality-audit` / `wire-check` / `trace-flow`) had no **front door** — the user had to know which audit fit. Vera is the router persona: describe a symptom in plain words ("Order Date empty for every row", "counter stuck on 0 after a handoff", "the webhook keeps getting rejected") and she confirms the right audit, lists inputs, and runs it inline. Modeled on the `jules-frontend-lead` persona (greet → menu/symptom → workflow-handler → "dismiss"); embodies the `webhook-contract-charter` discipline (fail-loud, no silent fallback, whole-column-empty = P1); power users skip her and call `/bmad:bmm:workflows:<name>` directly. **New fork capability:** `custom/agents/` is a distribution lane that didn't exist — the existing custom personas (`jules`/`rowan`/`design-pm`) were project-local orphans with no slash wrappers. `sync_agents_for_project()` mirrors `custom/agents/*.md` → `_bmad/bmm/agents/<name>.md` AND generates the `.claude/commands/bmad/bmm/agents/<name>.md` wrapper, so a custom persona becomes invokable as `/bmad:bmm:agents:<name>` everywhere — same shape the upstream installer emits. Additive (skill/script/workflow sync untouched), `bash -n` clean, check-mode verified (flagged Vera as drift in all 13). Fork pre-commit suite green. **Synced to inbound-flow + 2 others; 10 projects blocked by PRE-EXISTING `meta/` local-only drift (NOT this change — block check only scans workflow SYNC_DIRS, not `_bmad/bmm/agents/`).** **Follow-ups:** (1) **a parallel session is mid-build on `custom/workflows/meta/create-agent/` (untracked)** — that overlaps this manual agent + sync-lane work; reconcile so we don't ship two agent-authoring paths; (2) Vera's distribution to the other 12 completes on the next clean broad-sync once the `meta/` backlog clears (`--pull`/review/re-sync, not `--force`); (3) the 3 orphan personas (`jules`/`rowan`/`design-pm`) could be promoted into `custom/agents/` so they distribute + get wrappers too; (4) candidate skill `references/custom-agents.md` documenting the new lane.
- [x] **webhook-contract-charter — the declarative contract of record for webhook boundaries** — `custom/workflows/shared/webhook-contract-charter.md` (NEW) + `verify/webhook-contract-check/workflow.md` (new CRITICAL RULE + Input-section citation) (commit `1142471a`, 2026-06-06). The authoring/policy companion to the `webhook-contract-check` enforcer (`ccab668d`): that workflow *verifies* a payload contract sender→receiver but derived the contract **ad-hoc each run**, with the obligations living implicitly across its CRITICAL RULES, the inbound-flow `feedback_no_silent_fallbacks` memory, and the schema registry. This charter writes the discipline down **once, declaratively** — sender duties / receiver duties / the **sender-strict·receiver-lenient rollout-order rule** / breaking-change taxonomy / **fail-loud-no-silent-fallback at the boundary** / ambiguity ownership / a **per-boundary contract template**. Lives in `workflows/shared/` (universal scope, like `deployment-to-prod`/`delivery-to-main`). `webhook-contract-check` now cites it as its **contract of record** (new CRITICAL RULE; Input-section default when no per-boundary contract artifact exists), so enforcer + authoring workflows judge against one written standard. **Real origin:** inbound-flow session — user asked to "formalise this approach" for cross-app webhook contracts after a TheFBAPrep no-catalog-match data-quality investigation; I halted the reflex to build a *new* "discussion" workflow once the existing enforcer was found, and scoped the genuine gap to a policy artifact instead. Self-reviewed Mode 1: **0 blocking** (no brief consume/produce → 6 intake checks + 10-field provenance N/A; worktree-safe — cited by post-sync installed path `_bmad/bmm/workflows/shared/`). Fork pre-commit test suite green. **Synced to all 13 projects 2026-06-06.** **Follow-ups:** (1) an optional guided `author-contract` mode (per-boundary contract *minting* workflow) was deliberately NOT built — the charter's template covers minting by hand; add only if reuse demands it; (2) advances follow-up (2) of the `ccab668d` entry — webhook-contract-check now has a written standard — but "exercise against a real sender/receiver pair" remains open; (3) candidate `references/webhook-contract.md` + always-fork-trigger entry in this skill if the charter sees reuse.
- [x] **scrape-coverage-audit + webhook-contract-check — two verify-lane workflows for the silent-data-loss class** — `custom/workflows/verify/scrape-coverage-audit/` (workflow.md + 4 steps) + `custom/workflows/verify/webhook-contract-check/` (workflow.md + 4 steps) (commit `ccab668d`, 2026-06-06). Authored after a long bison-ops amazon.es inbounds-scraper session that surfaced a recurring **silent failure / null-conflation** class the existing verify workflows didn't catch at the record-set level (Order Date null for 100% of rows because the date extractor only knew English "Order placed"; cancelled/load-error orders emitting blank `Unknown` rows; a deals-carousel nearly leaking its prices in as order data). **scrape-coverage-audit** fills the missing altitude **between `trace-flow` (one pipeline) and `data-quality-audit` (one dimension): the RECORD SET** — derives the extraction contract from the app's own export schema/type, runs a live sample into a per-field **coverage matrix**, surfaces whole-column-empty (`0/N`) as P1, then drives Chrome to the live source page for a **null-conflation verdict** (present-but-dropped → producer fix / genuinely-absent → document / fetch-failure → retry-fallback) — `data-quality-audit`'s render-gap-vs-data-rot re-pointed at scraping. **webhook-contract-check** is the cross-service analogue of `wire-check`: walks the payload contract sender→receiver field-by-field, then adds the dimension wire-check lacks — **rollout-safety under the sender-strict/receiver-lenient discipline** (rollout-unsafe-addition ⇒ receiver-lenient-first; rollout-unsafe-removal ⇒ receiver-drops-requirement-first), keyed on the receiver's live unknown-field posture. Detect-and-report-and-route; never auto-edits across two deploys. Both: hard grounding gate, per-field disposition (silent-partial guard), house style mirrored from their templates. Authored by two parallel sub-agents, then **independently Mode-1 reviewed: 0 blocking each**. Fork pre-commit test suite green. **Sync to the 13 projects: pending (this session).** **Follow-ups:** (1) consider adding both to the skill's always-fork-trigger list (parallels `maintenance-triage`/`design-elevation`); (2) `webhook-contract-check`'s rollout-safety rules are original (no sibling precedent) — exercise once against a real sender/receiver pair before treating settled; (3) `scrape-coverage-audit` step-03 hard-codes the `mcp__claude-in-chrome__*` tool names by reuse of the design lane — shares that lane's rename-drift surface.
- [x] **design-review + design-review-pr — operationalize policy §13(a) identifier formatting (the named recurrence follow-up of `62f01df5`)** — `custom/workflows/design/design-review/steps/step-01-audit.md` (§3 capture + §5 compare) + `design-review-pr/` (workflow.md intrinsic-checks + step-02 §5 source arm + step-03 §3c dom-render arm + step-04 §1c/§5 deliver) (commit `246eec52`, 2026-06-02). The design-tuning §13(a) fix (`62f01df5`) flagged the same gap almost certainly existed in the two sibling review workflows — neither mentioned §13's "Canonical identifier" clause at all. Closed both. **design-review** (live-page Chrome audit): step-01 §3 now captures each canonical-identifier class's rendered form (works in all three measurement modes incl. `screenshot-only`, so it is degraded-safe), §5 adds the §13(a) consistency compare (cross-surface casing/format divergence + raw-enum leakage), severity proportional (systemic canonical-class = hard failure, one-off = minor). **design-review-pr** (PR-time enforcement): new workflow-intrinsic check **`C-IDENTFMT-01`**, modeled on `C-ARCHETYPE-01` but **three-arm so it is never silently skipped** — cheap **source-grep** advisory (step-02 §5, covers the Chrome-down case the design-tuning miss taught us about), authoritative **dom-render** harvest (step-03 §3c, P1 on a clear cross-surface divergence, with a casing-variant detector + raw-enum detector), and a **human-judgment** fallback prompt + coverage-notes line (step-04 §1c/§5). Ambiguous harvests defer to the human prompt (no false-fire, per the workflow's own anti-noise discipline). Self-reviewed Mode 1: **0 blocking**; faithful to the C-ARCHETYPE-01 idiom; no project-checklist dependency (the check is workflow-intrinsic + self-deriving from the rendered page). Fork pre-push test suite green. **Synced to all 13 projects 2026-06-02.** **Follow-up:** `data-quality-audit` remains the complementary *data-rot* detector (this pair catches the *render* form); no further sibling carries this gap (design-tuning/review/review-pr were the three §13 consumers).
- [x] **design-tuning — operationalize policy §13(a) "canonical identifier" formatting (the missing twin of §2a)** — `custom/workflows/design/design-tuning/steps/step-01` (§1e) + `step-02` (§0a third lane, §1b surfaces, new §2b, §7, metrics, failure-modes) + `step-03` (self-review) + `workflow.md` (SOURCE-OF-TRUTH note) (commit `62f01df5`, 2026-06-02). Root cause class **contract-dimension-gap**: step-02's comparison contract was missing the identifier-*formatting* axis. §2a operationalized only §13's "shared component language" clause (pill/chip/drawer *treatment*); §13's **"Canonical identifier — reads/formats the same way everywhere; do not relabel/reformat/re-key per surface"** clause had **no check**. **Real incident:** inbound-flow `/supply-sources` design-tuning iter-1 — the same brand/marketplace rendered three ways on one surface (supplier `amazon` lowercase, `marketplaceBuy` `AMAZON_ES` raw enum, `marketplaceSell` `Amazon UK` title-case), and the brief §2 documented `marketplaceBuy` as a label "(e.g. 'Amazon DE')" while the app shipped the raw enum — yet the run reported "0 hard failures." The reviewer (me) bucketed value-casing as "data, out of scope" because §0a's binary (treatment=sub-pixel-source / composition=layout-gestalt) had **no lane** for the literal rendered strings. **Fix:** §0a gains a third **content lane** — *three lanes but only two evidence sources*; content is screenshot-evidenced and sits on the rendered side, so the V18 source-precedence carve-out is untouched, and crucially it **runs at full strength in `screenshot-degraded` mode** (the miss happened with no artifact URL — the new check must not depend on the bundle). New **§2b** does the work: part (a) internal-consistency across cells/surfaces (no brief dependency — primary catch) + part (b) documented-format match vs brief §2 (`{identifier_format_expectations}` from new step-01 §1e). Severity **issue** by default, **escalates to hard-failure** for systemic canonical-identifier-class inconsistency (the badge-shape version was already a §5 hard failure; this is its text twin). Self-reviewed Mode 1: **0 blocking**; V-ID lineage additive, V18 carve-out preserved (workflow.md reconciled "two evidence sources / third rendered-side lane"), degraded-mode honesty *strengthened* (a screenshot-settleable check is correctly not downgraded). Fork pre-push test suite green. **Synced to all 13 projects 2026-06-02.** **Follow-ups (recurrence risk — same gap likely elsewhere):** `design-review` and `design-review-pr` audit live pages / enforce §13 at PR time and almost certainly have the same identifier-formatting blind spot (no content-lane / §13(a) check) — flag for the same treatment; `data-quality-audit` overlaps (it catches the *data-rot* form of this; §2b catches the *render* form — the §2b "raw enum where a label is expected" sub-case is a render gap, the two are complementary).
- [x] **`verify/data-quality-audit` — detection front door for value-level data defects** — `custom/workflows/verify/data-quality-audit/` (workflow.md + steps/01–04) (commit `d9f099a6`, 2026-06-02). New verify-family workflow. Reads a controlled-vocabulary dimension (supplier, marketplace, currency, status, …) **or a live-UI symptom**, runs production values through the **app's own canonical normalizer** (never hand-rolled SQL — the discipline that keeps the audit from disagreeing with the app), and classifies each finding. The load-bearing distinction: **RENDER GAP** (data is correct, the UI hides the distinguishing attribute → fix the component) vs **DATA ROT** (values genuinely collapsed/drifted → fix the producer). Render gaps route to design-review/wire-check; data rot routes to `maintenance-triage`/`quick-spec` framed as a **producer fix, not a backfill** — i.e. this is the *detection* complement to the `data-quality` root-cause rule (`72849dae`), which governs the *fix*. **Real origin:** inbound-flow Supply-orders "Same product across N supplier lanes" drawer showing two `amazon` lanes with different costs — turned out to be a render gap (`AMAZON_FR` + `AMAZON_ES` correctly distinct, drawer rendered only the supplier label). The user pushed back that a one-off `tsx` audit script was the wrong shape for a BMAD-native, slash-command-driven project — this workflow is the right interface; inbound-flow's existing `scripts/audit-supply-source-quality.ts` becomes its discovered engine, other projects generate one inline. **4 steps:** identify-dimension (grounding gate that **fires even in autonomous_mode** — auditing the wrong field is confident nonsense) → run-audit (prefer a vetted engine, else read-only inline through the real normalizer) → classify (render-gap vs data-rot per finding) → route (per-finding disposition table incl. benign rows — silent-partial guard). **Not a mode of trace-flow:** trace-flow maps pipeline *topology* for one anchor; this audits *values* of a dimension. They overlap only on the render-gap finding, which this routes (doesn't re-map). Self-reviewed Mode 1: **0 blocking**; no brief consumption/production (6 intake checks + 10-field provenance N/A); decision-autonomy-only. `test:refs`/lint green via fork pre-push. **Synced to all 13 projects 2026-06-02** (slash wrapper generated). **Follow-ups:** `dispatch-followups` could gain a data-quality-audit entry so routed findings show in the post-wave dispatcher; `design-review-pr` has no value-quality hook (none needed — it enforces policy/layout, this audits stored values); skill `SKILL.md` triggers + a `references/` note for the new verify-family workflow are candidates if it sees reuse.
- [x] **shared/claude-design-prompt.md — Claude Design prompt emission factored to one SoT** — `custom/workflows/design/shared/claude-design-prompt.md` (NEW) + `design-elevation/steps/step-04` §3a (pointer) + `maintenance-triage/steps/step-03` design lane (pointer) + both `workflow.md` frontmatters (`claude_design_prompt` ref) (commit `8f550012`, 2026-06-01). The named follow-up of `657913cd`/`83c9355e`: both added the same "emit a focused Claude Design paste prompt" route with the prompt's structure duplicated as prose in two step files. Consolidated per the fork's one-place-not-four principle (cf. `analytics-archetypes.md`, `analytics-surface-architect`). The SoT defines the prompt's required structure (connect line / files to read / keep-as-is guard / the change + core-job facet / sibling pattern w/ grounding caveat / policy constraints), the save-path convention (`claude-design-prompt-{surface}-{enhancement}.md`), the **brief-descendant-not-revision** rule (no Block A, no supersede, no 6 intake checks — intent unchanged), and **always-emit-never-invoke** (Claude Design is human-in-loop). Both call sites now POINT to it and keep only their route-specific framing. A format change now happens in one place. Self-reviewed Mode 1: 0 blocking; `test:refs` green (new frontmatter ref resolves). **Synced to all 13 projects 2026-06-01.**
- [x] **maintenance-triage shape gate — design-shaped clusters route to the design family, not code** — `custom/workflows/implement/maintenance-triage/steps/step-03-emit-tech-specs.md` §1/template/hand-off/failure-modes + `workflow.md` (description/architecture + design-family path refs) (commit `83c9355e`, 2026-06-01). **Sibling of `657913cd`** (design-elevation), same root cause class **contract-dimension-gap**, surfaced as the named follow-up of that wave. step-03 routed every prioritized cluster to `quick-dev` or `quick-spec` — both CODE lanes — so a triaged signal whose real problem is the *surface* ("the orders drawer can't act on a discrepancy", "the staged review page feels cluttered") was mis-framed as a code tech-spec. **Fix:** added a **shape gate** to §1 (code-shaped vs design-shaped, decided first; straddle clusters split into a design part + a code part). Code lane unchanged (quick-dev concrete / quick-spec investigate). NEW **design lane**, same concrete-vs-investigate split one lane over: concrete → a focused **Claude Design paste prompt**; needs-investigation → **design-review** (audit) or **design-elevation** (settled-surface deepening); "when in doubt → design-review" mirrors "when in doubt → quick-spec." triage-spec frontmatter gains `shape: code|design` + route enum `claude-design-prompt|design-review|design-elevation`; suggested-next-workflow + hand-off are route-aware ("paste into Claude Design" vs "run quick-dev"); design routes carry no rollback (no code yet, returns via design-implement/quick-dev). Self-reviewed Mode 1: **0 blocking**, 1 nit (relies on design-review/elevation present in all 13 — they are, sync distributes the `design/` tree). **Synced to all 13 projects 2026-06-01.** **Follow-up:** the §3a (design-elevation) / design-lane (triage) Claude-Design-prompt emission is now duplicated prose in two workflows — candidate to factor into `shared/` if a third consumer appears.
- [x] **design-elevation step-04 routes by shape (design-shaped vs code-shaped), not just scope** — `custom/workflows/design/design-elevation/steps/step-04-classify-and-route.md` §1/§3a/§3b/§4 + `workflow.md` ({routing_plan} + architecture line) (commit `657913cd`, 2026-06-01). Root cause class **contract-dimension-gap**: step-04's routing contract had ONE axis (intent-change vs in-surface refinement), so every in-surface refinement was forced to `quick-spec` — a *code* spec. A **design-shaped** refinement (a redesigned drawer region, a new commit footer, a verify-against-source layout) was therefore mis-routed as code when it should be authored in **Claude Design**. **Real incident this session:** inbound-flow Supply-orders (`/orders`) elevation pass — the recommended "reconcile-the-gap" enhancement is a drawer redesign (mirror the sibling *Staged orders review* "Correct & approve / Verify against source" pattern), but step-04 as written would have routed it to quick-spec. The user flagged it directly ("I can't keep asking you… this is a workflow issue"): they kept having to manually re-split *"paste this into Claude Design"* vs *"wire this in code"* every pass. **Fix:** added the missing axis (Axis B: design-shaped vs code-shaped) + a third route. §1 classifies on TWO axes; a straddling in-surface item splits into a design-shaped part (§3a) and a code-shaped part (§3b) rather than going down one route. **§3a (NEW):** design-shaped in-surface refinement → a focused, **brief-descended Claude Design paste prompt** (connect line, files to read, keep-as-is guard, the `{core_job}` facet, sibling pattern to borrow w/ grounding caveat, policy constraints), saved to `claude-design-prompt-{surface}-{enhancement}.md`. It is a brief **descendant** (no supersede, no Block A, no 6 intake checks) since intent is unchanged — distinct from §2 intent-change which still re-briefs via design-handoff. Always emitted regardless of `autonomous_mode`; never invokes the external design tool (human-in-loop). **§3b:** code-shaped → quick-spec/quick-dev (brownfield §4b/§6 carried) with a trivial-wiring direct-branch escape. **§4** closing summary humanized: leads with plain-language disposition (*"Paste into Claude Design"* / *"Wire in code"*) with the internal class·shape·route as the trailing audit column. Self-reviewed Mode 1: **0 blocking**, 1 nit (new `claude-design-prompt-*` artifact family — style-consistent). Note: this **updates the routing described in the design-elevation entry below** (which said "in-surface refinement → quick-spec→quick-dev" — now split by shape). **Synced to all 13 projects 2026-06-01** (re-sync after the upgrade's earlier sync, which predated this edit). **Follow-ups:** `maintenance-triage` may share the same code-only routing assumption for design-shaped maintenance items (flag, not yet fixed); the §3a prompt skeleton could be factored to `shared/` if another workflow needs the same Claude-Design-prompt emission.
- [x] **design-tuning worktree-aware brief + policy discovery** — `custom/workflows/design/design-tuning/steps/step-01-load-context.md` §1 + §1b (commit `84b31972`, 2026-06-01). Root cause class **worktree-sync-drift**: a `design-handoff` brief and a `create-design-policy`/`onboard-design-system` policy are routinely authored in a feature worktree and iterated on *before* merge to main. step-01's discovery globs only looked at the main checkout (`ls -t {implementation_artifacts}/design-brief-*.md`; `ls docs/design-policy.md`), so when the brief+policy lived in a worktree the workflow concluded "no brief / no policy" and silently fell back to generic-guardrail review — the exact loader-drift §5 of that step forbids. **Real incident this session:** inbound-flow Supply-orders (`/orders`) tuning. `design-brief-orders-2026-06-01.md` and `docs/design-policy.md` v1 were both in the `feat+mapping-queue-cockpit` worktree; the run reviewed contract-less and whipsawed (lenient pass → manufactured-hard-failure under user pushback → corrected only once the brief was hand-located). Three findings raised contract-less were all wrong against the brief: a §4b-commissioned coverage band mis-flagged as a stat-card hard failure; two documented joins (import account, route warehouse) mis-flagged as "invented" because only the base table was checked; brief-commissioned EOS prominence mis-flagged as over-weighting. **Fix:** §1 sweeps `.claude/worktrees/*/_bmad-output/implementation-artifacts/design-brief-*.md` (matched by target_slug/route, gated by §1a provenance, caveat recorded) when the main tree is empty; brief-less runs are now explicitly *degraded* (validate every displayed field against schema/MCP + resolve documented joins before flagging "invented"). §1b adds the same worktree sweep for `docs/design-policy.md`. Self-reviewed Mode 1: 0 blocking. Scope deliberately limited to discovery — the band/§4b/§5 check text was NOT broken (it already says "one shared strip, not framed mini-cards"); the failure was *reading without the brief*, so re-wording checks would have fixed a symptom. **Follow-up DONE (`efdfa028`, same wave):** applied the same worktree sweep, per each workflow's intake model — design-synthesize (brief Shape-B slug auto-discovery + step-02 policy halt), design-artifact-loop (**policy only** — its artifact-first brief/screen-review contract is explicit-path-by-design and was deliberately NOT given a fallback), design-elevation (policy §2 + else-most-recent brief/screen-review §3, 6 intake checks preserved). Both commits synced to all 13 projects this wave.
- [x] **design-elevation scope-expansion workflow** — `custom/workflows/design/design-elevation/` (workflow.md + steps/01–04 + checklist.md) (commit `a9a0431a`, 2026-06-01). New design-family workflow that formalizes the **"what would make THIS even better"** pass after a surface is settled (post handoff/synthesize/implement/tuning). Built because the chain had a `design-tuning` (constraint-enforce) and a `design-handoff` (design-from-blank) but nothing that *deliberately expands scope* on a working surface — that enhancement list was improvised at the end of a session with no provenance back into the build loop. **Standalone, NOT a step on design-tuning** — design-tuning's identity rule ("never suggest design ideas of your own; you are a constraint enforcer") is the opposite posture, so bolting a generative step on would break its contract. **Architecture:** step-01 loads the settled surface + artifacts of record, runs the 6 brief intake checks, derives a one-sentence `{core_job}` everything is judged against; step-02 sweeps wide then runs the **anti-chrome filter** (reject additive/decorative/generic-completeness ideas, disclose each rejection with a reason — the filter's receipt); step-03 ranks by a 4-dimension **leverage rubric** (core-job proximity / loop-closure-&-earliness / effort-on-what-matters / build-cost), recommends a focused 1–3 subset, presents recommendation-first, then **HALTS for selection** — the one deliberate halt in the design family, because expanding scope is *intent autonomy* handed back to the user (explicit Autonomy Model section); step-04 classifies each selected item (**intent-change** → re-run design-handoff to supersede the brief per `brief-revision-policy.md`; **in-surface refinement** → quick-spec→quick-dev, brownfield §4b/§6 carried) and emits a per-item disposition table so nothing selected silently vanishes (silent-partial-implementation guard). `checklist.md` carries the leverage rubric + reject gates. Self-reviewed Mode 1: **0 blocking**, 1 non-blocking (no `policy_version` capture — same gap as design-tuning). **Not yet synced — broad 13-project `sync-bmad-workflows.sh` pending** (generates the `/bmad:bmm:workflows:design-elevation` slash wrapper in each target; invisible to project sessions until then). **Follow-ups:** consider a `dispatch-followups` entry so an elevation pass's routed items show in the post-wave dispatcher; skill SKILL.md trigger entry added this wave; design-review-pr has no elevation-specific hook (none needed — it enforces policy, elevation proposes).
- [x] **design-handoff detail interaction-verb composition fit + capability completeness** — `design-handoff/steps/step-01-gather.md` §5a (new question 4 + reworded carve-out) + §4 ({must_support_capabilities} capture + line-116 arrangement-vs-job reconciliation) + `steps/step-03-generate-brief.md` §1 (capability render) + §4a (detail/verification override variant) + self-review (2 new items) + `workflow.md` (1 state var) (commit `20738ae9`, 2026-06-01). Extends the composition-fit machinery (`c33a3b24`) to two axes it did not reach — both surfaced by inbound-flow's **Log Order** redesign (Claude Design bundle) coming back **more basic than the legacy `/orders/new` it replaced**: it dropped (a) the source↔fields verify layout, (b) product-image upload, (c) skip-staging. Diagnosis: same disease as `c33a3b24`, one axis finer. **(1) Detail interaction-verb axis:** §5a's carve-out ("for `detail`, the record-view default almost always fits") waved through detail surfaces whose verb is *verification-against-a-source*, which need the source **co-present** (value↔source), not a grouped-fields record view. New §5a Q4 routes data-entry/passive-review → `policy-default`, verify-against-source → `recommended-alt` (named: source-co-present verification layout). The bundle's own **CDS customs page** (extracted record left / source PDF right) is the in-system exemplar — proof the flow reaches this correctly when the brief frames the job as verification; Log Order's brief framed it as "capture a form," so the source collapsed after extract. **(2) Capability-completeness axis:** the anti-bias strip (step-01 line 116, "no Key interactions") removes the *arrangement* but was also dropping capability *requirements* (product-image, skip-staging: **0 mentions** across the 9 handoff chats). New `{must_support_capabilities}` captures must-keep jobs as outcomes (NOT UI mechanics); line 116 sharpened to draw the line at *arrangement-vs-job* — strip the arrangement, never the capability. **No new Block B field, no `brief-revision-policy.md` edit, no backward-compat risk** — Change A reuses the existing `composition_provenance` field (its Check 1a default + material-revision trigger already cover detail flips), Change B is brief-body content like `user_goals`. Self-reviewed Mode 1: 0 blocking, 3 non-blocking follow-ups below. **Not yet pushed to `myfork/custom`; broad 13-project sync pending.** **Follow-ups:** design-review-pr `C-CAPABILITY-01` + detail-composition enforcement (capabilities/detail-fit have no PR-time hook, only the step-03 self-review); `design-synthesize` detail verify-against-source rendering (reads §4a body but has no detail-specific source-co-present handling); §5a list-modes-vs-detail question split (Q1/Q3 are N/A for detail).
- [x] **design-handoff composition-fit check (policy-DEFAULT bias guard)** — `design-handoff/steps/step-01-gather.md` §5a + `workflow.md` (Anti-Bias Principle II + 2 state vars) + `steps/step-03-generate-brief.md` (Block B emit + §4a composition-override render + self-review) + `shared/brief-revision-policy.md` Block B (new field + semantics row + Check 1a backward-compat + material-change trigger) (commit `c33a3b24`, 2026-06-01). The anti-bias machinery guarded only LEGACY-LAYOUT bias; it missed **POLICY-DEFAULT bias** — a "redesign/blank-canvas" brief still stamps in the page-mode's default composition (operational→table-first worklist+drawer; analytical→chart-led; detail→record-view) without asking whether THIS surface's job fits it. Exposed by inbound-flow's **Product Mapping Queue** — a pull-based dispensing queue whose per-item job is a full-width identity comparison, forced into table+drawer because `page_mode=operational`, pre-deciding the wrong IA before first-principles ran (confirmed via an 8-agent IA judge-panel workflow: all 3 adversarial judges found table+drawer genuinely beaten, the ~400px drawer being the decisive failure). New `composition_provenance` Block B field (`policy-default | recommended-alt`) decided in §5a from the **job** (selection model / per-item cost / dominant loop), NOT inherited. `recommended-alt` is a net-scope/IA recommendation, **veto-surfaced like `recommended-new`**, and **orthogonal to `page_mode`** (work type unchanged; only composition deviates — page_mode consumers untouched). Mirrors the `band_provenance` addition (`29f6c713`) field-for-field incl. the Check 1a **backward-compat default** (absent ⇒ `policy-default`, so existing briefs in all 13 projects survive intake). **Not a hard consumer dependency this wave** — consumers read the §4a body; frontmatter is the audit + future enforcement hook (same staging `band_provenance` used before `C-ARCHETYPE-01`). Self-reviewed Mode 1. **Broad 13-project sync pending** (this session syncs inbound-flow to re-run the fixed handoff on the mapping queue). **Follow-ups:** design-review-pr composition enforcement (`C-COMPOSITION-01`, sibling of `C-ARCHETYPE-01`); skill `references/brief-provenance.md` Block-B field count (now +1); `workflow.md` EXECUTION-list §5a mention.
- [x] **`analytics-surface-architect` skill + consolidation (steps 1–3 done; OSS step reverted)** — `custom/skills/analytics-surface-architect/SKILL.md` (commit `c00b42ea`) + design-handoff §5c/§4b/step-03b/workflow.md + `shared/analytics-archetypes.md` pointer (commit `c4b10fd5`) + design-review-pr rationale-awareness + operational-analytics-band §1b dedup (commit `4dcd0b05`), 2026-06-01. The domain-general **selection brain** for analytics presentation: given a data shape + the user's question, returns a structured decision — `archetype` + ground-or-flag pair + candidates-weighed (with the road not taken) + `secondary` + `time_present_check` + `drill_map` + `prohibited`. Output contract maps 1:1 onto the `design-rationale-*` artifact (§1–§4) and design-handoff step-01 §5c. **DRY:** defers the eight archetype *definitions* to `shared/analytics-archetypes.md` (trust-hierarchy item 1) and owns only the selection *procedure* + I/O contract + modes (select / critique / explain) — no 4th copy of the taxonomy. Scoped to **shape + reasoning, NOT visual treatment** (that stays with `design-policy-canonical` / `operational-analytics-band`), so it is the open-sourceable core with no `docs/design-policy.md` coupling.
  - **Step 1 (skill):** authored + self-reviewed Mode 1. **Step 2 (wiring):** design-handoff §5c now INVOKES the skill (mode: select) and captures its decision object field-for-field; §4b + step-03b RENDER from the capture instead of re-deriving (A←winner_reason, B←secondary, C←drill_map, E←prohibited). **Not a hard dependency** — §5c falls back to applying `analytics-archetypes.md` inline if the skill isn't synced, so handoff never hard-fails.
  - **Sync mechanism note (corrected):** the skill landed in all 13 projects' `.claude/skills/` shortly after its commit (some skills-specific watcher); but the step-2 *workflow* edits did NOT auto-sync and required an explicit `sync-bmad-workflows.sh` run. So it is **not** a blanket fork-write→projects sync — skills propagate on their own, workflow text needs the sync script. Both steps are now synced to all 13.
  - **Step 3 (consolidation) done** (`4dcd0b05`): design-review-pr C-ARCHETYPE-01 is now rationale-aware — step-01 §7 resolves the `design-rationale-*` via `accompanies_brief`; step-04 §1b adds cross-artifact archetype consistency (P1 on brief↔rationale divergence) + reasoning-completeness (`[note]`), disclosed-not-assumed when no rationale exists, and runs even when dom-render is skipped (it's a file read). `operational-analytics-band` §1b no longer restates the selection rule — it defers to `analytics-surface-architect`; trust-hierarchy item 4 reframed to architect-selects / band-places / policy-styles. The "don't default to trend" selection logic now lives in exactly ONE place (the skill + its taxonomy SoT), down from four.
  - **Net result of steps 1–3: the "don't default to trend" selection logic now lives in ONE place** (the skill + its taxonomy SoT), invoked by design-handoff, verified at PR time via the rationale, and deferred-to by operational-analytics-band — down from four copies.
  - **Step 4 (open-source) reverted — user declined.** A decoupled standalone copy was built in `oss/analytics-surface-architect/` (SKILL.md + README.md + MIT LICENSE, commits `6ca4ec54`/`dbc9f00d`), then removed at the user's request — open-source publication is not wanted. It was never pushed to any public repo. The coupled in-product skill (`custom/skills/analytics-surface-architect/`) is unaffected and remains live.
- [x] **design-handoff `detail` page_mode branch (contract-dimension-gap fix)** — `design-handoff/steps/step-01-gather.md` §5 + `steps/step-03-generate-brief.md` §4a/§6/self-review + `workflow.md` + `shared/analytics-rationale.md` (commit `f3f2bb54`, 2026-06-01). `page_mode` is a **three-value** enum across the whole contract — `brief-revision-policy.md` Block B (`operational | analytical | detail`), `design-synthesize` (Gate 1 resolve + detail density 32–40px + `operational-finance-ui` routing + manifest emit), `design-implement` (manifest ingest), project policy §6/§7 — but the **producer** (step-01 §5) only ever set `operational`/`analytical`, so `detail` was unreachable: a record drawer / single-entity page got mislabeled `operational` and downstream applied table-first composition + worklist density to one record. Root cause class `contract-dimension-gap` (producer's selection axis narrower than the contract's). Fixed by **wiring the branch in, not out** (removing `detail` would have broken four consumers): step-01 §5 gains the detail definition + signals (single-entity route, one-record surface, reached by drilling a worklist) + full-enum guard; step-03 §4a gains a record-view composition block, §6 a detail framing + question guidance, self-review now validates the 3-value enum and detail-carries-no-band (also fixed a stale "4a = Analytics View Addendum" line). Detail pages never carry a band, so the rationale artifact is unaffected. Surfaced during the analytics-rationale wave; the "vestigial enum" note in the entry below was wrong — it was a producer gap, not dead code. Self-reviewed Mode 1. **Synced to all 13 projects 2026-06-01.**
- [x] **design-handoff analytics presentation rationale (companion artifact)** — `custom/workflows/design/shared/analytics-rationale.md` (new SoT spec + template) + `design-handoff/steps/step-03b-emit-rationale.md` (new conditional step) + step-01 §5/§5b/§5c (reasoning capture) + step-03 §6 + step-04 (staging) + workflow.md (state vars, EXECUTION, contract section) + `shared/analytics-archetypes.md` (pointer) (commit `705bb912`, 2026-06-01). When a brief carries an analytics band, design-handoff now emits a SECOND artifact beside it — `design-rationale-{slug}-{date}.md` — recording **how** the page-mode/band/archetype were chosen: the page-mode signal, the three band-belongs answers, the archetype candidates weighed (with the *road not taken*), why the winner won (data-dimension + user-question), and the explicit time≠trend check. Motivation: the brief is a **bias filter** that records only the winning choices, so the deliberation (the most audit-worthy and most-likely-wrong part) had no home and was discarded. The rationale is that home — human/reviewer-facing, **never read by Claude Design** (one-way linkage `accompanies_brief` → brief; the brief never points back). **Explicitly out of scope for `brief-revision-policy.md`** (not a brief; no Block A, no 6 intake checks; own active/superseded lineage derived 1:1 from the brief — same posture as onboard-design-system). Conditional: no band → no file. Direct extension of the analytics-archetype wave (`29f6c713`). Also surfaced (and fixed in the entry above, `f3f2bb54`): step-01 had no path that set `page_mode: detail` even though the whole contract supports it — a producer `contract-dimension-gap`, NOT the "vestigial enum" this entry first guessed. Self-reviewed Mode 1. **Synced to all 13 projects 2026-06-01.**
- [x] **data-quality root-cause principle (quick-dev + maintenance-triage)** — `custom/workflows/implement/quick-dev/workflow.md` (CRITICAL RULES) + `quick-dev/steps/step-04-self-check.md` (§5 root-cause checklist) + `custom/workflows/implement/maintenance-triage/steps/step-03-emit-tech-specs.md` (§1 framing) (commit `72849dae`, 2026-05-31). A data-quality defect — null/missing fields, mislabeled records, format drift, double-counting — must be resolved at the **producing pipeline** (extractor/ingest/importer/sync/migration), not by amending production data. A one-time backfill is an adjunct to the source fix, never the whole fix (it recurs on the next write). Three reinforcing layers: maintenance-triage frames the spec around the producer (routes to quick-spec if the producer isn't identified), quick-dev's CRITICAL RULE forbids data-only fixes, step-04 §5 enforces it with a REQUIRED checklist (producer identified → fixed → backfill-as-adjunct) plus a "state why it can't recur" escape for genuinely historical data. Driven by the accounting-tools SellerSmart session (null `vat_rate`, receipts mislabeled as invoices, dropped `invoice_number` prefix — each fixed at extraction/ingest + adjunct backfill). Project CLAUDE.md adapter added in parallel (accounting-tools PR #946). **Broad 12-project sync pending.**
- [x] **operational-analytics-band skill reframed to archetype taxonomy** — `custom/skills/operational-analytics-band/SKILL.md` (commit `e8b5ef7a`, 2026-05-31). The skill (fork-distributed — lives in `custom/skills/`, syncs to projects' `.claude/skills/`) hard-coded "coverage → trend → readiness" as THE band narrative, making it a direct cause of the monoculture the archetype axis exists to break. Reframed: trust-hierarchy item 4 points band *shape* at `shared/analytics-archetypes.md`; new §1b "Choose the archetype — do NOT default to coverage→trend→readiness" (names the time≠trend trap); §3 changed from a fixed utility sequence to an archetype-driven vocabulary; refusal counter-offers no longer prescribe coverage→trend→readiness universally. Hierarchy/subordination/drill/anti-KPI-wall rules untouched. Completes the analytics-archetype wave (handoff produces archetype → review-pr enforces it → this skill picks it). Synced to taylor_work; broad 12-project sync pending.
- [x] **design-review-pr C-ARCHETYPE-01 (archetype enforcement at PR time)** — `custom/workflows/design/design-review-pr/` workflow.md + step-01 §7 + step-03 §3b + step-04 §1b (commit `077b5fe9`, 2026-05-31). PR-time counterpart to the design-handoff archetype axis: a new workflow-intrinsic check (sibling of `C-COMPOSITE-01`) that reads the affected route's active brief, and if it declares an analytics band (`band_provenance` ∈ inherited|recommended-new), verifies the rendered band's *form* matches the declared `analytics_archetype` (gap-strip for coverage, sorted list for ranking, small-multiples not single multi-series for trend, one number not a KPI wall for single-metric, every band element drills). **Brief-aware — a new dimension; the workflow was previously only project-policy-aware.** Honesty-preserving: route-with-no-brief and pre-contract briefs are skipped and DISCLOSED in coverage notes (never implied as verified); ambiguous harvest does not fire (defers to a human-judgment manual prompt, which is also the Chrome-unavailable fallback). DOM-level harvest so it runs on Next.js projects despite the workflow's SvelteKit source-grep lane. **Broad re-sync to all 13 projects pending (with the design-handoff change).**
- [x] **design-handoff analytics archetype axis + band-as-judgment** — `custom/workflows/design/shared/analytics-archetypes.md` (new: 8 archetypes — trend/distribution/composition/ranking/coverage/flow/single-metric/correlation — each with question/form/drill/avoid + a "pick from the user's question, ground-or-flag, never default to trend" selection rule) + design-handoff step-01 §5b/§5c, step-03 §4b, workflow.md, brief-revision-policy.md Block B (commit `29f6c713`, 2026-05-31). **Defect A (archetype monoculture):** §4b prescribed ONE shape (headline → trend-strip-of-small-multiples → drill table) as if it were generic, with one domain's VAT/quarters examples baked into placeholders, so every handoff came back identical. Classed `contract-dimension-gap` — §4b was missing an archetype axis. §4b is now archetype-conditional (A archetype&job / B passes-from-form / C drill / D palette / E prohibited) and references the taxonomy. **Defect B (legacy-mirroring):** §5b decided band presence by inspecting the legacy render — contradicting design-handoff's blank-canvas mandate — so a bare-table feature with a pattern/coverage/ranking job got no band. §5b reframed detection→judgment (3 grounded questions on data + user job); new `{band_provenance}` = inherited|recommended-new|recommended-drop|none with recommended-new/drop veto-surfaced (intent-autonomy guard). Block B contract gains `band_provenance` (every brief) + `analytics_archetype` (iff band present); invariant 1a has a backward-compat clause (absent band_provenance ⇒ none) so existing active briefs survive intake Check 1. Driven by taylor_work 2026-05-31: a "Weekly spend & coverage" band shipped as the same trend-strip archetype as every other analytics handoff. **Broad re-sync to all 13 projects pending.**
- [x] **Brief provenance contract** — 11-field frontmatter, 6 intake checks (commit `401e2d57`)
- [x] **design-handoff predecessor + supersession logic** — part of brief-revision-policy
- [x] **design-artifact-loop, design-synthesize, design-tuning intake checks** — wired to provenance contract
- [x] **design-synthesize synthesis honesty rules + under_grounded label** (commit `6e4cee98`)
- [x] **design-synthesize brief-faithfulness half (g/h/i)** — pre-visual gates for internal consistency, deliverable coverage, brief-question coverage; T4 strengthened to four sub-tests (T4a-T4d) (commit pending today, 2026-05-28). Driven by `amazon-transactions` bundle PR #800 in accounting-tools shipping as `pass / excellent / aligned` while containing 4 contradictions, missing 2 of 4 brief §7 deliverables, and abstractly answering 3 of 7 brief §6 questions.
- [x] **Quick-dev grounding gate (Mode B)** (commit `1979c48f`)
- [x] **Quick-dev autonomy scoping (decision vs intent)** (commit `1979c48f`)
- [x] **sync-bmad-workflows.sh** — base sync script
- [x] **sync `--worktree PATH` flag** — single-worktree minimal sync (commit `647c14e7`)
- [x] **project_phase config flag** — `greenfield | brownfield | mixed` (commit `0dd2e1ca`, shipped today)
- [x] **maintenance-triage workflow** — `custom/workflows/implement/maintenance-triage/` (commit `0dd2e1ca`, shipped today)
- [x] **design-review degraded-mode switch** — `measurement_method: chrome-live | source-derived | screenshot-only` + mandatory `measurement_caveat` (2026-05-28). Closes the gap where step-01 §3 assumed Chrome MCP always available and FAILURE MODES listed screenshot-only as failure with no sanctioned alternative.
- [x] **design-tuning two-lane evidence model (treatment-source vs composition-screenshot)** — step-01 §1c ingests the Claude Design artifact bundle (reuses design-implement URL PATH: `curl` → tar → JSX/`tokens.css`), §1d resolves the canonical codebase component as the §13 reference; step-02 §0a routes checks into a treatment lane (ring/opacity, radius, spacing, color, dot — settled by exact `{artifact_css_catalog}` vs `{canonical_components}`, new `source-exact` evidence class) vs a composition lane (screenshot-authoritative); §2a cross-surface compare. `{treatment_evidence_mode}: bundle-exact | screenshot-degraded` mirrors the design-review degraded switch and blocks APPROVAL on `treatment_unverified` (step-02 §7 / step-03 §1+§5a). workflow.md SOURCE-OF-TRUTH PRECEDENCE gains the self-contained-bundle carve-out (source==render, no override surface). (2026-05-30). Driven by accounting-tools Amazon transactions design-tuning iter-4: V18 status pill scored "resolved" off a PNG when a `ring-rose-500/20` inset ring is sub-visible; user had to step in.
- [x] **Source-vs-deployed drift gate** — design-review step-01 §1.5 pre-flight checks `git status` / recent local commits / local-vs-origin/main delta before reading the page; surfaces source_state_caveat in autonomous mode (2026-05-28).
- [x] **Deploy stale-checkout guard** — `bmad-deploy.sh` §3a-stale + exit 19 + `deployment-to-prod.md` §3a-stale. Refuses a deploy whose deploy-relevant content differs from `origin/<default-branch>` (config `deploy.default_branch`, default `main`), so a build behind main can't silently revert merged PRs in prod. Tree-diff (not ancestry) so freshly squash-merged worktrees pass; `deploy_irrelevant_paths` reused as diff excludes; offline-graceful. Driven by accounting-tools 2026-05-30: a deploy from a stale feature worktree (branched before a parallel session's worklist-redesign PR merged) reverted that PR in production until a redeploy from the branch tip (2026-05-30).
- [x] **Refine-screen Collapse allowance** — design-handoff workflow.md "Refine-screen rule" now allows one mechanical Vx+Vy collapse to free a slot for a design-requiring hard failure when strict top-3 severity ordering fills slots with token/class swaps. Capped at one collapse per brief; promoted violation must be hard failure (2026-05-28).
- [x] **Brief frontmatter Block A + Block B contract** — `brief-revision-policy.md` §2 split into Block A (Provenance, 11 fields) + Block B (Content, 3-6 fields mode-dependent). Block B fields: `mode`, `page_mode`, `route` always; `screen_review_ref`, `targeted_changes`, `unchanged_regions`, `deferred_violations` for refine-screen; `collapse_note` conditional. Producer (design-handoff step-03) updated; consumers reference by policy name so no inline updates needed. Check 1 expanded to validate both blocks (2026-05-28).
- [x] **shared/worktree-portability.md** — codified rule: every producer resolves `{project-root}` via `git rev-parse --show-toplevel` at write-time, refuses writes outside the active worktree, halts with named diagnostic. Applied in design-review step-01 §7 and design-handoff step-03 §1. Closes silent main-checkout-write bug (2026-05-28).
- [x] **shared/delivery-to-main.md + design-handoff step-04-deliver.md** — producer-side delivery sequence (commit → push → PR → merge → verify → fast-forward → exit-worktree). Closes the "file written to disk but not on origin/main" gap that caused Claude Design to fail-find a brief on 2026-05-28. Skippable via `--no-deliver` or `delivery.design-handoff: skip` config. Pattern is reusable for design-synthesize / maintenance-triage / artifact-loop — they should adopt step-N-deliver next (2026-05-28).
- [x] **Canonical implementation-artifacts path consolidation** — workflow text was referencing legacy `_bmad/bmm/implementation-artifacts/` literal in 11 places across 6 design workflows; config resolves `{implementation_artifacts}` to `_bmad-output/implementation-artifacts/`. Diagnosed in accounting-tools (27 orphaned artifacts), fixed at fork-level (commit `ae4bb805`, 2026-05-28). Audit of all 13 projects: only accounting-tools materially affected. Re-sync needed for the other 12 to refresh workflow text (no artifact moves needed there).
- [x] **design-implement state-axis (component × state × property grid)** — `design-implement/workflow.md`, `steps/step-01-ingest-design.md`, `steps/step-03-build-grid.md` + `design-synthesize/steps/step-07-emit-manifest.md`. The grid contract was Component × Property; the design's intent is Component × **State** × Property. Step-01 now catalogs state-conditional rules from three sources (inline `style=`, `<style>` blocks, `data-state` sibling variants); step-03's grid has a State column with per-state property sweeps and Tier-1 surfacing of state-coverage gaps; manifest schema gains mandatory `components_emitted[*].states_emitted`. Diagnosed in accounting-tools PR #827 retro (failed-row tint at 0.06 vs 0.10, failed-row hover regression, null-supplier rendered bold, null-total rendered bold — all four were state-conditional rules with no grid row). Driven by mason-bmad-workflow-expert Mode 3 diagnosis 2026-05-28; proposed new root cause class `contract-dimension-gap`. Re-sync needed for all 13 projects to pick up the new step text. (2026-05-28)
- [x] **onboard-design-system workflow** — `custom/workflows/design/onboard-design-system/` (workflow.md + 6 steps + claude-design-intake-template.md). The "set up a new design system" front door: orchestrates `create-design-policy` (strategic) → brand-identity population (tactical) → a code-shaped token bundle (`tokens.css` + `sample.html`), runs `delivery-to-main` so the bundle is on `origin/main`, then emits a `claude-design-intake.md` card mapped 1:1 to the claude.ai/design "Set up your design system" form fields. Motivated by the form being code+GitHub-first (markdown underfeeds it) and ingesting from `origin/main` (the 2026-05-28 fail-find gap). Explicitly scopes OUT the brief-revision-policy 6 intake checks (produces design-system artifacts, not briefs) via a PROVENANCE SCOPE section. Authored 2026-05-29; synced to taylor_work for first run; broad re-sync to other 12 projects pending. Open nit: step-06 commit type `feat(...)` vs delivery-to-main §3 bundle-type `design(...)`. (2026-05-29) **Revised same day:** added a `mode` axis — **`led` (default)** = Claude makes all decision-autonomy calls (palette/type/tokens) from an evidence-grounded internal brainstorm, commits to one direction, and surfaces a single end-of-run review (chosen direction + rationale + runners-up + confidence) for veto; `--collaborative` restores propose-and-confirm. Intent honesty rule keeps `led` inside the autonomy boundary (ground-or-flag, never fabricate positioning silently). Per-step behavior marked `[led]`/`[collaborative]`. (2026-05-29)
- [x] **Workflow humanization wave — verify + implement + design-policy families** — three commits (`cb0e0567`, `b19d1b7e`, `f03aef39`, 2026-05-28..29). Rewrote role / Key Insight / Critical Rules prose for the 9 fork-owned workflows that still read like upstream BMAD spec-language: `wire-check`, `trace-flow`, `quick-dev`, `quick-spec`, `create-design-policy`, `modify-design-policy`, `apply-design-policy-change`, `design-review-pr`, `design-agent`. House style now consistent across all 21 fork-owned workflows. Every safety/gate/state-variable/step-file pointer preserved byte-identical — pure prose changes. Skill `mason-bmad-workflow-expert` Mode 1 self-review run on each batch before push. Sync to 13 projects: see distribution step pending below.
- [x] **mason-bmad-workflow-expert skill 1.3 — Closing Out a Wave (Delivery Audit)** — new section inserted between Durable Principles and "What This Skill Does NOT Do". Codifies the post-wave audit (distribution + record-keeping + follow-up triage) that fires after Mode 1/2/4 work. Companion global feedback memory `feedback-delivery-audit-before-done.md` created. Driven by 2026-05-29 retro: after the humanization wave, the skill declared "done" without surfacing the sync / STATUS update / onboard-design-system triage — Mason had to ask "next steps?". The new section + memory close that gap. `last_verified_against_fork_commit` bumped to `f03aef39`.
- [x] **design-implement implementation-multiplicity + numeric-colour axes** — `custom/workflows/implement/design-implement/` step-02/03/04 + workflow.md (2026-05-30). Adds a 4th comparison axis beyond component × state × property: a design primitive can map to MANY implementations, so step-02 §3 enumerates every render site (`{impl_render_sites}`) and step-03 §2a cross-checks them — sibling-implementation divergence is Tier-1 *even if one copy matches the design*; step-04 mandates consolidation (not per-copy patching) as the fix. Secondary: step-02 §2 resolves every colour incl. default Tailwind palette to hex (`{impl_colors}`), step-03 §2b makes colour comparison numeric (ΔE) and bans "both green → ✓". Driven by accounting-tools invoices status pill shipping forked 3 ways (table emerald `#ecfdf5` vs drawer/detail sage `hsl(150 26%/40% …)`) — each per-page design-implement audit passed because the workflow mapped the primitive to one same-page file and never compared the copies. Same severity-tier treatment the state axis got after PR #827. Synced to accounting-tools; broad re-sync to other 12 projects pending.
- [x] **design-implement apply-ledger + mandatory not-applied disclosure** — `custom/workflows/implement/design-implement/` step-04 + workflow.md Critical Rules (2026-05-30). Closes the "floor" defect where step-04 said "fix EVERY delta" but had no per-row accountability: a holistic "rebuild until it looks like the design" pass silently dropped enumerated grid rows. Fix: (1) apply is grid-DRIVEN (holistic rebuild forbidden); every step-03 grid row ends with an explicit disposition `applied | deferred(reason: needs-data/out-of-scope/judgment) | dropped(reason)` — no undisposed rows (`A+D+X == delta_count`); (2) step-04 §9 completion report has a MANDATORY "Deltas not applied" section enumerating every deferred/dropped delta with reason, or "None — all N applied" (never omitted). Root cause class proposed: `silent-partial-implementation` (same family as `contract-dimension-gap`). Driven by accounting-tools /queries #900 silently dropping 6 detail deltas (band sender, source/sort, kbd nav, contextual secondary, flagged, footer hints), caught only by user review, fixed in #903. Synced to accounting-tools; broad re-sync pending.
- [x] **bmad-deploy.sh Cloudflare auth pre-flight (exit 18)** — `custom/scripts/bmad-deploy.sh` §4b + `deployment-to-prod.md` §6 (commit `aa23aa1c`, 2026-05-30). When `deploy.deploy_command` contains `wrangler`, runs `wrangler whoami` before the build and fails fast with a new named exit code **18** + actionable re-mint message if `CLOUDFLARE_API_TOKEN` is invalid/expired/revoked (API 9109/10000). Non-wrangler deploy targets skip it. Driven by accounting-tools: a token expired mid-session, so a merged PR failed to deploy only AFTER a full build, as an opaque post-build wrangler stack trace. Synced to accounting-tools (PR #882); broad re-sync to other 12 projects pending.
- [?] **Worktree creation hook → auto-sync** — partial: `--worktree` flag exists, automatic hook wiring needs confirmation per-project
- [?] **.gitignore entries for synced directories** — enforced per-project; audit across all 13 projects pending

## Wave one-liners moved out of STATUS.md `## Now` (2026-07-20)

> Moved verbatim from `STATUS.md` `## Now` to satisfy the `validate:status-budget` ceiling (`MAX_NOW_WAVE_BULLETS = 6`) WITHOUT relaxing the gate. Newest-first, exactly as they appeared. Nothing edited or dropped.

- **Latest wave:** **file-de-vat close-out — one legible command for an owner-filed period** (`1e2b2f39`, pushed `myfork/custom`, **SYNCED 14/14, 0 blocked**). `accounting-tools/scripts/close-out-filing.sh` maps the single "owner filed Q2" event to its two writes (marker `filed` + case `sent`) so nobody guesses the vocabulary or eats the silent-400; step-05 points at it for the manual-close-out path. accounting-tools **PR #1079 merged** (`b4be6ac2`, deploy-irrelevant). `fork-gaps.md` RESOLVED.
- **Latest wave:** **file-de-vat PA mode — the filing is an owned CASE, not a topic** (`922cf360`, pushed `myfork/custom`, **synced `--only accounting-tools`**; fan-out to other 12 owed, project-gated so low urgency). Durable `vat_filing_cases` in comms_dashboard (migration `0055` + ingest `PUT`/`GET`, **PR #257 merged + Railway-deployed**, migrate-on-release ran `0055`, endpoint live) advanced by a best-effort always-exit-0 write channel at each `file-de-vat` phase boundary; new session-start banner layer LEADS with an unsent/near-due/overdue filing (finish-or-park, snooze-aware, urgent override). accounting-tools **PR #1056 merged**. Real Q2 case seeded from ticket #98; banner verified end-to-end. Enforcement (enforcement-expert): session-start injection = deterministic delivery / probabilistic action; irreversible submit hard-gated; `sent` terminal + PostToolUse reminder that never auto-marks. New fork-gaps: worktree-isolation blocks cross-repo Edit.
- **Latest wave:** **`file-de-vat` — quarterly German VAT filing front door (project-gated)** (`4fbe1c5a`, pushed `myfork/custom`, **SYNCED 14/14, 0 blocked**). New `implement/file-de-vat`: 7-phase announced contract (pre-flight PASS/FAIL → data pull → validation → portal fill → **HALT human review** → submit → receipt) over accounting-tools' avask-filing MCP tools; runtime project gate BLOCKs the other 13 projects (no per-project sync scoping — new fork-gaps entry `sync-scoping`). Step-04 HALT overrides autonomous_mode (grounding-gate precedent); pre-flight + at-approval markers feed a project-side deterministic PreToolUse gate (accounting-tools PR #1050: hook + `avask_submit`/`avask_file_invoices` ask-pins, 6/6 behaviors smoke-tested). Built via create-workflow; step-03b adversarial review found 3 blocking (marker lifecycle never re-armed, `file_invoices`-as-trigger handoff had no receiver, phase-5 banner missing on approve) — all fixed pre-wire, C1–C10 same-pass. Origin: owner filing-anxiety complaint routed cold-dev → enforcement-expert → create-workflow.
- **Latest wave:** **design-review live-process check — the receiver for the `bb364660` runtime cede** (`35e1934f`, pushed `myfork/custom`, **sync to 14 owed**). step-01 §1.6 dimension 4 "Live-process behavioral contract" (gate: brief §2c / `{primary}--{state}` frames): states render (unreachable ⇒ `UNVERIFIED(state: …)`, never silent), §2c verbs reachable exactly where legal, observed cadence within the staleness budget — all blocking. Closes the fork-gaps "cede with no receiver" entry from the external review of the live-process pass. design-tuning mirror deliberately skipped (bundle/screenshot evidence model, not live behavior). Residue: step-01-audit.md 28.3KB vs 28KB soft budget — extract §1.6 to its own step on next growth.
- **Latest wave:** **`quick-brainstorm` — inferred-triage front door for brainstorms** (`db528746`, pushed `myfork/custom`, **SYNCED 14/14, 0 blocked**; convergent path live-tested end-to-end in bison-ops — triage verdict, veto window, inline deliberation all fired per contract; divergent path awaits first real open-ended ask). New `implement/quick-brainstorm`: infers convergent-vs-divergent from the ask (grounding gate: target from input alone or HALT with one question), announces a one-line verdict whose turn boundary IS the veto window, then either routes OUT (inline deliberation / quick-spec / design-router — never pre-picking a design specialist) or runs a repo-grounded 20-30-idea session with a MANDATORY convergence ending (themes → ranked shortlist → ONE recommendation → handoff artifact). SHADOWS core brainstorming (`_bmad/core/` has no fork lane — new fork-gaps entry 2026-07-03); core stays the deep-divergence specialist, reachable via a default-no escalation aside. design-router gained the reciprocal composition line (quick-brainstorm is a documented front door). Routing disambiguation ("brainstorm X" → quick-brainstorm first) added to the global `workflow-routing` memory since core's own trigger description can't be edited. Built via create-workflow; step-03b adversarial review found 2 blocking (illusory veto window; design-router ownership re-derivation) — both fixed pre-wire, C1-C4/C6 fixed same-pass.
- **Latest wave:** **STD-DATAFLOW-001 — data-flow map standard + warn-only detector** (`e629a93f`, **SYNCED 14/14, 0 blocked**; pushed `myfork/custom`). New governed standard (`shared/dataflow-standard.md`, registered in STANDARDS.md): every cross-boundary data flow is a code-anchored **Ingress Map** entry (source·ingress·payload·direction·authority) + hard **non-flows** (NF1 leads↔inventory). Enforcement (classified w/ enforcement-expert): AWARENESS (map + CLAUDE.md pointer) + a **warn-only pre-commit detector** (`custom/githooks/check-dataflow-map.sh` + fork-owned `gates.d/dataflow-map.conf`) flagging a tracked seam change (`schema-server/src/schemas/`, `*/webhooks/*`) with no matching `docs/data-flows.md` update — 5/5 behaviors verified. Warn-then-gate (`dataflow.mode: gate` arms per project); CI fail-closed tier deferred (STD-HOOKACTIVATE-001). Owner-driven after the mifarma incident (9 invoices lost ~2mo in an undocumented mifarma→accounting-tools→inbound-flow seam). **Instance #1 merged to inbound-flow** (`docs/data-flows.md`, PR #2519). Skill `last_verified` bump owed.
- **Latest wave:** **per-worktree `_bmad/` refresh → OPT-IN (default off)** (`sync-bmad-workflows.sh --worktree` no-ops unless `BMAD_WORKTREE_SYNC=1`; pushed `myfork/custom`; §8 doc sync owed). Kills the every-worktree teardown friction (tracked `_bmad/` churn made ExitWorktree demand `discard_changes`). Empirically ruled out the skip-worktree fix — it aborts the §A3 `git merge main` integrate. Default: worktree inherits main's `_bmad/`; sync MAIN to cure staleness. Routed via enforcement-expert (deterministic > prose); reversible escape hatch kept. `fork-gaps.md` RESOLVED.
- **Latest wave:** **cross-session WIP register** — `parallel-sessions.md §E` (pushed `myfork/custom`, **sync to 14 owed** for the prose; hooks live machine-local via `install-global-assets.sh`). Closes the gap that two sessions independently built the same listing failure-reason surface (one abandoned at merge vs #2484/#2485): §C claims only sprint stories, tech-spec-driven quick-flow claimed nothing. New `wip-register.sh` (claim/clear/enrich) + `check-wip-register.sh` (SessionStart awareness surfacer) + `wip-claim-on-worktree.sh` (PostToolUse EnterWorktree **deterministic** claim write); ledger at `<main>/.claude/wip-register.yaml` (anchored via git-common-dir, dead-claim self-GC). quick-spec step-01 + quick-dev step-03 read-and-enrich. **AWARENESS tier only — no gate** (same-feature isn't deterministically detectable; a block would be the indiscriminate-gate anti-pattern). Gates run first: tool-discovery (confirmed §C is story-only) + enforcement-expert (claim hangs off worktree-create, not a skippable step). Dry-run clean; markdownlint 0-err; 2 hooks registered idempotently. `fork-gaps.md` entry RESOLVED.
- **Latest wave:** `design-implement` **fixture-to-prod permission checkpoint** (step-02b §4c, `beee0cf2`, **SYNCED 14/14, 0 blocked**). The authorization tier the fixture stack lacked: when a run would ship a fixture-backed surface (`DATA_STATE="fixture"`, no live read path) to a PRODUCTION route, it HALTS-and-ADVISES before apply; autonomous mode does not auto-proceed. PROBABILISTIC awareness tier — its deterministic companion is the project's disclosure floor (mock marker + fixture banner + the fixture-disclosure CI gate, which cash-recovery made a required check the same day). Disclosure ≠ authorization. Surfaced live from the cash-recovery `/inbound` Received tab shipping to prod as a faithful-transcription default; implements Posture B of that policy review.
- **Latest wave:** design pipeline **discoverability gate** — built surfaces are no longer unlinked islands (`05e69392`, pushed `myfork/custom`, **sync to 14 owed**). Owner-escalated from a live miss (cash-recovery §L recovery-cross-check shipped at `/recovery/cross-check` with nothing linking to it — URL-only, owner added the link by hand, #195). `design-implement` step-04 §9 gains a MANDATORY **"Entry point / discoverability"** disclosure (grep the impl for a reachable affordance — global-nav / link-from-named-parent / row-drill — else report UNLINKED ISLAND, never declare complete); `design-handoff` §7 "Rules for the inventory" gains an **entry_point** rule making the primary frame's "Opens from / trigger" a contract implement verifies. Both encode **sub-surface ≠ nav-peer**. Canonical edit in `custom/workflows/` only (skills-native is the gitignored porter twin — regenerated on sync). Additive; markdownlint 0-err; Mode-1 self-review 0 blocking (PROBABILISTIC tier — model-executed like the sibling §9 disclosures; a deterministic CI tier (new-route-without-inbound-Link) is a separate hooks-track escalation). `fork-gaps.md` entry marked ACTIONED.
- **Latest wave:** completion-contract / STD-COMPLETION-001 — "finisher, not commentator" disposition (`8b37bc8b`, pushed `myfork/custom`, **SYNCED 14/14, 0 blocked**). New `shared/completion-contract.md`: a completion workflow's terminal step MUST declare a `completion_disposition` (`pr_merged`/`pr_open`/`owner_gated_residue`/`advisory`); diagnosis with no disposition = invalid commentator exit. Third sibling of the close-out family; wired into quick-dev/design-handoff/dev-story/design-implement terminal steps + `close-out-contract.md` §2. Detector `check:completion` default warn-only; **`--strict` ARMED in `npm test` + pre-commit** (owner-directed, ahead of soak — a missing disposition blocks the commit; reversible; runtime completion stays PROBABILISTIC). **Remaining residue:** per-project on-disk artifact check (separate track).
- **Latest wave:** design-lane register-optional backlog-triage close-out (`c65798c4`, SYNCED 14/14). `design-handoff` step-04 §10 + `design-router` step-03 append an owner-facing **Outstanding (design backlog)** triage before concluding (designed-not-built → built-no-brief reconcile → unowned concept gaps); register-OPTIONAL (reads `docs/surface-register.*` if present, else derives from briefs + built routes) so it degrades gracefully in the 12 register-less projects. Sanctioned in `close-out-contract.md` §2. PROBABILISTIC only (enforcement-expert: no gate — design-lane isn't deterministically detectable). The behavior lifts fork-wide; the register tooling stays project-local.
- **Latest wave:** portable surface-register residual catch + fork-gaps lint (`ade6f608`, `0d88a23e`, SYNCED 14/14). The lift-fork-wide half of the surface-register idea: design-handoff §1a gained a **soft cross-route duplicate WARN** — a slug-stem heuristic (drop role prefix, first token) that flags an `active` brief sharing the stem but resolving to a DIFFERENT surface id (the `/raw-records` vs `/ingestion-runs` residual the route-match can't catch). WARN not HALT (heuristic, can false-positive); reads brief frontmatter only → stack-agnostic, works in all 13 projects. The `src/app`-parsing register stays project-local tooling (cash-recovery PR #160) — deliberately NOT lifted (Next-specific, other projects are SvelteKit). Also fixed bare URLs (MD034) in the `fork-gaps.md` design-implement entry (`docs/` is the real file; `website/src/content/docs` is a symlink to it). Mode-1 self-review: 0 blocking (additive; PROBABILISTIC warn tier, honest).
- **Latest wave:** predecessor-detection re-keyed on **surface identity**, not slug (`6d306e53`). Owner-confirmed fix for the slug-EXACT collision logged today in `fork-gaps.md` (cash-recovery raw-records: `raw-records-surface` vs `raw-records` both stayed `active`). Surface identity = `normalise(route)` + new optional `surface_part` Block B field (absent ⇒ `""`, so old briefs are untouched), scoped within `mode`. `design-handoff` step-03 §1a now scans ALL `design-brief-*.md` (was the exact-slug glob): 0 same-surface actives → `original`, 1 same-slug → `material_revision`, **1 different-slug → HALT**, 2+ → HALT. `brief-revision-policy.md` invariant 6 + producer §4 + consumer Check 4 (×2: `design-artifact-loop`, `design-synthesize`) re-keyed per-surface; `surface_part` field row + invariant 1a backward-compat. `brief-template.md` frontmatter gains the field. `fork-gaps.md` entry marked RESOLVED and its **inverted source-of-record line corrected** (workflows/ is the editable source; skills-native is the porter-generated twin). Routed via mason-bmad-workflow-expert; **diff held for owner review before sync** (explicit ask). Mode-1 self-review: 0 blocking; honest residual surfaced — two authors expressing one surface with *different route strings* can still both go active (where a future canonical surface_id register would help); option (3) input-brief convention deferred (the different-slug HALT covers that path). Pre-commit validators green (0 errors). **Committed `6d306e53`, pushed `myfork/custom`, SYNCED 14/14 (0 blocked)** — live in every project. **Follow-up shipped (`12801ead`, SYNCED 14/14):** `step-01c-topology` §5d-i now DECIDES `{surface_part}` at topology time (in the gather COMPLETION checklist) so step-03 consumes it instead of re-inferring at brief-gen time. Remaining deferred: the canonical surface_id register (the different-route-string residual) — being proven as a cash-recovery project register first per the original plan, then lifted fork-wide if it works.
- **Latest wave:** address-fork-issues pass + deploy-method contract v2 (`019c5223`/`4c7a8a45`/`da986868` + fork-gaps markers). Owner: "address all fork issues" → triaged the open `docs/fork-gaps.md` backlog and shipped the bounded/verifiable subset: **bash edit-guard scratchpad allowlist** (`/private/tmp/` + `/private/var/folders/` now allowlisted so a harness-scratchpad redirect isn't hard-blocked; real `src/` writes still DENY — `019c5223`), **§D3 atomic fork-commit discipline** in `parallel-sessions.md` (`git commit -o <paths>`; closes index-commingling, doc tier — `4c7a8a45`), and **deploy-method contract v2** — `deployment-to-prod.md` §1A: three modes (push_auto/contract_script/manual_cli) + conservative inference (skip+undeclared → halt-and-state, never a guessed CLI) + method-bounded fallback ladder + auth-failure/durable-target-first branch + optional `deploy.method` field; §8 pointer made mode-aware; v1→2 additive/backward-compat, doc-only (`da986868`, **closes the high deploy-method gap**). Logged the **offer-then-affirm skill-invocation** dispatch gap. Deliberately NOT auto-fired the owner-investment/deferred items (nudge build-vs-consolidate, gateway Tier-3, astro-pre-commit, memory-slug sweep) nor a **blind cross-repo `deploy.method` fan-out** — an inbound-flow attempt empirically PROVED per-project adoption is required (blocked by its pre-push `tsc`-in-empty-worktree gate; declined `--no-verify` per prove-don't-assert; aborted clean). **cash-recovery adopted `manual_cli` in-session** (PR #98 on its own repo); the 4 push_auto repos + 5 unset recorded for their own sessions. Mode-1 self-review 0 blocking; all pre-commit validators green (hook-smoke 10/10). **Pushed `myfork/custom`; sync to 13 owed** (hooks.json + parallel-sessions + deployment-to-prod distribute on next fan-out; `fork-gaps.md` is fork-local).
