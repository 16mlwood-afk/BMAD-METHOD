# Mason-BMAD Fork — STATUS · Archive

> Complete verbatim wave log, moved out of STATUS.md's hot-read path. The skill reads STATUS.md's `## Now` + recent `## Changelog` on every invocation and reads this archive only on demand. The newest waves are rendered as discrete entries in STATUS.md; everything below is the original run-on log, preserved as-is.

## Changelog (archived — full verbatim log, newest-first)

**Last updated:** 2026-06-12 by inbound-flow session — **design-implement: the bundle is a generated PROPOSAL, the spec is `docs/design-policy.md`** `commit 9a0a1089` — follow-up correcting the page-shell wave below. User pulled a second real sheet (`Accounting Import v2`) to back the "what else are we dropping" claim, then asked "the readme is gened by Claude Design right" — **yes**, and it reframes the fix. The bundle README states its own provenance ("Trust hierarchy: `docs/design-policy.md` > … policy-first; foundations derived from the policy; scaffolded from the seed"), so it is a **generated, lossy, sometimes self-violated echo** of the project policy — proven by the `Accounting Import v2` bundle itself shipping a banned **colored-glow `@keyframes ai-pulse`** + **no `prefers-reduced-motion`**, both forbidden by the very `docs/design-policy.md` (v8, L50 full-width rule; v3/§5 colored-glow+motion bans) it was generated from. Consequence: my page-shell step-01 capture was pointed at the wrong source (the generated README), and a "prohibition lane" inside a bundle-diff would be a check that lies (matching a policy-violating bundle scores green). Correction: **(1)** step-01 re-points `{design_layout_constraints}` to read the layout rule from `docs/design-policy.md` **FIRST (authoritative)**, README+wrapper demoted to corroboration with an `authoritative` flag + a no-policy fallback (mark needs-confirmation, don't assert); step-03 §2d uses the policy-authoritative value. **(2)** New step-03 **§2e Ceded-dimensions** + step-04 §9 disclosure + a workflow.md Critical Rule: design-implement owns **treatment + structure + page-shell** and explicitly **CEDES policy-conformance (prohibitions/tone/motion/iconography) AND behavior wiring (drawer-stack/Esc/mutation/live-SSE/sort-filter)** to `design-review` / `design-review-pr` (which already enforce the policy) and `verify` (live behavior) — the content-lane disclosure model, not a faked grep. The principle: the bundle is authoritative for the pixels it proposes, never for policy it can itself break; the page-shell stays a CHECK (statically verifiable against policy+impl), the rest is CEDED (unverifiable from a generated bundle / needs live evidence). Self-reviewed Mode 1: 0 blocking, 1 nit fixed (a malformed `verify` command path in step-04). Fork pre-commit suite green; pushed `myfork/custom`; **synced 13/13, 0 blocked.** Skill `last_verified_against_fork_commit` → `9a0a1089`. **Prior wave (2026-06-11): design-implement gains the PAGE-SHELL axis** `commit 1febc8ce` — user (after a `/orders` redesign shipped narrow + centered): "how did design-implement miss this" → then "update the workflow." Root-cause class **contract-dimension-gap**, the fourth named instance after state (`PR #827`), implementation-multiplicity (the forked pill), and content-lane (`7a09717c`): the comparison grid is component × state × property, but the page CONTAINER's own width/centering/padding is owned by the page wrapper + ancestor layout (not any component), AND the Claude Design bundle renders its root `.app` full-bleed standalone, so **no per-component row could ever see a nested `max-width` cap** — inbound-flow `/orders` (PR #2017) nested an inner `max-width:1280` inside the layout's `max-w-[1440px]`, rendered narrow+centered against a README that said "full-width within the content container", and every component CSS matched ⇒ grid all-green. Unlike content-lane (CEDED — unverifiable from a mock bundle), the page-shell IS verifiable from evidence the workflow already holds (README prose + the full-bleed wrapper + the impl wrapper chain), so the fix **adds a real row**, not a cede. Axis threaded end-to-end: `workflow.md` `{design_layout_constraints}`+`{impl_page_shell}` state vars + a Critical Rule; **step-01** captures `{design_layout_constraints}` from README layout prose + the bundle wrapper width (URL.2) with a **SHARED note so the synthesize-bundle path — which has no README — populates it from the wrapper HTML + manifest `page_mode`**; **step-02 §1a** walks the wrapper chain OUTWARD through every ancestor layout and resolves the EFFECTIVE width (tightest nested cap wins) into `{impl_page_shell}`, noting sibling-page convention; **step-03 §2d** ALWAYS emits exactly one Page-shell row (width/centering mismatch = **Tier-1**; silence + full-bleed wrapper still scores; surface — don't auto-spread — a house-convention divergence) + Tier-1 list + success-metric + failure-mode; `template.md` Page-Shell section + `checklist.md` per-phase items. Self-reviewed Mode 1: **0 blocking, 1 concern caught + fixed** (first draft captured `{design_layout_constraints}` only in the URL path's README section → the bundle path would leave it empty; closed via the SHARED convergence note so both ingest paths populate it). Produces/consumes no brief ⇒ 6 intake checks + provenance N/A; decision-autonomy only; sync-safe fork edit. Fork pre-commit suite (markdownlint + full test run) green; pushed `myfork/custom`. **Synced 13/13 projects, 0 blocked.** Skill `last_verified_against_fork_commit` bumped `472fab1b`→`1febc8ce` and the `contract-dimension-gap` root-cause-class instance list extended with the page-shell axis (+ the verifiable-so-add vs unverifiable-so-cede contrast). The `/orders` page-shell fix itself shipped separately as inbound-flow **PR #2017** (the live bug the workflow change prevents recurring). **Prior wave (2026-06-10): relational-coherence-audit HUMANIZED as Wren + a maintained home + a linkage-aware handoff** `commits 49b771fd`+`472fab1b` — user: "humanise and assign a 'person' to the relational-coherence audit + a safe place to store and maintain the files it produces; the handoff human should be aware of page linkage and use them when making handoffs; the two characters should converse." Four moves. **(1) Wren — Relational Coherence Lead** (`custom/agents/relational-coherence-lead.md`, `/bmad:bmm:agents:relational-coherence-lead`): the cast member who owns the §13 linkage GRAPH across a surface set, sibling to **Vera** (data-integrity lead). Grounds the surface set from plain English, runs the audit, maintains the edge map, routes torn edges — re-design→Rowan/`design-handoff`, mechanical→`quick-spec`/`quick-dev`, out-of-scope→named-not-failed. Modeled on Vera's structure (grounding gate, menu-handlers, detect+route-never-edit). **(2) Maintained home `docs/relational-coherence/`** — `relational-edges.yaml` (hand-maintained edge+co-view map, source of truth) + `reports/` (dated history beside the map version walked against), both git-tracked + deploy-irrelevant, OUT of transient `implementation-artifacts/`. workflow.md/step-02/step-04 read+write the home (legacy flat-path fallback + "move it" route); `relational-edges.template.yaml` header repointed. **(3) Linkage-aware handoff** — `design-handoff` step-01 now SEEDS the §2a linked-records inventory FROM the maintained map + latest report before deriving from scratch (the map is authoritative since it carries the derived edges the schema can't see — the SellerSmart warehouse push; a routed handoff carries its §2a seed verbatim; an omitted reference routes "declare it" back, no-guessed-edges both sides). **(4) The two converse** — `wren-and-rowan-on-the-seam.md`: a dialogue encoding the audit→handoff seam (torn edge handed forward WITH its §2a context, consumed by the linkage-aware handoff). **KEY DISCOVERY (skill is STALE):** the `mason-bmad-workflow-expert` skill's "Authoring an Agent" section claims *"the fork has no `custom/agents/`"* and *"sync does NOT generate wrappers for custom agents"* — BOTH NOW FALSE. `custom/agents/` exists (Vera, now Wren) and `sync_agents_for_project` mirrors the persona AND generates the `.claude/commands/` wrapper. The "durable fix (not yet built)" the skill describes HAS SHIPPED. The skill section needs correction + `last_verified` bump. **Delivery:** fork pushed `myfork/custom` (both commits, pre-commit suite green). inbound-flow **PR #1992 MERGED** (focused: Wren persona+wrapper + the new relational-coherence-audit workflow dir + `docs/relational-coherence/` scaffold seeded with the canonical warehouse edge + queue⇄triage co-view; tsc+vitest 5697 green; `_bmad/`+`docs/` only, deploy-irrelevant, no deploy). Self-reviewed Mode 1: **0 blocking** (persona + read-only audit produce/consume no brief ⇒ 6 intake checks + provenance N/A; the design-handoff edit is producer-side body, decision-autonomy only, sync-safe fork edit; no-guessed-edges carried into both the audit and the brief). **Follow-ups (NAMED):** (1) **skill correction owed** — fix "Authoring an Agent" (custom/agents + wrapper-gen shipped) + bump `last_verified_against_fork_commit` to HEAD; (2) **Rowan PERSONA linkage rule is BLOCKED** — the project `_bmad/bmm/agents/rowan-product-designer.md` is sync-managed (hook refuses the edit) but Rowan has NO fork source (the design cast Devon/Rowan/Jules is project-local + wipe-exposed). The functional requirement is met by the design-handoff WORKFLOW seed (move 3) + Wren's hand-forward + the conversation; making the PERSONA itself linkage-aware durably requires promoting the design cast to `custom/agents/` — which DISTRIBUTES the cast to 12 other projects on next full sync = a deliberate decision for the user, NOT taken unprompted; (3) **full `sync bmad` OWED** to distribute Wren (+ new wrapper) and the design-handoff linkage-seed to all 13 projects — this PR only delivered to inbound-flow, and the design-handoff seed + the pre-existing fork-ahead `_bmad/` drift still sit uncommitted on inbound-flow main. **Prior wave: relational-coherence-audit gains the `co-view` relation kind (same-record sibling surfaces)** `commit 618fb56f` — user (designing the Listings Queue ↔ Upload Triage seam with Claude Design): "we have two extremely similar pages but they do not communicate at all" + "I thought the coherence skill would catch this." It couldn't: the audit modeled ONLY foreign-record §13 edges (FK + declared derived edges between DIFFERENT record types), so two surfaces over the SAME record type partitioned by status (`/listings/queue` all-statuses ⇄ `/listings/queue/triage` failure-tail) fell through all three gates — not an FK, not declared, and §13's "record owned by another surface" clause never fires on a sibling that isn't foreign. Root-cause class **contract-dimension-gap** (the same-record co-view axis missing from the relation-kind contract; sibling to the cross-PAGE linking axis `f4bf10e8` added, one level up again). Fix: a declared `co_views:` section (in `relational-edges.template.yaml`) + **CO-VIEW CHECKS CV1–CV5** (bidirectional per-row link · count reconciliation · consistent handler-split IA · shared status vocabulary · no orphaned partition) threaded through workflow.md + step-01 (single-owner **carve-out**: a co-viewed record legitimately has a `master` + `partition` co-viewers, NOT rival owners — must not be flagged dual-owner; an undeclared sibling routes "declare a `co_view`," never conjured) + step-02 (load `co_views` into `{expected_graph}`; scope-gated on ≥2 surfaces in the set) + step-03 (walk CV1–CV5, distinct from the §13 lookup checks) + step-04 (classify/route: seam-as-IA `no-row-link`/`ia-divergence` → `design-handoff` spanning BOTH surfaces; seam-as-mechanism `count-drift`/`vocabulary-drift`/`orphaned-partition` → `quick-dev`). Additive/non-breaking (no `co_views:` key ⇒ identical behavior). Self-reviewed Mode 1: **0 blocking** (read-only, no brief ⇒ 6 intake checks + provenance N/A; declared-not-inferred inherits no-guessed-edges; every CV verdict gets a disposition row = silent-partial guard; all five checks verifiable from the audit's evidence source ⇒ no dimension ceded). Fork pre-commit suite green; pushed `myfork/custom`. **Project side:** inbound-flow `docs/relational-edges.yaml` declares the queue⇄triage co-view (PR #1977); the re-run on the BUILT pages catches it — **C1: `no-row-link` + `count-drift` + `vocabulary-drift` + `orphaned-partition`** live, plus a **forthcoming `ia-divergence`** when the queue's per-handler redesign ships and triage stays flat. Skill `last_verified_against_fork_commit` bumped `91c8b334` → `618fb56f`. **Synced 13/13 projects** 2026-06-10, 0 blocked (verified CO-VIEW CHECKS block landed in accounting-tools + comms_dashboard). **Follow-ups:** (1) the triage v8 `design-handoff` (the CV3 seam) fires after the queue layout locks so triage INHERITS the per-handler split rather than designing it twice; (2) `design-review` (live-page audit) is a candidate host for the co-view checks at audit-time, complementing this standing-audit home. **Prior wave: design-lane digestibility wave + brief-provenance "ten"→ELEVEN ruling REVERSED** `commits 6b2632a5`+`91c8b334` — user ran `orchestrate-workflows` on the design lane (LLM-digestibility lens) → "do 1". **(1) design-handoff step-03 brief-template extraction:** the 768-line step (a thin procedural shell wrapping a ~580-line embedded brief template) was split — new `design-handoff/brief-template.md` holds the brief shape verbatim (inside a `​```​markdown` fence, as it was in-step) + a 189-line shell that points to it. **Provably lossless** — 3 empty diffs vs the original from git (template body 581 lines byte-for-byte, head 1–93, tail §3-Self-Review→Success-Metrics). `commit 6b2632a5`. **(2) brief-revision-policy restored a DROPPED Block-A field:** schematizing the provenance block surfaced a `contract-dimension-gap` — §2 asserts "11 fields" in three places AND §1a references `policy_version_required`, but the §2 YAML example/table enumerated only **10**. The producer (`brief-template.md:37`, `workflow.md:93`) DOES emit `policy_version_required` (the policy-version-drift pin). Restored it to the §2 example+table → enumeration now matches the count (11), the producer, and §1a. `commit 91c8b334`. **(3) RULING REVERSED — the prior wave's "ten is authoritative; the prose's 'eleven' is the long-known miscount" claim (recorded in the *Prior wave* line directly below) was WRONG and has been reversed.** Its premise ("ten is what `design-handoff` emits") was false — the producer emits **eleven**; the team verified against the stale §2 *example*, not the producer. Corrected `references/brief-provenance.md` §9 + the field table + every count ref to **eleven**, preserving the real nuance the old ruling half-reached for: `policy_version_required` is backward-compat-defaulted (§1a: absent⇒`0`, not a halt), so the Block-A enumeration = **11** while the strict halt-on-missing set = the **10 lifecycle fields**. Skill `last_verified_against_fork_commit` bumped → `91c8b334`. **Synced 13/13 projects** 2026-06-10 (verified inbound-flow: step-03=189 lines, `brief-template.md` present, policy = 11 Block-A fields). Digestibility queue remainder deliberately NOT done this wave (lower-value/higher-risk): thin the 2 oversized routers (`design-synthesize` 636 / `design-artifact-loop` 497 → `NOTES.md`), one-line the project `design-policy` change_log, and a `step-06-self-critique` safe-partial (rubric/detector-table extraction ONLY — the file is the lane's most *coupled*, so a blind file-split risks the untestable synthesis quality gate). Audit report: `_bmad-output/implementation-artifacts/orchestration-tuning-2026-06-10-design-lane-digestibility.md` (inbound-flow, local/gitignored). Prior wave: **multi-handler enforcement triangle completed + fork-expert skill re-verified & bumped + bison-ops sync unblocked** `commit c189f16a` — follow-up wave to the multi-handler change below; user: "do all" (four named follow-ups). **(1) `design-review` live-audit check** (`commit c189f16a`): §5 Compare gains a project-policy-gated multi-handler-split check — a live surface that **interleaves records from ≥2 operational handlers into one undifferentiated list** (instead of tabs / per-handler segments / sibling routes) is a `hard failure`. Kept **design-agnostic** (references the project policy for what counts as a "handler"; no-op where no such rule exists). Completes the **enforcement triangle**: design-time (`design-handoff` §5d) + PR-time (`design-review-pr` §12) + live-audit (`design-review` §5). Fork pre-push green; pushed `myfork/custom`. **(2) skill re-verified + SHA bumped `155cdca2`→`c189f16a`:** unlike the two prior waves that deliberately deferred, this wave **actually re-verified the load-bearing references against HEAD** before bumping — `brief-provenance.md`'s 10 fields (`target_slug` + 9; the policy prose's "eleven" is the long-known miscount the ref already documents and works around) + 6 intake checks (Check 1–6) confirmed against `brief-revision-policy.md`; grounding-gate + autonomy-scoping present in `quick-dev`; sync script present; `project_phase` greenfield/brownfield/mixed lifecycle intact. Honest bump (drift was ~230 commits of distance, not factual staleness — the cited invariants hold). **(3) bison-ops sync unblocked (was the recurring block across every prior wave):** the `--pull` diff review revealed bison-ops is **far behind, not ahead** — committing the pull would have DELETED 3148 fork lines incl. today's `relational-coherence-audit` (`f4bf10e8`) + `create-agent`. Discarded the pull, ran `--force`. Result: **13 synced, 0 blocked**; the `--force` run **self-injected the 3 missing CLAUDE.md sections** (Cross-Repo-Edits / Deployment-BMAD-contract / CI-Health-Check) into bison-ops via its `<!-- bmad:* -->` upsert anchors — the prior normal-sync "VERIFY FAIL" was a side-effect of the workflow block, now cleared. (bison-ops's synced `_bmad/`+CLAUDE.md sit uncommitted in its working tree — normal post-sync state; its own session commits them, not cross-committed from here.) **(4) Listings Queue brief §4c — confirmed, deliberately NOT edited:** the brief has no formal §4c verdict (predates v7; `policy_version_required: 6`) but is **already substantively compliant** — §1 mandates working the queue per-warehouse "*instead of a single undifferentiated list*", §5 mandates the segmenting lens + explicit never-dropped "Unrouted" group, §2 keeps route-warehouse vs SellerSmart-dispatch handlers distinct. Hand-adding the tag would flip `revision_mode → manual_minor_revision` while `change_class: material_revision` — manufacturing the **Check-5 forbidden "material-change-via-hand-edit" combination** on a merged brief — so **noted, not added** (next `design-handoff` regen emits §4c via the new §5d trigger; §12 enforces the split at PR-time regardless). Wave self-review: the only edit-equivalents the guard blocked (cross-repo `cat >>` to bison-ops CLAUDE.md; in-project brief edit) were *correctly* routed — the sync did the CLAUDE.md inject, the brief was left alone. **Follow-ups:** (1) skill `version` not bumped (no public-shape change — only the verification SHA moved); (2) `design-policy` §13 v5 §9's accounting-tools propagation of the linked-record mandate still owed by that project's next session (unchanged). Prior wave (2026-06-10, same session): **design-handoff §5d gains a generic multi-handler split trigger + inbound-flow design-policy v7 multi-handler rule** `commit 8c59ea10` — user (via `modify-design-policy`): "any surface whose records span more than one warehouse handler (route/dispatch warehouse, lane, or fulfilment provider — SellerSmart, TheFBAPrep DE, SmartPrepCentre US) must be split by view — per-handler tabs or per-handler routes — and **never** a single merged front-end list … co-residence in the data layer is fine; this governs **presentation only, not storage** … design-handoff §5d must detect the condition and recommend the split." **Policy side** (inbound-flow `docs/design-policy.md` v6→v7, PR #1961 merged): new **§6 "Multi-handler surfaces"** subsection (≥2-handler trigger, any number ≥2 → `needs-tab-views`/`needs-sibling-route`, never a merged list; presentation-only); one **§5 hard failure** (a merged front-end list interleaving ≥2 handlers — coverage-only add, six-category §5 audit unaffected); one **§12 contract-critical positive assertion** so `design-review-pr` enforces the split. **Fork side** (`design-handoff` step-01-gather §5d Surface Topology Assessment): adds a **5th question** — does the surface span ≥2 operational handlers the project policy requires be split? Kept **design-agnostic** — it references the **project** `design-policy.md` for what counts as a "handler" and is an explicit no-op where no such rule exists; ≥2 handlers is a **forcing condition** (verdict cannot be `single-page-appropriate` as a merged list). **Self-reviewed Mode 1: 1 BLOCKING caught + fixed** — the first draft hardcoded `warehouse handler`/SellerSmart/TheFBAPrep DE/SmartPrepCentre US into the **shared** §5d, violating the "shared BMAD design workflows remain design-agnostic — project-specific decisions live in the policy" invariant (accounting-tools must not inherit inbound-flow provider names); rewritten to policy-reference. Produces/consumes no brief → 6 intake checks + 10-field provenance N/A; decision-autonomy only (recommends split for operator confirm, never infers intent); sync-safe (fork edit, not project `_bmad/`). Fork pre-push suite green; pushed `myfork/custom`. **Synced 12/13 projects 2026-06-10** (bison-ops pre-existing CLAUDE.md-section block, unrelated; inbound-flow §5d marker verified). **Downstream — `apply-design-policy-change` NOT fired (expert call, autonomous mode):** the only impacted surface, the **Listings Queue**, is ALREADY mid-redesign with `design-brief-listings-queue-2026-06-10b.md` (PR #1957, "warehouse-routing lens") which implements the per-handler split — a route-warehouse grouping/segmentation lens ("clear one warehouse's intake at a time instead of a single undifferentiated list") + an explicit never-dropped "Unrouted" group. The page is actioned, not flagged-and-abandoned; firing the apply-workflow would emit a duplicate competing brief (forbidden by no-overengineering). The Listings Queue carries TWO handlers per row (route warehouse inbound + SellerSmart dispatch outbound) — the canonical multi-handler surface this rule governs, and its brief + the new policy now agree. **Follow-ups:** (1) skill `last_verified_against_fork_commit` STILL `155cdca2`, now further behind HEAD `8c59ea10` — deliberately NOT bumped (no re-verification of the skill's referenced checks this wave; same honesty discipline as the relational-coherence-audit wave); (2) recurrence: `design-review` (live-page audit) is a candidate host for a multi-handler-merge check at audit-time, complementing the §12 `design-review-pr` enforcement at PR-time. Prior wave (2026-06-10): **NEW `verify/relational-coherence-audit` workflow — the cross-surface §13 linkage-graph auditor** `commit f4bf10e8` — user: "I wish to evaluate the coherency/linkage across one or multiple pages... handoff is 1 input page only... listing queue should WITHOUT a doubt have a link to the warehouse page." `design-handoff` / `design-review` / `design-review-pr` all enforce §13 on ONE surface in isolation; none holds the cross-surface GRAPH, so §13's hardest clauses — "no relationship visible from one side only", "a page passes only if it coheres with AND links to its sibling records" — were stated-but-enforced-by-nothing. Root-cause class **contract-dimension-gap**: the cross-page linking axis, one level up from `155cdca2` (which added the linking axis WITHIN a page). The catch no per-page check can structurally make — an absent-but-required link leaves no diff, no failing assertion, no symptom; only an independent expectation (a schema FK or a declared edge) reveals it (canonical case: `/listings/queue` shows warehouse-derived dispatch state but never links the warehouse record, and every per-page check stays green). **Two-evidence contract** (the `webhook-contract-check` shape): EXPECTED = Drizzle FKs + a declared `relational-edges.yaml` for the derived/correlated edges the schema can't encode (the SellerSmart push is an `EXISTS`, not an FK); ACTUAL = live source/DOM; the finding lives in the join (displayed-but-not-linked), and **"displayed?" is decided from the page, not the schema** — the cede that kills the false-positive flood. Load-bearing split → opposite lanes: **missing-required-link** → `design-handoff` (re-design, flagged material-revision so it supersedes not duplicates); **unresolved-lookup / drift / loud-affordance / round-trip** → `quick-spec`/`quick-dev`; **out-of-scope-candidate** (schema edge never displayed) → named not failed; **edge-map gap** → declare + re-run (P1). Read-only, detect+route, never edits — joins the `verify/` audit family (data-quality-audit, scrape-coverage-audit, webhook-contract-check). 5 files (workflow.md + 4 steps + `relational-edges.template.yaml`, project-copied to `docs/`, never sync-overwritten). Self-reviewed Mode 1: **0 blocking** (1 nit fixed — step-02 accepts `relation_kind: fk-with-lookups` to match the template); produces/consumes no brief so the 6 intake checks + 10-field provenance are N/A; grounding gate + autonomy scoping + silent-partial guard present. Fork pre-commit suite green; pushed `myfork/custom`. **13-project `sync bmad` OWED** — new workflow → a new `/bmad:bmm:workflows:relational-coherence-audit` wrapper generated on sync. **Follow-ups:** (1) the `mason-bmad-workflow-expert` skill is **230 commits behind** (`last_verified 155cdca2` vs HEAD `f4bf10e8`) — re-verification owed, deliberately NOT bumped here (bumping without re-verifying its referenced checks would be dishonest); (2) no project has a `docs/relational-edges.yaml` yet, so until one exists every run is FK-only and must announce that P1 blind spot — inbound-flow's should be seeded first (the warehouse edge is in the template); (3) recurrence: `design-review` (live-page audit) is a candidate host for the same graph check at audit-time. Prior wave (2026-06-09): **design-handoff carries the §13 linked-records mandate into the brief** `commit 155cdca2` — user: "policy emphasis on the link-to-record lane — handoff does not seem to give it the space it needs." design-policy §13 v5 (commit `ec4272e0`, same day) promoted cross-surface linking from a coherence *check* to a contract-critical **§12 positive assertion that `design-review-pr` hard-fails**: every on-screen value that IS a record owned by another surface (ASIN/SKU/order/batch/shipment/supplier/customs-entry/listing) must be a navigable lookup/link, not inert text. design-handoff was **silent** on it — §2 Domain Data captured relations *descriptively* (a "Relationships:" line + FK notes), the brief deliberately strips the existing layout (blank canvas) so the one place the linking was visible is gone, and the regenerated design defaulted to inert duplicated text → **failed review on the way back in**. Root-cause class **contract-dimension-gap** (the relational-linking axis missing from the brief contract). Fix: step-01 **§3a Linked-records inventory** derives `{linked_records_inventory}` mechanically from the data surface (foreign reference · owning surface+route · drill target = drawer + "Open full {sibling} →" · inline read-only lookups; empty ONLY for a true leaf surface) + COMPLETION; step-03 **§2a Linked records & lookups** renders it as a labeled lane (mirrors §4b/§4c — render-iff-non-empty, full omission otherwise) with the §13 quiet-styling guardrail (never button/CTA/pill) + the round-trip review-test; self-review item + success metric so a shared-data surface is never handed off silent on §13. Honesty: a value no surface owns is a plain value, not a fabricated link. Self-reviewed Mode 1: **0 blocking** (producer-side body content → 6 intake checks + 10-field provenance N/A; decision-autonomy only — derives from schema FKs, no intent inference; sync-safe — fork edit not project `_bmad/`). markdownlint + full fork pre-push suite green; pushed `myfork/custom`. **Synced 12/13 projects 2026-06-09** (bison-ops pre-existing CLAUDE.md-section block — recurring, unrelated; inbound-flow markers verified: §3a, `{linked_records_inventory}`, brief §2a). **Follow-ups:** (1) design-policy §13 v5 §9 already flags the linked-record mandate SHOULD propagate to **accounting-tools** — that project's next session applies it (not auto-edited here); (2) existing shared-data surfaces that render foreign records as inert text now diverge from policy and route to **apply-design-policy-change** (per §13 v5's own downstream note); (3) `design-review-pr` already enforces §13/§12 at PR time — this closes the producer end so the two ends meet. Prior wave (same day): **decision-analysis — the executive/quant-desk layer above rigor** `commit 169520f4` — user asked "is it executive data-analyst level or not?"; honest answer was NO: `analytics-rigor` reaches *senior-analyst* grade (honest ranges/base-rates/deciding-fields/gaps) but is descriptive — a capital-commitment surface (buy/reorder/sizing) needs a decision-theoretic rung (model the outcome distribution, size to the loss tail, name the breakeven driver). New `decision-analysis` skill (project-local + hand-apply fallback) wired as the third layer — **stack is now shape → depth → decision** (archetype/rigor/decision). Eight decision moves; narrowly gated on a new `{is_capital_decision}` (stricter than `{has_decision_numbers}` — dashboards/coverage/status commit nothing → no §4e, `C-DECISION-01` no-op). Wired: handoff §5c-3 invoke + brief §4e + rationale §3c + `C-DECISION-01` (5th review check, reads brief §4e, conservative — hard finding only on an unsized BUY/PASS or a confident probability the brief flagged un-modellable) + taxonomy cross-link. Model-honesty gate (paramount): un-modellable decision → honest `single-scenario` + VOI gap, NEVER a fabricated outcome distribution. Mode 1: 0 blocking; pre-push green; synced 12/13 (bison-ops pre-existing block). Worked proof: in-browser Monte-Carlo demo (`/tmp/inbound-flow-probabilistic.html`). Residual: skill is project-local (not fork-distributed; `71a961e9` class). Prior wave (same day): **analytics-rigor gate broadened to decision surfaces (follow-up to `796fd990`)** `commit 3e994a6f` — user asked "does the handoff call it appropriately?" and it did NOT for the motivating case: rigor was gated on `{has_analytics_band}`, but step-03's self-check asserted detail pages carry no band → rigor never fired for lead-detail (a detail buy surface). Plus the worst offenders (verdict/econ numbers) lived in the §4a hero, outside the band §4b·F covered; not per-surface; and the line-673 detail-no-band absolute contradicted §5e. Root-cause class `contract-dimension-gap`. Fix: new `{has_decision_numbers}` gate (has_analytics_band OR a detail/analytical decision surface; not CRUD), per-surface; rigor lifted §4b·F → surface-level brief **§4d** (covers band AND record/hero figures); `C-RIGOR-01` reads brief §4d not rationale §3b (so bandless decision surfaces, which have no rationale, are still enforced); line-673 reconciled (detail CAN carry analytics per §5e; must not carry a dashboard). Honesty gate preserved. Mode 1: 0 blocking; pre-push green; synced 12/13 (bison-ops pre-existing block). Prior wave (same day): **analytics-rigor — the DEPTH axis of data presentation** `commit 796fd990` — the analytics presentation family governed SHAPE (`analytics-surface-architect` + taxonomy) and PIXELS (`design-policy`) but nothing governed DEPTH, so a correctly-shaped, policy-clean band could ship every figure as a naked point estimate, no base rate, no trend magnitude, no one-line read (correct and useless). User-surfaced example: the inbound-flow lead-detail buy surface — polished, compliant, schoolboy-grade analysis. Root-cause class `contract-dimension-gap` (the missing depth axis of the presentation contract). New `analytics-rigor` skill (eight rigor moves: lead-with-read · no-naked-number · sensitivity · base-rate · deciding-field · trend-magnitude · rank-by-impact · missing-vs-weak), wired across all three layers so depth is BUILT (brief §4b·F), RECORDED (rationale §3b), and ENFORCED (`C-RIGOR-01`, the depth counterpart to `C-ARCHETYPE-01`). Honesty gate (paramount): uncomputable uncertainty/base-rate → surfaced data gap, NEVER a fabricated interval; `C-RIGOR-01` is conservative (P1 only on the unambiguous naked-number case, else a precise human-judgment prompt) and never flags a named gap as a defect. Self-reviewed Mode 1: 0 blocking (rationale out of scope for `brief-revision-policy.md`; brief §4b·F is body not frontmatter; worktree-safe via documented fallback). `validate:refs` + markdownlint + full pre-push suite green; pushed `myfork/custom`. **Synced 12/13 projects 2026-06-09** (bison-ops blocked on its pre-existing CLAUDE.md-section drift — same recurring block as every prior wave, unrelated to this change; verified §5c-2/§4b·F/C-RIGOR-01/§3b/taxonomy markers landed in inbound-flow). Residual: `analytics-rigor` is project-local (not fork-distributed) — same drift class as `analytics-surface-architect` (`71a961e9` follow-up); other 12 projects use the hand-apply fallback. Prior wave: 2026-06-08 by inbound-flow session — **capability-drop hardening (design-handoff mutation-derivation audit + `{dropped_capabilities}` end-of-run disclosure · design-implement orphaned-action disclosure)** `commit 6b09432a` — design-handoff DERIVED the data shape from the schema but RECALLED capabilities, and the anti-bias "don't read the current UI" rule discouraged the inspection that would surface a non-obvious one. The ingest audit `16e7eeab` closed the record-CREATION subclass; this closes the sibling EXISTING-record-mutation subclass (resolve/remap/override/re-run/reprice). Canonical silent loss (user-surfaced): the EOS batch-detail EAN→ASIN remap — `overrideWholesaleAsinAction` left with zero callers after the funnel redesign deleted the components that called it. Root-cause class `contract-dimension-gap`. Fix: step-01 §3 **mutation-derivation audit** (grep the current surface's components for every action they invoke — verbs NOT layout, anti-bias preserved — each maps to `must_support_capabilities` OR a logged `{dropped_capabilities}` drop); workflow.md + step-01 COMPLETION add the state var; step-03 renders drops in brief §1 "Deliberately not carried forward" + **MANDATORY end-of-run user disclosure** in §5 + self-review item. design-implement step-04 §9 gains a **mandatory "Capabilities removed (orphaned actions)"** report when the apply deletes/replaces components (grep for now-zero-caller actions among those the removed files called) — the grid-driven apply ledger can't catch it because the lost capability was never a grid row. Self-reviewed Mode 1: 0 blocking. Fork pre-push suite green. Pushed `myfork/custom`; 12/13 `sync bmad` 2026-06-08 (bison-ops pre-existing CLAUDE.md drift). **Follow-ups:** (1) optional brief frontmatter flag for `{dropped_capabilities}` if `brief-revision-policy` should validate it (currently body-only, like `must_support_capabilities`); (2) the EOS batch-detail remap itself is being restored via `design-brief-eos-batch-error-resolution-2026-06-08.md` (PR #1882, merged). Prior wave: 2026-06-08 by inbound-flow session (**design-handoff §5e Analytics Surface Hierarchy — rank multiple co-resident surfaces** `commit 3e1be5ca` — the handoff could pick the shape of ONE analytics band (§5c) and split a page across routes (§5d), but had no step to rank MULTIPLE co-resident analytics surfaces on one page → briefs came back with no hierarchy, designs rendered the flat equal-weight panel-stack that policy §6/§5 ban, review caught it after the fact. Surfaced by a legacy Keepa-style product detail page (price + sales-rank + competition + live-stock + buy-box-ownership = 5 charts at equal weight, an archive not an argument). Same root pattern as the waterfall gap: the handoff's analytics reasoning was one-band-shaped; this adds the missing axis (surface hierarchy). Fix: step-01-gather §5e (NEW) — gated on ≥2 distinct analytics surfaces, fires regardless of page_mode INCLUDING detail (overrides the §5b/step-03 "a single record has no aggregate dimension" absolute that suppressed analytics-rich detail pages — the exact Keepa failure), ranks hero/supporting/drill by the page's PRIMARY question, ground-or-flag reuses §5d (can't name one hero → needs-tab-views/needs-sibling-route); §5c runs once per surface on multi-surface pages; step-03 §4b.0 (NEW) renders the ranked hierarchy before per-surface specs; step-03 detail-mode "no analytics band" softened to carve out research/monitoring detail pages. Operationalizes existing policy (§6 "one or two lead charts + supporting"); does not invent a rule. Pushed to `myfork/custom`; synced to inbound-flow + others 2026-06-08 (bison-ops blocked on pre-existing CLAUDE.md drift). Follow-on flagged (not built): design-review-pr `C-AHIER-01` to enforce the declared hierarchy at PR time. Prior 2026-06-08: (**`waterfall` analytics archetype — reconciliation-bridge split out of `flow`** `commit 71a961e9` — the archetype taxonomy had eight shapes and `flow` was overloaded to cover BOTH attrition-funnels AND running-total reconciliations, so a dataset shaped `opening − Δ − Δ = closing` (EOS batch-detail conversion run, 642 → −131/−41/−82/−57 → 331 winners, headlined by a single £4,790 figure) could only map to `flow`, whose prescribed form is "a funnel with drop deltas" — the architect selected `flow` correctly and the synthesizer rendered a funnel correctly, producing a funnel wearing a waterfall's data (deltas double-encoded as bar segments AND reason chips; the corporate bridge shape unreachable for lack of a name). Root-cause class contract-dimension-gap — missing dimension lived INSIDE the archetype vocabulary, not the §5c wiring (already correct). Fix: new `waterfall` archetype + `flow`-vs-`waterfall` disambiguation (independent gates/survival-rate → flow; one population reconciled by signed deltas → waterfall) with a question-driven tie-break; `brief-revision-policy.md` adds `waterfall` to the `analytics_archetype` allowed-values contract; design-handoff step-01/step-03/workflow.md bump enums + "eight"→"nine" + a waterfall form hint; design-review-pr C-ARCHETYPE-01 gains a `waterfall` row. Pushed to `myfork/custom`; **13-project `sync bmad` OWED** (workflow text, no new wrapper). Residual: the project-local `analytics-surface-architect` SKILL.md enum still lists eight (taxonomy-wins makes it functionally correct post-sync, output-contract enum stale) — skill is not fork-managed, a known drift class. Prior 2026-06-08: (**design-tuning craft/legibility checks + PASS-WITH-ISSUES gate** `commit 7f9fb748` — design-tuning auto-approved a Prep Partners Hub design on iter-1 (clean PASS) that the user then found riddled with craft defects. Root-cause class: contract-dimension-gap — the PASS gate was binary on §5 hard-failures, so issue-severity craft had no enumerated check and could not block approval; the reviewer also substituted holistic reasoning for the §1b/§2 grids. Three blind spots had ZERO check: status over-encoding by repetition (same severity repainted border+pill+colored-text+colored-count on one row — the 4-color cap counts breadth of hues, not per-row repetition), decorative element redundant with adjacent text (a per-row micro-bar restating its own caption — fails §5 "what job does this do?"), and label collision / label↔severity-wording / icon↔meaning mismatch. Fix: step-02 §1b role catch-all extended (status-indicator repetition test + analytics-viz per-row redundancy test); new §2 "Craft & legibility" table (issue-severity); §7 splits PASS into PASS-CLEAN vs PASS-WITH-ISSUES via a new {has_unresolved_issues} gate; step-03 §5 gains an APPROVED-WITH-ISSUES template with a mandatory Issues-to-resolve block, §5b brand-identity feedback scoped to PASS-CLEAN only, §6 summary/next-step updated; checklist.md gains matching role-keyed boxes. Pushed to `myfork/custom`; 12/13-project sync ran 2026-06-08 (bison-ops blocked on pre-existing CLAUDE.md drift, unrelated). Prior 2026-06-08: (**design-handoff §5d surface topology assessment** `commit 7e366b34` — adds a reasoning step (not a checklist) to step-01-gather.md §5d that uses gathered evidence — data model depth, capability breadth, user jobs, existing routes — to assess whether a feature's scope fits one route or warrants multiple surfaces. Four verdict classes: `single-page-appropriate | needs-detail-route | needs-tab-views | needs-sibling-route`. Verdict drives a new §4c template block in step-03 (rendered only when non-trivial) and a self-review checklist item. The step asks the LLM to reason from evidence rather than check thresholds — aware of right-drawer capacity, full-page detail ceiling, sibling-route sub-entities, and tab-as-mode-switch vs tab-as-chrome-overflow. Pushed to `myfork/custom`; 12/13-project sync ran 2026-06-08 (bison-ops blocked on pre-existing CLAUDE.md drift). Prior 2026-06-08: (**design-handoff ingest/entry-point hardening** `commit 16e7eeab` — two design-handoff runs on the same leads surface both silently dropped the wholesale upload action because the gather step asked about mutations but not how data *enters* the system. Root-cause class: `contract-dimension-gap` (the check was "capabilities not dropped" but the gap was "capability never added to must_support_capabilities"). Fix: step-01-gather.md §3 API Surface gets an explicit ingest/entry-point audit prompt; step-03-generate-brief.md §3 self-review gets a dedicated checklist item that is distinct from the existing must-support-capabilities check. Pushed to `myfork/custom`; 12/13-project sync ran 2026-06-08 (bison-ops blocked on pre-existing CLAUDE.md drift, unrelated). Corrected brief `design-brief-leads-2026-06-08.md` shipped to inbound-flow via PR #1859 (merged); 06-06 brief marked superseded.). Prior 2026-06-06: (**webhook-contract-charter** `commit 1142471a` — declarative contract-of-record companion to the `webhook-contract-check` enforcer; charter authored in `workflows/shared/`, enforcer now cites it as its source of truth; pushed `myfork/custom` + full 13-project sync 2026-06-06, 13 synced / 0 blocked. ALSO earlier this day: design-tuning **check-surface scoping** `commit 0539fd7a` + `d43830e8` + `b0d5a196` — element-type-scoping gaps in step-02-analyze, both surfaced by a user catching defects the workflow scored clean on a TheFBAPrep Audit History design: (1) the §1b/§2 band & "Dashboard card grid" check keyed only on per-tile FRAMING (border/fill/shadow), so a band whose content is big summary-count figures (a stat/KPI row) passed once the framing came off — added a content-keyed clause + framing-independent litmus ("delete the band and move its numbers to the inline summary line; if you lose only numbers and no visualization, it was a stat row → hard failure", routed through the §2 AI-fingerprint row so severity is forced hard-failure); (2) the §2 color-cap check was scoped to badges, so status color in a 3-segment progress bar (pink awaiting-delivery / orange awaiting-receipt / green received = a rainbow stage mapping + off-palette pink/orange) passed while the pills read clean — broadened to count status color across ALL state-encoding elements (progress/meter/segment/dot/series/strip) + off-palette/rainbow/same-state-different-tone sub-checks. Then a FULL audit of design-tuning (user: "we identified 2 gaps in 30 minutes") found the SAME defect throughout and produced the structural fix `b0d5a196`: §1b re-keyed by semantic ROLE (status-indicator / CTA / identifier / analytics-viz / surface-fill / detail-drawer) with a catch-all so a rule is inspected wherever its role appears, not only on named elements (retroactively covers the drawer-on-operational §7 gap, CTAs outside detail mode, status color on non-badge elements); explicit severity routing so no §1b finding silently defaults to "issue"; and 4 §5 hard-failures that had ZERO check (grep-confirmed) added — emoji-as-UI-icon, sidebar-in-feature-page, status meaning↔tone mismatch (red reserved for genuine failures), uppercase tracking-wide. checklist.md de-drifted from other-project specifics (emerald/Inter/ring-inset) into a project-agnostic role-keyed companion. Root-cause class throughout: contract-dimension-gap (check surface scoped by element TYPE). **All three commits pushed to `myfork/custom`; full 13-project `sync-bmad-workflows.sh` RAN 2026-06-06 — 13 synced, 0 blocked.** Sibling carry-over `commit 86e737cd`: design-review-pr step-03-dom-render role-keyed — S-STATUS-03 broadened to count status color across ALL state-encoding elements (`distinctStatusColorsAllEls`, not just badges), new S-STATUS-09 (per-bar rainbow stage mapping ≥3 hues), L-LAYOUT-03 broadened with `bigNumberBlocksAbove` (unframed big-number stat rows), C-ARCHETYPE-01 stat-row-in-disguise across all archetypes (the `bigNumbers` harvest was wired only to single-metric). design-implement assessed and deliberately NOT patched — it is a *fidelity* tool (matches the design bundle), has no band/policy/color-cap check, so role-keying it is a category error; catching a bad design is upstream (design-tuning/review) and it already cedes the content lane to them. Wave commits: 0539fd7a + d43830e8 + b0d5a196 + 86e737cd + `81568ed9` (the 3 remaining audit follow-ups: icon-cluster / colored-icon-circle check, centered-card-on-gray §5 row, and a §11 dropdown role+check) — all pushed and all distributed by three 2026-06-06 13-project syncs (verified in inbound-flow). **design-tuning audit fully closed.** Only residual: heuristic-threshold tuning of design-review-pr's new DOM harvests (`bigNumberBlocksAbove` fontSize≥22, `segBars`/`rainbowBars` selectors) after they run against a real PR or two — conservative by design, may need widening.). Prior 2026-06-02: inbound-flow session — design-implement **content-lane boundary** `commit 7a09717c` — the grid is treatment-only and compares against a mock-data bundle, so formatter/enum-driven canonical-identifier cells (marketplace/supplier/ASIN/…) whose value is wrong only on a real-data variant the bundle never contained were shipping invisibly: bundle `UK→UK` matches impl `UK→UK` while live leaks `amazon_us`. Fix cedes the content lane rather than faking certification — step-02 §3d tags value_source, step-03 §2c marks such cells CONTENT-LANE-UNVERIFIED (not a delta), step-04 §5/§9 adds `deferred(content-lane)` + a mandatory "Content-lane verification owed (live page)" report section routing to design-review §13(a) / design-tuning §2b. Root-cause class: contract-dimension-gap (dimension CEDED, not checked). **Pushed to `myfork/custom`; 13-project `sync bmad` still owed** — design-implement is an existing workflow so no new slash-command wrapper, sync just refreshes the workflow text.). Prior same day: NEW `verify/data-quality-audit` workflow `commit d9f099a6` — the detection front door that feeds the `data-quality` root-cause rule `72849dae`; pushed to `myfork/custom` + full 13-project sync, `/bmad:bmm:workflows:data-quality-audit` wrapper generated everywhere). Prior 2026-06-01 (design-elevation step-04 shape-axis routing + Claude Design prompt route `commit 657913cd`; upgrade-bmad.sh cleared the 2-commit upstream drift). Prior same day: worktree-aware discovery: design-tuning `84b31972` + follow-up across design-synthesize/artifact-loop/elevation `commit efdfa028`). Prior same day: design-handoff detail interaction-verb composition fit + capability completeness `commit 20738ae9` (**not yet pushed/synced** — author-only that wave). Prior same day: design-handoff composition-fit check / policy-default bias guard `commit c33a3b24`. Prior same day: accounting-tools session (design-handoff analytics presentation rationale companion artifact `commit 705bb912`). **A full 13-project `sync-bmad-workflows.sh` ran on 2026-06-01 — this distributed this wave AND cleared the prior broad-sync backlog** (analytics-archetype wave `29f6c713`/`077b5fe9`/`e8b5ef7a`, data-quality `72849dae`, design-implement multiplicity/apply-ledger axes, bmad-deploy auth pre-flight). Prior (2026-05-31): data-quality root-cause `72849dae` + CLAUDE.md adapter PR #946; operational-analytics-band reframe `e8b5ef7a`, design-review-pr C-ARCHETYPE-01 `077b5fe9`, design-handoff archetype axis `29f6c713`.
